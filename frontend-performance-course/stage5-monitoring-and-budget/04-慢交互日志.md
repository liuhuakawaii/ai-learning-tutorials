# 第4课：慢交互日志

> **课程定位**：追踪和分析导致 INP 差的具体交互，定位性能瓶颈
> **前置知识**：了解 INP 指标和 web-vitals 库
> **预计时长**：30 分钟

---

## 学习目标

1. 理解 INP 的组成和测量方式
2. 掌握慢交互的追踪和日志记录
3. 学会分析慢交互的原因
4. 了解 Long Animation Frames API

---

## 一、INP 的组成

```
┌──────────────────────────────────────────────────────────────┐
│              INP 的组成                                        │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  用户交互（click, keydown, etc.）                             │
│    ↓                                                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐       │
│  │ Input Delay  │→│ Process Time │→│Present Delay  │       │
│  │ 输入延迟      │  │ 处理时间     │  │ 呈现延迟      │       │
│  └──────────────┘  └──────────────┘  └──────────────┘       │
│                                                              │
│  Input Delay：从交互发生到事件处理开始的时间                  │
│  → 主线程被其他任务占用                                       │
│                                                              │
│  Process Time：事件处理函数执行时间                           │
│  → 事件处理器中的计算和 DOM 操作                              │
│                                                              │
│  Present Delay：从处理完成到浏览器绘制的时间                  │
│  → 样式计算、布局、绘制                                      │
│                                                              │
│  INP = max(所有交互的 Input Delay + Process Time + Present Delay)│
│  （取最慢的那次交互，通常是 P98）                             │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

---

## 二、追踪慢交互

### 2.1 使用 web-vitals

```javascript
import { onINP } from 'web-vitals';

onINP((metric) => {
  const entry = metric.entries[0];
  if (!entry) return;

  console.log('INP 详情：', {
    // 交互类型
    interactionType: entry.interactionType, // 'pointer', 'keyboard'
    // 交互目标
    target: entry.target,
    // 时间分解
    inputDelay: entry.processingStart - entry.startTime,
    processTime: entry.processingEnd - entry.processingStart,
    presentDelay: entry.startTime + entry.duration - entry.processingEnd,
    // 总延迟
    duration: entry.duration,
  });
});
```

### 2.2 记录慢交互上下文

```javascript
import { onINP } from 'web-vitals';

onINP((metric) => {
  const entry = metric.entries[0];
  if (!entry) return;

  const context = {
    // 交互信息
    type: entry.interactionType,
    target: getSelector(entry.target), // CSS 选择器
    timestamp: entry.startTime,

    // 时间分解
    inputDelay: entry.processingStart - entry.startTime,
    processTime: entry.processingEnd - entry.processingStart,
    presentDelay: entry.startTime + entry.duration - entry.processingEnd,

    // 页面信息
    page: window.location.pathname,
    viewport: `${window.innerWidth}x${window.innerHeight}`,

    // 设备信息
    deviceMemory: navigator.deviceMemory,
    connection: navigator.connection?.effectiveType,

    // INP 值
    value: metric.value,
    rating: metric.rating,
  };

  sendToAnalytics(context);
});

function getSelector(element) {
  if (!element) return 'unknown';
  const parts = [];
  while (element && element !== document.body) {
    let selector = element.tagName.toLowerCase();
    if (element.id) {
      selector += `#${element.id}`;
    } else if (element.className) {
      selector += `.${element.className.split(' ')[0]}`;
    }
    parts.unshift(selector);
    element = element.parentElement;
  }
  return parts.join(' > ');
}
```

---

## 三、Long Tasks API

```javascript
// 监控长任务（> 50ms）
const observer = new PerformanceObserver((list) => {
  list.getEntries().forEach((entry) => {
    console.warn(`长任务：${entry.duration.toFixed(1)}ms`, {
      startTime: entry.startTime,
      attribution: entry.attribution?.map(a => ({
        name: a.name,
        containerType: a.containerType,
        containerName: a.containerName,
        containerId: a.containerId,
      })),
    });
  });
});

observer.observe({ type: 'longtask', buffered: true });
```

---

## 四、Long Animation Frames API

### 4.1 什么是 Long Animation Frames

```
┌──────────────────────────────────────────────────────────────┐
│              Long Animation Frames (LoAF)                     │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  LoAF 是 Long Tasks API 的升级版：                           │
│  - 更准确地追踪主线程阻塞                                    │
│  - 包含脚本执行的详细信息                                    │
│  - 关联到具体的用户交互                                      │
│                                                              │
│  一个 LoAF 包含：                                             │
│  - 开始和结束时间                                            │
│  - 执行的脚本列表                                            │
│  - 每个脚本的执行时间                                        │
│  - 渲染时间                                                  │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

### 4.2 使用 LoAF

```javascript
const observer = new PerformanceObserver((list) => {
  list.getEntries().forEach((frame) => {
    if (frame.duration > 50) {
      console.log(`Long Animation Frame: ${frame.duration.toFixed(1)}ms`, {
        startTime: frame.startTime,
        renderStart: frame.renderStart,
        styleAndLayoutStart: frame.styleAndLayoutStart,
        scripts: frame.scripts?.map(script => ({
          name: script.sourceURL,
          functionName: script.sourceFunctionName,
          duration: script.duration,
          executionStart: script.executionStart,
          windowAttribution: script.windowAttribution,
        })),
      });
    }
  });
});

observer.observe({ type: 'long-animation-frame', buffered: true });
```

### 4.3 关联交互和 LoAF

```javascript
// 记录交互发生时的 LoAF
let currentLoAF = null;

const loafObserver = new PerformanceObserver((list) => {
  const entries = list.getEntries();
  currentLoAF = entries[entries.length - 1];
});
loafObserver.observe({ type: 'long-animation-frame', buffered: true });

// 记录交互
const inpObserver = new PerformanceObserver((list) => {
  list.getEntries().forEach((entry) => {
    if (entry.interactionId && currentLoAF) {
      console.log('交互关联的 LoAF:', {
        interactionType: entry.interactionType,
        duration: entry.duration,
        loafDuration: currentLoAF.duration,
        loafScripts: currentLoAF.scripts,
      });
    }
  });
});
inpObserver.observe({ type: 'event', buffered: true, durationThreshold: 0 });
```

---

## 五、分析慢交互原因

```
┌──────────────────────────────────────────────────────────────┐
│              慢交互原因分析                                    │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  Input Delay 大：                                             │
│  → 主线程被长任务占用                                        │
│  → 检查：是否有同步脚本、长任务、强制布局                    │
│                                                              │
│  Process Time 大：                                            │
│  → 事件处理函数执行时间长                                    │
│  → 检查：事件处理器中的计算、DOM 操作、状态更新              │
│                                                              │
│  Present Delay 大：                                           │
│  → 渲染时间长                                                │
│  → 检查：大量 DOM 变更、复杂样式计算、布局抖动              │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

### 5.1 常见原因和解决方案

```
┌─────────────────────────┬────────────────────────────────────┐
│ 原因                     │ 解决方案                            │
├─────────────────────────┼────────────────────────────────────┤
│ Input Delay 大           │                                    │
│ ├─ 同步脚本阻塞          │ defer/async 加载                   │
│ ├─ 长任务阻塞主线程      │ 拆分长任务                          │
│ └─ 强制同步布局          │ 读写分离                            │
│                          │                                    │
│ Process Time 大          │                                    │
│ ├─ 大量 DOM 操作         │ 虚拟滚动、分批渲染                  │
│ ├─ 复杂计算              │ Web Worker                         │
│ ├─ 同步状态更新          │ 拆分状态更新                        │
│ └─ 大量 React 渲染       │ memo、减少重渲染                    │
│                          │                                    │
│ Present Delay 大         │                                    │
│ ├─ 大量 DOM 变更         │ 减少变更范围                        │
│ ├─ 复杂 CSS 选择器       │ 简化选择器                          │
│ └─ 布局抖动              │ 避免读写交替                        │
└─────────────────────────┴────────────────────────────────────┘
```

---

## 六、慢交互日志最佳实践

```javascript
// 完整的慢交互日志方案
import { onINP } from 'web-vitals';

function setupSlowInteractionLogging() {
  // 采集 INP
  onINP((metric) => {
    if (metric.rating !== 'good') {
      logSlowInteraction(metric);
    }
  });

  // 监控长任务
  const longTaskObserver = new PerformanceObserver((list) => {
    list.getEntries().forEach((entry) => {
      if (entry.duration > 100) {
        logLongTask(entry);
      }
    });
  });
  longTaskObserver.observe({ type: 'longtask', buffered: true });

  // 监控 LoAF（如果支持）
  if (PerformanceObserver.supportedEntryTypes?.includes('long-animation-frame')) {
    const loafObserver = new PerformanceObserver((list) => {
      list.getEntries().forEach((entry) => {
        if (entry.duration > 100) {
          logLoAF(entry);
        }
      });
    });
    loafObserver.observe({ type: 'long-animation-frame', buffered: true });
  }
}

function logSlowInteraction(metric) {
  const entry = metric.entries[0];
  sendToAnalytics({
    type: 'slow-interaction',
    interactionType: entry?.interactionType,
    target: getSelector(entry?.target),
    inputDelay: entry ? entry.processingStart - entry.startTime : null,
    processTime: entry ? entry.processingEnd - entry.processingStart : null,
    duration: metric.value,
    page: window.location.pathname,
  });
}
```

---

## 七、检查清单

```
┌──────────────────────────────────────────────────────────────┐
│              慢交互日志检查清单                                │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  采集                                                        │
│  □ 采集 INP 并记录交互详情                                   │
│  □ 记录交互类型和目标元素                                    │
│  □ 记录 Input Delay / Process Time / Present Delay           │
│                                                              │
│  监控                                                        │
│  □ 监控长任务                                                │
│  □ 监控 Long Animation Frames                                │
│  □ 关联交互和主线程阻塞                                      │
│                                                              │
│  分析                                                        │
│  □ 能识别最慢的交互                                          │
│  □ 能定位慢交互的原因                                        │
│  □ 有优化建议                                                │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

---

## 动手练习

### 练习一：追踪慢交互

1. 在页面中添加 INP 日志
2. 故意制造慢交互（大量 DOM 更新）
3. 观察日志中的时间分解

### 练习二：关联长任务

1. 同时监控 INP 和长任务
2. 找到慢交互对应的长任务
3. 分析长任务的调用栈

### 练习三：优化慢交互

1. 找到一个慢交互
2. 用 setTimeout 拆分任务
3. 验证 INP 是否改善

---

## 小结

1. **INP 组成**：Input Delay + Process Time + Present Delay
2. **web-vitals**：采集 INP 并记录交互详情
3. **Long Tasks API**：监控主线程长任务
4. **LoAF API**：更准确地追踪主线程阻塞
5. **原因分析**：根据时间分解定位优化方向

---

## 下一课预告

下一课将学习性能回归分析——如何发现和诊断性能退化。
