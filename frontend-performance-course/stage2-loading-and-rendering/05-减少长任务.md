# 第5课：减少长任务

> **课程定位**：识别和拆分长任务，释放主线程，让交互更及时响应
> **前置知识**：了解浏览器加载链路和关键渲染路径
> **预计时长**：35 分钟

---

## 学习目标

1. 理解什么是长任务以及它如何影响 INP
2. 使用 DevTools 识别长任务
3. 掌握拆分长任务的方法（setTimeout、requestIdleCallback、scheduler）
4. 了解 Web Worker 的适用场景

---

## 一、什么是长任务

```
┌──────────────────────────────────────────────────────────────┐
│                     主线程时间线                               │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌──────────┐  ┌──────────────────────────┐  ┌───────────┐  │
│  │ 任务 A   │  │       任务 B（长任务）      │  │  任务 C   │  │
│  │  5ms     │  │        200ms              │  │   8ms     │  │
│  └──────────┘  └──────────────────────────┘  └───────────┘  │
│                                                              │
│                ↑ 用户在这 200ms 内点击 → 无法响应              │
│                                                              │
│  长任务：执行时间 > 50ms 的任务                                │
│  浏览器在任务执行期间无法响应用户输入                           │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

**长任务直接影响 INP（Interaction to Next Paint）**：
- 用户点击 → 浏览器要等当前长任务结束 → 才能处理点击 → 才能更新画面
- 长任务越多、越长，INP 越差

---

## 二、用 DevTools 识别长任务

### 2.1 Performance 面板

```
操作步骤：
1. 打开 DevTools → Performance 面板
2. 点击录制 → 操作页面（点击、滚动、输入）
3. 停止录制 → 查看 Main 轨道

长任务标记：
┌─────────────────────────────────────────┐
│  红色三角形 = 长任务（> 50ms）            │
│  灰色区域   = 任务执行时间               │
│  红色区域   = 超过 50ms 的部分            │
└─────────────────────────────────────────┘
```

### 2.2 Console 面板

```javascript
// Chrome 会自动在 Console 中警告长任务
// 看到以下提示说明有长任务：
// [Violation] 'click' handler took 250ms
// [Violation] 'setTimeout' handler took 120ms
```

### 2.3 Long Tasks API

```javascript
// 用代码监测长任务
const observer = new PerformanceObserver((list) => {
  list.getEntries().forEach((entry) => {
    console.warn(`长任务：${entry.duration.toFixed(1)}ms`, entry);
    console.warn(`  开始时间：${entry.startTime.toFixed(1)}ms`);
    console.warn(`  容器：${entry.containerType}`);
  });
});

observer.observe({ type: 'longtask', buffered: true });
```

---

## 三、长任务的常见来源

```
┌──────────────────────────────────────────────────────────────┐
│                    长任务常见来源                              │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  1. 大量 DOM 操作                                            │
│     一次性渲染 1000 个列表项                                  │
│                                                              │
│  2. 复杂计算                                                 │
│     大数组排序、过滤、聚合                                    │
│                                                              │
│  3. 同步脚本解析和执行                                        │
│     大 JS bundle 同步加载                                     │
│                                                              │
│  4. 复杂的 React 渲染                                        │
│     大量组件同时更新                                          │
│                                                              │
│  5. 布局抖动（读写交替）                                      │
│     读 offsetHeight → 修改样式 → 读 scrollTop → 修改样式      │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

---

## 四、拆分长任务

### 4.1 setTimeout 拆分

```javascript
// ❌ 一次处理 10000 项（长任务）
function processAll(items) {
  items.forEach(item => process(item));
}

// ✅ 分批处理，每批让出主线程
function processInBatches(items, batchSize = 100) {
  let index = 0;

  function processBatch() {
    const end = Math.min(index + batchSize, items.length);
    for (; index < end; index++) {
      process(items[index]);
    }

    if (index < items.length) {
      // 让出主线程，下一批稍后执行
      setTimeout(processBatch, 0);
    }
  }

  processBatch();
}
```

### 4.2 requestIdleCallback

```javascript
// 在浏览器空闲时执行低优先级工作
function processWhenIdle(items) {
  let index = 0;

  function process(deadline) {
    // 还有空闲时间 && 还有任务
    while (deadline.timeRemaining() > 0 && index < items.length) {
      process(items[index]);
      index++;
    }

    if (index < items.length) {
      requestIdleCallback(process);
    }
  }

  requestIdleCallback(process);
}
```

### 4.3 scheduler.yield（现代方案）

```javascript
// Scheduler API — 让出主线程，等浏览器处理完待处理的输入后继续
async function processWithYield(items) {
  for (let i = 0; i < items.length; i++) {
    process(items[i]);

    // 每处理 100 项让出一次主线程
    if (i % 100 === 0) {
      await scheduler.yield();
    }
  }
}

// 兼容性降级
async function yieldToMain() {
  if (typeof scheduler !== 'undefined' && scheduler.yield) {
    await scheduler.yield();
  } else {
    return new Promise(resolve => setTimeout(resolve, 0));
  }
}
```

### 4.4 对比

```
┌─────────────────────┬────────────────────────────────────────┐
│ 方法                 │ 特点                                    │
├─────────────────────┼────────────────────────────────────────┤
│ setTimeout(fn, 0)   │ 最简单，但优先级低，可能被延迟           │
│ requestIdleCallback │ 只在空闲时执行，适合低优先级任务         │
│ scheduler.yield     │ 现代方案，优先处理待处理输入              │
│ MessageChannel      │ 比 setTimeout 更快触发                   │
└─────────────────────┴────────────────────────────────────────┘
```

---

## 五、Web Worker

### 5.1 适用场景

```
┌──────────────────────────────────────────────────────────────┐
│              Web Worker 适用 vs 不适用                        │
├───────────────────────────┬──────────────────────────────────┤
│  适用                      │  不适用                          │
├───────────────────────────┼──────────────────────────────────┤
│  大数据计算                │  DOM 操作                        │
│  JSON 解析/序列化          │  访问 window/document            │
│  图片处理                  │  样式计算                        │
│  搜索/排序/过滤            │  Canvas 2D/WebGL 上下文          │
│  加密/解密                 │  React 状态更新                  │
└───────────────────────────┴──────────────────────────────────┘
```

### 5.2 基本用法

```javascript
// worker.js
self.addEventListener('message', (e) => {
  const { data, type } = e.data;

  if (type === 'sort') {
    const sorted = data.sort((a, b) => a - b);
    self.postMessage({ type: 'sorted', data: sorted });
  }
});

// main.js
const worker = new Worker('/worker.js');

worker.postMessage({ type: 'sort', data: largeArray });

worker.addEventListener('message', (e) => {
  if (e.data.type === 'sorted') {
    updateUI(e.data.data);
  }
});
```

### 5.3 React 中使用 Worker

```javascript
// useWorker.js
import { useEffect, useRef, useCallback } from 'react';

export function useWorker(workerPath) {
  const workerRef = useRef(null);

  useEffect(() => {
    workerRef.current = new Worker(workerPath);
    return () => workerRef.current?.terminate();
  }, [workerPath]);

  const postMessage = useCallback((message) => {
    return new Promise((resolve) => {
      const handler = (e) => {
        workerRef.current.removeEventListener('message', handler);
        resolve(e.data);
      };
      workerRef.current.addEventListener('message', handler);
      workerRef.current.postMessage(message);
    });
  }, []);

  return postMessage;
}

// 使用
function DataProcessor({ rawData }) {
  const postMessage = useWorker('/data-worker.js');

  useEffect(() => {
    postMessage({ type: 'process', data: rawData })
      .then(result => setProcessed(result));
  }, [rawData]);
}
```

---

## 六、实际优化模式

### 6.1 搜索输入防抖 + 分批渲染

```javascript
function SearchBox({ items }) {
  const [query, setQuery] = useState('');
  const [results, setResults] = useState([]);

  const handleSearch = useCallback(
    debounce((q) => {
      // 在 Worker 中过滤
      worker.postMessage({ type: 'filter', query: q, items });
    }, 300),
    [items]
  );

  return <input onChange={e => {
    setQuery(e.target.value);
    handleSearch(e.target.value);
  }} />;
}
```

### 6.2 虚拟列表（只渲染可见项）

```javascript
// 只渲染视口内的 20 项，而不是 10000 项
function VirtualList({ items, itemHeight }) {
  const [scrollTop, setScrollTop] = useState(0);
  const viewportHeight = 600;

  const startIndex = Math.floor(scrollTop / itemHeight);
  const endIndex = Math.ceil((scrollTop + viewportHeight) / itemHeight);
  const visibleItems = items.slice(startIndex, endIndex);

  return (
    <div onScroll={e => setScrollTop(e.target.scrollTop)}
         style={{ height: viewportHeight, overflow: 'auto' }}>
      <div style={{ height: items.length * itemHeight }}>
        <div style={{ transform: `translateY(${startIndex * itemHeight}px)` }}>
          {visibleItems.map(item => <Item key={item.id} {...item} />)}
        </div>
      </div>
    </div>
  );
}
```

---

## 动手练习

### 练习一：识别长任务

1. 打开一个有大量列表的页面
2. 用 Performance 面板录制滚动和点击操作
3. 找到所有长任务，记录它们的耗时和调用栈

### 练习二：拆分长任务

1. 写一个处理 10000 项数据的函数
2. 先用普通 forEach（观察长任务）
3. 改用 setTimeout 分批处理
4. 对比 INP 指标变化

### 练习三：Web Worker 实战

1. 把大数组排序操作移到 Worker 中
2. 主线程只负责接收结果和更新 UI
3. 观察主线程是否还有长任务

---

## 小结

1. **长任务 > 50ms** 会阻塞主线程，直接影响 INP
2. **用 DevTools** 识别长任务：红色三角形标记
3. **拆分任务**：setTimeout、requestIdleCallback、scheduler.yield
4. **Web Worker**：把计算密集型工作移到后台线程
5. **虚拟列表**：只渲染可见区域，避免大量 DOM 操作

---

## 下一课预告

下一课将学习防止布局抖动——你将了解什么是 CLS，为什么会发生布局偏移，以及如何从根源上避免。
