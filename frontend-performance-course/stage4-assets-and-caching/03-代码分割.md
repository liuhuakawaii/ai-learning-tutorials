# 第3课：代码分割

> **课程定位**：把大的 JS 包拆分成按需加载的小块，减少初始加载体积
> **前置知识**：了解 JavaScript 模块和打包工具
> **预计时长**：30 分钟

---

## 学习目标

1. 理解代码分割的原理和收益
2. 掌握路由级和组件级的代码分割
3. 学会分析和优化打包体积
4. 了解动态导入和预加载策略

---

## 一、为什么需要代码分割

```
┌──────────────────────────────────────────────────────────────┐
│              没有代码分割的问题                                │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  所有代码打包成一个文件：                                     │
│  ┌──────────────────────────────────────┐                    │
│  │  bundle.js (500KB gzipped)           │                    │
│  │  ├─ 首页代码 (50KB)                  │                    │
│  │  ├─ 仪表盘代码 (100KB)               │ ← 用户可能永远不去 │
│  │  ├─ 设置页代码 (80KB)                │ ← 用户可能永远不去 │
│  │  ├─ 图表库 (120KB)                   │ ← 不一定用到       │
│  │  ├─ 表格库 (80KB)                    │ ← 不一定用到       │
│  │  └─ 工具函数 (70KB)                  │                    │
│  └──────────────────────────────────────┘                    │
│                                                              │
│  问题：                                                       │
│  - 首页用户下载了 500KB，但只需要 50KB                        │
│  - 首屏加载时间长                                             │
│  - 解析和执行时间长                                           │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

```
┌──────────────────────────────────────────────────────────────┐
│              代码分割后                                        │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  按路由分割：                                                 │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐                   │
│  │ main.js  │  │ dash.js  │  │ sett.js  │                   │
│  │ 50KB     │  │ 180KB    │  │ 150KB    │                   │
│  │ 首页必需  │  │ 按需加载  │  │ 按需加载  │                   │
│  └──────────┘  └──────────┘  └──────────┘                   │
│                                                              │
│  首页只加载 50KB → 首屏快 10 倍                               │
│  访问仪表盘时才加载 180KB                                     │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

---

## 二、路由级代码分割

### 2.1 React.lazy

```jsx
import { lazy, Suspense } from 'react';

// 路由级代码分割
const Dashboard = lazy(() => import('./pages/Dashboard'));
const Settings = lazy(() => import('./pages/Settings'));
const Analytics = lazy(() => import('./pages/Analytics'));

function App() {
  return (
    <Suspense fallback={<LoadingSpinner />}>
      <Routes>
        <Route path="/" element={<Home />} />
        <Route path="/dashboard" element={<Dashboard />} />
        <Route path="/settings" element={<Settings />} />
        <Route path="/analytics" element={<Analytics />} />
      </Routes>
    </Suspense>
  );
}
```

### 2.2 Next.js 自动分割

```
App Router 自动按路由分割：

app/
├── page.tsx           → 首页 chunk
├── dashboard/
│   └── page.tsx       → 仪表盘 chunk
├── settings/
│   └── page.tsx       → 设置 chunk
└── analytics/
    └── page.tsx       → 分析 chunk

每个页面自动成为独立的 chunk，无需手动配置
```

---

## 三、组件级代码分割

```jsx
import { lazy, Suspense } from 'react';

// 重量级组件按需加载
const HeavyChart = lazy(() => import('./HeavyChart'));
const RichTextEditor = lazy(() => import('./RichTextEditor'));
const PDFViewer = lazy(() => import('./PDFViewer'));

function Dashboard() {
  const [showChart, setShowChart] = useState(false);

  return (
    <div>
      <h1>Dashboard</h1>

      {/* 图表只在用户点击时加载 */}
      <button onClick={() => setShowChart(true)}>Show Chart</button>
      {showChart && (
        <Suspense fallback={<ChartSkeleton />}>
          <HeavyChart />
        </Suspense>
      )}
    </div>
  );
}
```

---

## 四、动态导入

### 4.1 基本动态导入

```javascript
// 条件加载
async function loadData(useV2) {
  if (useV2) {
    const { fetchV2 } = await import('./api-v2');
    return fetchV2();
  } else {
    const { fetchV1 } = await import('./api-v1');
    return fetchV1();
  }
}

// 按需加载工具库
async function exportToPDF(data) {
  const { jsPDF } = await import('jspdf');
  const doc = new jsPDF();
  // ...
  doc.save('export.pdf');
}
```

### 4.2 命名导出的动态导入

```javascript
// 导入单个导出
const { formatDate } = await import('./utils');

// 导入默认导出
const Chart = (await import('./Chart')).default;

// 导入多个
const { useState, useEffect } = await import('react');
```

---

## 五、预加载策略

### 5.1 鼠标悬停预加载

```jsx
function NavLink({ to, children }) {
  const handleMouseEnter = () => {
    // 鼠标悬停时预加载路由
    const route = routes[to];
    if (route?.component) {
      route.component.preload?.();
    }
  };

  return (
    <Link to={to} onMouseEnter={handleMouseEnter}>
      {children}
    </Link>
  );
}
```

### 5.2 可视区域预加载

```jsx
import { useEffect, useRef } from 'react';

function PreloadOnVisible({ children, load }) {
  const ref = useRef();

  useEffect(() => {
    const observer = new IntersectionObserver(
      ([entry]) => {
        if (entry.isIntersecting) {
          load(); // 组件进入可视区域时预加载
          observer.disconnect();
        }
      },
      { rootMargin: '200px' } // 提前 200px 开始加载
    );

    if (ref.current) observer.observe(ref.current);
    return () => observer.disconnect();
  }, [load]);

  return <div ref={ref}>{children}</div>;
}

// 使用
<PreloadOnVisible load={() => import('./HeavyChart')}>
  <ChartPlaceholder />
</PreloadOnVisible>
```

### 5.3 空闲时预加载

```javascript
// requestIdleCallback 预加载
function preloadWhenIdle(modulePath) {
  if ('requestIdleCallback' in window) {
    requestIdleCallback(() => {
      import(modulePath);
    });
  } else {
    setTimeout(() => import(modulePath), 1);
  }
}

// 应用启动后预加载可能需要的模块
preloadWhenIdle('./pages/Dashboard');
preloadWhenIdle('./components/HeavyChart');
```

---

## 六、打包体积分析

### 6.1 Vite

```bash
# 安装分析插件
npm install rollup-plugin-visualizer

# vite.config.ts
import { visualizer } from 'rollup-plugin-visualizer';

export default {
  plugins: [
    visualizer({
      open: true,
      filename: 'stats.html',
    }),
  ],
};

# 构建后自动打开分析报告
npm run build
```

### 6.2 Next.js

```bash
# 安装分析工具
npm install @next/bundle-analyzer

# next.config.js
const withBundleAnalyzer = require('@next/bundle-analyzer')({
  enabled: process.env.ANALYZE === 'true',
});

module.exports = withBundleAnalyzer({});

# 运行分析
ANALYZE=true npm run build
```

### 6.3 通用分析

```bash
# source-map-explorer
npx source-map-explorer build/static/js/*.js

# webpack-bundle-analyzer
npx webpack-bundle-analyzer stats.json
```

---

## 七、优化打包体积的技巧

### 7.1 替换大型库

```
┌───────────────────────┬────────────────────┬─────────────────┐
│ 大型库                 │ 轻量替代            │ 体积减少         │
├───────────────────────┼────────────────────┼─────────────────┤
│ moment.js (300KB)     │ date-fns (5KB 按需) │ 98%             │
│ lodash (70KB)         │ lodash-es (按需)    │ 按需加载         │
│ axios (15KB)          │ fetch (0KB)         │ 100%            │
│ chart.js (200KB)      │ 按需导入            │ 60-80%          │
│ antd (500KB+)         │ 按需导入            │ 80%+            │
└───────────────────────┴────────────────────┴─────────────────┘
```

### 7.2 Tree Shaking

```javascript
// ❌ 导入整个库 → 无法 tree shake
import _ from 'lodash';
_.get(obj, 'a.b');

// ✅ 只导入需要的函数
import get from 'lodash-es/get';
get(obj, 'a.b');

// ✅ 或使用原生可选链
obj?.a?.b;
```

### 7.3 动态导入大型依赖

```javascript
// 只在需要时加载大型库
async function generateChart(data) {
  const { Chart } = await import('chart.js');
  // ...
}

async function exportPDF(data) {
  const { jsPDF } = await import('jspdf');
  // ...
}
```

---

## 八、检查清单

```
┌──────────────────────────────────────────────────────────────┐
│              代码分割检查清单                                  │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  分割策略                                                    │
│  □ 每个路由是独立的 chunk                                    │
│  □ 重量级组件（图表、编辑器）按需加载                         │
│  □ 大型依赖（PDF、Excel）动态导入                            │
│                                                              │
│  预加载                                                      │
│  □ 鼠标悬停预加载路由                                        │
│  □ 可视区域预加载组件                                        │
│  □ 空闲时预加载可能需要的模块                                │
│                                                              │
│  体积优化                                                    │
│  □ 已分析打包体积，找到大依赖                                │
│  □ 大型库已替换为轻量替代                                    │
│  □ Tree Shaking 生效（使用 ES 模块导入）                     │
│  □ 初始 JS < 200KB (gzipped)                                │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

---

## 动手练习

### 练习一：路由级分割

1. 创建一个多页面应用
2. 用 React.lazy 实现路由级代码分割
3. 用 Network 面板验证只加载当前路由的 JS

### 练习二：组件级分割

1. 找到项目中最重的组件
2. 用 lazy + Suspense 实现按需加载
3. 添加鼠标悬停预加载

### 练习三：体积分析

1. 用 bundle analyzer 分析项目打包体积
2. 找到最大的 3 个依赖
3. 尝试替换或按需导入，减少体积

---

## 小结

1. **代码分割**：把大包拆成按需加载的小块
2. **路由级分割**：每个路由一个 chunk，首屏只加载首页
3. **组件级分割**：重量级组件 lazy 加载
4. **预加载**：鼠标悬停、可视区域、空闲时
5. **体积优化**：替换大型库、Tree Shaking、动态导入

---

## 下一课预告

下一课将学习 HTTP 缓存和 CDN——让重复访问的资源直接从缓存加载。
