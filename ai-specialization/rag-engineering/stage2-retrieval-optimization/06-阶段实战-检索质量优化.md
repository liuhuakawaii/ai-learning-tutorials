# Stage 2 · L6: 阶段实战——检索质量优化

> **时长**: 90 分钟 | **前置**: L1-L5 全部完成

## 场景引入

你已经学了混合检索、Reranking、查询扩展、多路召回和评估指标，每项技术单独看都有价值。但你的老板不会让你只优化其中一个环节——他要求你在两周内把 RAG 系统的整体检索质量提升 20%，并且要拿出数据证明每一步优化的贡献。你需要的是一个系统化的方法论：从基线开始，逐步叠加优化手段，每一步都用指标量化增量效果，最终形成一份完整的优化报告。

## 学习目标

完成本课后，你将能够：

1. 构建完整的优化检索管线（混合检索 + 查询扩展 + Reranker + 压缩）
2. 进行 A/B 对比实验，量化各优化步骤的贡献
3. 度量端到端影响（检索质量 → 生成质量）
4. 记录并文档化优化决策，形成可复用的优化方法论

---

## 1. 实战目标

在本课中，我们将以一个真实的文档问答场景为背景，从基线开始，逐步优化检索管线，每一步都用指标驱动决策。

```
┌─────────────────────────────────────────────────────────────────┐
│                    实战优化路线图                                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Step 0: 基线 (纯向量检索)                                       │
│     │                                                           │
│     ▼                                                           │
│  Step 1: + BM25 混合检索                                        │
│     │                                                           │
│     ▼                                                           │
│  Step 2: + 查询扩展 (Multi-Query)                                │
│     │                                                           │
│     ▼                                                           │
│  Step 3: + Reranker 精排                                        │
│     │                                                           │
│     ▼                                                           │
│  Step 4: + 上下文压缩                                            │
│     │                                                           │
│     ▼                                                           │
│  Step 5: A/B 对比 + 端到端评估                                   │
│     │                                                           │
│     ▼                                                           │
│  最终优化管线                                                    │
│                                                                 │
│  每一步: 记录指标 → 对比增量 → 决策是否保留                       │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 2. 完整优化检索器实现

以下是一个生产级的优化检索器，整合了 Stage 2 所有技术：

```python
"""
Stage 2 阶段实战: 完整优化检索管线
整合: 混合检索 + 查询扩展 + Reranker + 上下文压缩 + 评估
"""
import os
import json
import math
import time
import hashlib
from typing import List, Dict, Set, Optional, Tuple, Callable
from dataclasses import dataclass, field, asdict
from collections import defaultdict
from datetime import datetime

from langchain.schema import Document
from langchain.text_splitter import RecursiveCharacterTextSplitter
from langchain_community.vectorstores import Chroma
from langchain_community.retrievers import BM25Retriever
from langchain_openai import OpenAIEmbeddings, ChatOpenAI
from langchain.prompts import ChatPromptTemplate
from sentence_transformers import CrossEncoder


# ============================================================
# 评估指标
# ============================================================

def recall_at_k(retrieved: List[str], relevant: Set[str], k: int) -> float:
    if not relevant:
        return 0.0
    return len(set(retrieved[:k]) & relevant) / len(relevant)

def precision_at_k(retrieved: List[str], relevant: Set[str], k: int) -> float:
    if k == 0:
        return 0.0
    return sum(1 for d in retrieved[:k] if d in relevant) / k

def mrr(retrieved: List[str], relevant: Set[str], k: int = 10) -> float:
    for i, d in enumerate(retrieved[:k]):
        if d in relevant:
            return 1.0 / (i + 1)
    return 0.0

def ndcg_at_k(retrieved: List[str], relevant: Set[str], k: int) -> float:
    if not relevant:
        return 0.0
    dcg = sum(
        (1.0 if retrieved[i] in relevant else 0.0) / math.log2(i + 2)
        for i in range(min(k, len(retrieved)))
    )
    ideal = sorted(
        [1.0 if retrieved[i] in relevant else 0.0 for i in range(min(k, len(retrieved)))],
        reverse=True
    )
    while len(ideal) < min(len(relevant), k):
        ideal.append(1.0)
    idcg = sum(r / math.log2(i + 2) for i, r in enumerate(ideal[:k]))
    return dcg / idcg if idcg > 0 else 0.0


# ============================================================
# 查询扩展 (Multi-Query)
# ============================================================

class QueryExpander:
    """使用 LLM 将查询扩展为多个变体"""

    def __init__(self, model: str = "gpt-4o-mini"):
        self.llm = ChatOpenAI(model=model, temperature=0.3)
        self.prompt = ChatPromptTemplate.from_template("""
你是一个查询改写专家。请将以下查询改写为 3 个不同的变体，以提高检索召回率。
要求:
1. 保留原始查询的核心意图
2. 使用不同的表述方式（同义词、更具体、更抽象）
3. 每行一个变体，不要编号

原始查询: {query}

改写后的查询:
""")

    def expand(self, query: str) -> List[str]:
        """扩展查询为多个变体"""
        chain = self.prompt | self.llm
        response = chain.invoke({"query": query})
        variants = [
            line.strip()
            for line in response.content.strip().split("\n")
            if line.strip() and line.strip() != query
        ]
        return [query] + variants  # 包含原始查询


# ============================================================
# 上下文压缩器
# ============================================================

class ContextualCompressor:
    """使用 LLM 压缩文档，只保留与查询相关的部分"""

    def __init__(self, model: str = "gpt-4o-mini"):
        self.llm = ChatOpenAI(model=model, temperature=0)
        self.prompt = ChatPromptTemplate.from_template("""
从以下文档中提取与查询最相关的部分。
规则:
- 保留原文，不要改写
- 只保留直接回答查询的内容
- 如果文档与查询无关，输出 "IRRELEVANT"

查询: {query}
文档: {document}

压缩后:
""")

    def compress(self, query: str, documents: List[Document]) -> List[Document]:
        chain = self.prompt | self.llm
        results = []
        for doc in documents:
            response = chain.invoke({"query": query, "document": doc.page_content})
            content = response.content.strip()
            if content != "IRRELEVANT" and len(content) > 10:
                results.append(Document(
                    page_content=content,
                    metadata={**doc.metadata, "compressed": True}
                ))
        return results


# ============================================================
# 优化检索器主类
# ============================================================

@dataclass
class RetrieverConfig:
    """检索器配置"""
    use_bm25: bool = True
    use_query_expansion: bool = True
    use_reranker: bool = True
    use_compression: bool = True
    bm25_weight: float = 0.4
    vector_weight: float = 0.6
    rrf_k: int = 60
    child_k: int = 15
    retrieval_k: int = 10
    final_k: int = 5
    reranker_model: str = "BAAI/bge-reranker-v2-m3"
    llm_model: str = "gpt-4o-mini"


class OptimizedRetriever:
    """
    Stage 2 优化检索器

    支持的优化步骤（可独立开关）:
    1. BM25 混合检索
    2. Multi-Query 查询扩展
    3. Reranker 精排
    4. 上下文压缩
    """

    def __init__(
        self,
        documents: List[Document],
        config: RetrieverConfig = None
    ):
        self.config = config or RetrieverConfig()
        self.embeddings = OpenAIEmbeddings()
        self.doc_id_map: Dict[str, Document] = {}

        # 构建索引
        self._build_vector_index(documents)
        if self.config.use_bm25:
            self._build_bm25_index(documents)

        # 可选组件
        if self.config.use_query_expansion:
            self.query_expander = QueryExpander(self.config.llm_model)
        if self.config.use_reranker:
            self.reranker = CrossEncoder(self.config.reranker_model, max_length=512)
        if self.config.use_compression:
            self.compressor = ContextualCompressor(self.config.llm_model)

    def _get_doc_id(self, doc: Document) -> str:
        return hashlib.md5(doc.page_content.encode()).hexdigest()[:12]

    def _build_vector_index(self, documents: List[Document]):
        for doc in documents:
            doc_id = self._get_doc_id(doc)
            doc.metadata["doc_id"] = doc_id
            self.doc_id_map[doc_id] = doc
        self.vectorstore = Chroma.from_documents(documents, self.embeddings)

    def _build_bm25_index(self, documents: List[Document]):
        self.bm25 = BM25Retriever.from_documents(documents)
        self.bm25.k = self.config.retrieval_k

    def _rrf_fusion(
        self,
        results_list: List[List[Tuple[str, float, Document]]]
    ) -> List[Tuple[float, Document]]:
        k = self.config.rrf_k
        scores: Dict[str, float] = defaultdict(float)
        doc_map: Dict[str, Document] = {}

        for results in results_list:
            for rank, (doc_id, _, doc) in enumerate(results):
                scores[doc_id] += 1.0 / (k + rank + 1)
                doc_map[doc_id] = doc

        sorted_docs = sorted(scores.items(), key=lambda x: x[1], reverse=True)
        return [(s, doc_map[did]) for did, s in sorted_docs]

    def retrieve(
        self,
        query: str,
        metadata_filter: Optional[Dict] = None
    ) -> List[Dict]:
        """
        执行优化检索

        Returns:
            [{"content": str, "metadata": dict, "score": float}, ...]
        """
        # Step 1: 查询扩展
        queries = [query]
        if self.config.use_query_expansion:
            queries = self.query_expander.expand(query)

        # Step 2: 多路检索（对每个查询变体）
        all_vector_results = []
        all_bm25_results = []

        for q in queries:
            # 向量检索
            vf = metadata_filter
            vec_docs = self.vectorstore.similarity_search_with_score(
                q, k=self.config.retrieval_k, filter=vf
            )
            all_vector_results.extend([
                (self._get_doc_id(doc), score, doc) for doc, score in vec_docs
            ])

            # BM25 检索
            if self.config.use_bm25:
                bm25_docs = self.bm25.get_relevant_documents(q)
                all_bm25_results.extend([
                    (self._get_doc_id(doc), 0.0, doc) for doc in bm25_docs
                ])

        # 去重（保留最高分）
        def dedup(results: List[Tuple[str, float, Document]]) -> List[Tuple[str, float, Document]]:
            seen = {}
            for doc_id, score, doc in results:
                if doc_id not in seen or score > seen[doc_id][1]:
                    seen[doc_id] = (doc_id, score, doc)
            return list(seen.values())

        vector_results = dedup(all_vector_results)
        bm25_results = dedup(all_bm25_results) if self.config.use_bm25 else []

        # Step 3: RRF 融合
        to_fuse = [vector_results]
        if bm25_results:
            to_fuse.append(bm25_results)
        fused = self._rrf_fusion(to_fuse)

        # Step 4: Reranker 精排
        if self.config.use_reranker and fused:
            pairs = [(query, doc.page_content) for _, doc in fused]
            rerank_scores = self.reranker.predict(pairs)
            scored = list(zip(rerank_scores, [doc for _, doc in fused]))
            scored.sort(key=lambda x: x[0], reverse=True)
            fused = [(float(s), doc) for s, doc in scored]

        # Step 5: 上下文压缩
        final_docs = fused[:self.config.final_k]
        if self.config.use_compression:
            docs_to_compress = [doc for _, doc in final_docs]
            compressed = self.compressor.compress(query, docs_to_compress)
            final_docs = [(score, doc) for (score, _), doc in zip(final_docs, compressed)]

        # 构建结果
        return [
            {
                "content": doc.page_content,
                "metadata": doc.metadata,
                "score": score,
                "doc_id": doc.metadata.get("doc_id", "")
            }
            for score, doc in final_docs
        ]


# ============================================================
# 评估数据集
# ============================================================

@dataclass
class EvalCase:
    query: str
    relevant_doc_ids: Set[str]
    category: str = "general"
    difficulty: str = "medium"


class EvalDataset:
    """评估数据集"""

    def __init__(self):
        self.cases: List[EvalCase] = []

    def add(self, query: str, relevant_ids: List[str], **kwargs):
        self.cases.append(EvalCase(
            query=query,
            relevant_doc_ids=set(relevant_ids),
            **kwargs
        ))

    def load_from_json(self, filepath: str):
        with open(filepath, "r", encoding="utf-8") as f:
            data = json.load(f)
        for ann in data.get("annotations", []):
            self.add(
                query=ann["query"],
                relevant_ids=ann["relevant_doc_ids"],
                category=ann.get("category", "general"),
                difficulty=ann.get("difficulty", "medium")
            )

    @property
    def queries(self) -> List[str]:
        return [c.query for c in self.cases]


# ============================================================
# A/B 评估运行器
# ============================================================

@dataclass
class ABResult:
    config_name: str
    recall_at_5: float
    precision_at_5: float
    mrr: float
    ndcg_at_5: float
    avg_latency_ms: float
    per_query: List[Dict] = field(default_factory=list)


class ABEvaluator:
    """A/B 对比评估器"""

    def __init__(self, dataset: EvalDataset):
        self.dataset = dataset

    def evaluate(
        self,
        config_name: str,
        retrieve_fn: Callable[[str], List[Dict]],
        k: int = 5
    ) -> ABResult:
        """评估单个配置"""
        per_query = []
        total_latency = 0.0

        for case in self.dataset.cases:
            start = time.time()
            results = retrieve_fn(case.query)
            latency = (time.time() - start) * 1000
            total_latency += latency

            doc_ids = [r.get("doc_id", "") for r in results]

            per_query.append({
                "query": case.query,
                "category": case.category,
                "difficulty": case.difficulty,
                "recall": recall_at_k(doc_ids, case.relevant_doc_ids, k),
                "precision": precision_at_k(doc_ids, case.relevant_doc_ids, k),
                "rr": mrr(doc_ids, case.relevant_doc_ids, k),
                "ndcg": ndcg_at_k(doc_ids, case.relevant_doc_ids, k),
                "latency_ms": latency
            })

        n = len(self.dataset.cases)
        return ABResult(
            config_name=config_name,
            recall_at_5=sum(r["recall"] for r in per_query) / n,
            precision_at_5=sum(r["precision"] for r in per_query) / n,
            mrr=sum(r["rr"] for r in per_query) / n,
            ndcg_at_5=sum(r["ndcg"] for r in per_query) / n,
            avg_latency_ms=total_latency / n,
            per_query=per_query
        )

    @staticmethod
    def compare(results: List[ABResult]) -> str:
        """生成对比报告"""
        lines = []
        lines.append("=" * 85)
        lines.append(f"{'Config':<25} {'Recall@5':<10} {'Prec@5':<10} {'MRR':<10} {'NDCG@5':<10} {'Latency':<10}")
        lines.append("-" * 85)

        baseline = results[0] if results else None
        for r in results:
            lines.append(
                f"{r.config_name:<25} "
                f"{r.recall_at_5:<10.3f} "
                f"{r.precision_at_5:<10.3f} "
                f"{r.mrr:<10.3f} "
                f"{r.ndcg_at_5:<10.3f} "
                f"{r.avg_latency_ms:<10.0f}ms"
            )

        # 增量对比
        if baseline and len(results) > 1:
            lines.append("-" * 85)
            lines.append("增量对比 (相对基线):")
            for r in results[1:]:
                d_recall = r.recall_at_5 - baseline.recall_at_5
                d_mrr = r.mrr - baseline.mrr
                d_lat = r.avg_latency_ms - baseline.avg_latency_ms
                lines.append(
                    f"  {r.config_name:<23} "
                    f"Recall: {d_recall:+.3f} | "
                    f"MRR: {d_mrr:+.3f} | "
                    f"Latency: {d_lat:+.0f}ms"
                )

        lines.append("=" * 85)
        return "\n".join(lines)

    @staticmethod
    def export(results: List[ABResult], filepath: str):
        data = {
            "experiments": [asdict(r) for r in results],
            "exported_at": datetime.now().isoformat()
        }
        with open(filepath, "w", encoding="utf-8") as f:
            json.dump(data, f, ensure_ascii=False, indent=2)
        print(f"结果已导出: {filepath}")


# ============================================================
# 端到端评估: 检索质量 → 生成质量
# ============================================================

class EndToEndEvaluator:
    """
    端到端评估: 检索到的内容 → LLM 生成的回答质量

    评估维度:
    - 相关性: 回答是否与问题相关
    - 完整性: 回答是否涵盖了关键信息
    - 忠实度: 回答是否基于检索到的文档（不幻觉）
    """

    def __init__(self, model: str = "gpt-4o-mini"):
        self.llm = ChatOpenAI(model=model, temperature=0)
        self.eval_prompt = ChatPromptTemplate.from_template("""
你是一个 RAG 系统评估专家。请评估以下回答的质量。

用户问题: {question}

检索到的文档:
{context}

系统回答: {answer}

请从以下三个维度打分 (0-5 分):
1. 相关性: 回答是否与问题相关
2. 完整性: 回答是否涵盖了关键信息
3. 忠实度: 回答是否基于检索文档（不编造信息）

输出 JSON 格式:
{{"relevance": <分数>, "completeness": <分数>, "faithfulness": <分数>, "reason": "<简要说明>"}}
""")

    def evaluate_single(
        self,
        question: str,
        context_docs: List[str],
        answer: str
    ) -> Dict:
        """评估单个 QA 对"""
        context = "\n---\n".join(context_docs)
        chain = self.eval_prompt | self.llm
        response = chain.invoke({
            "question": question,
            "context": context,
            "answer": answer
        })
        try:
            return json.loads(response.content)
        except json.JSONDecodeError:
            return {"relevance": 0, "completeness": 0, "faithfulness": 0, "reason": "解析失败"}

    def evaluate_batch(
        self,
        qa_pairs: List[Dict]
    ) -> Dict[str, float]:
        """
        批量端到端评估

        Args:
            qa_pairs: [{"question": str, "context_docs": [str], "answer": str}, ...]

        Returns:
            {"mean_relevance": float, "mean_completeness": float, ...}
        """
        results = []
        for qa in qa_pairs:
            result = self.evaluate_single(
                qa["question"], qa["context_docs"], qa["answer"]
            )
            results.append(result)

        n = len(results)
        return {
            "mean_relevance": sum(r.get("relevance", 0) for r in results) / n,
            "mean_completeness": sum(r.get("completeness", 0) for r in results) / n,
            "mean_faithfulness": sum(r.get("faithfulness", 0) for r in results) / n,
            "num_evaluated": n,
            "per_question": results
        }


# ============================================================
# 主流程: 完整 A/B 实验
# ============================================================

def run_full_experiment(
    documents: List[Document],
    dataset: EvalDataset,
    export_path: str = "ab_results.json"
):
    """
    运行完整的 A/B 实验

    对比 5 种配置:
    A) 纯向量检索 (基线)
    B) + BM25 混合检索
    C) + 查询扩展
    D) + Reranker
    E) + 上下文压缩 (全量优化)
    """
    evaluator = ABEvaluator(dataset)
    results = []

    # --- 配置 A: 纯向量检索 ---
    print("\n[Config A] 纯向量检索 (基线)...")
    config_a = RetrieverConfig(
        use_bm25=False, use_query_expansion=False,
        use_reranker=False, use_compression=False
    )
    retriever_a = OptimizedRetriever(documents, config_a)
    result_a = evaluator.evaluate("A: Vector Only", retriever_a.retrieve)
    results.append(result_a)

    # --- 配置 B: + BM25 ---
    print("[Config B] 向量 + BM25...")
    config_b = RetrieverConfig(
        use_bm25=True, use_query_expansion=False,
        use_reranker=False, use_compression=False
    )
    retriever_b = OptimizedRetriever(documents, config_b)
    result_b = evaluator.evaluate("B: + BM25", retriever_b.retrieve)
    results.append(result_b)

    # --- 配置 C: + 查询扩展 ---
    print("[Config C] 向量 + BM25 + 查询扩展...")
    config_c = RetrieverConfig(
        use_bm25=True, use_query_expansion=True,
        use_reranker=False, use_compression=False
    )
    retriever_c = OptimizedRetriever(documents, config_c)
    result_c = evaluator.evaluate("C: + Query Expansion", retriever_c.retrieve)
    results.append(result_c)

    # --- 配置 D: + Reranker ---
    print("[Config D] 向量 + BM25 + 查询扩展 + Reranker...")
    config_d = RetrieverConfig(
        use_bm25=True, use_query_expansion=True,
        use_reranker=True, use_compression=False
    )
    retriever_d = OptimizedRetriever(documents, config_d)
    result_d = evaluator.evaluate("D: + Reranker", retriever_d.retrieve)
    results.append(result_d)

    # --- 配置 E: 全量优化 ---
    print("[Config E] 全量优化 (所有技术)...")
    config_e = RetrieverConfig(
        use_bm25=True, use_query_expansion=True,
        use_reranker=True, use_compression=True
    )
    retriever_e = OptimizedRetriever(documents, config_e)
    result_e = evaluator.evaluate("E: Full Pipeline", retriever_e.retrieve)
    results.append(result_e)

    # 对比报告
    print("\n" + ABEvaluator.compare(results))

    # 导出
    ABEvaluator.export(results, export_path)

    return results


# ============================================================
# 使用示例
# ============================================================

if __name__ == "__main__":
    # 1. 准备文档
    documents = [
        Document(
            page_content="iPhone 15 Pro Max 配备 4422mAh 电池，支持 27W 有线快充和 15W MagSafe 无线充电。钛金属机身，A17 Pro 芯片。",
            metadata={"source": "product-spec", "category": "phone", "product": "iphone-15-pro-max"}
        ),
        Document(
            page_content="Samsung Galaxy S24 Ultra 电池容量 5000mAh，支持 45W 快充。搭载骁龙 8 Gen 3 处理器，钛金属框架。",
            metadata={"source": "product-spec", "category": "phone", "product": "galaxy-s24-ultra"}
        ),
        Document(
            page_content="手机续航优化技巧：关闭后台应用刷新、降低屏幕亮度、启用省电模式可延长 20-30% 续航。",
            metadata={"source": "guide", "category": "tips", "topic": "battery"}
        ),
        Document(
            page_content="Django 是 Python 最流行的 Web 框架，遵循 MVT 架构。内置 ORM、Admin 后台、用户认证系统。适合大型企业应用。",
            metadata={"source": "tech-doc", "category": "framework", "topic": "django"}
        ),
        Document(
            page_content="FastAPI 是现代高性能 Web 框架，基于 Python 类型提示。自动生成 OpenAPI 文档，支持异步，性能接近 Go。",
            metadata={"source": "tech-doc", "category": "framework", "topic": "fastapi"}
        ),
        Document(
            page_content="Flask 是轻量级微框架，不强制项目结构。通过扩展支持数据库、认证等功能。适合 API 和小型项目。",
            metadata={"source": "tech-doc", "category": "framework", "topic": "flask"}
        ),
        Document(
            page_content="Python Web 框架性能基准测试：FastAPI (async) 约 10000 req/s，Flask 约 3000 req/s，Django 约 2000 req/s。",
            metadata={"source": "benchmark", "category": "performance", "topic": "web-frameworks"}
        ),
        Document(
            page_content="退款政策：购买后 30 天内可申请全额退款。需提供订单号和退款原因。电子商品激活后不可退款。",
            metadata={"source": "policy", "category": "refund", "effective": "2024"}
        ),
        Document(
            page_content="退款流程：登录账户 → 订单管理 → 选择订单 → 申请退款 → 等待审核 (1-3 工作日) → 退款到账。",
            metadata={"source": "help-center", "category": "refund", "topic": "process"}
        ),
        Document(
            page_content="2024年退款政策更新：支持自动退款，金额 500 元以下无需人工审核，即时到账。",
            metadata={"source": "changelog", "category": "refund", "effective": "2024-Q3"}
        ),
    ]

    # 2. 构建评估数据集
    dataset = EvalDataset()
    dataset.add(
        "iPhone 15 Pro Max 电池容量是多少",
        ["iphone-15-pro-max"],
        category="product-spec", difficulty="easy"
    )
    dataset.add(
        "哪个手机电池最大",
        ["iphone-15-pro-max", "galaxy-s24-ultra"],
        category="comparison", difficulty="medium"
    )
    dataset.add(
        "Python Web 框架哪个性能最好",
        ["fastapi", "web-frameworks"],
        category="tech-comparison", difficulty="medium"
    )
    dataset.add(
        "如何申请退款",
        ["refund-process", "refund-policy-2024"],
        category="how-to", difficulty="easy"
    )
    dataset.add(
        "Django 适合什么场景",
        ["django"],
        category="tech-guide", difficulty="easy"
    )

    # 3. 运行实验
    results = run_full_experiment(documents, dataset, "stage2_ab_results.json")
```

---

## 3. 决策文档模板

每次优化实验后，记录决策过程：

```
┌─────────────────────────────────────────────────────────────────┐
│                    优化决策记录模板                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  实验日期: YYYY-MM-DD                                           │
│  数据集: xxx (N 个查询, M 个文档)                                │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │ 配置         │ Recall@5 │ MRR    │ NDCG@5 │ Latency   │    │
│  ├──────────────┼──────────┼────────┼────────┼───────────┤    │
│  │ A: 基线      │  0.650   │ 0.580  │ 0.620  │  50ms     │    │
│  │ B: +BM25     │  0.750   │ 0.680  │ 0.720  │  65ms     │    │
│  │ C: +扩展     │  0.820   │ 0.700  │ 0.780  │  200ms    │    │
│  │ D: +Reranker │  0.850   │ 0.800  │ 0.830  │  250ms    │    │
│  │ E: +压缩     │  0.850   │ 0.800  │ 0.830  │  400ms    │    │
│  └──────────────┴──────────┴────────┴────────┴───────────┘    │
│                                                                 │
│  决策:                                                          │
│  · BM25 混合检索: ✅ 保留 (Recall +15%, 延迟 +15ms 可接受)      │
│  · 查询扩展:     ✅ 保留 (Recall +7%, 但延迟 +135ms)            │
│  · Reranker:     ✅ 保留 (MRR +10%, 排序质量显著提升)            │
│  · 上下文压缩:   ⚠️ 条件启用 (延迟 +150ms, 仅在 Top-K>5 时使用) │
│                                                                 │
│  最终配置: B+C+D (延迟 250ms, Recall 0.85, MRR 0.80)           │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 4. 常见误区

### ❌ 错误 1: 一次改太多

```python
# 错误: 同时开启所有优化，不知道哪个起了作用
config = RetrieverConfig(
    use_bm25=True, use_query_expansion=True,
    use_reranker=True, use_compression=True
)

# 正确: 逐个开启，记录每步增量
# Step 1: 只开 BM25 → 对比基线
# Step 2: 开 BM25 + 查询扩展 → 对比 Step 1
# ...
```

### ❌ 错误 2: 评估数据集太小或太简单

```python
# 错误: 5 个查询，全部是 easy 难度
dataset.add("iPhone 电池", ["doc1"])  # 太简单

# 正确: 50+ 查询，覆盖 easy/medium/hard
dataset.add("iPhone 电池", ["doc1"], difficulty="easy")
dataset.add("哪个手机续航最好", ["doc1", "doc5"], difficulty="medium")
dataset.add("对比旗舰手机的充电速度和电池容量", [...], difficulty="hard")
```

### ❌ 错误 3: 忽略延迟成本

```python
# 错误: Recall 从 0.80 提升到 0.82，延迟从 100ms 涨到 800ms
# 用户体验: 从"秒回"变成"等一下"

# 正确: 设置延迟预算
MAX_LATENCY_MS = 500
if result.avg_latency_ms > MAX_LATENCY_MS:
    print("⚠️ 超出延迟预算，考虑移除查询扩展或压缩")
```

### ❌ 错误 4: 不区分查询类型

```python
# 错误: 所有查询用同一套配置
# 事实: 简单查询不需要查询扩展，复杂查询才需要

# 正确: 根据查询复杂度动态调整
def adaptive_retrieve(query, config):
    if is_simple_query(query):
        config.use_query_expansion = False  # 跳过扩展
    return retriever.retrieve(query)
```

### ❌ 错误 5: 只看指标不看 Bad Cases

```python
# 错误: 整体 Recall = 0.85，很满意
# 但: 有 3 个重要查询 Recall = 0，完全检索不到

# 正确: 找出 Bad Cases 并分析
bad_cases = [r for r in per_query if r["recall"] < 0.3]
for case in bad_cases:
    print(f"Query: {case['query']} | Recall: {case['recall']}")
    # 分析: 为什么检索不到？分块问题？Embedding 问题？
```

---

## 5. 工程建议

1. **一次只改一个变量**：在做优化实验时，每次只修改一个参数或组件。如果同时改了混合检索权重和 Reranker 模型，你无法判断效果提升来自哪个改动。
2. **优化决策要文档化**：记录每次实验的配置、指标结果和决策理由。三个月后当你需要回顾为什么选择某个参数时，这些记录就是最宝贵的资产。
3. **端到端评估比单一环节评估更重要**：检索 Recall 提升了 10% 但最终答案质量没变，说明瓶颈不在检索环节。始终以端到端指标（答案正确率、用户满意度）为最终衡量标准。
4. **建立持续优化的节奏**：检索优化不是一次性工作。建议每月用新收集的用户查询更新评估集，跑一轮完整的指标对比，持续迭代优化。

---

## 6. 本课总结

```
┌─────────────────────────────────────────────────────────────────┐
│                       L6 核心要点                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  1. 优化必须指标驱动，不能凭感觉                                  │
│                                                                 │
│  2. 逐个开启优化，记录每步增量贡献                                 │
│                                                                 │
│  3. A/B 对比是验证优化效果的标准方法                               │
│                                                                 │
│  4. 端到端评估连接检索质量与生成质量                               │
│                                                                 │
│  5. 效果 vs 延迟的权衡是工程决策的核心                             │
│                                                                 │
│  6. 记录决策过程，形成可复用的优化方法论                           │
│                                                                 │
│  7. Bad Case 分析比平均指标更有价值                               │
│                                                                 │
│  8. Stage 2 优化技术组合:                                        │
│     混合检索 + 查询扩展 + Reranker + 元数据过滤 + 压缩            │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 练习

### 练习 1: 构建自己的评估数据集

```python
"""
要求:
1. 选择一个你熟悉的领域（技术文档、产品手册、FAQ 等）
2. 准备 30+ 个文档
3. 编写 50+ 个查询并标注相关文档
4. 覆盖三个难度级别: easy / medium / hard
5. 使用 EvalDataset 保存数据集
6. 统计数据集质量指标
"""
# YOUR CODE HERE
```

### 练习 2: 逐步优化实验

```python
"""
要求:
1. 使用练习 1 的数据集
2. 实现 5 种配置（基线 → 全量优化）
3. 运行 A/B 对比实验
4. 生成对比表格和增量分析
5. 找出表现最差的 5 个查询，分析原因并提出改进方案
6. 记录决策过程（哪些优化保留，哪些移除，为什么）
"""
# YOUR CODE HERE
```

### 练习 3: 端到端评估

```python
"""
要求:
1. 在练习 2 的基础上，添加 LLM 生成环节
2. 对比两种最优配置的端到端效果:
   - 检索 + LLM 生成回答
   - 评估回答的相关性、完整性、忠实度
3. 分析: 检索质量提升是否传导到生成质量？
4. 输出端到端评估报告
"""
# YOUR CODE HERE
```

---

## 参考答案

### 练习 1: 构建自己的评估数据集

**思路**：选择一个熟悉的领域（如 Python 技术文档），准备足够多样化的文档覆盖不同主题，编写覆盖三种难度级别的查询并标注相关文档。关键是确保标注完整性和查询多样性——每个查询至少标注 1 个相关文档，每种难度至少 5 条。

**答案**：

```python
import json
import os
from typing import List, Dict
from dataclasses import dataclass, asdict, field
from datetime import datetime


@dataclass
class QueryAnnotation:
    query: str
    relevant_doc_ids: List[str]
    difficulty: str = "medium"
    category: str = "general"
    notes: str = ""
    created_at: str = field(default_factory=lambda: datetime.now().isoformat())


class EvalDatasetBuilder:
    def __init__(self, dataset_name: str = "rag_eval"):
        self.dataset_name = dataset_name
        self.annotations: List[QueryAnnotation] = []
        self.doc_store: Dict[str, str] = {}

    def add_documents(self, documents: List[Dict[str, str]]):
        for doc in documents:
            self.doc_store[doc["id"]] = doc["content"]
        print(f"已添加 {len(documents)} 个文档，总计 {len(self.doc_store)} 个")

    def add_annotation(self, query: str, relevant_doc_ids: List[str], **kwargs):
        for doc_id in relevant_doc_ids:
            if doc_id not in self.doc_store:
                print(f"⚠️ 文档 ID '{doc_id}' 不在文档库中")
        self.annotations.append(QueryAnnotation(
            query=query, relevant_doc_ids=relevant_doc_ids, **kwargs
        ))

    def save(self, filepath: str):
        dataset = {
            "name": self.dataset_name,
            "created_at": datetime.now().isoformat(),
            "num_documents": len(self.doc_store),
            "num_annotations": len(self.annotations),
            "documents": self.doc_store,
            "annotations": [asdict(a) for a in self.annotations],
        }
        os.makedirs(os.path.dirname(filepath) or ".", exist_ok=True)
        with open(filepath, "w", encoding="utf-8") as f:
            json.dump(dataset, f, ensure_ascii=False, indent=2)
        print(f"数据集已保存: {filepath}")
        print(f"  文档数: {len(self.doc_store)}")
        print(f"  标注数: {len(self.annotations)}")

    def stats(self) -> Dict:
        if not self.annotations:
            return {"num_annotations": 0}
        relevant_counts = [len(a.relevant_doc_ids) for a in self.annotations]
        difficulties, categories = {}, {}
        for a in self.annotations:
            difficulties[a.difficulty] = difficulties.get(a.difficulty, 0) + 1
            categories[a.category] = categories.get(a.category, 0) + 1
        return {
            "num_documents": len(self.doc_store),
            "num_annotations": len(self.annotations),
            "avg_relevant_per_query": sum(relevant_counts) / len(relevant_counts),
            "min_relevant": min(relevant_counts),
            "max_relevant": max(relevant_counts),
            "difficulty_distribution": difficulties,
            "category_distribution": categories,
        }


if __name__ == "__main__":
    builder = EvalDatasetBuilder("python_ecosystem_eval")

    documents = [
        {"id": "d01", "content": "Django 是 Python 最流行的 Web 框架，遵循 MVT 架构。内置 ORM、Admin 后台、用户认证系统。适合大型企业应用如内容管理系统、电商平台。"},
        {"id": "d02", "content": "Flask 是轻量级微框架，不强制项目结构。通过扩展支持数据库、认证等功能。适合 API 开发和小型项目。"},
        {"id": "d03", "content": "FastAPI 高性能异步 Web 框架，基于 Python 类型提示。自动生成 OpenAPI 文档，性能接近 Go。"},
        {"id": "d04", "content": "SQLAlchemy 是 Python ORM 工具，支持 PostgreSQL、MySQL、SQLite 等多种数据库。提供 Core 和 ORM 两种使用方式。"},
        {"id": "d05", "content": "Celery 分布式任务队列，支持异步任务和定时任务。常与 Redis 或 RabbitMQ 配合使用。"},
        {"id": "d06", "content": "Redis 内存数据库，常用于缓存、会话存储、消息队列。Python 通过 redis-py 库集成。"},
        {"id": "d07", "content": "pytest 是 Python 测试框架，支持参数化测试、fixture、插件扩展。比 unittest 更简洁灵活。"},
        {"id": "d08", "content": "Docker 容器化部署 Python 应用：Dockerfile 编写、多阶段构建、docker-compose 编排。"},
        {"id": "d09", "content": "Python 虚拟环境管理：venv（内置）、conda（科学计算）、poetry（依赖管理+打包）。"},
        {"id": "d10", "content": "Python 性能优化：异步编程 asyncio、C 扩展 Cython、NumPy 向量化计算、PyPy JIT 编译。"},
        {"id": "d11", "content": "Pydantic 数据验证库，基于 Python 类型提示。FastAPI 和 SQLModel 都依赖 Pydantic。"},
        {"id": "d12", "content": "Alembic 数据库迁移工具，配合 SQLAlchemy 使用。支持自动生成迁移脚本。"},
        {"id": "d13", "content": "Gunicorn WSGI 服务器，常用于部署 Django 和 Flask 应用。配合 Nginx 反向代理。"},
        {"id": "d14", "content": "uvicorn ASGI 服务器，支持异步，常用于部署 FastAPI 应用。"},
        {"id": "d15", "content": "Python 类型提示（Type Hints）从 3.5 开始支持，提升代码可读性和 IDE 支持。mypy 做静态类型检查。"},
        {"id": "d16", "content": "Python 数据处理三件套：pandas（表格数据）、numpy（数值计算）、polars（高性能 DataFrame）。"},
        {"id": "d17", "content": "Scrapy Python 爬虫框架，支持异步、中间件、管道。适合大规模数据采集。"},
        {"id": "d18", "content": "Python 机器学习生态：scikit-learn（传统 ML）、PyTorch（深度学习）、Hugging Face（NLP）。"},
        {"id": "d19", "content": "Python 日志最佳实践：使用 logging 模块，配置 handler、formatter、level。structlog 结构化日志。"},
        {"id": "d20", "content": "Python 配置管理：环境变量、.env 文件、pydantic-settings、dynaconf 多环境配置。"},
    ]
    builder.add_documents(documents)

    # Easy 查询
    builder.add_annotation("Django 是什么", ["d01"], difficulty="easy", category="conceptual")
    builder.add_annotation("pytest 怎么用", ["d07"], difficulty="easy", category="procedural")
    builder.add_annotation("Redis 是什么", ["d06"], difficulty="easy", category="conceptual")
    builder.add_annotation("如何用 Docker 部署 Python 应用", ["d08"], difficulty="easy", category="procedural")
    builder.add_annotation("Flask 适合什么场景", ["d02"], difficulty="easy", category="factual")
    builder.add_annotation("FastAPI 有什么优势", ["d03"], difficulty="easy", category="factual")
    builder.add_annotation("Python 怎么管理虚拟环境", ["d09"], difficulty="easy", category="procedural")

    # Medium 查询
    builder.add_annotation("Python Web 框架对比", ["d01", "d02", "d03"], difficulty="medium", category="comparative")
    builder.add_annotation("Python 异步任务怎么实现", ["d05", "d06"], difficulty="medium", category="procedural")
    builder.add_annotation("Python ORM 有哪些选择", ["d04", "d12"], difficulty="medium", category="factual")
    builder.add_annotation("Python 怎么做数据验证", ["d11", "d15"], difficulty="medium", category="procedural")
    builder.add_annotation("Django 和 FastAPI 哪个性能好", ["d01", "d03"], difficulty="medium", category="comparative")
    builder.add_annotation("Python 项目怎么管理依赖", ["d09"], difficulty="medium", category="procedural")
    builder.add_annotation("Python 怎么做数据库迁移", ["d12", "d04"], difficulty="medium", category="procedural")
    builder.add_annotation("Python 测试框架怎么选", ["d07"], difficulty="medium", category="comparative")

    # Hard 查询
    builder.add_annotation("如何提升 Python Web 应用性能", ["d10", "d03", "d06", "d14"], difficulty="hard", category="procedural")
    builder.add_annotation("Python 微服务架构完整技术栈", ["d03", "d05", "d06", "d08", "d14"], difficulty="hard", category="comparative")
    builder.add_annotation("Python Web 应用从开发到部署的完整流程", ["d01", "d04", "d07", "d08", "d13"], difficulty="hard", category="procedural")
    builder.add_annotation("Python 数据处理和机器学习怎么结合", ["d16", "d18"], difficulty="hard", category="procedural")
    builder.add_annotation("Python 企业级应用的日志和配置管理", ["d19", "d20"], difficulty="hard", category="procedural")
    builder.add_annotation("Python 爬虫框架对比和选型", ["d17", "d02"], difficulty="hard", category="comparative")

    stats = builder.stats()
    print("\n数据集统计:")
    for k, v in stats.items():
        print(f"  {k}: {v}")

    builder.save("python_ecosystem_eval_dataset.json")
```

**要点**：
- 文档应覆盖领域内多个子主题，避免集中在某一类
- 查询应覆盖 easy/medium/hard 三种难度，每种至少 5 条
- 相关文档标注应尽量完整，漏标会低估系统性能；建议多人交叉标注

---

### 练习 2: 逐步优化实验

**思路**：逐步开启优化组件，每步只改一个变量，记录增量贡献。对比 5 种配置（纯向量→+BM25→+查询扩展→+Reranker→+压缩）的 Recall@5、MRR、NDCG@5 和延迟，找出表现最差的查询分析原因。

**答案**：

```python
import math
import time
import hashlib
import numpy as np
from typing import List, Dict, Set, Callable
from collections import defaultdict
from dataclasses import dataclass, field


def recall_at_k(r, rel, k): return len(set(r[:k]) & rel) / len(rel) if rel else 0
def mrr_at_k(r, rel, k):
    for i, d in enumerate(r[:k]):
        if d in rel: return 1.0 / (i + 1)
    return 0.0
def ndcg_at_k(r, rel, k):
    if not rel: return 0.0
    dcg = sum((1.0 if r[i] in rel else 0.0) / math.log2(i+2) for i in range(min(k, len(r))))
    ideal = sorted([1.0 if r[i] in rel else 0.0 for i in range(min(k, len(r)))], reverse=True)
    while len(ideal) < min(len(rel), k): ideal.append(1.0)
    idcg = sum(v / math.log2(i+2) for i, v in enumerate(ideal[:k]))
    return dcg / idcg if idcg > 0 else 0.0


@dataclass
class EvalCase:
    query: str
    relevant_ids: Set[str]
    category: str = "general"
    difficulty: str = "medium"


@dataclass
class ABResult:
    config_name: str
    recall_at_5: float
    precision_at_5: float
    mrr: float
    ndcg_at_5: float
    avg_latency_ms: float
    per_query: List[Dict] = field(default_factory=list)


class ABEvaluator:
    def __init__(self, dataset: List[EvalCase]):
        self.dataset = dataset

    def evaluate(self, config_name: str, retrieve_fn: Callable, k: int = 5) -> ABResult:
        per_query = []
        total_latency = 0.0
        for case in self.dataset:
            start = time.time()
            results = retrieve_fn(case.query)
            latency = (time.time() - start) * 1000
            total_latency += latency
            per_query.append({
                "query": case.query, "category": case.category,
                "difficulty": case.difficulty,
                "recall": recall_at_k(results, case.relevant_ids, k),
                "precision": sum(1 for d in results[:k] if d in case.relevant_ids) / k,
                "mrr": mrr_at_k(results, case.relevant_ids, k),
                "ndcg": ndcg_at_k(results, case.relevant_ids, k),
                "latency_ms": latency,
            })
        n = len(self.dataset)
        return ABResult(
            config_name=config_name,
            recall_at_5=sum(r["recall"] for r in per_query) / n,
            precision_at_5=sum(r["precision"] for r in per_query) / n,
            mrr=sum(r["mrr"] for r in per_query) / n,
            ndcg_at_5=sum(r["ndcg"] for r in per_query) / n,
            avg_latency_ms=total_latency / n,
            per_query=per_query,
        )

    @staticmethod
    def compare(results: List[ABResult]) -> str:
        lines = ["=" * 85]
        lines.append(f"{'Config':<25} {'Recall@5':<10} {'Prec@5':<10} {'MRR':<10} {'NDCG@5':<10} {'Latency':<10}")
        lines.append("-" * 85)
        baseline = results[0] if results else None
        for r in results:
            lines.append(f"{r.config_name:<25} {r.recall_at_5:<10.3f} {r.precision_at_5:<10.3f} "
                         f"{r.mrr:<10.3f} {r.ndcg_at_5:<10.3f} {r.avg_latency_ms:<10.0f}ms")
        if baseline and len(results) > 1:
            lines.append("-" * 85)
            lines.append("增量对比 (相对基线):")
            for r in results[1:]:
                dr = r.recall_at_5 - baseline.recall_at_5
                dm = r.mrr - baseline.mrr
                dl = r.avg_latency_ms - baseline.avg_latency_ms
                lines.append(f"  {r.config_name:<23} Recall: {dr:+.3f} | MRR: {dm:+.3f} | Latency: {dl:+.0f}ms")
        lines.append("=" * 85)
        return "\n".join(lines)

    @staticmethod
    def find_worst(results: ABResult, n: int = 5) -> List[Dict]:
        return sorted(results.per_query, key=lambda x: x["recall"])[:n]


if __name__ == "__main__":
    dataset = [
        EvalCase("iPhone 15 电池容量", {"doc_iphone"}, "product", "easy"),
        EvalCase("哪个手机电池最大", {"doc_iphone", "doc_samsung"}, "comparison", "medium"),
        EvalCase("Python Web 框架对比", {"doc_django", "doc_fastapi"}, "tech", "medium"),
        EvalCase("如何申请退款", {"doc_refund"}, "how-to", "easy"),
        EvalCase("FastAPI 性能如何", {"doc_fastapi"}, "tech", "easy"),
        EvalCase("退款到账时间", {"doc_refund_time"}, "product", "easy"),
        EvalCase("Django 适合什么场景", {"doc_django"}, "tech", "easy"),
        EvalCase("对比旗舰手机充电速度", {"doc_iphone", "doc_samsung"}, "comparison", "hard"),
        EvalCase("Python 微服务技术栈", {"doc_fastapi", "doc_celery"}, "tech", "hard"),
        EvalCase("退款政策更新", {"doc_refund", "doc_refund_time"}, "how-to", "medium"),
    ]

    # 模拟 5 种配置的检索函数
    def make_retriever(base_recall_shift=0):
        def fn(query):
            base = ["doc_iphone", "doc_samsung", "doc_django", "doc_fastapi", "doc_refund"]
            return base
        return fn

    evaluator = ABEvaluator(dataset)

    # 模拟不同配置的结果（实际应调用真实检索器）
    configs = [
        ("A: Vector Only", 0),
        ("B: + BM25", 0.08),
        ("C: + Query Expansion", 0.12),
        ("D: + Reranker", 0.15),
        ("E: + Compression", 0.15),
    ]

    results = []
    for name, shift in configs:
        # 模拟评估（实际应使用真实检索器）
        per_query = []
        total_latency = 0
        for case in dataset:
            latency = 50 + shift * 500
            total_latency += latency
            recall = min(1.0, 0.6 + shift + np.random.uniform(-0.1, 0.1))
            mrr_val = min(1.0, 0.5 + shift + np.random.uniform(-0.1, 0.1))
            ndcg_val = min(1.0, 0.55 + shift + np.random.uniform(-0.1, 0.1))
            per_query.append({
                "query": case.query, "category": case.category,
                "difficulty": case.difficulty,
                "recall": recall, "precision": recall * 0.8,
                "mrr": mrr_val, "ndcg": ndcg_val, "latency_ms": latency,
            })
        n = len(dataset)
        results.append(ABResult(
            config_name=name,
            recall_at_5=sum(r["recall"] for r in per_query) / n,
            precision_at_5=sum(r["precision"] for r in per_query) / n,
            mrr=sum(r["mrr"] for r in per_query) / n,
            ndcg_at_5=sum(r["ndcg"] for r in per_query) / n,
            avg_latency_ms=total_latency / n,
            per_query=per_query,
        ))

    print(ABEvaluator.compare(results))

    # 最差查询分析
    print("\n表现最差的 5 个查询 (Config D):")
    worst = ABEvaluator.find_worst(results[3], 5)
    for q in worst:
        print(f"  - {q['query']} (recall={q['recall']:.2f}, difficulty={q['difficulty']})")

    # 决策记录
    print("\n" + "=" * 50)
    print("优化决策记录:")
    print("  BM25: ✅ 保留 (Recall 显著提升，延迟增加可接受)")
    print("  查询扩展: ✅ 保留 (Recall 进一步提升)")
    print("  Reranker: ✅ 保留 (MRR 显著提升，排序质量改善)")
    print("  压缩: ⚠️ 条件启用 (延迟增加较多，仅在 Top-K>5 时使用)")
```

**要点**：
- 一次只改一个变量，否则无法判断效果提升来自哪个改动
- 决策应基于数据：Recall 提升 > 5% 且延迟增加 < 100ms 的优化值得保留
- Bad Case 分析比平均指标更有价值——找到 Recall=0 的查询并分析根因

---

### 练习 3: 端到端评估

**思路**：端到端评估连接检索质量与生成质量。用 LLM 作为评估器，从相关性、完整性、忠实度三个维度打分。对比两种最优配置的端到端效果，分析检索质量提升是否传导到生成质量。

**答案**：

```python
import json
import time
from typing import List, Dict
from dataclasses import dataclass, field
from langchain_openai import ChatOpenAI
from langchain.prompts import ChatPromptTemplate


class EndToEndEvaluator:
    """端到端评估: 检索质量 → 生成质量"""

    def __init__(self, model: str = "gpt-4o-mini"):
        self.llm = ChatOpenAI(model=model, temperature=0)
        self.eval_prompt = ChatPromptTemplate.from_template("""
你是 RAG 系统评估专家。请评估以下回答的质量。

用户问题: {question}
检索到的文档:
{context}
系统回答: {answer}

从三个维度打分 (0-5 分):
1. 相关性: 回答是否与问题相关
2. 完整性: 回答是否涵盖了关键信息
3. 忠实度: 回答是否基于检索文档（不编造信息）

输出 JSON: {{"relevance": <分数>, "completeness": <分数>, "faithfulness": <分数>, "reason": "<简要说明>"}}
""")

    def evaluate_single(self, question: str, context_docs: List[str], answer: str) -> Dict:
        context = "\n---\n".join(context_docs)
        chain = self.eval_prompt | self.llm
        response = chain.invoke({
            "question": question, "context": context, "answer": answer
        })
        try:
            return json.loads(response.content)
        except json.JSONDecodeError:
            return {"relevance": 0, "completeness": 0, "faithfulness": 0, "reason": "解析失败"}

    def evaluate_batch(self, qa_pairs: List[Dict]) -> Dict:
        results = []
        for qa in qa_pairs:
            result = self.evaluate_single(
                qa["question"], qa["context_docs"], qa["answer"]
            )
            results.append(result)
        n = len(results)
        return {
            "mean_relevance": sum(r.get("relevance", 0) for r in results) / n,
            "mean_completeness": sum(r.get("completeness", 0) for r in results) / n,
            "mean_faithfulness": sum(r.get("faithfulness", 0) for r in results) / n,
            "num_evaluated": n,
            "per_question": results,
        }


if __name__ == "__main__":
    evaluator = EndToEndEvaluator()

    # 配置 A: 基线检索 + LLM 生成
    config_a_qa = [
        {
            "question": "iPhone 15 Pro Max 电池容量是多少",
            "context_docs": [
                "智能手机电池技术发展趋势：固态电池将取代锂离子电池",
                "手机续航优化指南：关闭后台应用可延长电池使用时间",
                "iPhone 15 Pro Max 配备 4422mAh 电池，支持 27W 快充",
            ],
            "answer": "iPhone 15 Pro Max 配备 4422mAh 电池，支持 27W 有线快充。此外还支持 15W MagSafe 无线充电。"
        },
        {
            "question": "如何申请退款",
            "context_docs": [
                "退款政策：购买后 30 天内可申请全额退款",
                "退款流程：登录账户 → 订单管理 → 选择订单 → 申请退款",
            ],
            "answer": "申请退款的流程：1. 登录账户 2. 进入订单管理 3. 选择需要退款的订单 4. 提交退款申请。购买后 30 天内可申请全额退款。"
        },
        {
            "question": "Python Web 框架哪个性能最好",
            "context_docs": [
                "FastAPI 是高性能异步 Web 框架，性能接近 Go",
                "Django 是最流行的 Python Web 框架",
            ],
            "answer": "FastAPI 是性能最好的 Python Web 框架，基于异步处理，性能接近 Go 语言。Django 虽然功能最全但性能不如 FastAPI。"
        },
    ]

    # 配置 B: 优化检索 + LLM 生成
    config_b_qa = [
        {
            "question": "iPhone 15 Pro Max 电池容量是多少",
            "context_docs": [
                "iPhone 15 Pro Max 配备 4422mAh 电池，支持 27W 快充。钛金属机身，A17 Pro 芯片。",
                "Samsung Galaxy S24 Ultra 电池容量 5000mAh，支持 45W 快充。",
            ],
            "answer": "iPhone 15 Pro Max 配备 4422mAh 电池，支持 27W 有线快充和 15W MagSafe 无线充电。作为对比，Samsung Galaxy S24 Ultra 的电池容量为 5000mAh。"
        },
        {
            "question": "如何申请退款",
            "context_docs": [
                "退款政策：购买后 30 天内可申请全额退款。需提供订单号和退款原因。电子商品激活后不可退款。",
                "退款流程：登录账户 → 订单管理 → 选择订单 → 申请退款 → 等待审核 (1-3 工作日) → 退款到账。",
                "2024年退款政策更新：支持自动退款，金额 500 元以下无需人工审核，即时到账。",
            ],
            "answer": "申请退款步骤：1. 登录账户 2. 进入订单管理 3. 选择订单 4. 点击申请退款 5. 填写退款原因。30 天内可全额退款，500 元以下自动退款即时到账，其他金额需 1-3 工作日审核。注意电子商品激活后不可退款。"
        },
        {
            "question": "Python Web 框架哪个性能最好",
            "context_docs": [
                "FastAPI 是现代高性能 Web 框架，基于 Python 类型提示。支持异步请求处理，性能接近 Go。",
                "Python Web 框架性能基准测试：FastAPI (async) 约 10000 req/s，Flask 约 3000 req/s，Django 约 2000 req/s。",
            ],
            "answer": "根据性能基准测试，FastAPI 性能最好，异步模式下约 10000 req/s，远超 Flask（约 3000 req/s）和 Django（约 2000 req/s）。FastAPI 基于 Python 类型提示，支持异步请求处理，性能接近 Go 语言。"
        },
    ]

    print("评估配置 A (基线检索)...")
    result_a = evaluator.evaluate_batch(config_a_qa)
    print(f"  相关性: {result_a['mean_relevance']:.2f}/5")
    print(f"  完整性: {result_a['mean_completeness']:.2f}/5")
    print(f"  忠实度: {result_a['mean_faithfulness']:.2f}/5")

    print("\n评估配置 B (优化检索)...")
    result_b = evaluator.evaluate_batch(config_b_qa)
    print(f"  相关性: {result_b['mean_relevance']:.2f}/5")
    print(f"  完整性: {result_b['mean_completeness']:.2f}/5")
    print(f"  忠实度: {result_b['mean_faithfulness']:.2f}/5")

    print("\n增量对比:")
    print(f"  相关性: {result_b['mean_relevance'] - result_a['mean_relevance']:+.2f}")
    print(f"  完整性: {result_b['mean_completeness'] - result_a['mean_completeness']:+.2f}")
    print(f"  忠实度: {result_b['mean_faithfulness'] - result_a['mean_faithfulness']:+.2f}")
    print("\n结论: 优化检索后，上下文质量提升，LLM 生成的回答完整性显著改善")
```

**要点**：
- 端到端评估的三个维度各有侧重：相关性看"是否对题"，完整性看"是否全面"，忠实度看"是否幻觉"
- 检索质量提升不一定传导到生成质量——如果 LLM 本身能力强，即使检索一般也能生成不错的回答
- 端到端评估应作为最终衡量标准，检索指标只是中间过程指标

---

> **恭喜完成 Stage 2！** 你已经掌握了 RAG 系统检索优化的核心技术。
>
> **下一步**: 进入 Stage 3，学习生成优化（Prompt Engineering、答案质量控制等）。
