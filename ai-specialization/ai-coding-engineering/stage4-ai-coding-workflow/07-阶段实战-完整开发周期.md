# 07 - 阶段实战：完整开发周期

> **阶段**：Stage 4 - AI 编程工作流  
> **前置知识**：01-06 课全部内容  
> **预计用时**：180 分钟  
> **难度**：⭐⭐⭐⭐⭐

---

## 场景引入

前面六课分别学习了 AI 辅助的需求分析、Git 工作流、文档自动化、调试、学习新技术和代码迁移。但真实项目不会把这些环节拆开给你——你需要在同一个项目里连续运用所有这些技能。假设你接到一个任务：在三天内开发一个 URL 短链接服务，从需求分析到部署上线。你需要用 AI 细化需求、设计 API、搭建项目骨架、编写核心逻辑、生成测试、撰写文档、配置 Docker 和 CI/CD。这个实战项目的目的，是让你把前六课的零散技巧串联成一个完整的开发周期，体验 AI 辅助开发的全流程协作模式。

## 学习目标

完成本课后，你将能够：

1. 使用 AI 完成从需求分析到部署的完整开发周期
2. 实践 AI 辅助的需求分析、架构设计、编码、测试、文档全流程
3. 建立可复用的 AI 辅助开发工作流
4. 独立完成一个完整的后端项目

---

## 一、项目概述

### 1.1 项目需求

我们将使用 AI 辅助开发一个完整的 **URL 短链接服务**：

```
URL 短链接服务功能需求：

┌─────────────────────────────────────────────────┐
│              功能需求 (MoSCoW)                   │
├─────────────────────────────────────────────────┤
│                                                  │
│  Must Have (必须有)：                            │
│  ├─ 创建短链接（长URL → 短码）                  │
│  ├─ 访问短链接重定向到原始URL                   │
│  ├─ 短链接点击统计                               │
│  └─ 短链接过期机制                               │
│                                                  │
│  Should Have (应该有)：                          │
│  ├─ 自定义短码                                   │
│  ├─ 批量创建短链接                               │
│  └─ API Key 认证                                 │
│                                                  │
│  Could Have (可以有)：                           │
│  ├─ QR 码生成                                    │
│  └─ 链接预览                                     │
│                                                  │
│  Won't Have (本期不做)：                         │
│  ├─ 用户注册/登录                                │
│  └─ 自定义域名                                   │
└─────────────────────────────────────────────────┘
```

### 1.2 技术选型

```
技术架构图：

                    ┌──────────────┐
                    │   客户端      │
                    │  (API 调用)   │
                    └──────┬───────┘
                           │ HTTP
                    ┌──────▼───────┐
                    │   Fastify    │  ← HTTP 框架
                    │   (入口)      │
                    └──────┬───────┘
                           │
              ┌────────────┼────────────┐
              │            │            │
       ┌──────▼──────┐ ┌──▼──────┐ ┌──▼──────┐
       │  Routes     │ │ Plugins │ │ Hooks   │
       │  (路由)     │ │ (插件)  │ │ (钩子)  │
       └──────┬──────┘ └────────┘ └─────────┘
              │
       ┌──────▼──────┐
       │  Services   │  ← 业务逻辑层
       │  (服务)     │
       └──────┬──────┘
              │
       ┌──────▼──────┐
       │    Drizzle   │  ← ORM
       │   (数据层)   │
       └──────┬──────┘
              │
       ┌──────▼──────┐
       │  PostgreSQL  │  ← 数据库
       │  (数据库)    │
       └─────────────┘
```

---

## 二、Phase 1：需求分析（AI 辅助）

### 2.1 AI Prompt：需求细化

```
# Prompt

我需要开发一个 URL 短链接服务。请帮我：

1. 细化功能需求，输出用户故事格式
2. 设计 RESTful API 接口（路径、方法、参数、响应）
3. 识别技术风险和注意事项
4. 制定开发里程碑

核心功能：
- 创建短链接
- 访问短链接重定向
- 点击统计
- 过期机制
- 自定义短码
- API Key 认证
```

### 2.2 AI 输出：API 设计

```
API 接口设计结果：

┌──────────────────────────────────────────────────────────────┐
│                     API 接口列表                              │
├────────┬─────────────────────┬───────────────────────────────┤
│ 方法   │ 路径                │ 说明                          │
├────────┼─────────────────────┼───────────────────────────────┤
│ POST   │ /api/v1/links       │ 创建短链接                    │
│ GET    │ /api/v1/links       │ 获取短链接列表                │
│ GET    │ /api/v1/links/:code │ 获取短链接详情                │
│ PATCH  │ /api/v1/links/:code │ 更新短链接                    │
│ DELETE │ /api/v1/links/:code │ 删除短链接                    │
│ GET    │ /api/v1/links/:code │ 获取点击统计                  │
│        │   /stats            │                               │
│ GET    │ /:code              │ 重定向到原始 URL              │
├────────┼─────────────────────┼───────────────────────────────┤
│ POST   │ /api/v1/api-keys    │ 创建 API Key                  │
│ GET    │ /api/v1/api-keys    │ 列出 API Keys                 │
│ DELETE │ /api/v1/api-keys/:id│ 删除 API Key                  │
└────────┴─────────────────────┴───────────────────────────────┘
```

---

## 三、Phase 2：架构设计（AI 辅助）

### 3.1 数据库 Schema 设计

```
# Prompt

根据以下 API 设计，使用 Drizzle ORM (PostgreSQL) 设计数据库 Schema。

要求：
- 支持软删除
- 包含索引
- 支持短链接过期
- 支持点击统计

请输出完整的 schema 文件。
```

```typescript
// src/db/schema.ts
import { pgTable, text, timestamp, integer, bigint, boolean, index, uniqueIndex } from 'drizzle-orm/pg-core';
import { createId } from '@paralleldrive/cuid2';

export const links = pgTable('links', {
  id: text('id').primaryKey().$defaultFn(() => createId()),
  code: text('code').notNull(),
  originalUrl: text('original_url').notNull(),
  title: text('title'),
  expiresAt: timestamp('expires_at'),
  isCustomCode: boolean('is_custom_code').default(false),
  apiKeyId: text('api_key_id').references(() => apiKeys.id),
  clickCount: bigint('click_count', { mode: 'number' }).default(0),
  createdAt: timestamp('created_at').defaultNow().notNull(),
  updatedAt: timestamp('updated_at').defaultNow().notNull(),
  deletedAt: timestamp('deleted_at'),
}, (table) => ({
  codeIdx: uniqueIndex('code_idx').on(table.code),
  apiKeyIdx: index('api_key_idx').on(table.apiKeyId),
  expiresIdx: index('expires_idx').on(table.expiresAt),
}));

export const clicks = pgTable('clicks', {
  id: text('id').primaryKey().$defaultFn(() => createId()),
  linkId: text('link_id').references(() => links.id).notNull(),
  ip: text('ip'),
  userAgent: text('user_agent'),
  referer: text('referer'),
  country: text('country'),
  createdAt: timestamp('created_at').defaultNow().notNull(),
}, (table) => ({
  linkIdx: index('click_link_idx').on(table.linkId),
  createdAtIdx: index('click_created_at_idx').on(table.createdAt),
}));

export const apiKeys = pgTable('api_keys', {
  id: text('id').primaryKey().$defaultFn(() => createId()),
  key: text('key').notNull(),
  name: text('name').notNull(),
  isActive: boolean('is_active').default(true),
  lastUsedAt: timestamp('last_used_at'),
  createdAt: timestamp('created_at').defaultNow().notNull(),
  deletedAt: timestamp('deleted_at'),
}, (table) => ({
  keyIdx: uniqueIndex('api_key_idx').on(table.key),
}));
```

### 3.2 项目结构设计

```
最终项目结构：

url-shortener/
├── src/
│   ├── db/
│   │   ├── schema.ts          # Drizzle Schema
│   │   ├── index.ts           # 数据库连接
│   │   └── migrate.ts         # 迁移脚本
│   ├── routes/
│   │   ├── links.ts           # 短链接路由
│   │   ├── apiKeys.ts         # API Key 路由
│   │   └── redirect.ts        # 重定向路由
│   ├── services/
│   │   ├── linkService.ts     # 短链接业务逻辑
│   │   ├── clickService.ts    # 点击统计逻辑
│   │   ├── apiKeyService.ts   # API Key 逻辑
│   │   └── codeGenerator.ts   # 短码生成器
│   ├── plugins/
│   │   ├── auth.ts            # 认证插件
│   │   └── rateLimit.ts       # 限流插件
│   ├── types/
│   │   └── index.ts           # 类型定义
│   ├── utils/
│   │   ├── errors.ts          # 错误定义
│   │   └── urlValidator.ts    # URL 校验
│   └── app.ts                 # Fastify 应用
├── tests/
│   ├── unit/                  # 单元测试
│   ├── integration/           # 集成测试
│   └── helpers/               # 测试工具
├── docs/
│   ├── api.md                 # API 文档
│   └── architecture.md        # 架构文档
├── drizzle.config.ts
├── package.json
├── tsconfig.json
└── README.md
```

---

## 四、Phase 3：核心实现（AI 辅助）

### 4.1 短码生成器

```typescript
// src/services/codeGenerator.ts
import { nanoid } from 'nanoid';
import { customAlphabet } from 'nanoid';

const alphabet = '0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz';
const generate = customAlphabet(alphabet, 7);

const MAX_RETRIES = 5;

export function generateCode(customCode?: string): string {
  if (customCode) {
    validateCustomCode(customCode);
    return customCode;
  }
  return generate();
}

function validateCustomCode(code: string): void {
  if (code.length < 3 || code.length < 20) {
    throw new Error('Custom code must be between 3 and 20 characters');
  }
  if (!/^[a-zA-Z0-9_-]+$/.test(code)) {
    throw new Error('Custom code can only contain letters, numbers, hyphens, and underscores');
  }
  const reserved = ['api', 'admin', 'health', 'metrics', 'docs'];
  if (reserved.includes(code.toLowerCase())) {
    throw new Error('This code is reserved and cannot be used');
  }
}

export function generateApiKey(): string {
  return `sk_${nanoid(48)}`;
}
```

### 4.2 短链接 Service

```typescript
// src/services/linkService.ts
import { db } from '../db';
import { links, clicks } from '../db/schema';
import { eq, and, isNull, gt, desc, sql } from 'drizzle-orm';
import { generateCode } from './codeGenerator';
import { validateUrl } from '../utils/urlValidator';
import { NotFoundError, ConflictError, ValidationError } from '../utils/errors';

interface CreateLinkInput {
  url: string;
  title?: string;
  customCode?: string;
  expiresAt?: string;
  apiKeyId?: string;
}

interface LinkListQuery {
  page?: number;
  pageSize?: number;
  apiKeyId?: string;
}

export async function createLink(input: CreateLinkInput) {
  validateUrl(input.url);

  const code = generateCode(input.customCode);

  const existing = await db.select().from(links).where(eq(links.code, code)).limit(1);
  if (existing.length > 0) {
    throw new ConflictError(`Code "${code}" already exists`);
  }

  const [link] = await db.insert(links).values({
    code,
    originalUrl: input.url,
    title: input.title || null,
    expiresAt: input.expiresAt ? new Date(input.expiresAt) : null,
    isCustomCode: !!input.customCode,
    apiKeyId: input.apiKeyId || null,
  }).returning();

  return link;
}

export async function getLinkByCode(code: string) {
  const [link] = await db.select().from(links).where(
    and(
      eq(links.code, code),
      isNull(links.deletedAt)
    )
  ).limit(1);

  if (!link) {
    throw new NotFoundError('Link not found');
  }

  if (link.expiresAt && link.expiresAt < new Date()) {
    throw new NotFoundError('Link has expired');
  }

  return link;
}

export async function listLinks(query: LinkListQuery) {
  const page = query.page || 1;
  const pageSize = Math.min(query.pageSize || 20, 100);
  const offset = (page - 1) * pageSize;

  const conditions = [isNull(links.deletedAt)];
  if (query.apiKeyId) {
    conditions.push(eq(links.apiKeyId, query.apiKeyId));
  }

  const [countResult] = await db.select({ count: sql<number>`count(*)` })
    .from(links)
    .where(and(...conditions));

  const data = await db.select().from(links)
    .where(and(...conditions))
    .orderBy(desc(links.createdAt))
    .limit(pageSize)
    .offset(offset);

  return {
    data,
    pagination: {
      page,
      pageSize,
      total: countResult.count,
      totalPages: Math.ceil(countResult.count / pageSize),
    },
  };
}

export async function deleteLink(code: string) {
  const [link] = await db.select().from(links).where(eq(links.code, code)).limit(1);
  if (!link) {
    throw new NotFoundError('Link not found');
  }

  await db.update(links)
    .set({ deletedAt: new Date(), updatedAt: new Date() })
    .where(eq(links.code, code));

  return { success: true };
}
```

### 4.3 点击统计 Service

```typescript
// src/services/clickService.ts
import { db } from '../db';
import { clicks, links } from '../db/schema';
import { eq, and, sql, gte, lte } from 'drizzle-orm';

export async function recordClick(linkId: string, data: {
  ip?: string;
  userAgent?: string;
  referer?: string;
}) {
  await db.insert(clicks).values({
    linkId,
    ip: data.ip || null,
    userAgent: data.userAgent || null,
    referer: data.referer || null,
  });

  await db.update(links)
    .set({ clickCount: sql`${links.clickCount} + 1` })
    .where(eq(links.id, linkId));
}

export async function getStats(linkId: string, days: number = 7) {
  const startDate = new Date();
  startDate.setDate(startDate.getDate() - days);

  const [totalClicks] = await db.select({ count: sql<number>`count(*)` })
    .from(clicks)
    .where(and(
      eq(clicks.linkId, linkId),
      gte(clicks.createdAt, startDate)
    ));

  const dailyClicks = await db.select({
    date: sql<string>`date(${clicks.createdAt})`,
    count: sql<number>`count(*)`,
  })
    .from(clicks)
    .where(and(
      eq(clicks.linkId, linkId),
      gte(clicks.createdAt, startDate)
    ))
    .groupBy(sql`date(${clicks.createdAt})`)
    .orderBy(sql`date(${clicks.createdAt})`);

  const topReferers = await db.select({
    referer: clicks.referer,
    count: sql<number>`count(*)`,
  })
    .from(clicks)
    .where(and(
      eq(clicks.linkId, linkId),
      gte(clicks.createdAt, startDate)
    ))
    .groupBy(clicks.referer)
    .orderBy(sql`count(*) desc`)
    .limit(10);

  return {
    totalClicks: totalClicks.count,
    dailyClicks,
    topReferers,
  };
}
```

### 4.4 路由实现

```typescript
// src/routes/links.ts
import { FastifyInstance, FastifyRequest, FastifyReply } from 'fastify';
import { z } from 'zod';
import * as linkService from '../services/linkService';
import * as clickService from '../services/clickService';

const createLinkSchema = z.object({
  url: z.string().url('Invalid URL format'),
  title: z.string().max(200).optional(),
  customCode: z.string().min(3).max(20).regex(/^[a-zA-Z0-9_-]+$/).optional(),
  expiresAt: z.string().datetime().optional(),
});

export async function linkRoutes(app: FastifyInstance) {
  // 创建短链接
  app.post('/api/v1/links', async (req: FastifyRequest, reply: FastifyReply) => {
    const body = createLinkSchema.parse(req.body);
    const apiKeyId = (req as any).apiKeyId;
    const link = await linkService.createLink({ ...body, apiKeyId });
    return reply.status(201).send({ code: 0, data: link, message: 'created' });
  });

  // 获取列表
  app.get('/api/v1/links', async (req: FastifyRequest, reply: FastifyReply) => {
    const query = req.query as any;
    const apiKeyId = (req as any).apiKeyId;
    const result = await linkService.listLinks({
      page: parseInt(query.page) || 1,
      pageSize: parseInt(query.pageSize) || 20,
      apiKeyId,
    });
    return reply.send({ code: 0, data: result, message: 'success' });
  });

  // 获取详情
  app.get('/api/v1/links/:code', async (req: FastifyRequest, reply: FastifyReply) => {
    const { code } = req.params as { code: string };
    const link = await linkService.getLinkByCode(code);
    return reply.send({ code: 0, data: link, message: 'success' });
  });

  // 删除
  app.delete('/api/v1/links/:code', async (req: FastifyRequest, reply: FastifyReply) => {
    const { code } = req.params as { code: string };
    await linkService.deleteLink(code);
    return reply.send({ code: 0, data: null, message: 'deleted' });
  });

  // 获取统计
  app.get('/api/v1/links/:code/stats', async (req: FastifyRequest, reply: FastifyReply) => {
    const { code } = req.params as { code: string };
    const query = req.query as any;
    const link = await linkService.getLinkByCode(code);
    const stats = await clickService.getStats(link.id, parseInt(query.days) || 7);
    return reply.send({ code: 0, data: stats, message: 'success' });
  });
}
```

### 4.5 重定向路由

```typescript
// src/routes/redirect.ts
import { FastifyInstance, FastifyRequest, FastifyReply } from 'fastify';
import * as linkService from '../services/linkService';
import * as clickService from '../services/clickService';

export async function redirectRoutes(app: FastifyInstance) {
  app.get('/:code', async (req: FastifyRequest, reply: FastifyReply) => {
    const { code } = req.params as { code: string };

    // 跳过 API 路由
    if (code === 'api') return;

    const link = await linkService.getLinkByCode(code);

    // 异步记录点击（不阻塞重定向）
    clickService.recordClick(link.id, {
      ip: req.ip,
      userAgent: req.headers['user-agent'],
      referer: req.headers.referer,
    }).catch(console.error);

    return reply.redirect(302, link.originalUrl);
  });
}
```

### 4.6 应用入口

```typescript
// src/app.ts
import Fastify from 'fastify';
import { linkRoutes } from './routes/links';
import { redirectRoutes } from './routes/redirect';
import { authPlugin } from './plugins/auth';
import { errorHandler } from './utils/errors';

export async function buildApp() {
  const app = Fastify({
    logger: true,
  });

  // 全局错误处理
  app.setErrorHandler(errorHandler);

  // 注册插件
  await app.register(authPlugin);

  // 注册路由
  await app.register(linkRoutes);
  await app.register(redirectRoutes);

  // 健康检查
  app.get('/health', async () => ({ status: 'ok' }));

  return app;
}

// 启动
async function start() {
  const app = await buildApp();
  const port = parseInt(process.env.PORT || '3000', 10);

  try {
    await app.listen({ port, host: '0.0.0.0' });
    console.log(`Server running on http://localhost:${port}`);
  } catch (err) {
    app.log.error(err);
    process.exit(1);
  }
}

start();
```

---

## 五、Phase 4：测试（AI 辅助）

### 5.1 AI Prompt：生成测试

```
# Prompt

请为 URL 短链接服务生成完整的测试套件，包括：

1. 单元测试：codeGenerator、urlValidator
2. 集成测试：linkService、clickService
3. API 测试：所有路由端点

使用 Vitest + Supertest，Mock 数据库。
每个测试文件要覆盖正常路径、边界条件、错误处理。
```

### 5.2 核心测试文件

```typescript
// tests/unit/codeGenerator.test.ts
import { describe, it, expect } from 'vitest';
import { generateCode, generateApiKey } from '../../src/services/codeGenerator';

describe('codeGenerator', () => {
  describe('generateCode', () => {
    it('应该生成 7 位随机码', () => {
      const code = generateCode();
      expect(code).toHaveLength(7);
      expect(code).toMatch(/^[a-zA-Z0-9]+$/);
    });

    it('应该使用自定义短码', () => {
      expect(generateCode('my-link')).toBe('my-link');
    });

    it('应该拒绝过短的自定义码', () => {
      expect(() => generateCode('ab')).toThrow('between 3 and 20');
    });

    it('应该拒绝保留字', () => {
      expect(() => generateCode('api')).toThrow('reserved');
      expect(() => generateCode('admin')).toThrow('reserved');
    });

    it('应该拒绝包含特殊字符的自定义码', () => {
      expect(() => generateCode('hello world')).toThrow('can only contain');
      expect(() => generateCode('test@123')).toThrow('can only contain');
    });

    it('应该生成不重复的码', () => {
      const codes = new Set(Array.from({ length: 1000 }, () => generateCode()));
      expect(codes.size).toBe(1000);
    });
  });

  describe('generateApiKey', () => {
    it('应该以 sk_ 开头', () => {
      expect(generateApiKey()).toMatch(/^sk_/);
    });

    it('应该生成足够长的 key', () => {
      const key = generateApiKey();
      expect(key.length).toBeGreaterThan(40);
    });
  });
});
```

```typescript
// tests/integration/linkService.test.ts
import { describe, it, expect, beforeEach, vi } from 'vitest';
import * as linkService from '../../src/services/linkService';

vi.mock('../../src/db', () => ({
  db: {
    select: vi.fn().mockReturnThis(),
    insert: vi.fn().mockReturnThis(),
    update: vi.fn().mockReturnThis(),
    from: vi.fn().mockReturnThis(),
    where: vi.fn().mockReturnThis(),
    limit: vi.fn().mockReturnThis(),
    returning: vi.fn().mockResolvedValue([{
      id: 'test-id',
      code: 'abc1234',
      originalUrl: 'https://example.com',
      title: null,
      clickCount: 0,
      createdAt: new Date(),
    }]),
  },
}));

describe('linkService', () => {
  describe('createLink', () => {
    it('应该创建短链接', async () => {
      const result = await linkService.createLink({
        url: 'https://example.com',
      });
      expect(result.code).toBeDefined();
      expect(result.originalUrl).toBe('https://example.com');
    });

    it('应该拒绝无效 URL', async () => {
      await expect(linkService.createLink({ url: 'not-a-url' }))
        .rejects.toThrow('Invalid URL');
    });
  });
});
```

```typescript
// tests/api/links.test.ts
import { describe, it, expect, beforeAll, afterAll } from 'vitest';
import { buildApp } from '../../src/app';

describe('Links API', () => {
  let app: Awaited<ReturnType<typeof buildApp>>;

  beforeAll(async () => {
    app = await buildApp();
    await app.ready();
  });

  afterAll(async () => {
    await app.close();
  });

  describe('POST /api/v1/links', () => {
    it('应该创建短链接', async () => {
      const response = await app.inject({
        method: 'POST',
        url: '/api/v1/links',
        payload: { url: 'https://example.com' },
      });

      expect(response.statusCode).toBe(201);
      const body = JSON.parse(response.body);
      expect(body.code).toBe(0);
      expect(body.data.code).toBeDefined();
    });
  });
});
```

---

## 六、Phase 5：文档生成（AI 辅助）

### 6.1 API 文档生成

```
# Prompt

根据以下代码，生成完整的 API 文档（Markdown 格式）。

包含：
- 接口描述
- 请求方法和路径
- 请求参数（Query/Body/Headers）
- 响应格式（成功和失败）
- 示例请求和响应
- 错误码说明

路由文件：src/routes/links.ts
类型定义：src/types/index.ts
```

### 6.2 生成的 API 文档

```markdown
# URL 短链接服务 API 文档

## 基础信息

- Base URL: `http://localhost:3000`
- 认证方式: API Key (Header: `X-API-Key`)
- 响应格式: JSON

## 认证

所有 `/api/v1/` 接口需要在 Header 中提供 API Key：

```
X-API-Key: sk_your_api_key_here
```

## 接口列表

### 创建短链接

**POST** `/api/v1/links`

**请求体：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| url | string | 是 | 原始 URL |
| title | string | 否 | 链接标题 |
| customCode | string | 否 | 自定义短码（3-20字符） |
| expiresAt | string | 否 | 过期时间（ISO 8601） |

**请求示例：**

```json
{
  "url": "https://www.example.com/very/long/path",
  "title": "示例链接",
  "customCode": "my-link",
  "expiresAt": "2025-12-31T23:59:59Z"
}
```

**成功响应（201）：**

```json
{
  "code": 0,
  "data": {
    "id": "clx1234567890",
    "code": "my-link",
    "originalUrl": "https://www.example.com/very/long/path",
    "title": "示例链接",
    "clickCount": 0,
    "expiresAt": "2025-12-31T23:59:59.000Z",
    "createdAt": "2025-06-21T10:00:00.000Z"
  },
  "message": "created"
}
```

**错误响应：**

| 状态码 | 说明 |
|--------|------|
| 400 | 请求参数错误 |
| 409 | 短码已存在 |
| 401 | 未提供 API Key |
```

---

## 七、Phase 6：部署准备

### 7.1 Docker 配置

```dockerfile
# Dockerfile
FROM node:20-alpine AS builder
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build

FROM node:20-alpine
WORKDIR /app
COPY --from=builder /app/dist ./dist
COPY --from=builder /app/node_modules ./node_modules
COPY --from=builder /app/package.json ./
EXPOSE 3000
CMD ["node", "dist/app.js"]
```

```yaml
# docker-compose.yml
version: '3.8'
services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=postgresql://postgres:password@db:5432/shortener
      - PORT=3000
    depends_on:
      - db

  db:
    image: postgres:16-alpine
    environment:
      - POSTGRES_PASSWORD=password
      - POSTGRES_DB=shortener
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432"

volumes:
  pgdata:
```

### 7.2 CI/CD 配置

```yaml
# .github/workflows/ci.yml
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: test
          POSTGRES_DB: test
        ports:
          - 5432:5432

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - run: npm ci
      - run: npm run lint
      - run: npm run typecheck
      - run: npm test -- --coverage
      - run: npm run build
```

---

## 八、完整开发流程总结

```
AI 辅助开发周期总览：

Phase 1: 需求分析 (30 min)
├── 用户故事
├── API 设计
└── 技术选型

Phase 2: 架构设计 (30 min)
├── 数据库 Schema
├── 项目结构
└── 模块划分

Phase 3: 核心实现 (60 min)
├── 短码生成器
├── 业务 Service
├── 路由 Handler
└── 应用入口

Phase 4: 测试 (30 min)
├── 单元测试
├── 集成测试
└── API 测试

Phase 5: 文档 (15 min)
├── API 文档
├── README
└── 架构文档

Phase 6: 部署准备 (15 min)
├── Dockerfile
├── docker-compose
└── CI/CD 配置

总计：~3 小时（含学习）
```

---

## 九、练习

### 练习 1：完成 URL 短链接服务

使用 AI 辅助，按照本课的流程完成 URL 短链接服务的全部功能。

**验收标准**：
- [ ] 所有 API 端点正常工作
- [ ] 测试覆盖率 > 80%
- [ ] 包含完整 API 文档
- [ ] 可通过 docker-compose 启动

### 练习 2：添加 QR 码生成功能

为短链接服务添加 QR 码生成功能：
- `GET /api/v1/links/:code/qr` 返回 QR 码图片
- 使用 AI 选择合适的 QR 码库
- 编写测试用例

### 练习 3：实现批量创建

实现批量创建短链接的 API：
- `POST /api/v1/links/batch` 接受 URL 数组
- 支持最多 100 个 URL
- 返回创建结果列表
- 包含部分失败的处理

---

## 常见误区

### ❌ 误区 1：跳过需求分析直接写代码
觉得需求简单就直接开写，结果中途发现遗漏了关键功能，不得不推倒重来。**即使用 AI 辅助，也要先完成需求结构化再动手。**

### ❌ 误区 2：每个阶段只和 AI 交互一次
把整个需求一次性丢给 AI 让它"全搞定"，生成的代码往往质量低下。**每个阶段应该多轮对话，逐步细化和验证。**

### ❌ 误区 3：测试和文档留到最后补
先写完所有功能再补测试和文档，结果测试覆盖不到关键路径，文档和代码已经脱节。**测试和文档应该与代码同步生成。**

### ❌ 误区 4：过度依赖 AI 生成的架构决策
AI 不了解你的团队规模、业务约束和技术债务。**架构决策必须由人来做，AI 只提供参考方案。**

---

## 工程建议

1. **建立阶段检查点**：每个 Phase 结束前，花 5 分钟回顾产出物是否满足验收标准，再进入下一阶段。不要因为"AI 已经生成了"就跳过审查。

2. **保持可运行状态**：每完成一个模块的实现，确保项目能正常启动并通过已有测试。增量式开发比"全部写完再跑"更安全。

3. **记录 AI 的贡献边界**：标注哪些代码是 AI 生成的、哪些是人工修改的。这不仅方便代码审查，也有助于团队评估 AI 辅助开发的实际效果。

4. **从核心功能开始**：优先实现 P0 需求（短链接创建和重定向），确保核心链路跑通后再扩展 P1/P2 功能。避免一开始就陷入 QR 码生成等次要功能。

---

## 十、总结

### 关键收获

1. **AI 是加速器**：AI 可以将开发时间缩短 50%+
2. **流程很重要**：先设计后编码，AI 辅助每个阶段
3. **质量不能省**：测试、文档、安全一个都不能少
4. **迭代式开发**：先核心后扩展，逐步完善
5. **人机协作**：AI 生成基础，人工优化细节

### 本阶段知识图谱

```
从需求到代码 ──→ Git 工作流 ──→ 文档自动化
      │              │              │
      ▼              ▼              ▼
  调试工作流 ──→ 学习新技术 ──→ 代码迁移
      │              │              │
      └──────────────┼──────────────┘
                     ▼
              完整开发周期
          (综合运用所有技巧)
```

---

> **学习笔记区**
>
> 我的 AI 辅助开发流程：
>
> 最有帮助的 AI 使用场景：
>
> 需要进一步探索的技术：

---

## 参考答案

### 练习 1：完成 URL 短链接服务

**思路**：按照课程中的六阶段流程逐步完成。关键是保持增量可运行状态，每完成一个模块就运行测试验证。核心链路（创建短链接 + 重定向）优先，再扩展统计、认证等功能。

**答案**：

验收清单对照检查的实现要点：

```typescript
// src/utils/errors.ts - 全局错误处理
import { FastifyRequest, FastifyReply } from 'fastify';

export class AppError extends Error {
  constructor(
    public statusCode: number,
    message: string,
    public code?: string,
  ) {
    super(message);
  }
}

export class NotFoundError extends AppError {
  constructor(message = 'Resource not found') {
    super(404, message, 'NOT_FOUND');
  }
}

export class ConflictError extends AppError {
  constructor(message = 'Resource already exists') {
    super(409, message, 'CONFLICT');
  }
}

export class ValidationError extends AppError {
  constructor(message = 'Validation failed') {
    super(400, message, 'VALIDATION_ERROR');
  }
}

export function errorHandler(
  error: Error,
  _req: FastifyRequest,
  reply: FastifyReply,
) {
  if (error instanceof AppError) {
    return reply.status(error.statusCode).send({
      code: error.code,
      message: error.message,
    });
  }

  if (error.name === 'ZodError') {
    return reply.status(400).send({
      code: 'VALIDATION_ERROR',
      message: '请求参数错误',
      details: JSON.parse(error.message),
    });
  }

  console.error('Unhandled error:', error);
  return reply.status(500).send({
    code: 'INTERNAL_ERROR',
    message: '服务器内部错误',
  });
}
```

```typescript
// src/plugins/auth.ts - API Key 认证插件
import { FastifyInstance, FastifyRequest, FastifyReply } from 'fastify';
import fp from 'fastify-plugin';
import { db } from '../db';
import { apiKeys } from '../db/schema';
import { eq, and, isNull } from 'drizzle-orm';

async function authPlugin(app: FastifyInstance) {
  app.addHook('onRequest', async (req: FastifyRequest, reply: FastifyReply) => {
    // 跳过健康检查和重定向路由
    if (req.url === '/health' || !req.url.startsWith('/api/')) {
      return;
    }

    const apiKey = req.headers['x-api-key'] as string;
    if (!apiKey) {
      return reply.status(401).send({
        code: 'UNAUTHORIZED',
        message: '缺少 API Key',
      });
    }

    const [key] = await db.select().from(apiKeys).where(
      and(eq(apiKeys.key, apiKey), eq(apiKeys.isActive, true), isNull(apiKeys.deletedAt))
    ).limit(1);

    if (!key) {
      return reply.status(401).send({
        code: 'INVALID_API_KEY',
        message: 'API Key 无效或已禁用',
      });
    }

    // 更新最后使用时间
    await db.update(apiKeys)
      .set({ lastUsedAt: new Date() })
      .where(eq(apiKeys.id, key.id));

    (req as any).apiKeyId = key.id;
  });
}

export const authPlugin = fp(authPlugin);
```

```typescript
// src/utils/urlValidator.ts - URL 校验
import { ValidationError } from './errors';

const BLOCKED_DOMAINS = ['localhost', '127.0.0.1', '0.0.0.0', 'example.com'];
const MAX_URL_LENGTH = 2048;

export function validateUrl(url: string): void {
  if (!url || url.length > MAX_URL_LENGTH) {
    throw new ValidationError('URL 长度必须在 1-2048 之间');
  }

  let parsed: URL;
  try {
    parsed = new URL(url);
  } catch {
    throw new ValidationError('URL 格式不正确');
  }

  if (!['http:', 'https:'].includes(parsed.protocol)) {
    throw new ValidationError('仅支持 http 和 https 协议');
  }

  if (BLOCKED_DOMAINS.includes(parsed.hostname)) {
    throw new ValidationError('不允许的域名');
  }
}
```

**要点**：
- 错误处理采用继承体系，AppError 子类自带状态码和错误码，全局 errorHandler 统一格式化输出
- 认证插件用 fastify-plugin 包装，通过 `addHook` 在请求前拦截，跳过健康检查等无需认证的路由
- URL 校验在 Service 层调用，拒绝内网地址和过长 URL，防止 SSRF 攻击
- 每个模块独立可测试，Service 层不依赖 HTTP 框架，方便单元测试

### 练习 2：添加 QR 码生成功能

**思路**：使用 `qrcode` 库生成 QR 码图片，新增一个 GET 端点返回图片数据。关键是选择成熟的库、处理错误情况、并编写对应的测试用例。

**答案**：

```typescript
// src/services/qrService.ts
import QRCode from 'qrcode';
import { getLinkByCode } from './linkService';

export async function generateQRCode(
  code: string,
  options: { size?: number; format?: 'png' | 'svg' } = {},
): Promise<{ data: Buffer | string; contentType: string }> {
  const link = await getLinkByCode(code);
  const shortUrl = `${process.env.BASE_URL || 'http://localhost:3000'}/${link.code}`;

  const size = options.size || 300;
  const format = options.format || 'png';

  if (format === 'svg') {
    const svg = await QRCode.toString(shortUrl, {
      type: 'svg',
      width: size,
      margin: 2,
      color: { dark: '#000000', light: '#ffffff' },
    });
    return { data: svg, contentType: 'image/svg+xml' };
  }

  const buffer = await QRCode.toBuffer(shortUrl, {
    type: 'png',
    width: size,
    margin: 2,
    errorCorrectionLevel: 'M',
  });
  return { data: buffer, contentType: 'image/png' };
}
```

```typescript
// src/routes/links.ts 中添加 QR 端点
import * as qrService from '../services/qrService';

// 在 linkRoutes 函数中添加：
app.get('/api/v1/links/:code/qr', async (req: FastifyRequest, reply: FastifyReply) => {
  const { code } = req.params as { code: string };
  const query = req.query as { size?: string; format?: 'png' | 'svg' };

  const { data, contentType } = await qrService.generateQRCode(code, {
    size: query.size ? parseInt(query.size) : 300,
    format: query.format || 'png',
  });

  return reply
    .header('Content-Type', contentType)
    .header('Cache-Control', 'public, max-age=86400')
    .send(data);
});
```

```typescript
// tests/unit/qrService.test.ts
import { describe, it, expect, vi } from 'vitest';
import { generateQRCode } from '../../src/services/qrService';

vi.mock('../../src/services/linkService', () => ({
  getLinkByCode: vi.fn().mockResolvedValue({
    id: 'test-id',
    code: 'abc1234',
    originalUrl: 'https://example.com',
  }),
}));

describe('qrService', () => {
  it('应该生成 PNG 格式的 QR 码', async () => {
    const result = await generateQRCode('abc1234');
    expect(result.contentType).toBe('image/png');
    expect(Buffer.isBuffer(result.data)).toBe(true);
  });

  it('应该生成 SVG 格式的 QR 码', async () => {
    const result = await generateQRCode('abc1234', { format: 'svg' });
    expect(result.contentType).toBe('image/svg+xml');
    expect(typeof result.data).toBe('string');
    expect(result.data).toContain('<svg');
  });

  it('应该支持自定义尺寸', async () => {
    const result = await generateQRCode('abc1234', { size: 500 });
    expect(result.contentType).toBe('image/png');
  });

  it('应该在链接不存在时抛出错误', async () => {
    const { getLinkByCode } = await import('../../src/services/linkService');
    vi.mocked(getLinkByCode).mockRejectedValueOnce(new Error('Link not found'));
    await expect(generateQRCode('nonexistent')).rejects.toThrow('Link not found');
  });
});
```

**要点**：
- 选择 `qrcode` 库（npm 周下载量 100 万+），支持 PNG 和 SVG 两种格式输出
- QR 码内容使用完整的短链接 URL 而非原始 URL，确保扫码后走重定向链路
- 设置 `Cache-Control` 缓存头，同一短码的 QR 码不变，减少重复生成开销
- 测试覆盖正常路径（PNG/SVG/自定义尺寸）和异常路径（链接不存在）

### 练习 3：实现批量创建

**思路**：批量创建需要处理并发、部分失败、事务一致性等问题。关键是使用事务保证原子性，返回每个 URL 的独立结果（成功或失败），并限制批量大小防止滥用。

**答案**：

```typescript
// src/routes/links.ts 中添加批量端点
const batchCreateSchema = z.object({
  links: z.array(z.object({
    url: z.string().url('Invalid URL format'),
    title: z.string().max(200).optional(),
    customCode: z.string().min(3).max(20).regex(/^[a-zA-Z0-9_-]+$/).optional(),
    expiresAt: z.string().datetime().optional(),
  })).min(1).max(100, '单次最多创建 100 个短链接'),
});

// 在 linkRoutes 函数中添加：
app.post('/api/v1/links/batch', async (req: FastifyRequest, reply: FastifyReply) => {
  const body = batchCreateSchema.parse(req.body);
  const apiKeyId = (req as any).apiKeyId;

  const results = await linkService.batchCreateLinks(body.links, apiKeyId);

  const successCount = results.filter(r => r.success).length;
  const failCount = results.filter(r => !r.success).length;

  return reply.status(failCount === 0 ? 201 : 207).send({
    code: 0,
    data: {
      results,
      summary: {
        total: results.length,
        success: successCount,
        failed: failCount,
      },
    },
    message: failCount === 0 ? '全部创建成功' : '部分创建失败',
  });
});
```

```typescript
// src/services/linkService.ts 中添加批量创建方法
import { db } from '../db';
import { links } from '../db/schema';
import { generateCode } from './codeGenerator';
import { validateUrl } from '../utils/urlValidator';

interface BatchLinkInput {
  url: string;
  title?: string;
  customCode?: string;
  expiresAt?: string;
}

interface BatchResult {
  index: number;
  success: boolean;
  data?: any;
  error?: string;
}

export async function batchCreateLinks(
  inputs: BatchLinkInput[],
  apiKeyId?: string,
): Promise<BatchResult[]> {
  const results: BatchResult[] = [];

  // 使用事务保证批量操作的原子性（可选：也可以逐条独立处理）
  await db.transaction(async (tx) => {
    for (let i = 0; i < inputs.length; i++) {
      const input = inputs[i];
      try {
        validateUrl(input.url);

        const code = generateCode(input.customCode);

        const existing = await tx.select().from(links).where(eq(links.code, code)).limit(1);
        if (existing.length > 0) {
          results.push({
            index: i,
            success: false,
            error: `短码 "${code}" 已存在`,
          });
          continue;
        }

        const [link] = await tx.insert(links).values({
          code,
          originalUrl: input.url,
          title: input.title || null,
          expiresAt: input.expiresAt ? new Date(input.expiresAt) : null,
          isCustomCode: !!input.customCode,
          apiKeyId: apiKeyId || null,
        }).returning();

        results.push({ index: i, success: true, data: link });
      } catch (error) {
        results.push({
          index: i,
          success: false,
          error: error instanceof Error ? error.message : '创建失败',
        });
      }
    }
  });

  return results;
}
```

```typescript
// tests/api/batchLinks.test.ts
import { describe, it, expect, beforeAll, afterAll } from 'vitest';
import { buildApp } from '../../src/app';

describe('Batch Links API', () => {
  let app: Awaited<ReturnType<typeof buildApp>>;

  beforeAll(async () => {
    app = await buildApp();
    await app.ready();
  });

  afterAll(async () => {
    await app.close();
  });

  it('应该批量创建多个短链接', async () => {
    const response = await app.inject({
      method: 'POST',
      url: '/api/v1/links/batch',
      headers: { 'X-API-Key': 'test-key' },
      payload: {
        links: [
          { url: 'https://example.com/1' },
          { url: 'https://example.com/2' },
          { url: 'https://example.com/3' },
        ],
      },
    });

    expect(response.statusCode).toBe(201);
    const body = JSON.parse(response.body);
    expect(body.data.summary.total).toBe(3);
    expect(body.data.summary.success).toBe(3);
    expect(body.data.summary.failed).toBe(0);
  });

  it('应该处理部分失败的情况', async () => {
    const response = await app.inject({
      method: 'POST',
      url: '/api/v1/links/batch',
      headers: { 'X-API-Key': 'test-key' },
      payload: {
        links: [
          { url: 'https://example.com/valid' },
          { url: 'not-a-valid-url' },
        ],
      },
    });

    expect(response.statusCode).toBe(207);
    const body = JSON.parse(response.body);
    expect(body.data.summary.success).toBe(1);
    expect(body.data.summary.failed).toBe(1);
    expect(body.data.results[1].error).toContain('Invalid URL');
  });

  it('应该拒绝超过 100 个 URL 的批量请求', async () => {
    const links = Array.from({ length: 101 }, (_, i) => ({
      url: `https://example.com/${i}`,
    }));

    const response = await app.inject({
      method: 'POST',
      url: '/api/v1/links/batch',
      headers: { 'X-API-Key': 'test-key' },
      payload: { links },
    });

    expect(response.statusCode).toBe(400);
  });
});
```

**要点**：
- 使用 Zod Schema 校验批量请求，限制数组长度 1-100，防止滥用
- 返回 207 Multi-Status 状态码表示部分成功，每个结果独立包含成功/失败状态和错误原因
- 事务内逐条处理，单条失败不影响其他条目（也可以选择全部回滚策略，取决于业务需求）
- 测试覆盖全部成功、部分失败、超出限制三种场景
