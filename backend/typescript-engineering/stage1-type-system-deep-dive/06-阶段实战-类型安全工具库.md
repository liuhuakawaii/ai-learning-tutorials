# 06 - 阶段实战：类型安全工具库

到这里，泛型、条件类型、映射类型、`infer` 这些工具你都单独见过了。但真实项目中它们不会单独出现——你需要在一个函数里同时用到好几个。

你的团队有多个项目，每个项目都有一份 `pick`、`omit`、`get`、`set` 的实现，大部分没有类型安全。现在要建一个统一的工具库，把这些函数的类型做到位。

这节课的重点不是"怎么实现 pick"，而是"怎么让 pick 的类型在调用方那里精确到每个字段"。

## pick 和 omit

```typescript
function pick<T extends object, K extends keyof T>(obj: T, ...keys: K[]): Pick<T, K> {
  const result = {} as Pick<T, K>
  for (const key of keys) result[key] = obj[key]
  return result
}

function omit<T extends object, K extends keyof T>(obj: T, ...keys: K[]): Omit<T, K> {
  const keysSet = new Set<string | number | symbol>(keys)
  const result = {} as Omit<T, K>
  for (const key of Object.keys(obj) as Array<keyof T>) {
    if (!keysSet.has(key)) (result as any)[key] = obj[key]
  }
  return result
}
```

`K extends keyof T` 约束 keys 只能是 obj 上存在的属性名。泛型参数 `K` 推断为具体的字面量联合类型，所以返回类型能精确到你传入的那几个字段。

omit 里用了 `as any` 来绕过赋值检查——编译器无法在运行时证明 `Object.keys(obj)` 的结果不包含 `K`。这是工程上的取舍，`as any` 控制在函数内部，不让 `any` 泄漏到返回类型。

```typescript
interface UserProfile {
  id: string; name: string; email: string; age: number; active: boolean
}
declare const user: UserProfile

const publicInfo = pick(user, "name", "email")
// 类型是 { name: string; email: string }

const safe = omit(user, "email", "age")
// 类型是 { id: string; name: string; active: boolean }
```

## merge 和 deepMerge

```typescript
type Merged<A, B> = {
  [K in keyof A | keyof B]: K extends keyof B ? B[K] : K extends keyof A ? A[K] : never
}

function merge<A extends object, B extends object>(a: A, b: B): Merged<A, B> {
  return { ...a, ...b } as Merged<A, B>
}
```

`Merged` 遍历 A 和 B 的所有键，B 有就用 B 的类型（后者覆盖），否则用 A 的。

深合并需要递归：

```typescript
type DeepMerge<A, B> = A extends object
  ? B extends object
    ? { [K in keyof A | keyof B]: K extends keyof B
        ? K extends keyof A ? DeepMerge<A[K], B[K]> : B[K]
        : K extends keyof A ? A[K] : never }
    : B
  : B

function isPlainObject(value: unknown): value is Record<string, unknown> {
  return typeof value === "object" && value !== null && !Array.isArray(value) &&
    Object.getPrototypeOf(value) === Object.prototype
}

function deepMerge<A extends object, B extends object>(a: A, b: B): DeepMerge<A, B> {
  const result: Record<string, unknown> = { ...a }
  for (const key of Object.keys(b)) {
    const aVal = (a as Record<string, unknown>)[key]
    const bVal = (b as Record<string, unknown>)[key]
    if (isPlainObject(aVal) && isPlainObject(bVal)) {
      result[key] = deepMerge(aVal, bVal)
    } else {
      result[key] = bVal
    }
  }
  return result as DeepMerge<A, B>
}

const baseConfig = {
  server: { host: "localhost", port: 3000 },
  logging: { level: "info" as const, format: "json" },
}
const userConfig = {
  server: { port: 8080 },
  logging: { level: "debug" as const },
}
const finalConfig = deepMerge(baseConfig, userConfig)
// { server: { host: string; port: number }; logging: { level: "debug"; format: string } }
```

`isPlainObject` 确保数组和 class 实例不会被递归展开——数组用后者覆盖，不是合并。

## get 和 set：类型安全的嵌套访问

这是难度最高的部分。给定路径字符串 `"user.profile.address.city"`，怎么让返回值类型精确到 `string`？

```typescript
type GetByPath<T, P extends string> =
  P extends `${infer Key}.${infer Rest}`
    ? Key extends keyof T ? GetByPath<T[Key], Rest> : never
    : P extends keyof T ? T[P] : never

function get<T extends object, P extends string>(obj: T, path: P): GetByPath<T, P> {
  const keys = path.split(".")
  let result: any = obj
  for (const key of keys) {
    if (result == null) return undefined as GetByPath<T, P>
    result = result[key]
  }
  return result
}

function set<T extends object, P extends string>(
  obj: T, path: P, value: GetByPath<T, P>
): void {
  const keys = path.split(".")
  let current: any = obj
  for (let i = 0; i < keys.length - 1; i++) {
    current = current[keys[i]]
    if (current == null) return
  }
  current[keys[keys.length - 1]] = value
}
```

`GetByPath` 用模板字面量类型的递归：每一步用 `infer` 提取路径的第一段，然后在对象类型上查找这个 key，递归处理剩余路径。`set` 复用 `GetByPath` 推断 value 的类型。

```typescript
interface AppState {
  user: {
    profile: { name: string; address: { city: string; zip: string } }
    settings: { theme: "light" | "dark" }
  }
}
declare const state: AppState

const city = get(state, "user.profile.address.city")  // string
const theme = get(state, "user.settings.theme")        // "light" | "dark"
set(state, "user.settings.theme", "dark")              // ✅
// set(state, "user.settings.theme", "blue")           // 编译报错
```

## 类型测试

工具函数的类型正确性和运行时正确性同样重要。用 `expect-type` 在编译期验证：

```typescript
import { expectTypeOf } from "expect-type"

const picked = pick(user, "name", "email")
expectTypeOf(picked).toEqualTypeOf<{ name: string; email: string }>()

const merged = merge({ host: "localhost" }, { port: 3000 })
expectTypeOf(merged).toEqualTypeOf<{ host: string; port: number }>()
```

类型测试在 CI 中运行，某次重构不小心破坏了类型推断，测试会立即失败。

## 工程取舍

1. **deepMerge 不处理数组**：数组合并语义不明确（追加？替换？去重？），用后者覆盖是最安全的默认行为
2. **get 路径中断时返回 undefined**：不抛错，和 lodash.get 行为一致
3. **有些地方需要 `as any`**：编译器无法证明某些运行时操作的类型安全性，但要控制在函数内部

## 练习

### 练习一：实现 mapValues

实现 `mapValues` 函数，对对象的每个值进行映射，保持键不变。要求返回类型精确反映映射后的值类型。

### 练习二：实现 groupBy

实现 `groupBy<T, K extends keyof T>(items: T[], key: K)` 函数，按指定属性对数组元素分组。

### 练习三：实现类型安全的 pipe

实现 `pipe` 函数，支持链式函数组合，每个函数的输出类型自动作为下一个函数的输入类型。

---

## 参考答案

### 练习一

```typescript
function mapValues<T extends Record<string, unknown>, R>(
  obj: T, fn: (value: T[keyof T], key: keyof T) => R
): { [K in keyof T]: R } {
  const result = {} as { [K in keyof T]: R }
  for (const key of Object.keys(obj) as Array<keyof T>) {
    (result as any)[key] = fn(obj[key], key)
  }
  return result
}
```

`{ [K in keyof T]: R }` 遍历 T 的所有键，将值类型统一替换为 R。

### 练习二

```typescript
function groupBy<T extends Record<string, unknown>, K extends keyof T>(
  items: T[], key: K
): Record<string, T[]> {
  const result: Record<string, T[]> = {}
  for (const item of items) {
    const groupKey = String(item[key])
    if (!result[groupKey]) result[groupKey] = []
    result[groupKey].push(item)
  }
  return result
}
```

### 练习三

```typescript
function pipe<A, B>(ab: (a: A) => B): (a: A) => B
function pipe<A, B, C>(ab: (a: A) => B, bc: (b: B) => C): (a: A) => C
function pipe<A, B, C, D>(ab: (a: A) => B, bc: (b: B) => C, cd: (c: C) => D): (a: A) => D
function pipe(...fns: Function[]) {
  return (input: unknown) => fns.reduce((result, fn) => fn(result), input)
}
```

函数重载让 TypeScript 自动推断每一步的类型。
