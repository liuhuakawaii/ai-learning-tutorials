# n8n 配置
N8N_HOST=n8n.example.com
N8N_USER=admin
N8N_PASSWORD=changeme-use-strong-password

# 加密密钥（用 openssl rand -hex 32 生成）
N8N_ENCRYPTION_KEY=changeme-generate-random-key

# PostgreSQL
DB_PASSWORD=changeme-use-strong-password

# 备份配置
BACKUP_DIR=/backups
BACKUP_RETENTION_DAYS=30
