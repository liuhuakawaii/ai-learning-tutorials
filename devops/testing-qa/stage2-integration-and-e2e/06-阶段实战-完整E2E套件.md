# 阶段实战：完�?E2E 测试套件

## 场景引入

单元测试验证单个函数，但用户关心的是"注册→登录→创建任务→搜索→删除"这条完整链路。E2E 测试模拟真实用户操作，从浏览器打开页面开始逐步验证每个交互流程。本节课�?Playwright 搭建完整�?E2E 测试套件，演�?Page Object 模式、fixture 复用、并行执行和测试报告生成�?
## 学习目标

- 掌握�?Page Object 模式组织 E2E 测试代码
- 学会使用 Playwright fixture 复用浏览器上下文
- 理解并行执行策略和测试隔离机�?
---

## 一、项目结�?
```
e2e/
├── playwright.config.ts
├── fixtures/index.ts          # 自定�?fixture
├── pages/
�?  ├── auth.page.ts           # 登录/注册�?�?  └── todo.page.ts           # Todo 主页
├── tests/
�?  ├── auth.spec.ts           # 认证流程
�?  ├── todo-crud.spec.ts      # 增删改查
�?  └── search-filter.spec.ts  # 搜索筛�?└── utils/test-data.ts         # 测试数据工厂
```

---

## 二、Playwright 配置

```typescript
// e2e/playwright.config.ts
import { defineConfig, devices } from '@playwright/test'

export default defineConfig({
  testDir: './tests',
  fullyParallel: true,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 2 : 0,
  workers: process.env.CI ? 2 : undefined,
  reporter: [['html', { open: 'never' }], ['list']],
  use: {
    baseURL: 'http://localhost:3000',
    trace: 'on-first-retry',
    screenshot: 'only-on-failure',
  },
  projects: [
    { name: 'chromium', use: { ...devices['Desktop Chrome'] } },
    { name: 'firefox', use: { ...devices['Desktop Firefox'] } },
  ],
  webServer: {
    command: 'npm run dev',
    url: 'http://localhost:3000',
    reuseExistingServer: !process.env.CI,
  },
})
```

---

## 三、测试数据工�?
```typescript
// e2e/utils/test-data.ts
let userCounter = 0

export function createTestUser(overrides: Record<string, string> = {}) {
  userCounter++
  return {
    email: `test_${Date.now()}_${userCounter}@example.com`,
    password: 'Test@123456',
    name: `测试用户${userCounter}`,
    ...overrides,
  }
}

export function createTestTodo(overrides: Record<string, string> = {}) {
  return { title: `测试任务 ${Date.now()}`, ...overrides }
}
```

---

## 四、Page Object 模式

```typescript
// e2e/pages/auth.page.ts
import { type Page, type Locator, expect } from '@playwright/test'

export class AuthPage {
  readonly email: Locator; readonly password: Locator; readonly name: Locator
  readonly submit: Locator; readonly toggle: Locator; readonly error: Locator

  constructor(private page: Page) {
    this.email = page.getByLabel('邮箱'); this.password = page.getByLabel('密码')
    this.name = page.getByLabel('用户�?); this.submit = page.getByRole('button', { name: /登录|注册/ })
    this.toggle = page.getByRole('link', { name: /注册|登录/ }); this.error = page.getByRole('alert')
  }

  async goto() { await this.page.goto('/login') }
  async register(email: string, password: string, name: string) {
    await this.toggle.click()
    await this.name.fill(name); await this.email.fill(email)
    await this.password.fill(password); await this.submit.click()
  }
  async login(email: string, password: string) {
    await this.email.fill(email); await this.password.fill(password); await this.submit.click()
  }
  async expectError(msg: string | RegExp) { await expect(this.error).toContainText(msg) }
  async expectLoggedIn(name: string) { await expect(this.page.getByText(`欢迎, ${name}`)).toBeVisible() }
}
```

```typescript
// e2e/pages/todo.page.ts
import { type Page, type Locator, expect } from '@playwright/test'

export class TodoPage {
  readonly newTodo: Locator; readonly addBtn: Locator; readonly search: Locator; readonly emptyState: Locator

  constructor(private page: Page) {
    this.newTodo = page.getByPlaceholder('添加新任�?..')
    this.addBtn = page.getByRole('button', { name: '添加' })
    this.search = page.getByPlaceholder('搜索任务...')
    this.emptyState = page.getByText('暂无任务')
  }

  private item(t: string) { return this.page.getByRole('listitem').filter({ hasText: t }) }
  async goto() { await this.page.goto('/todos') }
  async addTodo(t: string) { await this.newTodo.fill(t); await this.addBtn.click() }
  async editTodo(old: string, neo: string) {
    await this.item(old).getByRole('button', { name: '编辑' }).click()
    const input = this.page.getByLabel('编辑任务标题')
    await input.clear(); await input.fill(neo)
    await this.page.getByRole('button', { name: '保存' }).click()
  }
  async deleteTodo(t: string) {
    await this.item(t).getByRole('button', { name: '删除' }).click()
    await this.page.getByRole('button', { name: '确认删除' }).click()
  }
  async toggleTodo(t: string) { await this.item(t).getByRole('checkbox').click() }
  async searchFor(kw: string) { await this.search.fill(kw) }
  async filterBy(f: 'all' | 'active' | 'completed') {
    const map = { all: '全部', active: '进行�?, completed: '已完�? }
    await this.page.getByRole('tab', { name: map[f] }).click()
  }
  async expectVisible(t: string) { await expect(this.item(t)).toBeVisible() }
  async expectNotVisible(t: string) { await expect(this.item(t)).not.toBeVisible() }
  async expectCount(n: number) { await expect(this.page.getByRole('listitem')).toHaveCount(n) }
}
```

---

## 五、自定义 Fixture

```typescript
// e2e/fixtures/index.ts
import { test as base } from '@playwright/test'
import { AuthPage } from '../pages/auth.page'
import { TodoPage } from '../pages/todo.page'
import { createTestUser } from '../utils/test-data'

export const test = base.extend<{ authPage: AuthPage; todoPage: TodoPage; loginAndGo: void }>({
  authPage: async ({ page }, use) => { await use(new AuthPage(page)) },
  todoPage: async ({ page }, use) => { await use(new TodoPage(page)) },
  loginAndGo: async ({ page }, use) => {
    const auth = new AuthPage(page); const user = createTestUser()
    await auth.goto(); await auth.register(user.email, user.password, user.name)
    await auth.expectLoggedIn(user.name); await new TodoPage(page).goto(); await use()
  },
})
export { expect } from '@playwright/test'
```

---

## 六、测试用�?
### 认证流程

```typescript
// e2e/tests/auth.spec.ts
import { test, expect } from '../fixtures'
import { createTestUser } from '../utils/test-data'

test.describe('用户认证', () => {
  test('注册后自动登�?, async ({ authPage }) => {
    const u = createTestUser()
    await authPage.goto(); await authPage.register(u.email, u.password, u.name)
    await authPage.expectLoggedIn(u.name)
  })

  test('错误密码登录失败', async ({ authPage }) => {
    const u = createTestUser()
    await authPage.goto(); await authPage.register(u.email, u.password, u.name)
    await authPage.page.getByRole('button', { name: '退�? }).click()
    await authPage.goto(); await authPage.login(u.email, 'wrongpassword')
    await authPage.expectError(/密码错误/)
  })

  test('重复邮箱注册失败', async ({ authPage }) => {
    const u = createTestUser()
    await authPage.goto(); await authPage.register(u.email, u.password, u.name)
    await authPage.page.getByRole('button', { name: '退�? }).click()
    await authPage.goto(); await authPage.register(u.email, u.password, '另一用户')
    await authPage.expectError(/邮箱已注�?)
  })
})
```

### 任务增删改查

```typescript
// e2e/tests/todo-crud.spec.ts
import { test } from '../fixtures'

test.describe('任务增删改查', () => {
  test('创建任务', async ({ loginAndGo, todoPage }) => {
    await todoPage.addTodo('买咖�?); await todoPage.expectVisible('买咖�?)
  })
  test('编辑任务', async ({ loginAndGo, todoPage }) => {
    await todoPage.addTodo('原标�?); await todoPage.editTodo('原标�?, '新标�?)
    await todoPage.expectVisible('新标�?); await todoPage.expectNotVisible('原标�?)
  })
  test('删除任务', async ({ loginAndGo, todoPage }) => {
    await todoPage.addTodo('待删�?); await todoPage.deleteTodo('待删�?)
    await todoPage.expectNotVisible('待删�?)
  })
  test('完成任务', async ({ loginAndGo, todoPage }) => {
    await todoPage.addTodo('待完�?); await todoPage.toggleTodo('待完�?)
    await expect(todoPage.todoList.locator('li').filter({ hasText: '待完�? })).toHaveClass(/completed/)
  })
})
```

### 搜索与筛�?
```typescript
// e2e/tests/search-filter.spec.ts
import { test, expect } from '../fixtures'

test.describe('搜索与筛�?, () => {
  test.beforeEach(async ({ loginAndGo, todoPage }) => {
    await todoPage.addTodo('买牛�?); await todoPage.addTodo('买面�?)
    await todoPage.addTodo('写周�?); await todoPage.toggleTodo('买牛�?)
  })

  test('搜索任务', async ({ todoPage }) => {
    await todoPage.searchFor('�?)
    await todoPage.expectVisible('买牛�?); await todoPage.expectVisible('买面�?)
    await todoPage.expectNotVisible('写周�?)
  })
  test('搜索无结�?, async ({ todoPage }) => {
    await todoPage.searchFor('不存�?); await expect(todoPage.emptyState).toBeVisible()
  })
  test('筛选进行中/已完�?, async ({ todoPage }) => {
    await todoPage.filterBy('active')
    await todoPage.expectVisible('买面�?); await todoPage.expectNotVisible('买牛�?)
    await todoPage.filterBy('completed')
    await todoPage.expectVisible('买牛�?); await todoPage.expectNotVisible('买面�?)
  })
})
```

---

## 七、运行与报告

```bash
npx playwright test                       # 全部测试
npx playwright test --project=chromium    # 只跑 Chromium
npx playwright test --headed              # 有头模式调试
npx playwright show-report                # 查看报告
```

```json
{ "scripts": { "test:e2e": "playwright test", "test:e2e:debug": "playwright test --debug" } }
```

---

## 常见误区

1. **硬编码等�?*：用自动等待�?`expect(...).toBeVisible()` 替代 `waitForTimeout`
2. **测试间有依赖**：每个测试必须独立，不依赖上一个测试的数据
3. **脆弱选择�?*：优先用 `getByRole`、`getByLabel`、`getByText`，避�?CSS 类名
4. **Page Object 里写断言**：Page Object 只封装操作，断言留给测试用例

## 工程建议

1. Page Object 方法名用业务语言，如 `addTodo('买咖�?)`
2. CI �?`--retries=2` 减少误报，定期清�?flaky test
3. 测试数据工厂每次生成唯一数据，支持并行执�?4. `trace: 'on-first-retry'` �?`trace: 'on'` 更高�?
## 小结

关键收获：Page Object 模式封装页面操作，Playwright fixture 自动准备登录状态，测试数据工厂保证数据独立，语义化选择器让测试更稳定�?
## 练习

### 练习一：补充分页测�?假设每页显示 10 条，支持�?下页切换。编�?Page Object 方法和测试用例�?
### 练习二：补充批量删除测试
假设支持勾选多个任务后批量删除。编写完�?E2E 测试�?
### 练习三：修复不稳定测�?
```typescript
test('搜索', async ({ page }) => {
  await page.goto('/todos')
  await page.waitForTimeout(2000)
  await page.fill('#search', '�?)
  await page.waitForTimeout(1000)
  const items = await page.$$('.todo-item')
  expect(items.length).toBe(2)
})
```

---

## 参考答�?
### 练习一

```typescript
async goToNextPage() { await this.page.getByRole('button', { name: '下一�? }).click() }

test.describe('分页', () => {
  test.beforeEach(async ({ loginAndGo, todoPage }) => {
    for (let i = 1; i <= 15; i++) await todoPage.addTodo(`任务${i}`)
  })
  test('每页 10 �?, async ({ todoPage }) => { await todoPage.expectCount(10) })
  test('下一�?, async ({ todoPage }) => { await todoPage.goToNextPage(); await todoPage.expectCount(5) })
})
```

### 练习�?
```typescript
async selectTodo(t: string) { await this.item(t).getByRole('checkbox', { name: '选择' }).click() }
async batchDelete() { await this.page.getByRole('button', { name: '批量删除' }).click(); await this.page.getByRole('button', { name: '确认删除' }).click() }

test('批量删除', async ({ loginAndGo, todoPage }) => {
  await todoPage.addTodo('一'); await todoPage.addTodo('�?); await todoPage.addTodo('�?)
  await todoPage.selectTodo('一'); await todoPage.selectTodo('�?); await todoPage.batchDelete()
  await todoPage.expectNotVisible('一'); await todoPage.expectVisible('�?); await todoPage.expectCount(1)
})
```

### 练习�?
**问题**：`waitForTimeout`（固定等待）�?`$$`（脆弱选择器）�?
```typescript
test('搜索', async ({ todoPage }) => {
  await todoPage.goto()
  await todoPage.searchFor('�?)
  await todoPage.expectVisible('买牛�?)
  await todoPage.expectCount(2)
})
```

去掉 `waitForTimeout` 改用自动等待，去�?CSS 选择器改�?Page Object�