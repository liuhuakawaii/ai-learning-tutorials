# 05 能力协商——Client 与 Server 的能力发现与协商机制

> Client 和 Server 在连接时需要交换能力信息，确保双方理解对方支持的功能。

## 学习目标

- 理解能力协商的机制和流程
- 掌握能力定义和解析方法
- 学会实现能力协商逻辑

---

## 一、能力协商流程

```
能力协商流程：

1. Client 发送初始化请求
   {
     "method": "initialize",
     "params": {
       "capabilities": {
         "tools": {},
         "resources": {"subscribe": true},
         "prompts": {}
       }
     }
   }

2. Server 返回能力
   {
     "result": {
       "capabilities": {
         "tools": {"listChanged": true},
         "resources": {"subscribe": true},
         "prompts": {}
       }
     }
   }

3. Client 发送初始化完成通知
   {
     "method": "notifications/initialized"
   }
```

---

## 二、能力定义

```python
class ServerCapabilities:
    """Server 能力"""
    
    def __init__(self):
        self.tools = {"listChanged": True}
        self.resources = {"subscribe": True}
        self.prompts = {}
    
    def to_dict(self) -> dict:
        return {
            "tools": self.tools,
            "resources": self.resources,
            "prompts": self.prompts
        }

class ClientCapabilities:
    """Client 能力"""
    
    def __init__(self):
        self.tools = {}
        self.resources = {"subscribe": True}
        self.prompts = {}
    
    def to_dict(self) -> dict:
        return {
            "tools": self.tools,
            "resources": self.resources,
            "prompts": self.prompts
        }
```

---

## 三、能力检查

```python
class CapabilityChecker:
    """能力检查器"""
    
    def check_tool_support(self, server_caps: dict, tool_name: str) -> bool:
        """检查 Server 是否支持工具"""
        return "tools" in server_caps
    
    def check_resource_subscription(self, server_caps: dict) -> bool:
        """检查 Server 是否支持资源订阅"""
        resources = server_caps.get("resources", {})
        return resources.get("subscribe", False)
```

---

## 四、动态能力更新

```python
class DynamicCapabilities:
    """动态能力管理"""
    
    def __init__(self):
        self.capabilities = {}
        self.change_listeners = []
    
    def update(self, new_capabilities: dict):
        """更新能力"""
        self.capabilities = new_capabilities
        self._notify_change()
    
    def on_change(self, listener):
        """监听能力变更"""
        self.change_listeners.append(listener)
    
    def _notify_change(self):
        """通知变更"""
        for listener in self.change_listeners:
            listener(self.capabilities)
```

---

## 小结

```
本课核心要点：

1. 能力协商在初始化时进行
2. Client 和 Server 交换支持的功能列表
3. 能力检查确保调用时功能可用
4. 支持动态能力更新和通知

下一课：阶段实战——用原始 HTTP 请求实现一个最小 MCP 通信。
```

---

## 练习

1. **协商题**：实现一个能力协商流程。

2. **检查题**：实现能力检查功能。

3. **动态题**：实现动态能力更新机制。
