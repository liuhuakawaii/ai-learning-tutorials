# 03 消息格式详解——JSON-RPC 2.0 在 MCP 中的应用

> MCP 使用 JSON-RPC 2.0 作为消息格式。理解它是开发 MCP Server 的基础。

## 学习目标

- 掌握 JSON-RPC 2.0 的消息格式
- 理解请求、响应、通知的区别
- 学会构造和解析 MCP 消息

---

## 一、JSON-RPC 2.0 基础

```
JSON-RPC 2.0 消息类型：

1. Request（请求）
   {
     "jsonrpc": "2.0",
     "id": 1,
     "method": "tools/call",
     "params": {...}
   }

2. Response（响应）
   {
     "jsonrpc": "2.0",
     "id": 1,
     "result": {...}
   }

3. Error（错误响应）
   {
     "jsonrpc": "2.0",
     "id": 1,
     "error": {
       "code": -32600,
       "message": "Invalid Request"
     }
   }

4. Notification（通知，无 id）
   {
     "jsonrpc": "2.0",
     "method": "notifications/progress",
     "params": {...}
   }
```

---

## 二、MCP 特定方法

```
MCP 方法：

初始化：
- initialize: 初始化连接

工具：
- tools/list: 获取工具列表
- tools/call: 调用工具

资源：
- resources/list: 获取资源列表
- resources/read: 读取资源
- resources/subscribe: 订阅资源变更

提示：
- prompts/list: 获取提示列表
- prompts/get: 获取提示内容
```

---

## 三、消息处理

```python
import json

class MCPMessageHandler:
    """MCP 消息处理器"""
    
    def handle_request(self, request: dict) -> dict:
        """处理请求"""
        method = request.get("method")
        params = request.get("params", {})
        request_id = request.get("id")
        
        if method == "tools/list":
            return self._handle_tools_list(request_id)
        elif method == "tools/call":
            return self._handle_tools_call(request_id, params)
        else:
            return self._error_response(request_id, -32601, "Method not found")
    
    def _handle_tools_list(self, request_id: int) -> dict:
        """处理工具列表请求"""
        return {
            "jsonrpc": "2.0",
            "id": request_id,
            "result": {
                "tools": [
                    {
                        "name": "database_query",
                        "description": "执行数据库查询",
                        "inputSchema": {
                            "type": "object",
                            "properties": {
                                "sql": {"type": "string"}
                            }
                        }
                    }
                ]
            }
        }
    
    def _error_response(self, request_id: int, code: int, message: str) -> dict:
        """错误响应"""
        return {
            "jsonrpc": "2.0",
            "id": request_id,
            "error": {
                "code": code,
                "message": message
            }
        }
```

---

## 四、错误码

```
标准错误码：

-32700: Parse error（解析错误）
-32600: Invalid Request（无效请求）
-32601: Method not found（方法未找到）
-32602: Invalid params（无效参数）
-32603: Internal error（内部错误）

MCP 特定错误码：
-32000: Tool not found
-32001: Tool execution failed
-32002: Resource not found
```

---

## 小结

```
本课核心要点：

1. JSON-RPC 2.0 是 MCP 的消息格式
2. 消息类型：Request、Response、Error、Notification
3. MCP 特定方法：tools/*、resources/*、prompts/*
4. 错误处理使用标准错误码

---

**下一课**: [04 传输层——stdio / SSE / Streamable HTTP 的选型与实现](./04-传输层.md)
```

---

## 练习

1. **消息题**：构造一个 tools/call 请求和响应。

2. **处理题**：实现一个简单的消息处理器。

3. **错误题**：实现错误处理和错误码映射。
