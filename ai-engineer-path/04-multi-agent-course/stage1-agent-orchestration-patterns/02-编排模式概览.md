# 02 编排模式概览——Supervisor / Sequential / Parallel / Hierarchical

> 多 Agent 的核心问题：谁来决定哪个 Agent 干什么？

## 学习目标

- 掌握四种主流多 Agent 编排模式
- 理解每种模式的适用场景
- 学会根据任务特征选择合适的模式

---

## 一、四种编排模式

```
┌─────────────────────────────────────────────────────────────┐
│                    四种编排模式                               │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  1. Sequential（顺序执行）                                   │
│     Agent A → Agent B → Agent C                             │
│     适用：有明确先后依赖的任务                                │
│                                                             │
│  2. Parallel（并行执行）                                     │
│     Agent A ─┐                                              │
│     Agent B ─┼→ 汇总                                        │
│     Agent C ─┘                                              │
│     适用：无依赖、可并行的任务                                │
│                                                             │
│  3. Supervisor（监督者）                                     │
│           ┌─→ Agent A                                       │
│     监督者 ┼→ Agent B                                       │
│           └→ Agent C                                        │
│     适用：需要动态决策的复杂任务                              │
│                                                             │
│  4. Hierarchical（层级式）                                   │
│           ┌─→ 团队 1 ─┬→ Agent A                            │
│     总监 ─┼→ 团队 2 ─┼→ Agent B                            │
│           └→ 团队 3 ─┼→ Agent C                            │
│     适用：大型复杂项目                                        │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 二、模式实现

### 2.1 Sequential 模式

```python
class SequentialOrchestrator:
    """顺序执行编排器"""
    
    def __init__(self, agents: list):
        self.agents = agents
    
    def run(self, task: str) -> str:
        result = task
        for agent in self.agents:
            result = agent.execute(result)
        return result
```

### 2.2 Parallel 模式

```python
class ParallelOrchestrator:
    """并行执行编排器"""
    
    def __init__(self, agents: list, aggregator):
        self.agents = agents
        self.aggregator = aggregator
    
    def run(self, task: str) -> str:
        # 并行执行
        results = []
        with ThreadPoolExecutor() as executor:
            futures = [executor.submit(agent.execute, task) for agent in self.agents]
            results = [f.result() for f in futures]
        
        # 汇总结果
        return self.aggregator.aggregate(results)
```

### 2.3 Supervisor 模式

```python
class SupervisorOrchestrator:
    """监督者编排器"""
    
    def __init__(self, supervisor, agents: dict):
        self.supervisor = supervisor
        self.agents = agents
    
    def run(self, task: str) -> str:
        # 监督者决策
        decision = self.supervisor.decide(task)
        
        # 选择 Agent 执行
        agent = self.agents[decision["agent"]]
        result = agent.execute(decision["subtask"])
        
        # 监督者评估
        if self.supervisor.should_continue(result):
            return self.run(decision["next_task"])
        
        return result
```

---

## 三、模式选择

```
模式选择指南：

任务有明确顺序？
├── 是 → Sequential
└── 否 → 任务可并行？
    ├── 是 → Parallel
    └── 否 → 需要动态决策？
        ├── 是 → Supervisor
        └── 否 → 任务复杂度高？
            ├── 是 → Hierarchical
            └── 否 → Sequential
```

---

## 小结

```
本课核心要点：

1. Sequential：简单、可预测、适合有依赖的任务
2. Parallel：高效、适合无依赖的任务
3. Supervisor：灵活、适合需要动态决策的任务
4. Hierarchical：适合大型复杂项目

---

**下一课**: [Supervisor 模式——一个管理者调度多个专家 Agent](./03-Supervisor模式.md)
```

---

## 练习

1. **选择题**：你的任务适合用哪种编排模式？为什么？

2. **实现题**：实现一个 Parallel 编排器，支持 3 个 Agent 并行执行。

3. **设计题**：设计一个 Supervisor 模式的架构图。
