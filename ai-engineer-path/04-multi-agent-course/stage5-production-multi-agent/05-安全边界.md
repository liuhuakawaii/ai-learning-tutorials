# 05 安全边界——Agent 权限隔离、输出审核、越权防护

> 多 Agent 系统的安全比单 Agent 更复杂。权限隔离是关键。

## 学习目标

- 掌握多 Agent 系统的安全设计
- 理解权限隔离、输出审核、越权防护
- 学会设计安全的多 Agent 系统

---

## 一、权限隔离

```python
class AgentPermissions:
    """Agent 权限管理"""
    
    def __init__(self):
        self.permissions = {}
    
    def set_permissions(self, agent_name: str, permissions: list):
        """设置 Agent 权限"""
        self.permissions[agent_name] = permissions
    
    def check_permission(self, agent_name: str, action: str) -> bool:
        """检查权限"""
        agent_perms = self.permissions.get(agent_name, [])
        return action in agent_perms

# 使用示例
permissions = AgentPermissions()
permissions.set_permissions("researcher", ["search", "read"])
permissions.set_permissions("writer", ["read", "write"])
permissions.set_permissions("admin", ["search", "read", "write", "delete"])

# 检查权限
if permissions.check_permission("researcher", "delete"):
    # 执行删除
    pass
else:
    raise PermissionError("Agent 没有删除权限")
```

---

## 二、输出审核

```python
class OutputAuditor:
    """输出审核器"""
    
    def __init__(self, llm):
        self.llm = llm
    
    def audit(self, agent_name: str, output: str) -> dict:
        """审核输出"""
        prompt = f"""审核以下 Agent 输出的安全性。

Agent: {agent_name}
输出: {output}

检查项：
1. 是否包含有害内容
2. 是否泄露敏感信息
3. 是否包含幻觉
4. 是否符合规范

请以 JSON 格式输出审核结果。"""
        
        response = self.llm.invoke(prompt)
        return json.loads(response.content)
```

---

## 三、越权防护

```python
class AuthorizationGuard:
    """越权防护"""
    
    def __init__(self, permissions: AgentPermissions):
        self.permissions = permissions
    
    def guard(self, agent_name: str, action: str, resource: str):
        """防护检查"""
        if not self.permissions.check_permission(agent_name, action):
            raise AuthorizationError(f"Agent {agent_name} 没有 {action} 权限")
        
        # 检查资源访问权限
        if not self._check_resource_access(agent_name, resource):
            raise AuthorizationError(f"Agent {agent_name} 没有访问 {resource} 的权限")
    
    def _check_resource_access(self, agent_name: str, resource: str) -> bool:
        """检查资源访问权限"""
        # 实现资源级别的权限检查
        return True
```

---

## 四、审计日志

```python
class AuditLogger:
    """审计日志"""
    
    def log_action(self, agent_name: str, action: str, resource: str, result: str):
        """记录操作日志"""
        log_entry = {
            "agent": agent_name,
            "action": action,
            "resource": resource,
            "result": result,
            "timestamp": datetime.now().isoformat()
        }
        # 存储到审计日志
```

---

## 五、安全最佳实践

```
安全最佳实践：

1. 最小权限原则
   - 每个 Agent 只拥有必要的权限
   - 定期审查和调整权限

2. 输出审核
   - 所有输出都要经过安全审核
   - 高风险操作需要人工审核

3. 越权防护
   - 检查每个操作的权限
   - 记录所有越权尝试

4. 审计日志
   - 记录所有操作
   - 支持追溯和审计

5. 定期安全评估
   - 定期进行安全测试
   - 修复发现的安全漏洞
```

---

## 小结

```
本课核心要点：

1. 权限隔离限制 Agent 的操作范围
2. 输出审核确保输出安全
3. 越权防护阻止非法操作
4. 审计日志支持追溯和审计

下一课：阶段实战——将多 Agent 系统部署到生产环境并完成压力测试。
```

---

## 练习

1. **权限题**：为你的 Agent 系统设计权限模型。

2. **审核题**：实现一个输出审核器。

3. **防护题**：实现越权防护功能。
