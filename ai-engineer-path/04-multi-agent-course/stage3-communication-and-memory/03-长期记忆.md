# 03 长期记忆——跨会话的知识持久化与检索

> 短期记忆会话结束就没了。长期记忆让 Agent 能"记住"之前学到的东西。

## 学习目标

- 掌握长期记忆的设计方法
- 理解知识持久化和检索机制
- 学会实现跨会话的记忆

---

## 一、长期记忆架构

```
长期记忆架构：

用户输入
    │
    ▼
┌─────────────────────────────────────────┐
│  记忆检索                                │
│  ├── 相关记忆查询                        │
│  └── 记忆排序和过滤                      │
└────────────┬────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────┐
│  Agent 执行                             │
│  ├── 使用检索到的记忆                    │
│  └── 生成响应                           │
└────────────┬────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────┐
│  记忆存储                                │
│  ├── 提取关键信息                        │
│  └── 存储到长期记忆                      │
└─────────────────────────────────────────┘
```

---

## 二、记忆存储实现

```python
import chromadb
from datetime import datetime

class LongTermMemory:
    """长期记忆"""
    
    def __init__(self, collection_name: str = "agent_memory"):
        self.client = chromadb.Client()
        self.collection = self.client.get_or_create_collection(collection_name)
    
    def store(self, content: str, metadata: dict = None):
        """存储记忆"""
        doc_id = f"mem_{datetime.now().timestamp()}"
        
        self.collection.add(
            documents=[content],
            metadatas=[metadata or {}],
            ids=[doc_id]
        )
    
    def retrieve(self, query: str, top_k: int = 5) -> list:
        """检索相关记忆"""
        results = self.collection.query(
            query_texts=[query],
            n_results=top_k
        )
        
        return [
            {"content": doc, "metadata": meta, "distance": dist}
            for doc, meta, dist in zip(
                results["documents"][0],
                results["metadatas"][0],
                results["distances"][0]
            )
        ]
    
    def forget(self, older_than_days: int = 30):
        """遗忘旧记忆"""
        cutoff = datetime.now() - timedelta(days=older_than_days)
        # 实现遗忘逻辑
```

---

## 三、记忆整合

```python
class MemoryConsolidator:
    """记忆整合器"""
    
    def __init__(self, llm, memory: LongTermMemory):
        self.llm = llm
        self.memory = memory
    
    def consolidate(self, conversation: list):
        """整合对话中的关键信息"""
        
        conversation_text = "\n".join([f"{m['role']}: {m['content']}" for m in conversation])
        
        prompt = f"""从以下对话中提取关键信息，用于长期记忆。

对话：
{conversation_text}

请提取：
1. 用户的偏好
2. 重要的决策
3. 学到的知识
4. 需要记住的事实

请以 JSON 格式输出。"""
        
        response = self.llm.invoke(prompt)
        memories = json.loads(response.content)
        
        for memory in memories:
            self.memory.store(memory["content"], memory.get("metadata"))
```

---

## 四、记忆检索策略

```
记忆检索策略：

1. 语义检索
   - 用向量相似度检索
   - 适用于概念性记忆

2. 时间检索
   - 按时间顺序检索
   - 适用于事件性记忆

3. 关键词检索
   - 用关键词匹配
   - 适用于特定事实

4. 混合检索
   - 结合多种策略
   - 提高检索准确率
```

---

## 小结

```
本课核心要点：

1. 长期记忆支持跨会话的知识持久化
2. 用向量数据库存储和检索记忆
3. 记忆整合提取对话中的关键信息
4. 检索策略：语义、时间、关键词、混合

下一课：共享工作空间——Agent 间的文件/数据共享机制。
```

---

## 练习

1. **实现题**：实现一个长期记忆系统。

2. **整合题**：实现记忆整合功能，从对话中提取关键信息。

3. **检索题**：实现语义检索功能。
