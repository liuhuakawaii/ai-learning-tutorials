# 02 短期记忆——会话上下文在 Agent 间的传递策略

> 多 Agent 系统中，上下文如何在 Agent 间流转？

## 学习目标

- 掌握短期记忆的设计方法
- 理解上下文传递策略
- 学会管理会话状态

---

## 一、短期记忆设计

```python
class ShortTermMemory:
    """短期记忆"""
    
    def __init__(self, max_messages: int = 50):
        self.messages = []
        self.max_messages = max_messages
    
    def add(self, role: str, content: str, metadata: dict = None):
        """添加消息"""
        self.messages.append({
            "role": role,
            "content": content,
            "metadata": metadata or {},
            "timestamp": datetime.now().isoformat()
        })
        
        # 超过限制时裁剪
        if len(self.messages) > self.max_messages:
            self.messages = self.messages[-self.max_messages:]
    
    def get_context(self, max_tokens: int = 4000) -> str:
        """获取上下文（带 Token 限制）"""
        context = []
        total_tokens = 0
        
        for msg in reversed(self.messages):
            msg_tokens = count_tokens(msg["content"])
            if total_tokens + msg_tokens > max_tokens:
                break
            context.insert(0, msg)
            total_tokens += msg_tokens
        
        return "\n".join([f"{m['role']}: {m['content']}" for m in context])
```

---

## 二、上下文传递策略

```
上下文传递策略：

1. 全量传递
   - 将所有上下文传递给下一个 Agent
   - 简单，但可能超出 Token 限制

2. 摘要传递
   - 用 LLM 生成上下文摘要
   - 减少 Token 消耗，但可能丢失细节

3. 选择性传递
   - 只传递相关的上下文
   - 需要相关性判断

4. 分层传递
   - 不同 Agent 接收不同详细程度的上下文
   - 平衡信息量和 Token 消耗
```

---

## 三、上下文压缩

```python
class ContextCompressor:
    """上下文压缩器"""
    
    def __init__(self, llm):
        self.llm = llm
    
    def compress(self, messages: list, max_length: int = 500) -> str:
        """压缩上下文"""
        full_context = "\n".join([f"{m['role']}: {m['content']}" for m in messages])
        
        prompt = f"""请将以下对话历史压缩为简洁的摘要，保留关键信息。

对话历史：
{full_context}

要求：
1. 保留关键决策和结论
2. 去除冗余信息
3. 控制在 {max_length} 字以内

请输出压缩后的摘要。"""
        
        response = self.llm.invoke(prompt)
        return response.content
```

---

## 四、LangGraph 中的上下文管理

```python
class AgentState(TypedDict):
    messages: Annotated[list, add]  # 消息历史
    context: str  # 压缩后的上下文
    current_agent: str

def compress_context(state: AgentState) -> AgentState:
    """压缩上下文"""
    compressor = ContextCompressor(llm)
    context = compressor.compress(state["messages"])
    return {**state, "context": context}
```

---

## 小结

```
本课核心要点：

1. 短期记忆管理会话上下文
2. 传递策略：全量、摘要、选择性、分层
3. 上下文压缩减少 Token 消耗
4. LangGraph 用 TypedDict 管理状态

---

**下一课**: [长期记忆——跨会话的知识持久化与检索](./03-长期记忆.md)
```

---

## 练习

1. **实现题**：实现一个短期记忆管理器。

2. **压缩题**：实现一个上下文压缩器。

3. **策略题**：设计一个适合你的场景的上下文传递策略。
