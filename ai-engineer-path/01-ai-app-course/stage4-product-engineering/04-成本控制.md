# 第4课：成本控制——token 统计、缓存、限流、降级

> **课程定位**：控制 AI 应用的运营成本
> **前置知识**：第 2 课的用户和配额
> **预计时长**：45 分钟

---

## 学习目标

完成本课学习后，你将能够：

1. 统计 token 使用量和成本
2. 实现缓存减少重复调用
3. 设计限流保护系统
4. 实现降级策略

---

## 一、Token 统计

### 1.1 记录每次调用

```typescript
// lib/usage-tracker.ts
import { prisma } from './prisma'

interface UsageRecord {
  userId: string
  conversationId: string
  model: string
  promptTokens: number
  completionTokens: number
  totalTokens: number
  cost: number
  duration: number
}

// 模型与价格不要硬编码在业务逻辑里。
// 价格会变化，生产环境建议从配置中心或环境变量读取，并定期按官方价格页更新。
interface ModelCostConfig {
  model: string
  inputCostPer1M: number
  outputCostPer1M: number
}

const MODEL_CATALOG: Record<string, ModelCostConfig> = {
  primary: {
    model: process.env.OPENAI_PRIMARY_MODEL ?? 'your-primary-model',
    inputCostPer1M: Number(process.env.OPENAI_PRIMARY_INPUT_COST_PER_1M ?? 0),
    outputCostPer1M: Number(process.env.OPENAI_PRIMARY_OUTPUT_COST_PER_1M ?? 0),
  },
  fast: {
    model: process.env.OPENAI_FAST_MODEL ?? 'your-fast-model',
    inputCostPer1M: Number(process.env.OPENAI_FAST_INPUT_COST_PER_1M ?? 0),
    outputCostPer1M: Number(process.env.OPENAI_FAST_OUTPUT_COST_PER_1M ?? 0),
  },
}

function getModelConfig(modelKey: string): ModelCostConfig {
  return MODEL_CATALOG[modelKey] ?? MODEL_CATALOG.primary
}

export function calculateCost(
  modelKey: string,
  promptTokens: number,
  completionTokens: number
): number {
  const prices = getModelConfig(modelKey)
  return (
    (promptTokens * prices.inputCostPer1M +
      completionTokens * prices.outputCostPer1M) / 1000000
  )
}

export async function trackUsage(record: UsageRecord): Promise<void> {
  // 记录使用量
  await prisma.usageRecord.create({
    data: {
      userId: record.userId,
      conversationId: record.conversationId,
      model: record.model,
      promptTokens: record.promptTokens,
      completionTokens: record.completionTokens,
      totalTokens: record.totalTokens,
      cost: record.cost,
      duration: record.duration,
    },
  })

  // 更新用户本月使用量
  await prisma.user.update({
    where: { id: record.userId },
    data: {
      tokenUsed: { increment: record.totalTokens },
    },
  })
}
```

### 1.2 在 API 调用中记录

```typescript
// lib/chat.ts
export async function chat(
  userId: string,
  conversationId: string,
  input: any[],
  modelKey: string = 'primary'
) {
  const startTime = Date.now()
  const modelConfig = getModelConfig(modelKey)

  const response = await openai.responses.create({
    model: modelConfig.model,
    input,
  })

  const duration = Date.now() - startTime
  const usage = response.usage!

  const cost = calculateCost(modelKey, usage.input_tokens, usage.output_tokens)

  // 记录使用量
  await trackUsage({
    userId,
    conversationId,
    model: modelConfig.model,
    promptTokens: usage.input_tokens,
    completionTokens: usage.output_tokens,
    totalTokens: usage.total_tokens,
    cost,
    duration,
  })

  return response
}
```

---

## 二、使用统计查询

### 2.1 统计 API

```typescript
// app/api/usage/route.ts
export async function GET(req: NextRequest) {
  const user = await getCurrentUser()
  if (!user) {
    return Response.json({ error: '未登录' }, { status: 401 })
  }

  const period = req.nextUrl.searchParams.get('period') || 'month'

  // 计算时间范围
  const now = new Date()
  let startDate: Date

  switch (period) {
    case 'day':
      startDate = new Date(now.getFullYear(), now.getMonth(), now.getDate())
      break
    case 'week':
      startDate = new Date(now.getTime() - 7 * 24 * 60 * 60 * 1000)
      break
    case 'month':
    default:
      startDate = new Date(now.getFullYear(), now.getMonth(), 1)
  }

  // 查询使用量
  const stats = await prisma.usageRecord.aggregate({
    where: {
      userId: user.id,
      createdAt: { gte: startDate },
    },
    _sum: {
      totalTokens: true,
      cost: true,
      promptTokens: true,
      completionTokens: true,
    },
    _count: true,
    _avg: {
      duration: true,
    },
  })

  // 按模型分组
  const byModel = await prisma.usageRecord.groupBy({
    by: ['model'],
    where: {
      userId: user.id,
      createdAt: { gte: startDate },
    },
    _sum: {
      totalTokens: true,
      cost: true,
    },
    _count: true,
  })

  return Response.json({
    period,
    summary: {
      totalTokens: stats._sum.totalTokens || 0,
      totalCost: stats._sum.cost || 0,
      requestCount: stats._count,
      avgDuration: stats._avg.duration || 0,
    },
    byModel,
  })
}
```

### 2.2 前端统计展示

```typescript
// components/UsageStats.tsx
'use client'

import { useState, useEffect } from 'react'

export function UsageStats() {
  const [stats, setStats] = useState<any>(null)
  const [period, setPeriod] = useState('month')

  useEffect(() => {
    fetch(`/api/usage?period=${period}`)
      .then(res => res.json())
      .then(setStats)
  }, [period])

  if (!stats) return <div>加载中...</div>

  return (
    <div className="usage-stats">
      <div className="period-selector">
        <button onClick={() => setPeriod('day')}>今日</button>
        <button onClick={() => setPeriod('week')}>本周</button>
        <button onClick={() => setPeriod('month')}>本月</button>
      </div>

      <div className="stats-grid">
        <div className="stat-card">
          <h3>总 Token 数</h3>
          <p>{stats.summary.totalTokens.toLocaleString()}</p>
        </div>
        <div className="stat-card">
          <h3>总费用</h3>
          <p>${stats.summary.totalCost.toFixed(4)}</p>
        </div>
        <div className="stat-card">
          <h3>请求数</h3>
          <p>{stats.summary.requestCount}</p>
        </div>
        <div className="stat-card">
          <h3>平均耗时</h3>
          <p>{(stats.summary.avgDuration / 1000).toFixed(2)}s</p>
        </div>
      </div>

      <div className="model-breakdown">
        <h3>按模型分组</h3>
        {stats.byModel.map((item: any) => (
          <div key={item.model} className="model-row">
            <span>{item.model}</span>
            <span>{item._sum.totalTokens.toLocaleString()} tokens</span>
            <span>${item._sum.cost.toFixed(4)}</span>
          </div>
        ))}
      </div>
    </div>
  )
}
```

---

## 三、缓存策略

### 3.1 相同请求缓存

```typescript
// lib/cache.ts
import crypto from 'crypto'

const cache = new Map<string, { data: any; expiry: number }>()

function getCacheKey(input: any[], model: string): string {
  const content = JSON.stringify({ input, model })
  return crypto.createHash('md5').update(content).digest('hex')
}

export function getCachedResponse(input: any[], model: string): any | null {
  const key = getCacheKey(input, model)
  const cached = cache.get(key)

  if (!cached) return null
  if (cached.expiry < Date.now()) {
    cache.delete(key)
    return null
  }

  return cached.data
}

export function setCachedResponse(
  input: any[],
  model: string,
  response: any,
  ttlMs: number = 5 * 60 * 1000  // 默认 5 分钟
): void {
  const key = getCacheKey(input, model)
  cache.set(key, {
    data: response,
    expiry: Date.now() + ttlMs,
  })
}
```

### 3.2 使用缓存

```typescript
export async function chatWithCache(
  input: any[],
  model: string
) {
  // 检查缓存
  const cached = getCachedResponse(input, model)
  if (cached) {
    console.log('Cache hit!')
    return cached
  }

  // 调用 API
  const response = await openai.responses.create({
    model,
    input,
  })

  // 缓存结果
  setCachedResponse(input, model, response)

  return response
}
```

---

## 四、限流策略

### 4.1 限流器实现

```typescript
// lib/rate-limiter.ts
class RateLimiter {
  private windows = new Map<string, { count: number; resetTime: number }>()

  constructor(
    private maxRequests: number,
    private windowMs: number
  ) {}

  check(key: string): { allowed: boolean; remaining: number; resetTime: number } {
    const now = Date.now()
    let window = this.windows.get(key)

    if (!window || window.resetTime < now) {
      window = { count: 0, resetTime: now + this.windowMs }
      this.windows.set(key, window)
    }

    const remaining = this.maxRequests - window.count

    if (window.count >= this.maxRequests) {
      return { allowed: false, remaining: 0, resetTime: window.resetTime }
    }

    window.count++
    return { allowed: true, remaining: remaining - 1, resetTime: window.resetTime }
  }
}

// 创建限流器
const chatLimiter = new RateLimiter(20, 60 * 1000)  // 每分钟 20 次
const searchLimiter = new RateLimiter(50, 60 * 1000)  // 每分钟 50 次
```

### 4.2 在 API 中使用

```typescript
export async function POST(req: NextRequest) {
  const user = await getCurrentUser()
  if (!user) {
    return Response.json({ error: '未登录' }, { status: 401 })
  }

  // 检查限流
  const rateLimit = chatLimiter.check(user.id)
  if (!rateLimit.allowed) {
    return Response.json(
      { error: '请求太频繁，请稍后再试' },
      {
        status: 429,
        headers: {
          'X-RateLimit-Remaining': '0',
          'X-RateLimit-Reset': rateLimit.resetTime.toString(),
        },
      }
    )
  }

  // 处理请求...
}
```

---

## 五、降级策略

### 5.1 模型降级

```typescript
// lib/fallback.ts
// 使用业务层级描述降级链，具体模型名由配置决定，避免把过期模型写死在代码里。
const MODEL_FALLBACK_CHAIN = ['primary', 'fast']

export async function chatWithFallback(
  input: any[],
  preferredModelKey: string
) {
  const startIndex = MODEL_FALLBACK_CHAIN.indexOf(preferredModelKey)
  const models = MODEL_FALLBACK_CHAIN.slice(startIndex >= 0 ? startIndex : 1)

  for (const modelKey of models) {
    try {
      const modelConfig = getModelConfig(modelKey)
      const response = await openai.responses.create({
        model: modelConfig.model,
        input,
      })
      return { response, model: modelConfig.model, modelKey }
    } catch (error) {
      console.warn(`Model tier ${modelKey} failed, trying next...`)
      continue
    }
  }

  throw new Error('所有模型都不可用')
}
```

### 5.2 降级提示

```typescript
// 在前端展示降级信息
function ChatMessage({
  message,
  modelName,
  modelKey,
}: {
  message: string
  modelName: string
  modelKey: string
}) {
  return (
    <div className="message">
      <div className="content">{message}</div>
      {modelKey !== 'primary' && (
        <div className="model-notice">
          当前使用备用模型 {modelName}（高级能力暂时不可用）
        </div>
      )}
    </div>
  )
}
```

---

## 六、成本优化建议

```
┌─────────────────────────────────────────────────────────────────┐
│                    成本优化策略                                   │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  1. 选择合适的模型                                               │
│     - 简单任务用 mini 模型                                       │
│     - 复杂任务才用高级模型                                       │
│                                                                 │
│  2. 控制输出长度                                                 │
│     - 设置合理的 max_tokens                                      │
│     - prompt 中指定期望长度                                      │
│                                                                 │
│  3. 使用缓存                                                     │
│     - 相同请求直接返回缓存                                       │
│     - 减少重复调用                                               │
│                                                                 │
│  4. 压缩历史                                                     │
│     - 定期裁剪对话历史                                           │
│     - 用摘要替代长历史                                           │
│                                                                 │
│  5. 批量处理                                                     │
│     - 多个请求合并成一个                                         │
│     - 减少 API 调用次数                                          │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 动手练习

### 练习一：实现 token 统计

1. 记录每次 API 调用的 token 使用量
2. 计算费用
3. 展示统计图表

### 练习二：实现缓存

1. 实现请求缓存
2. 设置合理的 TTL
3. 测试缓存命中率

### 练习三：实现限流

1. 设计限流规则
2. 实现限流器
3. 返回正确的状态码和头信息

---

## 小结

本课的核心要点：

1. **Token 统计**：记录每次调用的 token 和费用
2. **缓存**：相同请求直接返回，减少 API 调用
3. **限流**：保护系统不被过度使用
4. **降级**：高级模型不可用时自动切换到低级模型
5. **成本优化**：选模型、控长度、用缓存、压缩历史

---

**下一课**: [第5课：提示注入与越权检索防护](./05-提示注入防护.md)
