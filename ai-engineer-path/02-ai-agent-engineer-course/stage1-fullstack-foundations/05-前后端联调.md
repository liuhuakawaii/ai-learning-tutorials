# 05 前后端联调

> 前后端分开写都对，合在一起就报错——这是最常见的开发痛点。

## 学习目标

- 实现前后端完整联调流程
- 掌握 API 错误处理的前端最佳实践
- 理解加载状态、乐观更新、错误恢复
- 解决跨域、类型不一致、超时等常见问题

## 前置要求

- 已完成后端 API（阶段 1 第 2-3 课）
- 已完成前端工程搭建（阶段 1 第 4 课）
- 了解 axios 和 fetch 的基本用法

## 联调的核心问题

前后端联调的本质是**两套独立系统之间的通信**。出问题的永远是边界：

1. **数据格式不一致**：后端返回 `snake_case`，前端用 `camelCase`
2. **错误处理不统一**：后端抛 500，前端不知道怎么展示
3. **超时和重试**：LLM API 调用可能很慢，前端默认超时不够
4. **类型不安全**：后端改了字段，前端编译不报错

## API 错误处理体系

### 后端统一错误格式

```python
# backend/app/core/exceptions.py
from fastapi import Request
from fastapi.responses import JSONResponse

class AppError(Exception):
    def __init__(self, code: str, message: str, status_code: int = 400):
        self.code = code
        self.message = message
        self.status_code = status_code

async def app_error_handler(request: Request, exc: AppError):
    return JSONResponse(
        status_code=exc.status_code,
        content={
            "error": {
                "code": exc.code,
                "message": exc.message,
            }
        },
    )
```

注册到 FastAPI：

```python
# backend/app/main.py
from app.core.exceptions import AppError, app_error_handler

app.add_exception_handler(AppError, app_error_handler)
```

### 前端错误处理

```typescript
// frontend/src/api/client.ts
import axios, { type AxiosError } from 'axios'
import { useMessage } from 'naive-ui'

interface ApiErrorResponse {
  error: {
    code: string
    message: string
  }
}

const client = axios.create({
  baseURL: '/api/v1',
  timeout: 60000, // LLM API 可能很慢，给足超时
})

client.interceptors.response.use(
  (response) => response.data,
  (error: AxiosError<ApiErrorResponse>) => {
    const status = error.response?.status
    const apiError = error.response?.data?.error
    
    // 网络错误
    if (!error.response) {
      return Promise.reject(new AppError('NETWORK_ERROR', '网络连接失败，请检查网络'))
    }
    
    // 401 → 跳转登录
    if (status === 401) {
      localStorage.removeItem('token')
      window.location.href = '/login'
      return Promise.reject(new AppError('UNAUTHORIZED', '请重新登录'))
    }
    
    // 429 → 限流
    if (status === 429) {
      return Promise.reject(new AppError('RATE_LIMITED', '请求过于频繁，请稍后再试'))
    }
    
    // 其他错误
    const code = apiError?.code || 'UNKNOWN_ERROR'
    const message = apiError?.message || '未知错误'
    return Promise.reject(new AppError(code, message, status))
  }
)

export class AppError extends Error {
  constructor(
    public code: string,
    message: string,
    public status?: number,
  ) {
    super(message)
    this.name = 'AppError'
  }
}

export default client
```

### 全局错误提示

```typescript
// frontend/src/composables/useApiError.ts
import { useMessage } from 'naive-ui'
import { AppError } from '@/api/client'

export function useApiError() {
  const message = useMessage()

  function handleError(error: unknown) {
    if (error instanceof AppError) {
      // 已知错误，显示友好提示
      message.error(error.message)
    } else if (error instanceof Error) {
      // 未知错误
      message.error(`操作失败：${error.message}`)
    } else {
      message.error('未知错误')
    }
  }

  return { handleError }
}
```

在组件中使用：

```vue
<script setup lang="ts">
import { useApiError } from '@/composables/useApiError'
import { chatApi } from '@/api/chat'

const { handleError } = useApiError()

async function sendMessage(content: string) {
  try {
    await chatApi.sendMessage(sessionId.value, content)
  } catch (error) {
    handleError(error)
  }
}
</script>
```

## 加载状态管理

### 全局加载状态

```typescript
// frontend/src/stores/app.ts
import { defineStore } from 'pinia'
import { ref } from 'vue'

export const useAppStore = defineStore('app', () => {
  const loadingCount = ref(0)
  const isLoading = computed(() => loadingCount.value > 0)

  function startLoading() {
    loadingCount.value++
  }

  function stopLoading() {
    loadingCount.value = Math.max(0, loadingCount.value - 1)
  }

  return { isLoading, startLoading, stopLoading }
})
```

配合 axios 拦截器自动管理：

```typescript
client.interceptors.request.use((config) => {
  appStore.startLoading()
  return config
})

client.interceptors.response.use(
  (response) => {
    appStore.stopLoading()
    return response
  },
  (error) => {
    appStore.stopLoading()
    return Promise.reject(error)
  }
)
```

### 组件级加载状态

```vue
<template>
  <n-spin :show="sending">
    <n-input
      v-model:value="inputText"
      type="textarea"
      placeholder="输入消息..."
      :disabled="sending"
      @keydown.enter.ctrl="handleSend"
    />
    <n-button
      type="primary"
      :loading="sending"
      :disabled="!inputText.trim()"
      @click="handleSend"
    >
      发送
    </n-button>
  </n-spin>
</template>

<script setup lang="ts">
const sending = ref(false)

async function handleSend() {
  if (sending.value) return
  sending.value = true
  try {
    await chatApi.sendMessage(sessionId.value, inputText.value)
    inputText.value = ''
  } catch (error) {
    handleError(error)
  } finally {
    sending.value = false
  }
}
</script>
```

## 乐观更新

乐观更新的核心思想：先更新 UI，后发请求。如果请求失败，再回滚。

```typescript
// frontend/src/stores/chat.ts
async function sendMessage(sessionId: string, content: string) {
  // 1. 乐观更新：立即显示用户消息
  const tempMessage: Message = {
    id: `temp-${Date.now()}`,
    role: 'user',
    content,
    token_usage: 0,
    created_at: new Date().toISOString(),
  }
  messages.value.push(tempMessage)

  try {
    // 2. 发送请求
    const res = await chatApi.sendMessage(sessionId, content)
    
    // 3. 用真实数据替换临时消息
    const index = messages.value.findIndex((m) => m.id === tempMessage.id)
    if (index !== -1) {
      messages.value.splice(index, 1, res.message)
    }
    
    // 4. 添加 AI 回复
    messages.value.push(res.ai_message)
  } catch (error) {
    // 5. 失败回滚：移除临时消息
    const index = messages.value.findIndex((m) => m.id === tempMessage.id)
    if (index !== -1) {
      messages.value.splice(index, 1)
    }
    throw error
  }
}
```

## 类型共享策略

前后端共享类型定义，避免"后端改了字段，前端编译不报错"的问题。

### 方案一：共享 TypeScript 类型文件

```typescript
// frontend/src/types/api.ts
// 这个文件和后端 Schema 保持同步
export interface ChatRequest {
  message: string
  session_id?: string
}

export interface ChatResponse {
  session_id: string
  message: Message
}
```

### 方案二：从后端自动生成（推荐）

用 `openapi-typescript` 从 FastAPI 的 OpenAPI 文档自动生成类型：

```bash
cd frontend
npm install -D openapi-typescript
```

```json
// frontend/package.json
{
  "scripts": {
    "gen:api": "openapi-typescript http://localhost:8000/openapi.json -o src/types/api-generated.ts"
  }
}
```

```bash
npm run gen:api
```

生成的类型文件自动和后端同步，后端改了字段，前端 `npm run gen:api` 就能更新。

## 联调检查清单

### 后端检查

- [ ] 所有 API 都有正确的状态码
- [ ] 错误返回统一格式 `{ "error": { "code": "...", "message": "..." } }`
- [ ] CORS 配置正确（开发环境允许前端域名）
- [ ] 请求体大小限制（防止上传过大文件）
- [ ] 分页参数有默认值和最大值

### 前端检查

- [ ] API 调用有超时设置（LLM API 用 60s+）
- [ ] 错误处理覆盖所有已知错误码
- [ ] 加载状态正确显示和隐藏
- [ ] 乐观更新有回滚机制
- [ ] 401 自动跳转登录页
- [ ] 网络断开有友好提示

## 练习

### 练习 1：完整联调

将第 4 课的静态页面和后端 API 完整联调：

1. 登录页 → 对接登录 API → 存储 token → 跳转主页
2. 对话页 → 加载会话列表 → 创建会话 → 发送消息 → 显示回复
3. 处理所有错误情况（网络错误、401、500、超时）

### 练习 2：自动生成类型

1. 配置 `openapi-typescript`
2. 从后端自动生成 TypeScript 类型
3. 替换手动定义的类型
4. 测试类型是否正确

### 练习 3：错误模拟

1. 后端故意返回各种错误（400、401、403、500）
2. 前端验证错误提示是否正确显示
3. 模拟网络断开，验证前端表现

## 本节要点

- 统一错误格式是前后端协作的基础
- 加载状态要全局管理，不要每个组件各管各的
- 乐观更新提升体验，但必须有回滚机制
- 类型自动生成消除前后端类型不同步的问题

## 常见错误

| 错误 | 原因 | 解决 |
|------|------|------|
| CORS 错误 | 后端没配置允许前端域名 | 添加 CORSMiddleware |
| 422 前端发了请求但后端报错 | 请求体字段名不一致 | 检查 `snake_case` vs `camelCase` |
| 超时错误 | LLM API 调用很慢 | 增加 axios timeout 到 60-120s |
| token 过期后页面卡死 | 401 没有自动跳转登录 | 在拦截器中处理 401 |
