# 06 短期记忆与长期记忆

> 没有记忆的 Agent 每次对话都是陌生人——记忆让 AI 真正理解用户。

## 学习目标

- 设计 Agent 的短期记忆（会话内）和长期记忆（跨会话）
- 实现会话摘要、用户画像、向量记忆
- 理解记忆的存储、检索和更新策略

## 记忆类型

| 类型 | 作用域 | 存储 | 生命周期 |
|------|--------|------|----------|
| 工作记忆 | 当前推理步骤 | 内存 | 毫秒级 |
| 短期记忆 | 当前会话 | Redis | 小时级 |
| 长期记忆 | 跨会话 | 数据库 + 向量库 | 永久 |

## 短期记忆

```python
class ShortTermMemory:
    """短期记忆——会话级"""
    
    def __init__(self, redis: redis.Redis, session_id: str):
        self.redis = redis
        self.session_id = session_id
        self.key = f"memory:short:{session_id}"
        self.max_items = 100
        self.ttl = 3600 * 24  # 24 小时
    
    async def add(self, item: dict):
        """添加记忆项"""
        data = json.dumps(item, ensure_ascii=False)
        await self.redis.rpush(self.key, data)
        await self.redis.ltrim(self.key, -self.max_items, -1)
        await self.redis.expire(self.key, self.ttl)
    
    async def get_recent(self, n: int = 10) -> list[dict]:
        """获取最近 N 条记忆"""
        data = await self.redis.lrange(self.key, -n, -1)
        return [json.loads(d) for d in data]
    
    async def get_all(self) -> list[dict]:
        """获取所有短期记忆"""
        data = await self.redis.lrange(self.key, 0, -1)
        return [json.loads(d) for d in data]
    
    async def clear(self):
        """清除短期记忆"""
        await self.redis.delete(self.key)
```

## 长期记忆

```python
class LongTermMemory:
    """长期记忆——跨会话"""
    
    def __init__(self, db: AsyncSession, embedding: EmbeddingService):
        self.db = db
        self.embedding = embedding
    
    async def store(self, user_id: str, content: str, memory_type: str, metadata: dict = None):
        """存储长期记忆"""
        embedding = await self.embedding.embed(content)
        
        await self.db.execute(
            text("""
                INSERT INTO long_term_memories 
                (id, user_id, content, memory_type, embedding, metadata)
                VALUES (:id, :user_id, :content, :type, :embedding, :metadata)
            """),
            {
                "id": str(uuid.uuid4()),
                "user_id": user_id,
                "content": content,
                "type": memory_type,
                "embedding": str(embedding),
                "metadata": json.dumps(metadata or {}, ensure_ascii=False),
            },
        )
    
    async def recall(
        self,
        user_id: str,
        query: str,
        memory_type: str | None = None,
        top_k: int = 5,
    ) -> list[dict]:
        """根据查询召回相关记忆"""
        query_embedding = await self.embedding.embed(query)
        
        sql = """
            SELECT content, memory_type, metadata,
                   1 - (embedding <=> :query_embedding) AS score
            FROM long_term_memories
            WHERE user_id = :user_id
        """
        params = {
            "user_id": user_id,
            "query_embedding": str(query_embedding),
        }
        
        if memory_type:
            sql += " AND memory_type = :type"
            params["type"] = memory_type
        
        sql += " ORDER BY embedding <=> :query_embedding LIMIT :top_k"
        params["top_k"] = top_k
        
        result = await self.db.execute(text(sql), params)
        return [dict(row) for row in result]
```

## 记忆类型

```python
class MemoryType(str, Enum):
    CONVERSATION = "conversation"    # 对话历史
    USER_PROFILE = "user_profile"    # 用户画像
    PREFERENCE = "preference"        # 用户偏好
    FACT = "fact"                    # 事实性知识
    SKILL = "skill"                  # 学到的技能
```

## 会话摘要

```python
class ConversationSummarizer:
    """会话摘要生成器"""
    
    async def summarize(self, messages: list[dict], llm: LLMService) -> str:
        """将对话历史压缩成摘要"""
        conversation = "\n".join(
            f"{m['role']}: {m['content']}" for m in messages if m.get('content')
        )
        
        prompt = f"""请将以下对话压缩成简洁的摘要，保留：
1. 用户的核心需求和偏好
2. 关键决策和结论
3. 未完成的任务

对话：
{conversation}

摘要："""
        
        response = await llm.chat(
            messages=[{"role": "user", "content": prompt}],
            model="gpt-4o-mini",
        )
        
        return response.content
```

## 用户画像

```python
class UserProfileManager:
    """用户画像管理"""
    
    async def update_profile(
        self,
        user_id: str,
        conversation_summary: str,
        llm: LLMService,
        memory: LongTermMemory,
    ):
        """根据对话更新用户画像"""
        # 获取现有画像
        existing = await memory.recall(user_id, "用户画像", "user_profile", top_k=1)
        current_profile = existing[0]["content"] if existing else "暂无画像"
        
        prompt = f"""根据以下对话摘要，更新用户画像。

现有画像：
{current_profile}

最新对话摘要：
{conversation_summary}

请输出更新后的用户画像（包含：姓名/称呼、偏好、需求、行为特征等）："""
        
        response = await llm.chat(
            messages=[{"role": "user", "content": prompt}],
            model="gpt-4o-mini",
        )
        
        await memory.store(user_id, response.content, "user_profile")
```

## 练习

### 练习 1：短期记忆

实现短期记忆的完整功能：

1. 存储对话历史
2. 存储工具调用结果
3. 获取最近 N 条记忆

### 练习 2：长期记忆

实现长期记忆的完整功能：

1. 会话结束后自动生成摘要
2. 摘要存储到长期记忆
3. 下次对话时召回相关记忆

### 练习 3：用户画像

实现用户画像的自动构建：

1. 从对话中提取用户偏好
2. 更新用户画像
3. 在 Prompt 中使用用户画像

## 本节要点

- 短期记忆保证会话内连贯，长期记忆保证跨会话连贯
- 会话摘要把长对话压缩成有用信息
- 用户画像让 AI 越来越了解用户
- 向量检索让记忆召回更精准

## 常见错误

| 错误 | 原因 | 解决 |
|------|------|------|
| 记忆太多太杂 | 没有过滤和排序 | 按相关性和时间排序 |
| 用户画像不准 | 提取逻辑太简单 | 用 LLM 结构化提取 |
| 记忆冲突 | 新旧信息矛盾 | 更新时合并而非覆盖 |
| 记忆泄露 | 不同用户记忆串了 | 严格按 user_id 隔离 |
