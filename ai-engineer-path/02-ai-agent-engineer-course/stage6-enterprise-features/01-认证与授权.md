# 01 认证与授权

> 没有认证的系统是裸奔——没有授权的系统是无差别放行。

## 学习目标

- 实现 JWT 认证和 Token 刷新
- 设计 RBAC 权限模型
- 支持多组织空间和 API Key 管理

## JWT 认证

```python
# backend/app/core/security.py
from datetime import datetime, timedelta
from jose import JWTError, jwt
from passlib.context import CryptContext

pwd_context = CryptContext(schemes=["bcrypt"], deprecated="auto")

class AuthService:
    def __init__(self, secret_key: str):
        self.secret_key = secret_key
        self.access_token_expire = timedelta(hours=2)
        self.refresh_token_expire = timedelta(days=30)
    
    def create_tokens(self, user_id: str) -> dict:
        """创建 access_token 和 refresh_token"""
        access_token = jwt.encode(
            {"sub": user_id, "type": "access", "exp": datetime.utcnow() + self.access_token_expire},
            self.secret_key,
            algorithm="HS256",
        )
        refresh_token = jwt.encode(
            {"sub": user_id, "type": "refresh", "exp": datetime.utcnow() + self.refresh_token_expire},
            self.secret_key,
            algorithm="HS256",
        )
        return {"access_token": access_token, "refresh_token": refresh_token}
    
    def decode_token(self, token: str) -> dict | None:
        try:
            return jwt.decode(token, self.secret_key, algorithms=["HS256"])
        except JWTError:
            return None
```

## RBAC 权限模型

```python
# 权限定义
PERMISSIONS = {
    "chat:read": "查看对话",
    "chat:write": "发送消息",
    "agent:read": "查看 Agent",
    "agent:write": "编辑 Agent",
    "agent:publish": "发布 Agent",
    "knowledge:read": "查看知识库",
    "knowledge:write": "编辑知识库",
    "workflow:read": "查看工作流",
    "workflow:write": "编辑工作流",
    "workflow:execute": "执行工作流",
    "admin:users": "管理用户",
    "admin:settings": "管理设置",
}

# 角色定义
ROLES = {
    "viewer": ["chat:read", "agent:read", "knowledge:read", "workflow:read"],
    "user": ["chat:*", "agent:read", "knowledge:read", "workflow:read"],
    "editor": ["chat:*", "agent:*", "knowledge:*", "workflow:*"],
    "admin": ["*"],
}

class RBACService:
    async def check_permission(self, user_id: str, permission: str) -> bool:
        user_role = await self._get_user_role(user_id)
        role_permissions = ROLES.get(user_role, [])
        
        # 通配符匹配
        for rp in role_permissions:
            if rp == "*" or rp == permission:
                return True
            if rp.endswith(":*") and permission.startswith(rp[:-2]):
                return True
        
        return False
```

## API Key 管理

```python
class APIKeyService:
    async def create_key(self, user_id: str, name: str, permissions: list[str]) -> dict:
        key = f"sk-{secrets.token_hex(32)}"
        key_hash = hashlib.sha256(key.encode()).hexdigest()
        
        await db.execute(
            text("INSERT INTO api_keys (id, user_id, name, key_hash, permissions) VALUES (...)"),
            {"id": str(uuid.uuid4()), "user_id": user_id, "name": name,
             "key_hash": key_hash, "permissions": json.dumps(permissions)},
        )
        
        return {"key": key, "name": name}
    
    async def validate_key(self, key: str) -> dict | None:
        key_hash = hashlib.sha256(key.encode()).hexdigest()
        result = await db.execute(
            text("SELECT * FROM api_keys WHERE key_hash = :hash AND is_active = true"),
            {"hash": key_hash},
        )
        row = result.first()
        return dict(row) if row else None
```

## 练习

### 练习 1：JWT 认证

实现完整的 JWT 认证：

1. 注册、登录、Token 签发
2. Token 刷新机制
3. 多设备管理

### 练习 2：RBAC 权限

实现 RBAC 权限控制：

1. 角色定义和权限分配
2. API 级别的权限检查
3. Skill 级别的权限控制

## 本节要点

- JWT 是无状态认证的标准方案
- RBAC 让权限管理可配置、可扩展
- API Key 让外部系统也能安全接入
- Token 刷新避免用户频繁登录

## 常见错误

| 错误 | 原因 | 解决 |
|------|------|------|
| Token 过期频繁 | 有效期太短 | access 2h + refresh 30d |
| 权限检查遗漏 | 没统一用依赖注入 | 封装 get_current_user 依赖 |
| API Key 泄露 | 明文存储 | 只存 hash，创建时返回一次 |
