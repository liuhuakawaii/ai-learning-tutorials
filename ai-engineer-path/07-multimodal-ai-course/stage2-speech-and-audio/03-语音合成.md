# 03 语音合成——OpenAI TTS / Edge-TTS 的集成与音色选择

> 语音合成让 AI 能开口说话。

## 学习目标

- 掌握语音合成 API 的使用方法
- 理解不同音色的特点
- 学会集成语音合成能力

---

## 一、OpenAI TTS

```python
def synthesize_speech_openai(text: str, voice: str = "alloy") -> str:
    """OpenAI 语音合成"""
    response = client.audio.speech.create(
        model="tts-1",
        voice=voice,
        input=text
    )
    
    output_path = "output.mp3"
    response.stream_to_file(output_path)
    return output_path
```

---

## 二、Edge-TTS（免费）

```python
import edge_tts
import asyncio

async def synthesize_speech_edge(text: str, voice: str = "zh-CN-XiaoxiaoNeural") -> str:
    """Edge-TTS 语音合成"""
    output_path = "output.mp3"
    
    communicate = edge_tts.Communicate(text, voice)
    await communicate.save(output_path)
    
    return output_path
```

---

## 三、音色选择

```
OpenAI 音色：
- alloy: 中性
- echo: 男性
- fable: 男性
- onyx: 男性
- nova: 女性
- shimmer: 女性

Edge-TTS 中文音色：
- zh-CN-XiaoxiaoNeural: 女性，温柔
- zh-CN-YunxiNeural: 男性，成熟
- zh-CN-YunyangNeural: 男性，专业
- zh-CN-XiaoyiNeural: 女性，活泼
```

---

## 四、流式合成

```python
async def stream_tts(text: str):
    """流式语音合成"""
    communicate = edge_tts.Communicate(text, "zh-CN-XiaoxiaoNeural")
    
    async for chunk in communicate.stream():
        if chunk["type"] == "audio":
            # 处理音频数据
            yield chunk["data"]
```

---

## 五、批量合成

```python
async def batch_synthesize(texts: list, voice: str = "zh-CN-XiaoxiaoNeural") -> list:
    """批量语音合成"""
    tasks = []
    
    for i, text in enumerate(texts):
        output_path = f"output_{i}.mp3"
        task = edge_tts.Communicate(text, voice).save(output_path)
        tasks.append(task)
    
    await asyncio.gather(*tasks)
    
    return [f"output_{i}.mp3" for i in range(len(texts))]
```

---

## 六、应用场景

```
语音合成应用场景：

1. 语音助手
   - 对话回复
   - 语音播报

2. 内容创作
   - 有声读物
   - 播客制作

3. 教育培训
   - 课程讲解
   - 语言学习

4. 客服系统
   - 自动回复
   - 语音通知
```

---

## 小结

```
本课核心要点：

1. OpenAI TTS 和 Edge-TTS 是主流语音合成方案
2. Edge-TTS 免费，支持中文
3. 多种音色可选
4. 支持流式和批量合成

下一课：实时语音交互——构建一个语音对话的 AI 助手原型。
```

---

## 练习

1. **合成题**：用 Edge-TTS 合成一段语音。

2. **音色题**：测试不同音色的效果。

3. **应用题**：设计一个语音合成应用场景。
