# 03 表格提取——从 PDF 和图片中提取结构化表格数据

> 表格是文档中最重要的结构化数据。

## 学习目标

- 掌握表格提取的方法
- 理解表格识别和结构化
- 学会处理各种表格格式

---

## 一、PDF 表格提取

```python
from unstructured.partition.pdf import partition_pdf

def extract_tables_from_pdf(pdf_path: str) -> list:
    """从 PDF 提取表格"""
    elements = partition_pdf(pdf_path, strategy="hi_res")
    
    tables = []
    for element in elements:
        if element.category == "Table":
            tables.append(element.text)
    
    return tables
```

---

## 二、图片表格提取

```python
def extract_table_from_image(image_url: str) -> list:
    """从图片提取表格"""
    response = client.chat.completions.create(
        model="gpt-4o",
        messages=[{
            "role": "user",
            "content": [
                {"type": "text", "text": "请提取这张图片中的表格，以 JSON 数组格式输出"},
                {"type": "image_url", "image_url": {"url": image_url}}
            ]
        }],
        response_format={"type": "json_object"}
    )
    return json.loads(response.choices[0].message.content)["table"]
```

---

## 三、表格解析

```python
import pandas as pd

def parse_table(table_text: str) -> pd.DataFrame:
    """解析表格"""
    # 使用 pandas 解析
    lines = table_text.strip().split("\n")
    headers = lines[0].split("\t")
    
    data = []
    for line in lines[1:]:
        row = line.split("\t")
        data.append(row)
    
    return pd.DataFrame(data, columns=headers)
```

---

## 四、表格增强

```python
def enhance_table(table: pd.DataFrame) -> pd.DataFrame:
    """增强表格"""
    # 清洗数据
    table = table.dropna(how="all")
    
    # 转换类型
    for col in table.columns:
        try:
            table[col] = pd.to_numeric(table[col])
        except:
            pass
    
    return table
```

---

## 五、批量处理

```python
def batch_table_extraction(pdf_paths: list) -> dict:
    """批量表格提取"""
    results = {}
    
    for path in pdf_paths:
        tables = extract_tables_from_pdf(path)
        results[path] = tables
    
    return results
```

---

## 小结

```
本课核心要点：

1. 从 PDF 和图片中提取表格
2. 表格解析转换为结构化数据
3. 表格增强提高数据质量
4. 支持批量处理

---

**下一课**: [04 长文档处理——大文档的分块策略与上下文管理](./04-长文档处理.md)
```

---

## 练习

1. **提取题**：从一个 PDF 中提取表格。

2. **解析题**：将提取的表格转换为 DataFrame。

3. **增强题**：清洗和增强表格数据。
