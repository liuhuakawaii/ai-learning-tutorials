# 02 文档解析——Unstructured / Docling 的文档拆分与结构化

> 文档解析是文档处理的基础能力。

## 学习目标

- 掌握文档解析的方法
- 理解文档拆分和结构化
- 学会处理各种文档格式

---

## 一、Unstructured 使用

```python
from unstructured.partition.pdf import partition_pdf
from unstructured.partition.docx import partition_docx

# PDF 解析
pdf_elements = partition_pdf("document.pdf")

# Word 解析
docx_elements = partition_docx("document.docx")

# 提取文本
for element in pdf_elements:
    print(f"类型：{element.category}")
    print(f"内容：{element.text}")
    print("---")
```

---

## 二、Docling 使用

```python
from docling.document_converter import DocumentConverter

# 创建转换器
converter = DocumentConverter()

# 解析文档
result = converter.convert("document.pdf")

# 获取结构化内容
document = result.document
print(document.export_to_markdown())
```

---

## 三、文档拆分

```python
def split_document(elements: list, chunk_size: int = 1000) -> list:
    """文档拆分"""
    chunks = []
    current_chunk = ""
    
    for element in elements:
        if len(current_chunk) + len(element.text) > chunk_size:
            chunks.append(current_chunk)
            current_chunk = element.text
        else:
            current_chunk += "\n" + element.text
    
    if current_chunk:
        chunks.append(current_chunk)
    
    return chunks
```

---

## 四、结构化提取

```python
def extract_structured_data(elements: list) -> dict:
    """结构化提取"""
    structured = {
        "title": "",
        "sections": [],
        "tables": [],
        "images": []
    }
    
    for element in elements:
        if element.category == "Title":
            structured["title"] = element.text
        elif element.category == "Table":
            structured["tables"].append(element.text)
        elif element.category == "Image":
            structured["images"].append(element.metadata.image_path)
        else:
            structured["sections"].append(element.text)
    
    return structured
```

---

## 五、多格式支持

```python
def parse_document(file_path: str) -> list:
    """解析文档"""
    ext = file_path.split(".")[-1].lower()
    
    if ext == "pdf":
        return partition_pdf(file_path)
    elif ext == "docx":
        return partition_docx(file_path)
    elif ext == "txt":
        with open(file_path, "r") as f:
            return [f.read()]
    else:
        raise ValueError(f"不支持的格式：{ext}")
```

---

## 小结

```
本课核心要点：

1. Unstructured 和 Docling 是主流文档解析工具
2. 文档拆分支持分块处理
3. 结构化提取获取关键信息
4. 支持多种文档格式

---

**下一课**: [03 表格提取——从 PDF 和图片中提取结构化表格数据](./03-表格提取.md)
```

---

## 练习

1. **解析题**：解析一个 PDF 文档。

2. **拆分题**：实现文档拆分功能。

3. **提取题**：提取文档的结构化数据。
