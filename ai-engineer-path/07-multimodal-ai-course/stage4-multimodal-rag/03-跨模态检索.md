# 03 跨模态检索——用文字搜图片、用图片搜文字

> 跨模态检索是多模态 RAG 的核心能力。

## 学习目标

- 掌握跨模态检索的方法
- 理解图文匹配的原理
- 学会实现跨模态搜索

---

## 一、文字搜图片

```python
def search_images_by_text(query: str, top_k: int = 5) -> list:
    """用文字搜图片"""
    # 获取文本向量
    text_embedding = get_text_embedding(query)
    
    # 检索图片
    results = collection.query(
        query_embeddings=[text_embedding],
        n_results=top_k,
        where={"type": "image"}
    )
    
    return results
```

---

## 二、图片搜文字

```python
def search_texts_by_image(image_path: str, top_k: int = 5) -> list:
    """用图片搜文字"""
    # 获取图片向量
    image_embedding = get_image_embedding(image_path)
    
    # 检索文本
    results = collection.query(
        query_embeddings=[image_embedding],
        n_results=top_k,
        where={"type": "text"}
    )
    
    return results
```

---

## 三、相似图片搜索

```python
def search_similar_images(image_path: str, top_k: int = 5) -> list:
    """相似图片搜索"""
    # 获取图片向量
    image_embedding = get_image_embedding(image_path)
    
    # 检索相似图片
    results = collection.query(
        query_embeddings=[image_embedding],
        n_results=top_k,
        where={"type": "image"}
    )
    
    return results
```

---

## 四、混合检索

```python
def hybrid_search(query: str, query_type: str, target_type: str, top_k: int = 5) -> list:
    """混合检索"""
    if query_type == "text":
        embedding = get_text_embedding(query)
    else:
        embedding = get_image_embedding(query)
    
    where_filter = {"type": target_type} if target_type else None
    
    results = collection.query(
        query_embeddings=[embedding],
        n_results=top_k,
        where=where_filter
    )
    
    return results
```

---

## 五、应用场景

```
跨模态检索应用：

1. 电商搜索
   - 用文字搜商品图片
   - 用图片搜相似商品

2. 文档检索
   - 从文档图片中检索
   - 支持复杂布局

3. 内容推荐
   - 相似内容推荐
   - 跨模态推荐
```

---

## 六、评估

```python
def evaluate_retrieval(query: str, expected_results: list, actual_results: list) -> dict:
    """评估检索效果"""
    # 计算准确率
    correct = len(set(expected_results) & set(actual_results))
    precision = correct / len(actual_results) if actual_results else 0
    recall = correct / len(expected_results) if expected_results else 0
    
    return {
        "precision": precision,
        "recall": recall,
        "f1": 2 * precision * recall / (precision + recall) if (precision + recall) > 0 else 0
    }
```

---

## 小结

```
本课核心要点：

1. 跨模态检索：文字搜图片、图片搜文字
2. 向量相似度匹配
3. 支持混合检索和过滤
4. 应用广泛：电商、文档、推荐

---

**下一课**: [04 多模态 RAG Pipeline——图文混合检索 + 多模态 LLM 生成](./04-多模态RAG-Pipeline.md)
```

---

## 练习

1. **文字搜图片**：实现用文字搜图片功能。

2. **图片搜文字**：实现用图片搜文字功能。

3. **评估题**：评估检索效果。
