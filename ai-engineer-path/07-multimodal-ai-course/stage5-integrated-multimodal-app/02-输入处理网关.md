# 02 输入处理网关——统一处理文本、图片、语音、文档的输入

> 输入处理网关是多模态系统的入口。

## 场景引入

你的多模态系统需要处理来自不同渠道的输入：用户可能上传一张 JPG 图片、一段 MP3 语音、一份 PDF 合同，或者直接输入文字。每种输入的预处理逻辑完全不同——图片需要压缩和格式转换、语音需要降噪和采样率标准化、文档需要解析和分块。你需要设计一个统一的输入处理网关，对外暴露一致的接口，对内路由到不同的处理器。

## 学习目标

- 掌握输入处理网关的设计
- 理解不同输入的预处理方法
- 学会构建统一的输入处理层

---

## 一、网关架构

```
输入处理网关：

输入 → 格式识别 → 预处理 → 标准化 → 输出
  │        │          │          │      │
  ▼        ▼          ▼          ▼      ▼
各种格式  识别类型   预处理    统一格式  标准输出
```

---

## 二、文本处理

```python
class TextProcessor:
    """文本处理器"""
    
    def process(self, text: str) -> dict:
        """处理文本"""
        return {
            "type": "text",
            "content": text,
            "tokens": count_tokens(text)
        }
```

---

## 三、图片处理

```python
class ImageProcessor:
    """图片处理器"""
    
    def process(self, image_input) -> dict:
        """处理图片"""
        # 下载图片
        if isinstance(image_input, str) and image_input.startswith("http"):
            image = download_image(image_input)
        else:
            image = image_input
        
        # 预处理
        image = self._preprocess(image)
        
        return {
            "type": "image",
            "data": image,
            "metadata": self._extract_metadata(image)
        }
    
    def _preprocess(self, image):
        """预处理图片"""
        # 调整大小
        # 增强质量
        return image
```

---

## 四、语音处理

```python
class AudioProcessor:
    """语音处理器"""
    
    def __init__(self):
        self.asr = whisper.load_model("base")
    
    def process(self, audio_input) -> dict:
        """处理语音"""
        # 语音识别
        text = self.asr.transcribe(audio_input)["text"]
        
        return {
            "type": "audio",
            "transcript": text,
            "metadata": self._extract_metadata(audio_input)
        }
```

---

## 五、文档处理

```python
class DocumentProcessor:
    """文档处理器"""
    
    def process(self, doc_input) -> dict:
        """处理文档"""
        # 解析文档
        elements = parse_document(doc_input)
        
        # 提取内容
        text = "\n".join([e.text for e in elements])
        tables = [e.text for e in elements if e.category == "Table"]
        images = [e.metadata.image_path for e in elements if e.category == "Image"]
        
        return {
            "type": "document",
            "text": text,
            "tables": tables,
            "images": images
        }
```

---

## 六、统一网关

```python
class InputGateway:
    """输入网关"""
    
    def __init__(self):
        self.processors = {
            "text": TextProcessor(),
            "image": ImageProcessor(),
            "audio": AudioProcessor(),
            "document": DocumentProcessor()
        }
    
    def process(self, input_data) -> dict:
        """处理输入"""
        # 识别类型
        input_type = self._identify_type(input_data)
        
        # 选择处理器
        processor = self.processors.get(input_type)
        if not processor:
            raise ValueError(f"不支持的输入类型：{input_type}")
        
        # 处理
        return processor.process(input_data)
    
    def _identify_type(self, input_data) -> str:
        """识别输入类型"""
        if isinstance(input_data, str):
            if input_data.endswith((".pdf", ".docx", ".doc")):
                return "document"
            elif input_data.endswith((".jpg", ".png", ".gif")):
                return "image"
            elif input_data.endswith((".mp3", ".wav", ".m4a")):
                return "audio"
            else:
                return "text"
        return "unknown"
```

---

## 常见误区

1. **不做输入验证就处理**：用户上传的文件可能是损坏的、格式不支持的、或超大文件，不做前置验证会导致处理失败或资源浪费。
2. **忽略文件大小限制**：大图片、长音频、大 PDF 会消耗大量内存和处理时间，必须设置合理的文件大小限制。
3. **同步处理所有输入**：大文件的预处理（如音频降噪、PDF 解析）耗时很长，同步处理会阻塞请求，应该用异步任务队列。
4. **不做标准化输出**：不同处理器的输出格式不统一，下游需要写适配代码，应该定义统一的输出 schema。

## 工程建议

1. **前置校验拦截非法输入**：检查文件格式、大小、完整性，不合法的输入直接返回明确的错误信息，不要进入处理流程。
2. **大文件用异步处理**：超过阈值的文件（如 >10MB 图片、>5 分钟音频）走异步任务队列，返回任务 ID 让用户轮询结果。
3. **定义统一的输出 schema**：所有处理器输出相同结构的 JSON（type, content, metadata），下游统一处理。
4. **做好资源限制和熔断**：每种处理器设置并发上限和超时时间，避免单种输入占用过多资源影响其他处理器。

## 小结

```
本课核心要点：

1. 输入处理网关统一处理各种输入
2. 不同类型有不同的处理器
3. 预处理和标准化确保质量
4. 统一接口简化下游处理

---

**下一课**: [03 多模态 Agent——让 Agent 具备视觉和听觉能力](./03-多模态Agent.md)
```

---

## 练习

1. **网关题**：实现输入处理网关。

2. **处理器题**：实现一个图片处理器。

3. **统一题**：实现统一的输入处理接口。
