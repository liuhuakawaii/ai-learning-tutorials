# 06 阶段实战——构建一个商品图片智能分析系统

> 把前 5 课学到的视觉知识整合成一个完整的商品分析系统。

## 学习目标

- 构建商品图片智能分析系统
- 集成图片理解、OCR、信息提取
- 输出一个可运行的应用

---

## 一、系统架构

```
商品图片分析系统：

图片输入 → 图片理解 → 信息提取 → 描述生成 → 结构化输出
  │          │          │          │          │
  ▼          ▼          ▼          ▼          ▼
上传图片  分类识别  提取属性  生成文案  JSON/表格
```

---

## 二、核心实现

```python
class ProductImageAnalyzer:
    """商品图片分析器"""
    
    def __init__(self):
        self.client = OpenAI()
    
    def analyze(self, image_url: str) -> dict:
        """分析商品图片"""
        
        # 1. 图片理解
        description = self._describe_image(image_url)
        
        # 2. 信息提取
        attributes = self._extract_attributes(image_url)
        
        # 3. 描述生成
        marketing_copy = self._generate_marketing_copy(attributes)
        
        return {
            "description": description,
            "attributes": attributes,
            "marketing_copy": marketing_copy
        }
    
    def _describe_image(self, image_url: str) -> str:
        """描述图片"""
        response = self.client.chat.completions.create(
            model="gpt-4o",
            messages=[{
                "role": "user",
                "content": [
                    {"type": "text", "text": "请详细描述这张商品图片"},
                    {"type": "image_url", "image_url": {"url": image_url}}
                ]
            }]
        )
        return response.choices[0].message.content
    
    def _extract_attributes(self, image_url: str) -> dict:
        """提取商品属性"""
        response = self.client.chat.completions.create(
            model="gpt-4o",
            messages=[{
                "role": "user",
                "content": [
                    {"type": "text", "text": "请提取商品的名称、类别、颜色、材质、尺寸等属性，以 JSON 格式输出"},
                    {"type": "image_url", "image_url": {"url": image_url}}
                ]
            }],
            response_format={"type": "json_object"}
        )
        return json.loads(response.choices[0].message.content)
    
    def _generate_marketing_copy(self, attributes: dict) -> str:
        """生成营销文案"""
        response = self.client.chat.completions.create(
            model="gpt-4o",
            messages=[{
                "role": "user",
                "content": f"根据以下商品属性生成一段营销文案：{json.dumps(attributes, ensure_ascii=False)}"
            }]
        )
        return response.choices[0].message.content
```

---

## 三、使用示例

```python
analyzer = ProductImageAnalyzer()
result = analyzer.analyze("https://example.com/product.jpg")

print(f"描述：{result['description']}")
print(f"属性：{result['attributes']}")
print(f"文案：{result['marketing_copy']}")
```

---

## 四、批量处理

```python
def batch_analyze(image_urls: list) -> list:
    """批量分析"""
    results = []
    
    for url in image_urls:
        result = analyzer.analyze(url)
        results.append(result)
    
    return results
```

---

## 小结

```
本课核心要点：

1. 集成图片理解、OCR、信息提取
2. 自动生成营销文案
3. 结构化输出便于后续处理
4. 支持批量处理

阶段总结：
  你已经掌握了视觉与图像处理能力。
  下一阶段，我们将学习语音与音频处理。
```

---

## 作业

1. **完成实战**：运行商品图片分析系统。

2. **扩展题**：添加更多分析功能。

3. **批量题**：实现批量图片分析。
