# 04 模型合并——LoRA 权重与基础模型的合并导出

> 训练完成后，需要将 LoRA 权重合并到基础模型中。

## 学习目标

- 掌握 LoRA 权重的合并方法
- 理解模型导出的格式和用途
- 学会部署微调后的模型

---

## 一、模型合并

```python
from peft import PeftModel
from transformers import AutoModelForCausalLM

# 加载基础模型
base_model = AutoModelForCausalLM.from_pretrained(
    "Qwen/Qwen2.5-7B",
    torch_dtype=torch.float16
)

# 加载 LoRA 权重
model = PeftModel.from_pretrained(
    base_model,
    "./output/checkpoint-1000"
)

# 合并权重
merged_model = model.merge_and_unload()

# 保存合并后的模型
merged_model.save_pretrained("./merged_model")
```

---

## 二、模型导出格式

```
导出格式：

1. HuggingFace 格式
   - 最常用
   - 支持 Transformers
   - 目录结构：
     ├── config.json
     ├── pytorch_model.bin
     ├── tokenizer.json
     └── tokenizer_config.json

2. GGUF 格式
   - 用于 llama.cpp
   - 支持量化
   - 单文件

3. ONNX 格式
   - 跨平台
   - 推理优化
   - 需要转换
```

---

## 三、GGUF 导出

```bash
# 转换为 GGUF
python convert_hf_to_gguf.py ./merged_model --outfile model.gguf

# 量化
./llama-quantize model.gguf model-q4_k_m.gguf Q4_K_M
```

---

## 四、模型测试

```python
from transformers import AutoModelForCausalLM, AutoTokenizer

# 加载合并后的模型
model = AutoModelForCausalLM.from_pretrained("./merged_model")
tokenizer = AutoTokenizer.from_pretrained("./merged_model")

# 测试
input_text = "什么是高血压？"
inputs = tokenizer(input_text, return_tensors="pt")
outputs = model.generate(**inputs, max_length=200)
print(tokenizer.decode(outputs[0]))
```

---

## 五、模型版本管理

```
模型版本管理：

models/
├── qwen2.5-7b-medical-v1/
│   ├── config.json
│   ├── pytorch_model.bin
│   └── metadata.json
├── qwen2.5-7b-medical-v2/
│   └── ...
└── latest -> qwen2.5-7b-medical-v2/
```

---

## 小结

```
本课核心要点：

1. LoRA 权重需要合并到基础模型
2. 支持多种导出格式：HuggingFace、GGUF、ONNX
3. 合并后测试确保效果正确
4. 版本管理支持模型迭代

---

**下一课**: [05 模型量化导出——将微调模型导出为 GGUF / GPTQ 格式](./05-模型量化导出.md)
```

---

## 练习

1. **合并题**：将 LoRA 权重合并到基础模型。

2. **导出题**：将模型导出为 GGUF 格式。

3. **测试题**：测试合并后的模型效果。
