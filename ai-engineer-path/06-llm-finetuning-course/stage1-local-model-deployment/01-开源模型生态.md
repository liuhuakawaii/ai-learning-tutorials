# 01 开源模型生态——Llama / Qwen / Mistral / DeepSeek 的选型指南

> **课程定位**：理解开源大模型生态，掌握模型选型方法论
> **前置知识**：了解大模型基本概念（参数、推理、量化）
> **预计时长**：50 分钟

---

## 场景引入

你正在为公司搭建一个本地 AI 助手，预算有限但需求明确——需要支持中文问答、代码生成、长文本理解。打开 Hugging Face，面对上百个开源模型，参数从 0.5B 到 405B 不等，架构有 Dense 也有 MoE，量化格式更是五花八门。选错模型意味着浪费数天的下载和调试时间，甚至推倒重来。如何在众多模型中快速找到最适合你场景的那一个？

---

## 学习目标

完成本课学习后，你将能够：

1. 列出主流开源大模型家族及其特点
2. 根据任务类型、硬件资源、语言需求选择合适的模型
3. 理解模型量化对性能和显存的影响
4. 用代码加载和测试一个开源模型

---

## 一、开源模型全景

```
2024-2025 年主流开源大模型：

┌──────────────────────────────────────────────────────────────────┐
│  Meta — Llama 系列                                               │
│  ├── Llama 3.1: 8B / 70B / 405B                                  │
│  ├── 特点：性能均衡、社区最大、工具链最成熟                         │
│  ├── 许可证：Llama License（商用友好）                             │
│  └── 适合：通用对话、RAG、Agent、微调基座                          │
│                                                                   │
│  阿里 — Qwen 系列                                                │
│  ├── Qwen 2.5: 0.5B / 1.5B / 3B / 7B / 14B / 32B / 72B          │
│  ├── 特点：中文最强、多模态支持、尺寸齐全                           │
│  ├── 许可证：Apache 2.0                                           │
│  └── 适合：中文场景、多模态、端侧部署                              │
│                                                                   │
│  Mistral AI — Mistral 系列                                       │
│  ├── Mistral 7B / Mixtral 8x7B / Mixtral 8x22B                   │
│  ├── 特点：MoE 架构高效、推理速度快                                │
│  ├── 许可证：Apache 2.0                                           │
│  └── 适合：资源受限、高吞吐推理                                    │
│                                                                   │
│  DeepSeek — DeepSeek 系列                                        │
│  ├── DeepSeek V2: 16B / 236B (MoE)                                │
│  ├── DeepSeek Coder V2: 16B / 236B                                │
│  ├── 特点：代码能力顶级、数学推理强、MoE 架构                      │
│  ├── 许可证：MIT                                                  │
│  └── 适合：代码生成、数学推理、技术问答                             │
│                                                                   │
│  Google — Gemma 系列                                             │
│  ├── Gemma 2: 2B / 9B / 27B                                      │
│  ├── 特点：同参数量性能优秀、Google 工程优化                       │
│  ├── 许可证：Gemma License                                         │
│  └── 适合：端侧部署、学术研究                                      │
│                                                                   │
│  Microsoft — Phi 系列                                            │
│  ├── Phi-3: 3.8B / 7B / 14B                                      │
│  ├── 特点：小模型性能惊人、适合端侧                                │
│  ├── 许可证：MIT                                                  │
│  └── 适合：手机/嵌入式部署、低资源场景                              │
└──────────────────────────────────────────────────────────────────┘
```

---

## 二、选型核心因素

```
选型四象限：

                    高质量
                      │
         Qwen 72B ───┼─── Llama 405B
         DeepSeek 236B│
                      │
  易部署 ─────────────┼─────────────── 难部署
                      │
         Phi-3 3.8B ─┼─── Mistral 7B
         Gemma 2B    │
                      │
                    低质量

关键因素详解：

1. 任务类型
   ┌──────────────────────────────────────────────────────┐
   │  通用对话/写作    → Llama 3.1 / Qwen 2.5             │
   │  代码生成/补全    → DeepSeek Coder V2 / CodeLlama    │
   │  数学/推理       → DeepSeek V2 / Qwen 2.5-Math      │
   │  中文 NLP       → Qwen 2.5（中文 benchmark 领先）    │
   │  多模态         → Qwen 2.5-VL / Llama 3.2-Vision    │
   │  长文本         → Qwen 2.5（128K）/ Llama 3.1（128K）│
   └──────────────────────────────────────────────────────┘

2. 硬件资源（显存需求）
   ┌──────────────────────────────────────────────────────┐
   │  模型参数    FP16显存    INT8量化    INT4量化          │
   │  1-3B       2-6GB      1-3GB      0.5-1.5GB         │
   │  7-8B       14-16GB    7-8GB      3.5-4GB           │
   │  13-14B     26-28GB    13-14GB    6.5-7GB           │
   │  70-72B     140-144GB  70-72GB    35-36GB           │
   └──────────────────────────────────────────────────────┘
   例：RTX 4090 (24GB) → 7B FP16 或 14B INT4

3. 量化等级
   ┌──────────────────────────────────────────────────────┐
   │  FP16  → 原始精度，质量最好，显存最大                  │
   │  INT8  → 8-bit 量化，质量损失极小，显存减半            │
   │  INT4  → 4-bit 量化，质量有损，显存减 75%             │
   │  GPTQ  → 训练后量化，GPU 推理快                       │
   │  GGUF  → llama.cpp 格式，CPU/GPU 混合推理             │
   │  AWQ   → 激活感知量化，质量优于 GPTQ                   │
   └──────────────────────────────────────────────────────┘
```

---

## 三、Benchmark 对比

```
主流 7B 模型在各 benchmark 上的表现（分数越高越好）：

┌─────────────┬────────┬────────┬────────┬────────┐
│  Benchmark   │ Llama  │ Qwen   │ Mistral│ DeepSeek│
│              │ 3.1 8B │ 2.5 7B │ 7B v3  │ 7B     │
├─────────────┼────────┼────────┼────────┼────────┤
│  MMLU        │ 73.0   │ 74.2   │ 72.8   │ 71.5   │
│  HumanEval   │ 72.0   │ 75.6   │ 69.5   │ 79.3   │
│  GSM8K       │ 84.5   │ 85.3   │ 78.2   │ 88.1   │
│  C-Eval      │ 65.8   │ 89.2   │ 62.1   │ 68.4   │
│  CMMLU       │ 67.2   │ 90.1   │ 63.5   │ 70.2   │
│  ARC-C       │ 83.4   │ 82.1   │ 81.2   │ 80.8   │
└─────────────┴────────┴────────┴────────┴────────┘

观察：
  - 通用能力（MMLU）：Qwen ≈ Llama > Mistral ≈ DeepSeek
  - 代码能力（HumanEval）：DeepSeek > Qwen > Llama > Mistral
  - 数学能力（GSM8K）：DeepSeek > Qwen > Llama > Mistral
  - 中文能力（C-Eval/CMMLU）：Qwen >> 其他

选择建议：
  - 面向中文用户 → Qwen 2.5（中文 benchmark 断崖领先）
  - 面向代码场景 → DeepSeek Coder V2
  - 面向英文通用 → Llama 3.1
  - 面向资源受限 → Phi-3 Mini 或 Gemma 2B
```

---

## 四、模型加载实战

```python
# 用 Hugging Face Transformers 加载模型
from transformers import AutoTokenizer, AutoModelForCausalLM
import torch

# 1. 选择模型
model_name = "Qwen/Qwen2.5-7B-Instruct"

# 2. 加载 Tokenizer
tokenizer = AutoTokenizer.from_pretrained(model_name, trust_remote_code=True)

# 3. 加载模型（自动检测设备）
model = AutoModelForCausalLM.from_pretrained(
    model_name,
    torch_dtype=torch.bfloat16,      # 使用 BF16 精度
    device_map="auto",                # 自动分配 GPU
    trust_remote_code=True,
)

# 4. 推理测试
messages = [
    {"role": "system", "content": "你是一个有用的助手。"},
    {"role": "user", "content": "用 Python 写一个快速排序"},
]

text = tokenizer.apply_chat_template(messages, tokenize=False, add_generation_prompt=True)
inputs = tokenizer(text, return_tensors="pt").to(model.device)

outputs = model.generate(**inputs, max_new_tokens=512, temperature=0.7)
print(tokenizer.decode(outputs[0][inputs.input_ids.shape[1]:], skip_special_tokens=True))
```

```python
# 用 Ollama 一行命令运行（推荐入门方式）
# 终端执行：
# ollama run qwen2.5:7b

# Python 调用 Ollama
import requests

response = requests.post("http://localhost:11434/api/chat", json={
    "model": "qwen2.5:7b",
    "messages": [
        {"role": "user", "content": "用 Python 写一个快速排序"}
    ],
    "stream": False,
})

print(response.json()["message"]["content"])
```

---

## 五、选型决策树

```
你的主要任务是什么？
├── 通用对话/写作
│   ├── 中文为主 → Qwen 2.5 7B/14B
│   └── 英文为主 → Llama 3.1 8B/70B
│
├── 代码生成
│   ├── 通用代码 → DeepSeek Coder V2
│   └── Python 专注 → CodeLlama 7B/13B
│
├── 数学/推理
│   └── DeepSeek V2 / Qwen 2.5-Math
│
├── 多模态（图文）
│   ├── 中文 → Qwen 2.5-VL
│   └── 英文 → Llama 3.2 Vision
│
└── 端侧部署（手机/嵌入式）
    ├── 最小 → Phi-3 Mini (3.8B) / Gemma 2B
    └── 平衡 → Qwen 2.5 3B

你的显存有多少？
├── 4GB 以下  → Qwen 2.5 0.5B / Gemma 2B（INT4）
├── 8GB      → 7B 模型（INT4 量化）
├── 16GB     → 7B FP16 或 14B INT4
├── 24GB     → 14B FP16 或 70B INT4
└── 48GB+    → 70B FP16 / INT8
```

---

## 常见误区

1. **只看参数量选模型**：参数大不等于效果好。Qwen 2.5 7B 在中文任务上可能比 Llama 3.1 70B 表现更好，因为训练数据和优化方向不同。选型要看 benchmark 而不是参数规模。

2. **忽略许可证限制**：部分模型的许可证限制商用场景（如早期 Llama License），部署前务必确认许可证条款，避免法律风险。

3. **盲目追求最大量化**：INT4 量化虽然显存减 75%，但在数学推理和代码生成任务上质量损失明显。需要根据任务类型选择合适的量化等级。

4. **忽略中文场景的特殊需求**：英文 benchmark 表现好的模型在中文任务上可能拉胯。面向中文用户时，C-Eval 和 CMMLU 比 MMLU 更有参考价值。

---

## 工程建议

1. **先用 Ollama 快速验证**：在正式选型前，用 Ollama 跑几个候选模型做定性测试，比纯看 benchmark 数据更直观。5 分钟内就能对比多个模型的输出质量。

2. **建立选型 checklist**：每次选型时列出任务类型、显存预算、语言需求、量化等级四个维度，按优先级筛选，避免在众多模型中迷失方向。

3. **量化版本先测试再上线**：量化模型在通用对话任务上通常表现良好，但在专业领域（医疗、法律、代码）可能出现严重退化。上线前必须在目标领域做质量回归测试。

4. **关注社区生态**：选择社区活跃度高的模型系列（如 Llama、Qwen），可以获得更完善的工具链支持、更多的微调数据和更及时的问题修复。

---

## 小结

```
本课核心要点：

1. 主流开源模型：Llama（通用）、Qwen（中文）、DeepSeek（代码/推理）、Mistral（高效）
2. 选型四要素：任务类型、硬件资源、语言需求、量化等级
3. 中文场景首选 Qwen 2.5，代码场景首选 DeepSeek
4. 量化是平衡质量和资源的关键手段
5. Ollama 是最简单的本地运行方式

---

**下一课**: [02 Ollama 快速上手——一行命令运行本地大模型](./02-Ollama快速上手.md)
```

---

## 练习

1. **选型题**：你的场景是"为一个中文客服系统选择基座模型，显存 16GB，需要支持长文本"，你会选哪个模型？为什么？

2. **对比题**：用 Ollama 分别运行 Qwen 2.5 7B 和 Llama 3.1 8B，对同一个中文 prompt 对比输出质量。

3. **量化题**：对同一个模型分别用 FP16 和 INT4 加载，对比显存占用和推理速度。

4. **实战题**：用 Hugging Face Transformers 加载一个 7B 模型，完成一次对话推理。
