# 05 训练监控——Loss 曲线解读、过拟合检测、训练中断恢复

> 训练监控让你及时发现问题，避免浪费时间和资源。

## 场景引入

你启动了微调任务，Loss 一直在下降，看起来很顺利。跑了 3 个小时后你满怀期待地测试模型，结果发现输出全是重复的"的的的的的"——模型早就过拟合了，但你没有及时发现，白白浪费了 3 小时的 GPU 时间。如果能在训练过程中实时监控 Loss 曲线、检测过拟合、设置自动停止，就能避免这种损失。

---

## 学习目标

- 掌握训练监控的方法和工具
- 理解 Loss 曲线的含义
- 学会检测过拟合和恢复训练

---

## 一、TensorBoard 监控

```bash
# 启动 TensorBoard
tensorboard --logdir ./output

# 访问 http://localhost:6006
```

---

## 二、Loss 曲线解读

```
Loss 曲线解读：

1. 正常下降
   Loss 持续下降，最终趋于平稳
   → 训练正常

2. 震荡
   Loss 上下波动
   → 学习率太大，降低学习率

3. 不下降
   Loss 保持不变
   → 学习率太小，或数据有问题

4. 上升
   Loss 持续上升
   → 学习率太大，或梯度爆炸
```

---

## 三、过拟合检测

```python
def detect_overfitting(train_losses: list, eval_losses: list) -> bool:
    """检测过拟合"""
    if len(eval_losses) < 5:
        return False
    
    # 检查 eval loss 是否开始上升
    recent_eval = eval_losses[-5:]
    if all(recent_eval[i] <= recent_eval[i+1] for i in range(len(recent_eval)-1)):
        return True
    
    return False
```

---

## 四、训练中断恢复

```python
from transformers import Trainer

# 从检查点恢复
trainer.train(resume_from_checkpoint="./output/checkpoint-1000")

# 自动保存检查点
training_args = TrainingArguments(
    output_dir="./output",
    save_strategy="steps",
    save_steps=500,
    save_total_limit=3
)
```

---

## 五、训练日志

```python
import logging

logging.basicConfig(
    level=logging.INFO,
    format='%(asctime)s - %(levelname)s - %(message)s',
    handlers=[
        logging.FileHandler('training.log'),
        logging.StreamHandler()
    ]
)

logger = logging.getLogger(__name__)

# 记录训练信息
logger.info(f"Epoch {epoch}, Loss: {loss}")
```

---

## 六、告警机制

```python
class TrainingMonitor:
    """训练监控器"""
    
    def __init__(self):
        self.train_losses = []
        self.eval_losses = []
    
    def check(self, train_loss: float, eval_loss: float):
        """检查训练状态"""
        self.train_losses.append(train_loss)
        self.eval_losses.append(eval_loss)
        
        # 检查过拟合
        if self._detect_overfitting():
            self._alert("检测到过拟合！")
        
        # 检查 loss 异常
        if train_loss > 10:
            self._alert("Loss 异常高！")
    
    def _alert(self, message: str):
        """发送告警"""
        logger.warning(message)
```

---

## 常见误区

1. **只看 Loss 数值不看趋势**：Loss 从 2.3 降到 1.5 不代表效果好，关键是趋势——是否持续下降、是否趋于平稳、是否出现反弹。

2. **过拟合了就加更多数据**：过拟合的根因可能是数据量不足，但也可能是训练轮数太多或 dropout 太小。先尝试减少 epochs 和增加 dropout，而不是急着加数据。

3. **训练中断就从头开始**：Transformers Trainer 支持 `resume_from_checkpoint`，可以从最近的 checkpoint 恢复训练。定期保存 checkpoint 可以避免大量重复计算。

4. **不用 TensorBoard 等可视化工具**：纯看日志中的 Loss 数字很难发现趋势问题。TensorBoard 的曲线图能让你一眼看出过拟合、学习率问题等异常。

---

## 工程建议

1. **Loss 曲线是最基本的监控**：训练过程中实时查看 Loss 曲线，发现异常立即排查。正常曲线应该是先快速下降再缓慢趋于平稳。

2. **设置自动告警**：当 Loss 连续 N 步不下降、或 Loss 突然飙升、或 GPU 显存接近上限时，自动发送告警通知，避免白白浪费 GPU 时间。

3. **eval_loss 是过拟合的信号灯**：当 eval_loss 开始上升而 train_loss 继续下降时，就是过拟合的信号。此时应该停止训练或减少 epochs。

4. **保存最佳模型而非最后一个模型**：用 `load_best_model_at_end=True` 配合 `metric_for_best_model="eval_loss"`，确保最终保存的是验证集上表现最好的模型。

---

## 小结

```
本课核心要点：

1. TensorBoard 可视化训练过程
2. Loss 曲线解读判断训练状态
3. 过拟合检测及时发现问题
4. 训练中断恢复避免重复训练

---

**下一课**: [06 阶段实战——用领域数据完成一次完整的 LoRA 微调并对比效果](./06-阶段实战-完整LoRA微调.md)
```

---

## 练习

1. **监控题**：使用 TensorBoard 监控训练过程。

2. **检测题**：检测过拟合并调整策略。

3. **恢复题**：从检查点恢复训练。
