# 02 第一次微调——用 LoRA 微调 Qwen2.5-7B 的完整流程

> 动手完成第一次微调，理解完整的微调流程。

## 学习目标

- 掌握用 LoRA 微调模型的完整流程
- 理解训练配置和参数设置
- 完成一次可运行的微调实验

---

## 一、完整代码

```python
import torch
from transformers import AutoModelForCausalLM, AutoTokenizer, TrainingArguments
from peft import LoraConfig, get_peft_model
from trl import SFTTrainer
from datasets import load_dataset

# 1. 加载模型和分词器
model_name = "Qwen/Qwen2.5-7B"
tokenizer = AutoTokenizer.from_pretrained(model_name)
model = AutoModelForCausalLM.from_pretrained(
    model_name,
    torch_dtype=torch.float16,
    device_map="auto"
)

# 2. 配置 LoRA
lora_config = LoraConfig(
    r=16,
    lora_alpha=32,
    target_modules=["q_proj", "v_proj", "k_proj", "o_proj"],
    lora_dropout=0.05,
    bias="none",
    task_type="CAUSAL_LM"
)

# 3. 应用 LoRA
model = get_peft_model(model, lora_config)
model.print_trainable_parameters()

# 4. 加载数据集
dataset = load_dataset("json", data_files="training_data.json")

# 5. 训练配置
training_args = TrainingArguments(
    output_dir="./output",
    num_train_epochs=3,
    per_device_train_batch_size=4,
    gradient_accumulation_steps=4,
    learning_rate=2e-4,
    fp16=True,
    logging_steps=10,
    save_strategy="epoch",
    warmup_ratio=0.1
)

# 6. 创建训练器
trainer = SFTTrainer(
    model=model,
    args=training_args,
    train_dataset=dataset["train"],
    tokenizer=tokenizer,
    max_seq_length=2048
)

# 7. 开始训练
trainer.train()

# 8. 保存模型
trainer.save_model("./output/final")
```

---

## 二、训练配置

```
训练配置建议：

| 参数 | 推荐值 | 说明 |
|------|--------|------|
| epochs | 3-5 | 训练轮数 |
| batch_size | 4-8 | 批次大小 |
| learning_rate | 2e-4 | 学习率 |
| warmup_ratio | 0.1 | 预热比例 |
| max_seq_length | 2048 | 最大序列长度 |
```

---

## 三、训练监控

```python
# 查看训练日志
# tensorboard --logdir ./output

# 训练损失
# loss 应该持续下降
# 如果 loss 不降或上升，调整学习率
```

---

## 四、常见问题

```
常见问题：

1. 内存不足
   - 减小 batch_size
   - 使用 gradient_checkpointing
   - 使用 QLoRA

2. 训练不收敛
   - 降低学习率
   - 增加 warmup
   - 检查数据质量

3. 过拟合
   - 增加 dropout
   - 减少 epochs
   - 增加数据量
```

---

## 五、运行

```bash
# 运行训练
python train.py

# 监控训练
tensorboard --logdir ./output
```

---

## 小结

```
本课核心要点：

1. 完整的 LoRA 微调流程
2. 训练配置的关键参数
3. 训练监控和问题诊断
4. 常见问题的解决方法

下一课：QLoRA 微调——4-bit 量化 + LoRA，在 8GB 显存上微调 7B 模型。
```

---

## 练习

1. **微调题**：完成一次 LoRA 微调。

2. **监控题**：使用 TensorBoard 监控训练过程。

3. **调优题**：调整参数优化训练效果。
