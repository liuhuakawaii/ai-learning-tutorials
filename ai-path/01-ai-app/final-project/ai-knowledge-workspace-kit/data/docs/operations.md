# AI 产品运营规范

## 成本和配额

系统需要记录每次请求的模型、输入 token、输出 token、耗时、状态和失败原因。免费用户应有每日额度，超过额度时给出清晰提示。

## 安全边界

知识库必须按工作区隔离。用户不能检索其他工作区的文档。遇到提示注入内容时，系统应优先遵守开发者规则和权限边界。

## 失败处理

常见失败包括超时、限流、空答案、检索不到资料和工具执行失败。失败信息要能被用户理解，也要能被开发者追踪。
