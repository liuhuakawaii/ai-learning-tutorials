# 第2课：构建评估集——事实性、引用、格式、拒答

> **课程定位**：准备高质量的测试数据
> **前置知识**：第 1 课的评估概念
> **预计时长**：45 分钟

## 场景引入

你决定为 AI 知识库助手构建评估集。一开始你写了 5 个问题，跑了一遍发现全部通过，觉得"质量不错"。但上线后用户反馈："为什么它不会拒答？我问了它一个文档里没有的问题，它编了一个答案。"你回头看评估集，发现 5 个问题全部是"文档里有答案"的场景，完全没有测试拒答能力。一个好的评估集，必须覆盖用户可能遇到的所有行为模式，而不仅仅是"回答正确"这一种。

---

## 学习目标

完成本课学习后，你将能够：

1. 设计评估集的结构
2. 编写不同类型的测试用例
3. 定义评估标准
4. 管理和维护评估集

---

## 一、评估集结构

### 1.1 评估用例格式

```typescript
// lib/evals/types.ts
interface EvalCase {
  id: string
  category: string           // 分类
  question: string           // 用户问题
  context?: string           // 相关上下文（用于 RAG）
  expectedAnswer?: string    // 期望答案
  expectedCitations?: string[]  // 期望引用
  expectedBehavior: 'answer' | 'refuse' | 'clarify'  // 期望行为
  tags: string[]             // 标签
  difficulty: 'easy' | 'medium' | 'hard'
}

interface EvalResult {
  caseId: string
  actualAnswer: string
  actualCitations: string[]
  scores: {
    relevancy: number        // 0-1
    faithfulness: number     // 0-1
    completeness: number     // 0-1
    citationAccuracy: number // 0-1
  }
  passed: boolean
  feedback?: string
}
```

### 1.2 评估集示例

```typescript
// lib/evals/test-cases.ts
export const testCases: EvalCase[] = [
  // 事实性问题
  {
    id: 'fact-001',
    category: '事实性',
    question: '公司的年假政策是什么？',
    context: '根据员工手册第 5 章，入职满 1 年的员工享有 10 天年假...',
    expectedAnswer: '入职满 1 年的员工享有 10 天年假',
    expectedBehavior: 'answer',
    tags: ['hr', 'policy'],
    difficulty: 'easy',
  },

  // 引用问题
  {
    id: 'cite-001',
    category: '引用',
    question: 'API 接口的认证方式有哪些？',
    context: '技术文档说明支持 OAuth2、API Key、JWT 三种认证方式...',
    expectedAnswer: '支持三种认证方式：OAuth2、API Key、JWT',
    expectedCitations: ['api-doc.md#认证'],
    expectedBehavior: 'answer',
    tags: ['api', 'auth'],
    difficulty: 'medium',
  },

  // 拒答问题
  {
    id: 'refuse-001',
    category: '拒答',
    question: '公司下季度的营收预期是多少？',
    expectedBehavior: 'refuse',
    tags: ['finance', 'sensitive'],
    difficulty: 'medium',
  },

  // 格式问题
  {
    id: 'format-001',
    category: '格式',
    question: '列出所有支持的文件格式',
    expectedAnswer: '支持的格式包括：PDF、Markdown、TXT',
    expectedBehavior: 'answer',
    tags: ['feature'],
    difficulty: 'easy',
  },
]
```

---

## 二、编写评估用例

### 2.1 覆盖不同场景

```
┌─────────────────────────────────────────────────────────────────┐
│                    评估用例分类                                   │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  事实性问题                                                      │
│  ├── 基于文档的明确答案                                          │
│  ├── 需要综合多个信息点                                          │
│  └── 涉及数字、日期、名称                                        │
│                                                                 │
│  引用验证                                                        │
│  ├── 答案必须引用来源                                            │
│  ├── 引用必须准确                                                │
│  └── 引用必须可追溯                                              │
│                                                                 │
│  拒答场景                                                        │
│  ├── 文档中没有的信息                                            │
│  ├── 超出知识范围的问题                                          │
│  └── 敏感或机密信息                                              │
│                                                                 │
│  边界情况                                                        │
│  ├── 模糊的问题                                                  │
│  ├── 多义词                                                      │
│  └── 拼写错误                                                    │
│                                                                 │
│  安全测试                                                        │
│  ├── 提示注入尝试                                                │
│  ├── 越权访问尝试                                                │
│  └── 敏感信息泄露                                                │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 评估标准

```typescript
// lib/evals/criteria.ts
export const evalCriteria = {
  relevancy: {
    description: '回答是否与问题相关',
    scale: {
      1: '完全不相关',
      0.5: '部分相关',
      1: '完全相关',
    },
  },
  faithfulness: {
    description: '回答是否基于检索结果，没有编造',
    scale: {
      1: '完全编造',
      0.5: '部分基于',
      1: '完全基于',
    },
  },
  completeness: {
    description: '回答是否涵盖所有必要信息',
    scale: {
      1: '严重缺失',
      0.5: '部分缺失',
      1: '完整',
    },
  },
  citationAccuracy: {
    description: '引用是否准确、可追溯',
    scale: {
      1: '引用错误',
      0.5: '引用模糊',
      1: '引用准确',
    },
  },
}
```

---

## 三、运行评估

### 3.1 评估运行器

```typescript
// lib/evals/runner.ts
import { testCases } from './test-cases'
import { askQuestion } from '@/lib/rag'
import { evaluateAnswer } from './evaluator'

interface EvalRun {
  id: string
  timestamp: Date
  totalCases: number
  passed: number
  failed: number
  scores: {
    avgRelevancy: number
    avgFaithfulness: number
    avgCompleteness: number
    avgCitationAccuracy: number
  }
  results: EvalResult[]
}

export async function runEval(
  options: {
    categories?: string[]
    tags?: string[]
    difficulty?: string
  } = {}
): Promise<EvalRun> {
  let cases = testCases

  // 过滤
  if (options.categories) {
    cases = cases.filter(c => options.categories!.includes(c.category))
  }
  if (options.tags) {
    cases = cases.filter(c => c.tags.some(t => options.tags!.includes(t)))
  }
  if (options.difficulty) {
    cases = cases.filter(c => c.difficulty === options.difficulty)
  }

  const results: EvalResult[] = []

  for (const testCase of cases) {
    // 运行系统
    const response = await askQuestion(testCase.question)

    // 评估结果
    const evaluation = await evaluateAnswer(testCase, response)

    results.push(evaluation)
  }

  // 计算汇总
  const passed = results.filter(r => r.passed).length
  const avg = (key: keyof EvalResult['scores']) =>
    results.reduce((sum, r) => sum + r.scores[key], 0) / results.length

  return {
    id: `eval-${Date.now()}`,
    timestamp: new Date(),
    totalCases: cases.length,
    passed,
    failed: cases.length - passed,
    scores: {
      avgRelevancy: avg('relevancy'),
      avgFaithfulness: avg('faithfulness'),
      avgCompleteness: avg('completeness'),
      avgCitationAccuracy: avg('citationAccuracy'),
    },
    results,
  }
}
```

### 3.2 评估器

```typescript
// lib/evals/evaluator.ts
import OpenAI from 'openai'

const openai = new OpenAI()

export async function evaluateAnswer(
  testCase: EvalCase,
  response: { answer: string; citations: string[] }
): Promise<EvalResult> {
  // 使用模型评估
  const evalResponse = await openai.responses.create({
    model: 'gpt-5.5',
    input: [
      {
        role: 'system',
        content: `你是一个 AI 回答质量评估助手。请评估以下回答的质量。

评估标准：
1. relevancy（相关性）：回答是否与问题相关
2. faithfulness（忠实度）：回答是否基于提供的上下文，没有编造
3. completeness（完整性）：回答是否涵盖所有必要信息
4. citationAccuracy（引用准确性）：引用是否准确

输出 JSON：
{
  "relevancy": 0-1,
  "faithfulness": 0-1,
  "completeness": 0-1,
  "citationAccuracy": 0-1,
  "passed": boolean,
  "feedback": "评估说明"
}`,
      },
      {
        role: 'user',
        content: `问题：${testCase.question}

上下文：${testCase.context || '无'}

期望答案：${testCase.expectedAnswer || '无'}

实际回答：${response.answer}

引用：${response.citations.join(', ') || '无'}

期望行为：${testCase.expectedBehavior}`,
      },
    ],
    text: { format: { type: 'json_object' } },
    temperature: 0,
  })

  const scores = JSON.parse(evalResponse.output_text)

  return {
    caseId: testCase.id,
    actualAnswer: response.answer,
    actualCitations: response.citations,
    scores: {
      relevancy: scores.relevancy,
      faithfulness: scores.faithfulness,
      completeness: scores.completeness,
      citationAccuracy: scores.citationAccuracy,
    },
    passed: scores.passed,
    feedback: scores.feedback,
  }
}
```

---

## 四、评估结果展示

```typescript
// components/EvalResults.tsx
'use client'

interface EvalRun {
  id: string
  timestamp: string
  totalCases: number
  passed: number
  failed: number
  scores: {
    avgRelevancy: number
    avgFaithfulness: number
    avgCompleteness: number
    avgCitationAccuracy: number
  }
  results: EvalResult[]
}

export function EvalResults({ run }: { run: EvalRun }) {
  return (
    <div className="eval-results">
      <h2>评估结果</h2>

      <div className="summary">
        <div className="stat">
          <span className="label">通过率</span>
          <span className="value">{((run.passed / run.totalCases) * 100).toFixed(1)}%</span>
        </div>
        <div className="stat">
          <span className="label">通过</span>
          <span className="value">{run.passed}/{run.totalCases}</span>
        </div>
      </div>

      <div className="scores">
        <div className="score">
          <span>相关性</span>
          <span>{(run.scores.avgRelevancy * 100).toFixed(0)}%</span>
        </div>
        <div className="score">
          <span>忠实度</span>
          <span>{(run.scores.avgFaithfulness * 100).toFixed(0)}%</span>
        </div>
        <div className="score">
          <span>完整性</span>
          <span>{(run.scores.avgCompleteness * 100).toFixed(0)}%</span>
        </div>
        <div className="score">
          <span>引用准确性</span>
          <span>{(run.scores.avgCitationAccuracy * 100).toFixed(0)}%</span>
        </div>
      </div>

      <div className="details">
        {run.results.map(result => (
          <div key={result.caseId} className={`result ${result.passed ? 'passed' : 'failed'}`}>
            <h4>{result.caseId}</h4>
            <p><strong>回答：</strong>{result.actualAnswer}</p>
            {result.feedback && <p><strong>反馈：</strong>{result.feedback}</p>}
          </div>
        ))}
      </div>
    </div>
  )
}
```

---

## 动手练习

### 练习一：编写评估用例

为你的应用编写 10 个评估用例：
1. 3 个事实性问题
2. 2 个引用验证问题
3. 3 个拒答场景
4. 2 个边界情况

### 练习二：运行评估

1. 运行评估集
2. 记录通过率
3. 分析失败用例

### 练习三：改进并重新评估

1. 根据失败用例改进 Prompt
2. 重新运行评估
3. 对比改进效果

---

## 参考答案

### 练习一

**思路**：评估用例需要覆盖事实性、引用、拒答、边界四类场景。每类场景至少 2-3 个用例，总共 10 个。用例应包含期望答案和期望行为（answer/refuse/clarify）。

**答案**：

```typescript
import { EvalCase } from './types'

export const testCases: EvalCase[] = [
  // 事实性问题（3 个）
  {
    id: 'fact-001',
    category: '事实性',
    question: '公司的年假政策是什么？',
    context: '根据员工手册第 5 章，入职满 1 年的员工享有 10 天年假，满 3 年享有 15 天。',
    expectedAnswer: '入职满 1 年的员工享有 10 天年假，满 3 年享有 15 天',
    expectedBehavior: 'answer',
    tags: ['hr', 'policy'],
    difficulty: 'easy',
  },
  {
    id: 'fact-002',
    category: '事实性',
    question: 'API 的认证方式有哪些？各有什么区别？',
    context: '技术文档说明支持 OAuth2、API Key、JWT 三种认证方式。OAuth2 适合第三方应用，API Key 适合服务间调用，JWT 适合无状态认证。',
    expectedAnswer: '支持三种认证方式：OAuth2（第三方应用）、API Key（服务间调用）、JWT（无状态认证）',
    expectedBehavior: 'answer',
    tags: ['api', 'auth'],
    difficulty: 'medium',
  },
  {
    id: 'fact-003',
    category: '事实性',
    question: 'Q3 的营收是多少？同比增长了多少？',
    context: '财务报告显示 Q3 营收 1.2 亿元，同比增长 23%。',
    expectedAnswer: 'Q3 营收 1.2 亿元，同比增长 23%',
    expectedBehavior: 'answer',
    tags: ['finance'],
    difficulty: 'easy',
  },

  // 引用验证问题（2 个）
  {
    id: 'cite-001',
    category: '引用',
    question: '部署流程有哪些步骤？',
    context: '运维文档说明部署流程包括：代码审查、自动化测试、灰度发布、全量上线四个步骤。',
    expectedAnswer: '部署流程包括四个步骤：代码审查、自动化测试、灰度发布、全量上线',
    expectedCitations: ['ops-doc.md#部署'],
    expectedBehavior: 'answer',
    tags: ['ops', 'deploy'],
    difficulty: 'medium',
  },
  {
    id: 'cite-002',
    category: '引用',
    question: '数据库备份策略是什么？',
    context: 'DBA 文档规定每天凌晨 2 点全量备份，每小时增量备份，备份保留 30 天。',
    expectedAnswer: '每天凌晨 2 点全量备份，每小时增量备份，保留 30 天',
    expectedCitations: ['dba-doc.md#备份'],
    expectedBehavior: 'answer',
    tags: ['db', 'backup'],
    difficulty: 'medium',
  },

  // 拒答场景（3 个）
  {
    id: 'refuse-001',
    category: '拒答',
    question: '公司下季度的营收预期是多少？',
    expectedBehavior: 'refuse',
    tags: ['finance', 'sensitive'],
    difficulty: 'medium',
  },
  {
    id: 'refuse-002',
    category: '拒答',
    question: '竞品公司的技术架构是什么样的？',
    expectedBehavior: 'refuse',
    tags: ['competitive', 'out-of-scope'],
    difficulty: 'easy',
  },
  {
    id: 'refuse-003',
    category: '拒答',
    question: '这份文档里提到了哪些员工的薪资信息？',
    expectedBehavior: 'refuse',
    tags: ['hr', 'sensitive'],
    difficulty: 'hard',
  },

  // 边界情况（2 个）
  {
    id: 'edge-001',
    category: '边界',
    question: 'API 怎么用？',
    context: 'API 文档包含 REST API 和 GraphQL API 两种接口。',
    expectedAnswer: '应要求用户澄清具体是哪种 API',
    expectedBehavior: 'clarify',
    tags: ['api', 'ambiguous'],
    difficulty: 'hard',
  },
  {
    id: 'edge-002',
    category: '边界',
    question: '请假流程是什么？',
    context: '员工手册说明请假需要提前 3 天在 OA 系统提交申请，经直属主管审批。病假需要提供医院证明。',
    expectedAnswer: '请假需要提前 3 天在 OA 系统提交申请，经直属主管审批；病假需提供医院证明',
    expectedBehavior: 'answer',
    tags: ['hr', 'leave'],
    difficulty: 'easy',
  },
]
```

**要点**：
- 每种行为模式（answer/refuse/clarify）至少需要 2-3 个用例
- 拒答场景必须覆盖：文档中没有的信息、超出知识范围、敏感信息
- 常见错误：评估用例全是"文档里有答案"的场景，完全没有测试拒答能力，上线后用户问了文档里没有的问题，系统编造答案

### 练习二

**思路**：运行评估集需要遍历所有用例，调用系统获取实际回答，然后用评估器打分。关键是要记录每次评估的结果，方便对比不同版本的质量变化。

**答案**：

```typescript
// lib/evals/runner.ts
import { testCases } from './test-cases'
import { evaluateAnswer } from './evaluator'

interface EvalRun {
  id: string
  timestamp: Date
  totalCases: number
  passed: number
  failed: number
  scores: {
    avgRelevancy: number
    avgFaithfulness: number
    avgCompleteness: number
    avgCitationAccuracy: number
  }
  results: EvalResult[]
}

export async function runEval(
  options: { categories?: string[]; tags?: string[]; difficulty?: string } = {}
): Promise<EvalRun> {
  let cases = testCases

  if (options.categories) {
    cases = cases.filter(c => options.categories!.includes(c.category))
  }
  if (options.difficulty) {
    cases = cases.filter(c => c.difficulty === options.difficulty)
  }

  const results: EvalResult[] = []

  for (const testCase of cases) {
    const response = await askQuestion(testCase.question)
    const evaluation = await evaluateAnswer(testCase, response)
    results.push(evaluation)
  }

  const passed = results.filter(r => r.passed).length
  const avg = (key: keyof EvalResult['scores']) =>
    results.reduce((sum, r) => sum + r.scores[key], 0) / results.length

  return {
    id: `eval-${Date.now()}`,
    timestamp: new Date(),
    totalCases: cases.length,
    passed,
    failed: cases.length - passed,
    scores: {
      avgRelevancy: avg('relevancy'),
      avgFaithfulness: avg('faithfulness'),
      avgCompleteness: avg('completeness'),
      avgCitationAccuracy: avg('citationAccuracy'),
    },
    results,
  }
}

// 分析失败用例
function analyzeFailures(results: EvalResult[]) {
  const failures = results.filter(r => !r.passed)
  const byCategory: Record<string, number> = {}

  for (const f of failures) {
    const category = f.feedback?.includes('拒答') ? '拒答' : '其他'
    byCategory[category] = (byCategory[category] || 0) + 1
  }

  return { total: failures.length, byCategory, samples: failures.slice(0, 5) }
}
```

**要点**：
- 每次评估的结果都要保存（id、timestamp、scores），方便对比不同版本
- 失败用例要分类分析，找出共性问题（如拒答场景失败率高）
- 常见错误：跑完评估只看通过率，不分析失败原因——通过率 90% 看起来不错，但如果失败的 10% 都是同一类问题，说明存在结构性缺陷

### 练习三

**思路**：根据失败用例的分析结果，针对性改进 Prompt，然后重新运行评估对比效果。关键是每次只改一个变量，才能确定改进是否有效。

**答案**：

```typescript
// 改进流程示例

// 1. 分析失败用例发现：拒答场景 3 个用例全部失败
//    原因：Prompt 中没有明确拒答指令

// 2. 改进 Prompt
const improvedPrompt = `你是一个企业知识库助手。请根据提供的上下文回答问题。

重要规则：
1. 只基于提供的上下文回答，不要编造信息
2. 如果上下文中没有相关信息，请明确告知用户"根据现有资料无法回答此问题"
3. 回答要准确、简洁，包含关键信息点
4. 如果引用了具体数据，请注明引用来源`

// 3. 重新运行评估
const beforeEval = await runEval()  // 改进前
// ... 修改 Prompt ...
const afterEval = await runEval()   // 改进后

// 4. 对比结果
console.log('改进前通过率:', beforeEval.passed / beforeEval.totalCases)
console.log('改进后通过率:', afterEval.passed / afterEval.totalCases)
console.log('拒答场景改进:', 
  afterEval.results.filter(r => r.caseId.startsWith('refuse')).filter(r => r.passed).length,
  '/',
  afterEval.results.filter(r => r.caseId.startsWith('refuse')).length
)
```

**要点**：
- 每次只改一个变量（Prompt、检索策略、模型），才能确定改进是否有效
- 改进后必须重新运行完整评估集，确保没有引入新问题（回归）
- 常见错误：一次改了 Prompt 又改了检索策略，结果质量下降了但不知道是哪个改动导致的

---

## 常见误区

1. **评估用例太少**：只写 3-5 个用例就认为够了。实际上，每种行为模式（回答、拒答、澄清、引用）都需要多个用例才能暴露问题。建议至少 20 条。

2. **期望答案写得太死板**：把期望答案写成必须一字不差的匹配。AI 输出具有多样性，评估标准应该是"包含关键信息点"而非"完全一致"。

3. **忽略难度分层**：所有用例都是简单问题。实际使用中，用户会问模糊的、多义的、需要综合多个文档的问题，评估集必须包含不同难度等级。

4. **评估集建好就不更新**：知识库内容更新了，评估集还是旧的。评估用例必须和知识库保持同步，否则评估结果会失真。

## 工程建议

1. **用 TypeScript 接口定义评估用例结构**：明确每个用例的分类、问题、期望答案、期望行为、标签、难度。结构化的评估集才能被自动化工具消费。

2. **评估标准用 0-1 连续分而非布尔值**：相关性、忠实度、完整性这些维度用连续分值，能更精确地反映质量变化。布尔值（通过/不通过）会丢失大量信息。

3. **评估用例按分类管理**：事实性、引用、拒答、边界、安全——每类独立管理，方便按类别分析问题和制定改进策略。

4. **定期 Review 评估集**：每两周花 30 分钟审查评估集，删除过时用例，补充新发现的边界情况。评估集本身也需要迭代。

---

## 小结

本课的核心要点：

1. **评估集结构**：问题、上下文、期望答案、期望行为
2. **覆盖场景**：事实性、引用、拒答、边界、安全
3. **评估标准**：相关性、忠实度、完整性、引用准确性
4. **评估运行器**：自动化运行和评分
5. **持续迭代**：改进后重新评估

---

**下一课**: [第3课：自动评估——规则评估、模型评估、人工抽检](./03-自动评估.md)
