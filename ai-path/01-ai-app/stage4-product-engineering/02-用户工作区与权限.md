# 第2课：用户、工作区和知识库权限

> **课程定位**：实现用户系统和权限控制
> **前置知识**：第 1 课的信息架构
> **预计时长**：45 分钟

## 场景引入

你的 AI 知识工作台上线第一天，两个同事同时在用。同事 A 上传了公司的财务报告到知识库，同事 B 随手问了一句"帮我查一下 Q3 的营收数据"——结果系统把同事 A 的财务报告内容返回给了同事 B。这不是 AI 的幻觉，而是你没有做数据隔离。更糟糕的是，一个免费用户一口气发了 500 条消息，把你的 API 额度打爆了，账单直接飙到几百美元。用户认证、数据隔离、权限控制、配额限制——这些在传统应用里就该做的安全基础，在 AI 产品里因为按量计费的成本模型而变得更加紧迫。

---

## 学习目标

完成本课学习后，你将能够：

1. 实现用户认证（登录/注册）
2. 设计工作区隔离
3. 实现知识库权限控制
4. 设计配额限制

---

## 一、用户认证

### 1.1 使用 NextAuth.js

```typescript
// app/api/auth/[...nextauth]/route.ts
import NextAuth from 'next-auth'
import GithubProvider from 'next-auth/providers/github'
import GoogleProvider from 'next-auth/providers/google'
import { PrismaAdapter } from '@auth/prisma-adapter'
import { prisma } from '@/lib/prisma'

export const authOptions = {
  adapter: PrismaAdapter(prisma),
  providers: [
    GithubProvider({
      clientId: process.env.GITHUB_ID!,
      clientSecret: process.env.GITHUB_SECRET!,
    }),
    GoogleProvider({
      clientId: process.env.GOOGLE_ID!,
      clientSecret: process.env.GOOGLE_SECRET!,
    }),
  ],
  callbacks: {
    async session({ session, user }) {
      session.user.id = user.id
      return session
    },
  },
}

export default NextAuth(authOptions)
```

### 1.2 获取当前用户

```typescript
// lib/auth.ts
import { getServerSession } from 'next-auth'
import { authOptions } from '@/app/api/auth/[...nextauth]/route'

export async function getCurrentUser() {
  const session = await getServerSession(authOptions)
  return session?.user
}

// 在 API Route 中使用
export async function GET(req: Request) {
  const user = await getCurrentUser()
  if (!user) {
    return Response.json({ error: '未登录' }, { status: 401 })
  }
  // ...
}
```

---

## 二、工作区隔离

### 2.1 工作区概念

```
┌─────────────────────────────────────────────────────────────────┐
│                    工作区隔离                                     │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  用户 A 的工作区                                                 │
│  ├── 对话 1                                                     │
│  ├── 对话 2                                                     │
│  ├── 知识库 1                                                   │
│  └── 知识库 2                                                   │
│                                                                 │
│  用户 B 的工作区                                                 │
│  ├── 对话 1                                                     │
│  ├── 对话 2                                                     │
│  └── 知识库 1                                                   │
│                                                                 │
│  用户 A 无法看到用户 B 的数据                                    │
│  用户 B 无法看到用户 A 的数据                                    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 数据隔离实现

```typescript
// lib/data-access.ts
import { prisma } from './prisma'
import { getCurrentUser } from './auth'

// 获取用户的对话列表
export async function getUserConversations() {
  const user = await getCurrentUser()
  if (!user) throw new Error('未登录')

  return prisma.conversation.findMany({
    where: { userId: user.id },
    orderBy: { updatedAt: 'desc' },
  })
}

// 获取用户的知识库
export async function getUserKnowledgeBases() {
  const user = await getCurrentUser()
  if (!user) throw new Error('未登录')

  return prisma.knowledgeBase.findMany({
    where: { userId: user.id },
    orderBy: { createdAt: 'desc' },
  })
}

// 检查用户是否有权限访问某个对话
export async function canAccessConversation(
  userId: string,
  conversationId: string
): Promise<boolean> {
  const conversation = await prisma.conversation.findUnique({
    where: { id: conversationId },
  })

  return conversation?.userId === userId
}
```

### 2.3 中间件权限检查

```typescript
// lib/middleware/auth.ts
import { NextRequest, NextResponse } from 'next/server'
import { getCurrentUser } from '@/lib/auth'

export function withAuth(handler: Function) {
  return async (req: NextRequest, ...args: any[]) => {
    const user = await getCurrentUser()

    if (!user) {
      return NextResponse.json(
        { error: '请先登录' },
        { status: 401 }
      )
    }

    return handler(req, { user }, ...args)
  }
}

// 使用
export const GET = withAuth(async (req: NextRequest, { user }) => {
  const conversations = await prisma.conversation.findMany({
    where: { userId: user.id },
  })
  return NextResponse.json(conversations)
})
```

---

## 三、知识库权限

### 3.1 权限级别

```typescript
enum PermissionLevel {
  OWNER = 'owner',     // 所有者：完全控制
  EDITOR = 'editor',   // 编辑者：可以修改内容
  VIEWER = 'viewer',   // 查看者：只能查看
}

interface KnowledgeBasePermission {
  id: string
  knowledgeBaseId: string
  userId: string
  level: PermissionLevel
  createdAt: Date
}
```

### 3.2 权限检查

```typescript
// lib/permissions.ts
import { prisma } from './prisma'

export async function checkKnowledgeBasePermission(
  userId: string,
  knowledgeBaseId: string,
  requiredLevel: PermissionLevel
): Promise<boolean> {
  // 检查是否是所有者
  const knowledgeBase = await prisma.knowledgeBase.findUnique({
    where: { id: knowledgeBaseId },
  })

  if (!knowledgeBase) return false
  if (knowledgeBase.userId === userId) return true  // 所有者

  // 检查是否有权限
  const permission = await prisma.knowledgeBasePermission.findUnique({
    where: {
      knowledgeBaseId_userId: {
        knowledgeBaseId,
        userId,
      },
    },
  })

  if (!permission) return false

  // 检查权限级别
  const levelHierarchy = {
    [PermissionLevel.OWNER]: 3,
    [PermissionLevel.EDITOR]: 2,
    [PermissionLevel.VIEWER]: 1,
  }

  return levelHierarchy[permission.level] >= levelHierarchy[requiredLevel]
}

// 中间件
export function withKnowledgeBasePermission(requiredLevel: PermissionLevel) {
  return (handler: Function) => {
    return async (req: NextRequest, context: any) => {
      const user = await getCurrentUser()
      if (!user) {
        return NextResponse.json({ error: '未登录' }, { status: 401 })
      }

      const knowledgeBaseId = context.params?.id
      if (!knowledgeBaseId) {
        return NextResponse.json({ error: '缺少知识库 ID' }, { status: 400 })
      }

      const hasPermission = await checkKnowledgeBasePermission(
        user.id,
        knowledgeBaseId,
        requiredLevel
      )

      if (!hasPermission) {
        return NextResponse.json({ error: '没有权限' }, { status: 403 })
      }

      return handler(req, context)
    }
  }
}

// 使用
export const DELETE = withKnowledgeBasePermission(PermissionLevel.OWNER)(
  async (req: NextRequest, context: any) => {
    // 删除知识库逻辑
  }
)
```

---

## 四、配额限制

### 4.1 配额设计

```typescript
interface UserQuota {
  userId: string
  plan: 'free' | 'pro' | 'enterprise'
  limits: {
    conversationsPerMonth: number    // 每月对话数
    messagesPerDay: number           // 每日消息数
    tokensPerMonth: number           // 每月 token 数
    knowledgeBases: number           // 知识库数量
    documentsPerBase: number         // 每个知识库的文档数
    storageMB: number                // 存储空间（MB）
  }
  usage: {
    conversationsThisMonth: number
    messagesToday: number
    tokensThisMonth: number
    knowledgeBasesCount: number
    storageUsedMB: number
  }
}

const PLAN_LIMITS: Record<string, UserQuota['limits']> = {
  free: {
    conversationsPerMonth: 50,
    messagesPerDay: 100,
    tokensPerMonth: 100000,
    knowledgeBases: 3,
    documentsPerBase: 10,
    storageMB: 100,
  },
  pro: {
    conversationsPerMonth: 500,
    messagesPerDay: 1000,
    tokensPerMonth: 1000000,
    knowledgeBases: 20,
    documentsPerBase: 100,
    storageMB: 1000,
  },
  enterprise: {
    conversationsPerMonth: -1,  // 无限制
    messagesPerDay: -1,
    tokensPerMonth: -1,
    knowledgeBases: -1,
    documentsPerBase: -1,
    storageMB: -1,
  },
}
```

### 4.2 配额检查

```typescript
// lib/quota.ts
import { prisma } from './prisma'

export async function checkQuota(
  userId: string,
  action: 'message' | 'conversation' | 'knowledge_base' | 'document'
): Promise<{ allowed: boolean; reason?: string }> {
  const user = await prisma.user.findUnique({
    where: { id: userId },
    include: { usage: true },
  })

  if (!user) return { allowed: false, reason: '用户不存在' }

  const limits = PLAN_LIMITS[user.plan]
  const usage = user.usage

  switch (action) {
    case 'message':
      if (limits.messagesPerDay !== -1 && usage.messagesToday >= limits.messagesPerDay) {
        return { allowed: false, reason: '今日消息数已达上限' }
      }
      if (limits.tokensPerMonth !== -1 && usage.tokensThisMonth >= limits.tokensPerMonth) {
        return { allowed: false, reason: '本月 token 数已达上限' }
      }
      break

    case 'conversation':
      if (limits.conversationsPerMonth !== -1 && usage.conversationsThisMonth >= limits.conversationsPerMonth) {
        return { allowed: false, reason: '本月对话数已达上限' }
      }
      break

    case 'knowledge_base':
      if (limits.knowledgeBases !== -1 && usage.knowledgeBasesCount >= limits.knowledgeBases) {
        return { allowed: false, reason: '知识库数量已达上限' }
      }
      break
  }

  return { allowed: true }
}
```

### 4.3 配额中间件

```typescript
export function withQuota(action: string) {
  return (handler: Function) => {
    return async (req: NextRequest, context: any) => {
      const user = await getCurrentUser()
      if (!user) {
        return NextResponse.json({ error: '未登录' }, { status: 401 })
      }

      const quotaCheck = await checkQuota(user.id, action as any)
      if (!quotaCheck.allowed) {
        return NextResponse.json(
          { error: quotaCheck.reason },
          { status: 429 }
        )
      }

      return handler(req, context)
    }
  }
}
```

---

## 五、前端权限控制

```typescript
// components/QuotaWarning.tsx
'use client'

interface Props {
  plan: string
  usage: {
    tokensThisMonth: number
    tokenLimit: number
  }
}

export function QuotaWarning({ plan, usage }: Props) {
  const percentage = (usage.tokensThisMonth / usage.tokenLimit) * 100

  if (percentage < 80) return null

  return (
    <div className="quota-warning">
      {percentage >= 100 ? (
        <p>您本月的 token 配额已用完，请升级套餐或等待下月重置。</p>
      ) : (
        <p>您本月的 token 配额已使用 {percentage.toFixed(0)}%，请注意控制用量。</p>
      )}
      {plan === 'free' && (
        <a href="/settings/billing">升级到 Pro</a>
      )}
    </div>
  )
}
```

---

## 动手练习

### 练习一：实现用户认证

1. 配置 NextAuth.js
2. 实现 GitHub 登录
3. 获取当前用户信息

### 练习二：实现数据隔离

1. 确保用户只能访问自己的对话
2. 确保用户只能访问自己的知识库
3. 测试跨用户访问

### 练习三：实现配额限制

1. 设计配额规则
2. 实现配额检查
3. 展示配额警告

---

## 参考答案

### 练习一

**思路**：使用 NextAuth.js 实现用户认证，配置 GitHub OAuth Provider，通过 PrismaAdapter 将用户信息持久化到数据库。关键是在 session callback 中注入用户 ID，方便后续鉴权使用。

**答案**：

```typescript
// app/api/auth/[...nextauth]/route.ts
import NextAuth from 'next-auth'
import GithubProvider from 'next-auth/providers/github'
import { PrismaAdapter } from '@auth/prisma-adapter'
import { prisma } from '@/lib/prisma'

export const authOptions = {
  adapter: PrismaAdapter(prisma),
  providers: [
    GithubProvider({
      clientId: process.env.GITHUB_ID!,
      clientSecret: process.env.GITHUB_SECRET!,
    }),
  ],
  callbacks: {
    async session({ session, user }) {
      session.user.id = user.id
      return session
    },
  },
}

export default NextAuth(authOptions)

// lib/auth.ts
import { getServerSession } from 'next-auth'
import { authOptions } from '@/app/api/auth/[...nextauth]/route'

export async function getCurrentUser() {
  const session = await getServerSession(authOptions)
  return session?.user
}

// 在 API Route 中使用
// app/api/conversations/route.ts
import { getCurrentUser } from '@/lib/auth'

export async function GET(req: Request) {
  const user = await getCurrentUser()
  if (!user) {
    return Response.json({ error: '未登录' }, { status: 401 })
  }
  // 后续业务逻辑...
}
```

**要点**：
- `getCurrentUser()` 必须在服务端调用（API Route 或 Server Component），不能在客户端调用
- session callback 中必须注入 `user.id`，否则默认 session 里只有 name 和 email
- 常见错误：只在前端用 `useSession()` 做登录判断，后端 API 不校验 session，攻击者可以直接调用 API 绕过前端限制

### 练习二

**思路**：数据隔离的核心是所有数据库查询都带 `userId` 过滤条件。不能依赖应用层"记得加"，应该用中间件或 Prisma middleware 自动注入。同时需要实现权限检查函数，防止用户通过 API 直接访问他人数据。

**答案**：

```typescript
// lib/data-access.ts
import { prisma } from './prisma'
import { getCurrentUser } from './auth'

// 获取用户的对话列表（强制 userId 过滤）
export async function getUserConversations() {
  const user = await getCurrentUser()
  if (!user) throw new Error('未登录')

  return prisma.conversation.findMany({
    where: { userId: user.id },
    orderBy: { updatedAt: 'desc' },
  })
}

// 获取用户的知识库（强制 userId 过滤）
export async function getUserKnowledgeBases() {
  const user = await getCurrentUser()
  if (!user) throw new Error('未登录')

  return prisma.knowledgeBase.findMany({
    where: { userId: user.id },
    orderBy: { createdAt: 'desc' },
  })
}

// 检查用户是否有权限访问某个对话
export async function canAccessConversation(
  userId: string,
  conversationId: string
): Promise<boolean> {
  const conversation = await prisma.conversation.findUnique({
    where: { id: conversationId },
  })
  return conversation?.userId === userId
}

// 中间件：统一鉴权
import { NextRequest, NextResponse } from 'next/server'

export function withAuth(handler: Function) {
  return async (req: NextRequest, ...args: any[]) => {
    const user = await getCurrentUser()
    if (!user) {
      return NextResponse.json({ error: '请先登录' }, { status: 401 })
    }
    return handler(req, { user }, ...args)
  }
}

// 测试跨用户访问：用用户 A 的 session 请求用户 B 的对话
// 应返回 403，而非返回数据
export const GET = withAuth(async (req: NextRequest, { user }) => {
  const conversations = await prisma.conversation.findMany({
    where: { userId: user.id },
  })
  return NextResponse.json(conversations)
})
```

**要点**：
- 所有查询必须带 `where: { userId: user.id }`，不能省略
- 建议用 Prisma middleware 自动注入 userId 条件，从根源杜绝遗漏
- 常见错误：前端隐藏了"删除"按钮就认为安全了，但后端 API 没有校验 userId，攻击者可以直接调用 API 删除他人数据

### 练习三

**思路**：配额限制需要设计套餐层级（free/pro/enterprise），在 API 层做配额检查，返回 429 状态码。关键点是配额检查和扣减要用原子操作，避免并发窗口导致超额使用。

**答案**：

```typescript
// lib/quota.ts
import { prisma } from './prisma'

const PLAN_LIMITS = {
  free: {
    conversationsPerMonth: 50,
    messagesPerDay: 100,
    tokensPerMonth: 100000,
    knowledgeBases: 3,
  },
  pro: {
    conversationsPerMonth: 500,
    messagesPerDay: 1000,
    tokensPerMonth: 1000000,
    knowledgeBases: 20,
  },
  enterprise: {
    conversationsPerMonth: -1,
    messagesPerDay: -1,
    tokensPerMonth: -1,
    knowledgeBases: -1,
  },
}

export async function checkQuota(
  userId: string,
  action: 'message' | 'conversation' | 'knowledge_base'
): Promise<{ allowed: boolean; reason?: string }> {
  const user = await prisma.user.findUnique({
    where: { id: userId },
    include: { usage: true },
  })

  if (!user) return { allowed: false, reason: '用户不存在' }

  const limits = PLAN_LIMITS[user.plan]
  const usage = user.usage

  switch (action) {
    case 'message':
      if (limits.messagesPerDay !== -1 && usage.messagesToday >= limits.messagesPerDay) {
        return { allowed: false, reason: '今日消息数已达上限' }
      }
      if (limits.tokensPerMonth !== -1 && usage.tokensThisMonth >= limits.tokensPerMonth) {
        return { allowed: false, reason: '本月 token 数已达上限' }
      }
      break
    case 'conversation':
      if (limits.conversationsPerMonth !== -1 && usage.conversationsThisMonth >= limits.conversationsPerMonth) {
        return { allowed: false, reason: '本月对话数已达上限' }
      }
      break
    case 'knowledge_base':
      if (limits.knowledgeBases !== -1 && usage.knowledgeBasesCount >= limits.knowledgeBases) {
        return { allowed: false, reason: '知识库数量已达上限' }
      }
      break
  }

  return { allowed: true }
}

// 配额警告组件
// components/QuotaWarning.tsx
'use client'

export function QuotaWarning({ plan, usage }: {
  plan: string
  usage: { tokensThisMonth: number; tokenLimit: number }
}) {
  const percentage = (usage.tokensThisMonth / usage.tokenLimit) * 100
  if (percentage < 80) return null

  return (
    <div className="quota-warning">
      {percentage >= 100 ? (
        <p>您本月的 token 配额已用完，请升级套餐或等待下月重置。</p>
      ) : (
        <p>您本月的 token 配额已使用 {percentage.toFixed(0)}%，请注意控制用量。</p>
      )}
      {plan === 'free' && <a href="/settings/billing">升级到 Pro</a>}
    </div>
  )
}
```

**要点**：
- 配额检查要在服务端做，不能只在前端做——前端可以被绕过
- 配额扣减要用数据库原子操作（`increment`），避免先检查再扣减之间的并发窗口
- 常见错误：配额检查和扣减分成两步，高并发下可能超额使用；或者配额快到上限时不提醒用户，等用户发消息被拒绝才告知

---

## 常见误区

1. **只在前端做权限校验**：前端隐藏了"删除"按钮，但后端 API 没有检查 `userId`，任何人都能通过直接调用 API 删除别人的知识库。权限检查必须在后端完成，前端只是体验优化。
2. **用 `session.user.id` 直接查数据库而不校验**：NextAuth 返回的 session 里有用户 ID，但如果不在每个 API Route 里调用 `getCurrentUser()` 做服务端校验，攻击者可以伪造 session。
3. **配额检查和业务操作不是原子的**：先检查配额再扣减，两步之间存在并发窗口，可能导致超额使用。应该用数据库的原子操作（如 `increment`）配合条件更新来保证一致性。
4. **知识库共享用 URL 参数传权限**：把 `?kbId=xxx&level=editor` 放在 URL 里，权限信息会出现在浏览器历史、服务器日志、Referer 头里，造成信息泄露。权限应该存在数据库的权限表中。

---

## 工程建议

1. **用中间件统一鉴权**：写一个 `withAuth` 高阶函数包裹所有 API Route，避免每个接口重复写 `getCurrentUser()` + 未登录判断。权限检查同理，用 `withPermission(PermissionLevel.EDITOR)` 这样的装饰器模式。
2. **数据库层面强制隔离**：所有查询都带 `WHERE userId = ?` 条件，不要依赖应用层逻辑"记得加"。可以考虑用 Prisma 的 middleware 自动注入 userId 过滤条件，从根源杜绝遗漏。
3. **配额用 Redis 做计数器**：数据库的 `UPDATE SET tokenUsed = tokenUsed + N` 在高并发下会锁行。用 Redis 的 `INCRBY` 做实时计数，定期同步回数据库，既保证性能又保证最终一致性。
4. **配额快到上限时提前通知**：不要等用户发消息被拒绝才告知，在用量达到 80% 时就通过前端 Banner 提醒，给用户升级套餐或等待重置的缓冲时间。

---

## 小结

本课的核心要点：

1. **用户认证**：使用 NextAuth.js 实现登录/注册
2. **工作区隔离**：每个用户只能访问自己的数据
3. **知识库权限**：owner、editor、viewer 三级权限
4. **配额限制**：按套餐限制消息数、token 数、知识库数
5. **前端控制**：展示配额警告，限制操作

---

**下一课**: [第3课：会话管理——历史、收藏、归档、搜索](./03-会话管理.md)
