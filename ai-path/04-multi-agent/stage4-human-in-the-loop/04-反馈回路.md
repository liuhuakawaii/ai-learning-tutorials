# 04 反馈回路——人类反馈如何影响 Agent 后续行为

> 人类的反馈不只是"批准/拒绝"——它应该影响 Agent 未来的行为。

## 场景引入

用户每次都拒绝 Agent 生成的长篇报告，要求改成简洁风格。但 Agent 下次还是生成长篇报告——因为人类的反馈没有被"记住"和"学习"。反馈回路让人类的偏好、修改和评价真正影响 Agent 的后续行为，而不是被丢进垃圾桶。

---

## 学习目标

- 掌握反馈回路的设计方法
- 理解反馈如何影响 Agent 行为
- 学会实现自适应的 Agent 系统

---

## 一、反馈类型

```
反馈类型：

1. 显式反馈
   - 点赞/点踩
   - 评分
   - 文字评论

2. 隐式反馈
   - 用户是否采纳了建议
   - 用户是否重新提问
   - 用户是否修改了结果

3. 结果反馈
   - 操作是否成功
   - 结果是否符合预期
   - 是否产生了问题
```

---

## 二、反馈收集

```python
class FeedbackCollector:
    """反馈收集器"""
    
    def __init__(self, memory: LongTermMemory):
        self.memory = memory
        self.feedback_data = []
    
    def collect_feedback(self, task_id: str, feedback: dict):
        """收集反馈"""
        feedback_entry = {
            "task_id": task_id,
            "feedback": feedback,
            "timestamp": datetime.now().isoformat()
        }
        
        self.feedback_data.append(feedback_entry)
        
        # 存储到长期记忆
        self.memory.store(
            f"任务 {task_id} 的反馈：{json.dumps(feedback, ensure_ascii=False)}",
            {"type": "feedback", "task_id": task_id}
        )
    
    def get_feedback_summary(self, agent_name: str = None) -> dict:
        """获取反馈摘要"""
        relevant_feedback = self.feedback_data
        if agent_name:
            relevant_feedback = [f for f in relevant_feedback if f.get("agent") == agent_name]
        
        positive = sum(1 for f in relevant_feedback if f["feedback"].get("rating", 0) > 3)
        negative = sum(1 for f in relevant_feedback if f["feedback"].get("rating", 0) < 3)
        
        return {
            "total": len(relevant_feedback),
            "positive": positive,
            "negative": negative,
            "satisfaction_rate": positive / len(relevant_feedback) if relevant_feedback else 0
        }
```

---

## 三、反馈驱动优化

```python
class FeedbackDrivenOptimizer:
    """反馈驱动优化器"""
    
    def __init__(self, llm, memory: LongTermMemory):
        self.llm = llm
        self.memory = memory
    
    def optimize_based_on_feedback(self, agent_name: str, current_prompt: str) -> str:
        """基于反馈优化"""
        
        # 检索相关反馈
        feedback = self.memory.retrieve(f"{agent_name} 反馈", top_k=10)
        
        # 分析反馈
        feedback_text = "\n".join([f["content"] for f in feedback])
        
        prompt = f"""基于以下用户反馈，优化 Agent 的提示词。

当前提示词：
{current_prompt}

用户反馈：
{feedback_text}

请生成优化后的提示词。"""
        
        response = self.llm.invoke(prompt)
        return response.content
```

---

## 四、自适应行为

```python
class AdaptiveAgent:
    """自适应 Agent"""
    
    def __init__(self, name: str, llm, feedback_collector: FeedbackCollector):
        self.name = name
        self.llm = llm
        self.feedback = feedback_collector
        self.base_prompt = "你是..."
        self.current_prompt = self.base_prompt
    
    def execute(self, task: str) -> str:
        # 检查反馈，决定是否调整行为
        summary = self.feedback.get_feedback_summary(self.name)
        
        if summary["satisfaction_rate"] < 0.7:
            # 满意度低，调整行为
            self.current_prompt = self._adapt_prompt()
        
        # 执行任务
        response = self.llm.invoke(f"{self.current_prompt}\n\n任务：{task}")
        return response.content
    
    def _adapt_prompt(self) -> str:
        """根据反馈调整提示词"""
        optimizer = FeedbackDrivenOptimizer(self.llm, self.feedback.memory)
        return optimizer.optimize_based_on_feedback(self.name, self.current_prompt)
```

---

## 五、反馈闭环

```
反馈闭环流程：

Agent 执行 → 用户反馈 → 收集反馈 → 分析反馈 → 优化 Agent → Agent 执行
    │                                              │
    └──────────────── 循环 ────────────────────────┘
```

---


---

## 常见误区

1. **只收集显式反馈，忽略隐式反馈**：用户没有点赞/点踩，但悄悄修改了 Agent 的输出——这就是重要的隐式反馈。应该同时收集显式和隐式反馈。
2. **反馈没有和具体 Agent 关联**：只知道"用户对这次结果不满意"，但不知道是哪个 Agent 的输出有问题。反馈应该关联到具体的 Agent 和具体的输出。
3. **反馈优化过于激进**：收到几条负面反馈就大幅修改 Prompt，导致 Agent 在其他场景的表现下降。反馈优化应该是渐进式的，基于大量反馈数据的统计趋势。

---

## 工程建议

1. **从单 Agent 开始，按需演进**：先用单 Agent 验证核心逻辑，当遇到上下文瓶颈、能力稀释或需要并行处理时，再拆分为多 Agent。不要为了"看起来高级"而引入多 Agent 架构。
2. **为每个 Agent 定义清晰的职责边界**：每个 Agent 应该有单一、明确的职责（如"只负责搜索""只负责分析"），输入输出格式在设计阶段就确定下来，避免职责重叠和数据格式混乱。
3. **建立可观测性基础设施**：从第一版开始就为每个 Agent 添加结构化日志和追踪机制，记录输入、输出、耗时、错误。多 Agent 系统的调试难度远高于单 Agent，没有日志就是在"盲人摸象"。
4. **在关键决策节点加入人工审批**：涉及高风险操作（删除数据、发送消息、支付）和不可逆操作时，使用 Human-in-the-loop 机制暂停执行，等待人类确认后再继续。

---

## 小结

```
本课核心要点：

1. 反馈类型：显式、隐式、结果
2. 反馈收集存储到长期记忆
3. 反馈驱动优化：分析反馈 → 优化 Prompt
4. 自适应 Agent 根据反馈调整行为

---

**下一课**: [审批面板开发——构建一个可视化的审批 UI](./05-审批面板开发.md)
```

---

## 练习

1. **收集题**：实现一个反馈收集器。

2. **优化题**：基于反馈优化 Agent 的提示词。

3. **自适应题**：实现一个自适应 Agent。
