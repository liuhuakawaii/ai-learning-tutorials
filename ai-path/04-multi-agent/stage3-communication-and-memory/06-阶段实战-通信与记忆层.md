# 06 阶段实战——为多 Agent 系统实现完整的通信与记忆层

> 把前 5 课学到的通信和记忆知识整合成一个完整的系统。

## 场景引入

你已经分别学习了消息传递、共享黑板、短期记忆、长期记忆和冲突解决。但它们在实际系统中是如何协同工作的？这个实战项目会带你搭建一个完整的通信与记忆层，让多个 Agent 能真正高效地协作。

---

## 学习目标

- 搭建完整的通信与记忆层
- 集成消息传递、共享黑板、长期记忆
- 输出一个可运行的多 Agent 协作系统

---

## 一、系统架构

```
通信与记忆层架构：

┌─────────────────────────────────────────────────────────────┐
│                    通信层                                    │
│  ├── 消息代理（MessageBroker）                               │
│  ├── 共享黑板（Blackboard）                                  │
│  └── 工作空间（Workspace）                                   │
├─────────────────────────────────────────────────────────────┤
│                    记忆层                                    │
│  ├── 短期记忆（ShortTermMemory）                             │
│  ├── 长期记忆（LongTermMemory）                              │
│  └── 记忆整合（MemoryConsolidator）                          │
├─────────────────────────────────────────────────────────────┤
│                    Agent 层                                  │
│  ├── Agent A                                                 │
│  ├── Agent B                                                 │
│  └── Agent C                                                 │
└─────────────────────────────────────────────────────────────┘
```

---

## 二、核心实现

```python
class MultiAgentCommunicationLayer:
    """多 Agent 通信层"""
    
    def __init__(self, llm):
        self.llm = llm
        self.message_broker = MessageBroker()
        self.blackboard = Blackboard()
        self.workspace = Workspace("./workspace")
        self.short_term_memory = ShortTermMemory()
        self.long_term_memory = LongTermMemory()
        self.conflict_resolver = ConflictResolver(llm)
    
    def send_message(self, from_agent: str, to_agent: str, content: str):
        """发送消息"""
        self.message_broker.send(to_agent, {
            "from": from_agent,
            "content": content,
            "timestamp": datetime.now().isoformat()
        })
    
    def write_to_blackboard(self, key: str, value: any, author: str):
        """写入黑板"""
        self.blackboard.write(key, value, author)
    
    def store_memory(self, content: str, metadata: dict = None):
        """存储长期记忆"""
        self.long_term_memory.store(content, metadata)
    
    def retrieve_memory(self, query: str, top_k: int = 5) -> list:
        """检索记忆"""
        return self.long_term_memory.retrieve(query, top_k)
    
    def resolve_conflict(self, task: str, results: list) -> dict:
        """解决冲突"""
        return self.conflict_resolver.resolve_by_llm(task, results)
```

---

## 三、Agent 集成

```class CommunicatingAgent:
    """支持通信的 Agent"""
    
    def __init__(self, name: str, llm, comm_layer: MultiAgentCommunicationLayer):
        self.name = name
        self.llm = llm
        self.comm = comm_layer
    
    def execute(self, task: str) -> str:
        # 检索相关记忆
        memories = self.comm.retrieve_memory(task)
        
        # 读取黑板数据
        context = self.comm.blackboard.read("context")
        
        # 执行任务
        prompt = f"""你是 {self.name}。

任务：{task}

相关记忆：{json.dumps(memories, ensure_ascii=False)}
上下文：{context}

请完成任务。"""
        
        response = self.llm.invoke(prompt)
        result = response.content
        
        # 存储记忆
        self.comm.store_memory(f"任务：{task}\n结果：{result}", {"agent": self.name})
        
        # 写入黑板
        self.comm.write_to_blackboard(f"{self.name}_result", result, self.name)
        
        return result
```

---

## 四、运行示例

```python
def main():
    llm = ChatOpenAI(model="gpt-4o")
    comm = MultiAgentCommunicationLayer(llm)
    
    agents = {
        "researcher": CommunicatingAgent("researcher", llm, comm),
        "analyst": CommunicatingAgent("analyst", llm, comm),
        "writer": CommunicatingAgent("writer", llm, comm)
    }
    
    # 执行任务
    task = "分析 2024 年 AI 行业趋势"
    
    research = agents["researcher"].execute(task)
    analysis = agents["analyst"].execute(f"分析：{research}")
    report = agents["writer"].execute(f"撰写报告：{analysis}")
    
    print(report)

if __name__ == "__main__":
    main()
```

---


---

## 常见误区

1. **通信层和记忆层耦合过紧**：通信层负责数据传递，记忆层负责知识持久化，两者应该独立设计。如果通信层直接调用记忆层的存储接口，会导致循环依赖。
2. **没有统一的 Agent 生命周期管理**：Agent 的创建、初始化、执行、销毁没有标准化流程，导致资源泄漏（如数据库连接未关闭、消息队列未注销）。
3. **忽略跨 Agent 的事务一致性**：Agent A 写入了黑板，Agent B 读取后执行了操作，但 Agent A 的写入后来被回滚了。需要考虑分布式事务或最终一致性。

---

## 工程建议

1. **从单 Agent 开始，按需演进**：先用单 Agent 验证核心逻辑，当遇到上下文瓶颈、能力稀释或需要并行处理时，再拆分为多 Agent。不要为了"看起来高级"而引入多 Agent 架构。
2. **为每个 Agent 定义清晰的职责边界**：每个 Agent 应该有单一、明确的职责（如"只负责搜索""只负责分析"），输入输出格式在设计阶段就确定下来，避免职责重叠和数据格式混乱。
3. **建立可观测性基础设施**：从第一版开始就为每个 Agent 添加结构化日志和追踪机制，记录输入、输出、耗时、错误。多 Agent 系统的调试难度远高于单 Agent，没有日志就是在"盲人摸象"。
4. **在关键决策节点加入人工审批**：涉及高风险操作（删除数据、发送消息、支付）和不可逆操作时，使用 Human-in-the-loop 机制暂停执行，等待人类确认后再继续。

---

## 小结

```
本课核心要点：

1. 通信层：消息代理、共享黑板、工作空间
2. 记忆层：短期记忆、长期记忆、记忆整合
3. Agent 通过通信层协作，通过记忆层学习
4. 冲突解决确保结果一致性

阶段总结：
  你已经掌握了多 Agent 系统的通信与记忆机制。
  下一阶段，我们将学习人机协作。
```

---

## 作业

1. **完成实战**：搭建完整的通信与记忆层。

2. **协作题**：实现两个 Agent 通过通信层协作完成任务。

3. **记忆题**：实现记忆整合功能，从对话中提取关键信息。
