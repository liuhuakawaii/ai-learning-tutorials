# 第 2 课：第一次微调——用 LoRA 微调 Qwen2.5-7B 的完整流程

> **课程定位**：动手完成第一次 LoRA 微调，理解从数据到模型的完整链路
> **前置知识**：第 2 阶段的 LoRA 原理和数据准备
> **预计时长**：60 分钟（不含训练时间）

---

## 场景引入

环境搭好了，LoRA 原理也理解了，数据也准备好了，但真正动手写训练代码时，你发现事情没那么简单：模型加载报错、数据格式对不上、训练 Loss 一直不下降、显存不够用……每一个小问题都可能让你卡半天。你需要一个完整的、带详细注释的微调代码模板，让你能跑通第一次 LoRA 微调，理解每一步在做什么。

---

## 学习目标

完成本课学习后，你将能够：

1. 独立完成一次 LoRA 微调的全流程
2. 理解每一步在做什么以及为什么
3. 读懂训练日志并判断训练状态
4. 解决训练中的常见问题

---

## 一、环境检查

```bash
# 检查 GPU 是否可用
python -c "import torch; print(f'CUDA: {torch.cuda.is_available()}'); print(f'GPU: {torch.cuda.get_device_name(0) if torch.cuda.is_available() else \"N/A\"}')"

# 检查显存
nvidia-smi

# 安装依赖
pip install torch transformers peft trl datasets accelerate bitsandbytes
```

```
显存需求估算（Qwen2.5-7B + LoRA r=16）：

  模型权重（float16）：~14 GB
  LoRA 参数：~130 MB
  优化器状态：~260 MB
  激活值缓存：~2-4 GB（取决于 batch_size）
  ─────────────────────────
  总计：~17-19 GB

  显存不足的解决方案：
  - 减小 batch_size（4 → 2 → 1）
  - 使用 gradient_checkpointing
  - 使用 QLoRA（4-bit 量化 + LoRA）
  - 使用更小的模型（3B 代替 7B）
```

---

## 二、完整代码（带详细注释）

```python
"""
第一次 LoRA 微调：用 Qwen2.5-7B 微调法律问答模型。

运行前确保：
1. 有可用的 GPU（至少 12GB 显存，推荐 16GB+）
2. 已安装所有依赖
3. 已准备好训练数据（training_data.json）
"""
import torch
from transformers import AutoModelForCausalLM, AutoTokenizer, TrainingArguments
from peft import LoraConfig, get_peft_model
from trl import SFTTrainer
from datasets import load_dataset

# ============================================================
# 第一步：加载模型和分词器
# ============================================================
model_name = "Qwen/Qwen2.5-7B"

print(f"加载模型: {model_name}")
tokenizer = AutoTokenizer.from_pretrained(model_name)
model = AutoModelForCausalLM.from_pretrained(
    model_name,
    torch_dtype=torch.float16,   # 用 float16 节省显存
    device_map="auto",           # 自动分配到 GPU
    trust_remote_code=True,      # Qwen 需要这个参数
)

# 确保有 pad_token
if tokenizer.pad_token is None:
    tokenizer.pad_token = tokenizer.eos_token

print(f"模型加载完成，参数量: {model.num_parameters():,}")

# ============================================================
# 第二步：配置 LoRA
# ============================================================
lora_config = LoraConfig(
    r=16,                                    # 秩：表达能力和参数量的平衡
    lora_alpha=32,                           # 缩放因子：通常 = 2 * r
    target_modules=[                         # 目标模块：注意力层
        "q_proj", "v_proj", "k_proj", "o_proj"
    ],
    lora_dropout=0.05,                       # Dropout：防过拟合
    bias="none",                             # 不训练偏置
    task_type="CAUSAL_LM",                   # 任务类型
)

# 应用 LoRA
model = get_peft_model(model, lora_config)

# 打印可训练参数量（这是你真正要训练的部分）
model.print_trainable_parameters()
# 输出示例:
# trainable params: 13,107,200 || all params: 7,614,939,136 || trainable%: 0.1721

# ============================================================
# 第三步：加载数据集
# ============================================================
print("加载训练数据...")
dataset = load_dataset("json", data_files="training_data.json")

# 查看数据样例
print(f"训练样本数: {len(dataset['train'])}")
print(f"数据样例: {dataset['train'][0]}")

# ============================================================
# 第四步：配置训练参数
# ============================================================
training_args = TrainingArguments(
    output_dir="./output",                   # 输出目录
    num_train_epochs=3,                      # 训练轮数
    per_device_train_batch_size=4,           # 每 GPU 批次大小
    gradient_accumulation_steps=4,           # 梯度累积步数
                                           #  effective batch_size = 4 * 4 = 16
    learning_rate=2e-4,                      # 学习率（LoRA 推荐 1e-4 ~ 3e-4）
    warmup_ratio=0.1,                        # 预热比例
    weight_decay=0.01,                       # 权重衰减
    fp16=True,                               # 混合精度训练
    logging_steps=10,                        # 每 10 步打印 loss
    save_strategy="epoch",                   # 每轮保存一次
    save_total_limit=2,                      # 最多保留 2 个 checkpoint
    report_to="none",                        # 不上报到 wandb
    dataloader_pin_memory=True,              # 加速数据加载
    remove_unused_columns=False,             # SFTTrainer 需要
)

# ============================================================
# 第五步：创建训练器
# ============================================================
trainer = SFTTrainer(
    model=model,
    args=training_args,
    train_dataset=dataset["train"],
    tokenizer=tokenizer,
    max_seq_length=2048,                     # 最大序列长度
)

# ============================================================
# 第六步：开始训练
# ============================================================
print("开始训练...")
print(f"  Epochs: {training_args.num_train_epochs}")
print(f"  Batch size: {training_args.per_device_train_batch_size}")
print(f"  Gradient accumulation: {training_args.gradient_accumulation_steps}")
print(f"  Effective batch size: {training_args.per_device_train_batch_size * training_args.gradient_accumulation_steps}")
print(f"  Learning rate: {training_args.learning_rate}")
print("=" * 50)

train_result = trainer.train()

# 打印训练结果
print(f"\n训练完成!")
print(f"  总步数: {train_result.global_step}")
print(f"  最终 loss: {train_result.training_loss:.4f}")
print(f"  训练时长: {train_result.metrics['train_runtime']:.0f} 秒")

# ============================================================
# 第七步：保存模型
# ============================================================
output_dir = "./output/final"
trainer.save_model(output_dir)
tokenizer.save_pretrained(output_dir)
print(f"模型已保存到: {output_dir}")
```

---

## 三、训练数据格式

```json
[
  {
    "instruction": "什么是合同的要约？",
    "input": "",
    "output": "要约是希望与他人订立合同的意思表示。要约应当符合以下条件：1）内容具体确定；2）表明经受要约人承诺，要约人即受该意思表示约束。"
  },
  {
    "instruction": "什么是不可抗力？",
    "input": "",
    "output": "不可抗力是不能预见、不能避免且不能克服的客观情况。因不可抗力不能履行民事义务的，不承担民事责任。法律另有规定的，依照其规定。"
  }
]
```

---

## 四、训练日志解读

```
正常训练日志：

  Step 10  | Loss: 2.3456 | LR: 0.000020
  Step 20  | Loss: 1.9876 | LR: 0.000040
  Step 30  | Loss: 1.6543 | LR: 0.000060
  Step 40  | Loss: 1.4321 | LR: 0.000080
  Step 50  | Loss: 1.2345 | LR: 0.000100  ← warmup 结束
  Step 60  | Loss: 1.1234 | LR: 0.000098
  ...

  ✓ 正常：Loss 持续下降，LR 先升后降

异常训练日志：

  Step 10  | Loss: 2.3456
  Step 20  | Loss: 2.3567  ← Loss 不降
  Step 30  | Loss: 2.3678

  ✗ 异常：Loss 不降或上升
  原因：学习率太高、数据质量差、模型配置错误
  解决：降低学习率、检查数据、重新加载模型

  Step 10  | Loss: 2.3456
  Step 20  | Loss: 1.5678
  Step 30  | Loss: 0.8901
  Step 40  | Loss: 0.3456  ← Loss 下降太快
  Step 50  | Loss: 0.1234

  ⚠ 警惕：Loss 下降过快可能是过拟合
  原因：数据量太少、训练轮数太多
  解决：减少 epochs、增加数据量、增加 dropout
```

---

## 五、常见问题排查

```
问题 1：CUDA Out of Memory

  错误信息：RuntimeError: CUDA out of memory

  排查步骤：
  1. nvidia-smi 查看当前显存占用
  2. 减小 per_device_train_batch_size（4 → 2 → 1）
  3. 增加 gradient_accumulation_steps（保持 effective batch_size）
  4. 减小 max_seq_length（2048 → 1024）
  5. 使用 gradient_checkpointing=True
  6. 使用 QLoRA（下一课）

问题 2：Loss 不下降

  排查步骤：
  1. 检查数据格式是否正确（instruction/output 字段）
  2. 检查数据质量（是否有空值、乱码）
  3. 降低学习率（2e-4 → 1e-4 → 5e-5）
  4. 增加 warmup_ratio（0.1 → 0.2）
  5. 检查 tokenizer 是否正确配置

问题 3：训练中断

  恢复方法：
  trainer.train(resume_from_checkpoint=True)

  或指定 checkpoint：
  trainer.train(resume_from_checkpoint="./output/checkpoint-100")

问题 4：模型输出质量差

  排查步骤：
  1. 检查训练数据质量（最重要）
  2. 增加训练轮数（3 → 5）
  3. 调整 LoRA 参数（r=16 → r=32）
  4. 扩展 target_modules（加 gate_proj, up_proj, down_proj）
```

---

## 六、训练后验证

```python
"""训练后快速验证模型效果。"""
from transformers import pipeline
from peft import PeftModel

# 加载基座模型
base_model = AutoModelForCausalLM.from_pretrained(
    model_name, torch_dtype=torch.float16, device_map="auto"
)

# 加载 LoRA 权重
model = PeftModel.from_pretrained(base_model, "./output/final")

# 测试
test_questions = [
    "什么是合同？",
    "什么是违约责任？",
    "什么是诉讼时效？",
]

for q in test_questions:
    inputs = tokenizer(q, return_tensors="pt").to(model.device)
    outputs = model.generate(**inputs, max_new_tokens=200)
    answer = tokenizer.decode(outputs[0], skip_special_tokens=True)
    print(f"Q: {q}")
    print(f"A: {answer}")
    print("-" * 50)
```

---

## 常见误区

1. **不检查显存就直接跑训练**：7B 模型 + LoRA 至少需要 16GB 显存。不检查就启动，跑到一半 OOM 白白浪费时间。先用 `nvidia-smi` 确认可用显存，再决定 batch_size。

2. **训练完不验证直接用**：训练 Loss 低不代表模型效果好。必须用测试问题验证模型输出质量，确认模型学到了目标行为而不是记住了训练数据。

3. **数据格式和模板不匹配**：不同模型的 Chat Template 不同（Qwen、Llama、Mistral 各有差异）。数据格式和模型模板不匹配会导致训练效果极差。

4. **一次性跑太多 epochs**：第一次微调应该先跑 1-2 个 epoch 快速验证流程是否正确，确认无误后再跑完整的 3-5 个 epoch。

---

## 工程建议

1. **先用小数据集验证流程**：取 100 条数据跑 1 个 epoch，确认数据加载、训练、保存、推理全流程正确。调试阶段不要用完整数据集。

2. **训练日志要记录完整**：保存 Loss 曲线、学习率变化、训练时长等关键指标，方便后续分析和复现。用 `report_to="tensorboard"` 自动记录。

3. **checkpoint 要定期保存**：设置 `save_strategy="steps"` 和 `save_steps=100`，避免训练中断后从头开始。用 `save_total_limit=3` 控制磁盘占用。

4. **训练后做 A/B 测试**：用同一组测试问题分别调用基座模型和微调模型，对比输出差异，量化微调带来的改进。

---

## 小结

```
本课核心要点：

1. 完整流程：加载模型 → 配置 LoRA → 加载数据 → 训练 → 保存
2. 关键参数：r=16, alpha=32, lr=2e-4, epochs=3
3. 训练监控：Loss 持续下降是正常，不降或上升需要排查
4. 常见问题：OOM（减 batch_size）、Loss 不降（降 lr）、中断（resume）
5. 训练后必须验证：用测试问题检查模型效果

---

**下一课**: [03 QLoRA 微调——4-bit 量化 + LoRA，在 8GB 显存上微调 7B 模型](./03-QLoRA微调.md)
```

---

## 练习

1. **环境题**：检查你的 GPU 型号和显存大小，计算能支持的最大 batch_size。

2. **训练题**：用课程提供的示例数据完成一次 LoRA 微调，记录训练日志中的 Loss 变化。

3. **调优题**：尝试不同的 r 值（8、16、32）和学习率（1e-4、2e-4、3e-4），对比训练效果。

---

## 参考答案

### 练习一：检查 GPU 型号和显存大小，计算能支持的最大 batch_size

**思路**：先查显存总量，再根据模型大小估算各部分显存占用，反推可用空间能支持多大的 batch_size。关键是理解显存由模型权重 + LoRA 参数 + 优化器状态 + 激活值缓存四部分组成。

**答案**：
```python
"""计算最大 batch_size"""
import torch

if not torch.cuda.is_available():
    print("CUDA 不可用")
    exit()

gpu_name = torch.cuda.get_device_name(0)
total_mem = torch.cuda.get_device_properties(0).total_memory / 1024**3
print(f"GPU: {gpu_name}")
print(f"总显存: {total_mem:.1f} GB")

# Qwen2.5-7B + LoRA r=16 的显存估算
model_size_gb = 14.0      # float16 模型权重
lora_size_gb = 0.13       # LoRA 参数
optimizer_gb = 0.26       # 优化器状态
overhead_gb = 1.5         # 系统开销（CUDA context 等）

available_gb = total_mem - model_size_gb - lora_size_gb - optimizer_gb - overhead_gb
print(f"可用显存: {available_gb:.1f} GB")

# 每个 batch 的激活值缓存约 0.5-1 GB（取决于 seq_length）
activation_per_batch = 0.7  # GB，seq_length=2048 时的估算值
max_batch_size = int(available_gb / activation_per_batch)

# batch_size 通常取 2 的幂次
for bs in [1, 2, 4, 8, 16]:
    needed = model_size_gb + lora_size_gb + optimizer_gb + bs * activation_per_batch + overhead_gb
    status = "✓ 可行" if needed <= total_mem else "✗ OOM"
    print(f"  batch_size={bs}: 需要 {needed:.1f} GB {status}")
```

**要点**：
- 显存估算只是粗略值，实际可能因碎片化等原因略高，建议留 10-20% 余量
- 如果 batch_size=1 都 OOM，需要使用 QLoRA 或更小的模型
- 常见错误：只看模型大小不算优化器和激活值，以为 14GB 显存就能跑 7B 模型的 float16 训练

### 练习二：用课程提供的示例数据完成一次 LoRA 微调

**思路**：按课程代码的七个步骤依次执行，关键是先用小数据集（10-20 条）快速验证流程正确，再换完整数据集训练。重点观察 Loss 变化趋势。

**答案**：
```python
"""第一次 LoRA 微调完整流程"""
import torch
from transformers import AutoModelForCausalLM, AutoTokenizer, TrainingArguments
from peft import LoraConfig, get_peft_model
from trl import SFTTrainer
from datasets import load_dataset
import json

# 准备训练数据（10 条法律问答样例）
data = [
    {"instruction": "什么是合同？", "input": "", "output": "合同是民事主体之间设立、变更、终止民事法律关系的协议。"},
    {"instruction": "什么是违约责任？", "input": "", "output": "违约责任是当事人不履行合同义务或履行不符合约定时应承担的民事责任。"},
    {"instruction": "什么是诉讼时效？", "input": "", "output": "诉讼时效是权利人在法定期间内不行使权利即丧失请求法院保护其权利的制度。普通诉讼时效期间为三年。"},
    {"instruction": "什么是善意取得？", "input": "", "output": "善意取得是无权处分人将不动产或动产转让给受让人，受让人善意且支付合理对价的，取得该财产的所有权。"},
    {"instruction": "什么是不当得利？", "input": "", "output": "不当得利是没有法律根据取得利益而使他人受到损失的事实。受损失的人可以请求得利人返还取得的利益。"},
    {"instruction": "什么是侵权责任？", "input": "", "output": "侵权责任是行为人因过错侵害他人民事权益造成损害时应承担的民事责任。"},
    {"instruction": "什么是抵押权？", "input": "", "output": "抵押权是债权人对债务人或第三人提供的担保财产，在债务人不履行到期债务时，依法享有的优先受偿的权利。"},
    {"instruction": "什么是保证？", "input": "", "output": "保证是保证人和债权人约定，当债务人不履行到期债务时，保证人按照约定履行债务或承担责任的担保方式。"},
    {"instruction": "什么是定金？", "input": "", "output": "定金是当事人约定的，为保证合同履行，一方预先向对方给付的一定金额的货币。定金罚则：给付方违约丧失定金，收受方违约双倍返还。"},
    {"instruction": "什么是不可抗力？", "input": "", "output": "不可抗力是不能预见、不能避免且不能克服的客观情况。因不可抗力不能履行民事义务的，不承担民事责任。"},
]

with open("training_data.json", "w", encoding="utf-8") as f:
    json.dump(data, f, ensure_ascii=False, indent=2)

# 加载模型
model_name = "Qwen/Qwen2.5-7B"
tokenizer = AutoTokenizer.from_pretrained(model_name)
model = AutoModelForCausalLM.from_pretrained(
    model_name, torch_dtype=torch.float16, device_map="auto", trust_remote_code=True
)
if tokenizer.pad_token is None:
    tokenizer.pad_token = tokenizer.eos_token

# 配置 LoRA
lora_config = LoraConfig(
    r=16, lora_alpha=32,
    target_modules=["q_proj", "v_proj", "k_proj", "o_proj"],
    lora_dropout=0.05, bias="none", task_type="CAUSAL_LM"
)
model = get_peft_model(model, lora_config)
model.print_trainable_parameters()

# 加载数据
dataset = load_dataset("json", data_files="training_data.json")

# 训练
training_args = TrainingArguments(
    output_dir="./output",
    num_train_epochs=3,
    per_device_train_batch_size=2,
    gradient_accumulation_steps=4,
    learning_rate=2e-4,
    warmup_ratio=0.1,
    fp16=True,
    logging_steps=5,
    save_strategy="epoch",
    report_to="none",
)
trainer = SFTTrainer(
    model=model, args=training_args,
    train_dataset=dataset["train"],
    tokenizer=tokenizer, max_seq_length=512
)
trainer.train()
trainer.save_model("./output/final")
print("训练完成")
```

**要点**：
- 先用小数据集验证流程，确认无 OOM、数据格式正确、Loss 下降后再跑完整数据
- 训练日志关注：Loss 是否持续下降、是否有 NaN、最终 Loss 是否合理（通常 1.0-2.0 之间）
- 常见错误：数据格式和模型 Chat Template 不匹配（如 Qwen 需要特定的对话格式），导致 Loss 不下降

### 练习三：尝试不同的 r 值和学习率，对比训练效果

**思路**：控制变量法——固定其他参数，分别对比 r 值和学习率的影响。每组实验用相同的测试问题评估，记录 Loss 和输出质量。

**答案**：
```python
"""超参数对比实验"""
import torch
from transformers import AutoModelForCausalLM, AutoTokenizer, TrainingArguments
from peft import LoraConfig, get_peft_model
from trl import SFTTrainer
from datasets import load_dataset

def train_and_evaluate(r, lr, dataset, tokenizer):
    """训练并返回结果"""
    model = AutoModelForCausalLM.from_pretrained(
        "Qwen/Qwen2.5-7B", torch_dtype=torch.float16, device_map="auto"
    )
    if tokenizer.pad_token is None:
        tokenizer.pad_token = tokenizer.eos_token
    
    lora_config = LoraConfig(
        r=r, lora_alpha=2*r,
        target_modules=["q_proj", "v_proj", "k_proj", "o_proj"],
        lora_dropout=0.05, bias="none", task_type="CAUSAL_LM"
    )
    model = get_peft_model(model, lora_config)
    
    training_args = TrainingArguments(
        output_dir=f"./output_r{r}_lr{lr}",
        num_train_epochs=3,
        per_device_train_batch_size=2,
        gradient_accumulation_steps=4,
        learning_rate=lr,
        warmup_ratio=0.1,
        fp16=True,
        logging_steps=5,
        save_strategy="no",
        report_to="none",
    )
    trainer = SFTTrainer(
        model=model, args=training_args,
        train_dataset=dataset["train"],
        tokenizer=tokenizer, max_seq_length=512
    )
    result = trainer.train()
    
    # 测试推理
    test_q = "什么是合同？"
    inputs = tokenizer(test_q, return_tensors="pt").to(model.device)
    output = model.generate(**inputs, max_new_tokens=100)
    answer = tokenizer.decode(output[0], skip_special_tokens=True)
    
    return {
        "r": r, "lr": lr,
        "final_loss": result.training_loss,
        "answer": answer
    }

# 实验矩阵
dataset = load_dataset("json", data_files="training_data.json")
tokenizer = AutoTokenizer.from_pretrained("Qwen/Qwen2.5-7B")

experiments = [
    (8, 1e-4), (8, 2e-4), (8, 3e-4),
    (16, 1e-4), (16, 2e-4), (16, 3e-4),
    (32, 1e-4), (32, 2e-4), (32, 3e-4),
]

for r, lr in experiments:
    print(f"\n实验: r={r}, lr={lr}")
    result = train_and_evaluate(r, lr, dataset, tokenizer)
    print(f"  最终 Loss: {result['final_loss']:.4f}")
    print(f"  输出: {result['answer'][:100]}...")
    # 清理显存
    torch.cuda.empty_cache()
```

**要点**：
- r=16 和 lr=2e-4 通常是 LoRA 的最佳起点，大多数任务在这个配置附近效果最好
- r 从 16 增到 32 效果提升通常只有 1-2%，但参数量翻倍，需要权衡
- 常见错误：同时改多个参数，无法判断是哪个参数导致效果变化；应该控制变量，每次只改一个
