# 05 指令微调数据——Alpaca / ShareGPT 格式的数据构造方法

> 高质量的指令数据是微调成功的关键。

## 场景引入

你知道数据很重要，但手动写 1000 条高质量训练样本？这工作量也太大了。用 LLM 自动生成？质量参差不齐，格式也对不上。Alpaca 格式和 ShareGPT 格式到底有什么区别？用哪个效果更好？如何在有限的时间和精力下，高效地构造出高质量的指令微调数据？

---

## 学习目标

- 掌握指令微调数据的构造方法
- 理解 Alpaca 和 ShareGPT 格式的差异
- 学会用 LLM 辅助生成训练数据

---

## 一、Alpaca 格式构造

```python
def create_alpaca_sample(instruction: str, input: str, output: str) -> dict:
    """创建 Alpaca 格式样本"""
    return {
        "instruction": instruction,
        "input": input,
        "output": output
    }

# 示例
sample = create_alpaca_sample(
    instruction="将以下文本翻译成英文",
    input="人工智能正在改变世界",
    output="Artificial intelligence is changing the world"
)
```

---

## 二、ShareGPT 格式构造

```python
def create_sharegpt_sample(conversations: list) -> dict:
    """创建 ShareGPT 格式样本"""
    return {
        "conversations": [
            {"from": "human", "content": conv[0]},
            {"from": "gpt", "content": conv[1]}
            for conv in conversations
        ]
    }
```

---

## 三、LLM 辅助生成

```python
def generate_training_data_with_llm(task: str, count: int, client) -> list:
    """用 LLM 生成训练数据"""
    
    prompt = f"""请生成 {count} 条用于微调的训练数据。

任务类型：{task}

要求：
1. 数据要多样化
2. 质量要高
3. 格式为 JSON 数组

请以 JSON 格式输出。"""
    
    response = client.chat.completions.create(
        model="gpt-4o",
        messages=[{"role": "user", "content": prompt}],
        response_format={"type": "json_object"}
    )
    
    return json.loads(response.choices[0].message.content)["data"]
```

---

## 四、数据增强

```python
def augment_data(data: list, client) -> list:
    """数据增强"""
    augmented = []
    
    for item in data:
        # 原始数据
        augmented.append(item)
        
        # 同义词替换
        prompt = f"""请用不同的表达方式重写以下文本，保持意思不变：

原文：{item['output']}"""
        
        response = client.chat.completions.create(
            model="gpt-4o",
            messages=[{"role": "user", "content": prompt}]
        )
        
        augmented.append({
            **item,
            "output": response.choices[0].message.content
        })
    
    return augmented
```

---

## 五、质量检查

```python
def check_data_quality(data: list) -> dict:
    """检查数据质量"""
    issues = []
    
    for i, item in enumerate(data):
        # 检查格式
        if not item.get("instruction"):
            issues.append(f"样本 {i}: 缺少 instruction")
        
        # 检查长度
        if len(item.get("output", "")) < 10:
            issues.append(f"样本 {i}: output 过短")
    
    return {
        "total": len(data),
        "issues": issues,
        "quality_score": 1 - len(issues) / len(data)
    }
```

---

## 常见误区

1. **完全依赖 LLM 生成训练数据**：LLM 生成的数据质量参差不齐，且可能引入系统性偏差。应该以人工编写为核心，LLM 生成为辅助，最后做人工审核。

2. **Alpaca 和 ShareGPT 格式随便选**：单轮任务用 Alpaca 格式更简洁，多轮对话必须用 ShareGPT 格式。格式选错会导致训练时对话上下文丢失。

3. **数据增强不加控制地扩增**：同义词替换和回译会引入语义偏差。增强后的数据必须人工验证，确保没有改变原始含义。

4. **忽略指令多样性**：1000 条数据但指令只有 5 种模板，模型会过拟合到这几种指令格式。需要确保指令的表达方式足够多样。

---

## 工程建议

1. **先手写 50 条高质量样本**：这 50 条作为种子数据和质量标杆，后续用 LLM 生成的数据都要以它们为参照。质量比数量更重要。

2. **用 JSON Schema 验证数据格式**：在数据管道中加入格式校验环节，自动检查每条数据是否符合目标格式（Alpaca/ShareGPT），避免格式错误的数据混入训练集。

3. **LLM 生成时用强模型**：如果用 LLM 辅助生成数据，用 GPT-4 级别的强模型，生成质量显著高于小模型。成本差异不大但质量差距明显。

4. **保留数据血缘**：记录每条训练数据的来源（人工编写 / LLM 生成 / 数据增强），方便后续分析哪些类型的数据对效果贡献最大。

---

## 小结

```
本课核心要点：

1. Alpaca 格式：instruction + input + output
2. ShareGPT 格式：对话列表
3. LLM 辅助生成训练数据
4. 数据增强提高多样性
5. 质量检查确保数据质量

---

**下一课**: [06 阶段实战——为一个垂直领域构造微调数据集](./06-阶段实战-垂直领域数据集.md)
```

---

## 练习

1. **构造题**：构造 100 条 Alpaca 格式的训练数据。

2. **生成题**：用 LLM 辅助生成训练数据。

3. **质量题**：对数据集进行质量检查。

---

## 参考答案

### 练习一：构造题

**思路**：构造 100 条 Alpaca 格式的训练数据，覆盖多种 NLP 任务类型，确保数据质量和多样性。

**答案**：

```python
import json
import random

def create_alpaca_sample(instruction: str, input_text: str, output: str) -> dict:
    return {
        "instruction": instruction,
        "input": input_text,
        "output": output,
    }

# 种子数据模板（实际项目应人工编写高质量种子）
task_templates = {
    "情感分类": [
        {"input": "这家餐厅的菜太难吃了，服务态度也差", "output": "负面"},
        {"input": "新买的耳机音质非常好，佩戴舒适", "output": "正面"},
        {"input": "今天天气一般，不冷不热", "output": "中性"},
    ],
    "文本摘要": [
        {"input": "人工智能（AI）是计算机科学的一个分支，它企图了解智能的实质，并生产出一种新的能以人类智能相似的方式做出反应的智能机器。自诞生以来，AI 的理论和技术日益成熟，应用领域也不断扩大。", "output": "人工智能是计算机科学的分支，旨在理解智能本质并开发类人智能机器，其理论和应用领域持续发展。"},
    ],
    "中英翻译": [
        {"input": "深度学习是机器学习的一个子集", "output": "Deep learning is a subset of machine learning"},
        {"input": "自然语言处理技术正在改变人机交互方式", "output": "Natural language processing technology is changing the way humans interact with machines"},
    ],
    "代码生成": [
        {"input": "用 Python 实现一个栈数据结构", "output": "class Stack:\n    def __init__(self):\n        self.items = []\n\n    def push(self, item):\n        self.items.append(item)\n\n    def pop(self):\n        if not self.is_empty():\n            return self.items.pop()\n        raise IndexError('栈为空')\n\n    def peek(self):\n        if not self.is_empty():\n            return self.items[-1]\n        raise IndexError('栈为空')\n\n    def is_empty(self):\n        return len(self.items) == 0\n\n    def size(self):\n        return len(self.items)"},
    ],
    "问答": [
        {"input": "", "output": "Python 是一种高级编程语言，由 Guido van Rossum 于 1991 年发布。它以简洁易读的语法著称，支持面向对象、函数式和过程式编程范式。"},
    ],
}

# 构造指令变体，增加多样性
instruction_variants = {
    "情感分类": [
        "判断以下文本的情感倾向",
        "对以下文本进行情感分析",
        "以下文本表达了什么情感？",
        "请判断这段话的情感是正面、负面还是中性",
    ],
    "文本摘要": [
        "用一句话概括以下文本",
        "请对以下文本进行摘要",
        "提取以下文本的核心信息",
        "用简洁的语言总结以下内容",
    ],
    "中英翻译": [
        "将以下中文翻译成英文",
        "请把这段中文翻译为英文",
        "翻译以下文本为英文",
    ],
    "代码生成": [
        "根据以下描述生成 Python 代码",
        "用 Python 实现以下功能",
        "请编写 Python 代码完成以下任务",
    ],
    "问答": [
        "请回答以下问题",
        "用简洁的语言回答这个问题",
    ],
}

# 生成 100 条数据
dataset = []
task_types = list(task_templates.keys())

while len(dataset) < 100:
    task = random.choice(task_types)
    template = random.choice(task_templates[task])
    instruction = random.choice(instruction_variants[task])

    sample = create_alpaca_sample(instruction, template["input"], template["output"])

    # 简单去重
    if sample not in dataset:
        dataset.append(sample)

# 保存
with open("alpaca_dataset_100.json", "w", encoding="utf-8") as f:
    json.dump(dataset, f, ensure_ascii=False, indent=2)

print(f"生成 {len(dataset)} 条数据")
print(f"任务分布: {Counter(json.dumps(t['instruction']) for t in dataset).most_common(5)}")
print(f"\n示例：{json.dumps(dataset[0], ensure_ascii=False, indent=2)}")
```

**要点**：
- 种子数据的质量决定最终数据集的质量——垃圾种子扩增出垃圾数据
- 指令要有多种表达方式，避免模型过拟合到固定模板
- 常见错误：100 条数据只有 2-3 种指令模板——模型会学会模板而不是任务本身

### 练习二：生成题

**思路**：使用 LLM 辅助生成训练数据，设计好 Prompt 模板确保输出格式正确、质量可控。

**答案**：

```python
import json
from openai import OpenAI

client = OpenAI(base_url="http://localhost:11434/v1", api_key="not-needed")

def generate_with_llm(task_description: str, count: int, model: str = "qwen2.5:7b") -> list:
    """用 LLM 生成 Alpaca 格式的训练数据"""

    prompt = f"""请生成 {count} 条用于模型微调的训练数据。

任务描述：{task_description}

要求：
1. 每条数据必须包含 instruction、input、output 三个字段
2. instruction 的表达方式要多样化，不要用同一个模板
3. output 必须准确、完整、高质量
4. 覆盖该任务的不同难度和场景

请直接输出 JSON 数组，格式如下：
[
  {{"instruction": "...", "input": "...", "output": "..."}},
  ...
]"""

    response = client.chat.completions.create(
        model=model,
        messages=[{"role": "user", "content": prompt}],
        temperature=0.8,  # 适当增加随机性以提高多样性
        response_format={"type": "json_object"},
    )

    result = json.loads(response.choices[0].message.content)

    # 兼容不同的返回格式
    if isinstance(result, list):
        return result
    elif isinstance(result, dict) and "data" in result:
        return result["data"]
    elif isinstance(result, dict) and "items" in result:
        return result["items"]
    else:
        # 尝试找到第一个列表类型的值
        for v in result.values():
            if isinstance(v, list):
                return v
        return []

# 生成不同任务类型的数据
tasks = [
    "中文文本情感分类（正面/负面/中性）",
    "中文文本摘要生成",
    "中文到英文的翻译",
    "根据需求描述生成 Python 代码",
]

all_data = []
for task in tasks:
    print(f"生成: {task}")
    samples = generate_with_llm(task, count=25)
    all_data.extend(samples)
    print(f"  生成 {len(samples)} 条")

# 保存
with open("llm_generated_dataset.json", "w", encoding="utf-8") as f:
    json.dump(all_data, f, ensure_ascii=False, indent=2)

print(f"\n总计生成 {len(all_data)} 条数据")
```

**要点**：
- 用强模型（如 GPT-4 级别）生成数据质量更高，本地小模型可能格式不稳定
- `temperature=0.8` 增加多样性，但不要设太高（>1.0）否则质量下降
- 常见错误：不校验 LLM 输出的格式——LLM 可能返回非标准 JSON 或缺少字段，必须做格式校验

### 练习三：质量题

**思路**：对生成的数据集进行多维度质量检查，包括格式校验、长度分布、多样性和内容质量抽检。

**答案**：

```python
import json
from collections import Counter

def quality_check(dataset: list) -> dict:
    """数据集质量检查"""
    report = {
        "total": len(dataset),
        "issues": [],
        "stats": {},
    }

    # 1. 格式校验
    for i, item in enumerate(dataset):
        if not isinstance(item, dict):
            report["issues"].append(f"样本 {i}: 不是字典类型")
            continue
        if "instruction" not in item:
            report["issues"].append(f"样本 {i}: 缺少 instruction")
        if "output" not in item:
            report["issues"].append(f"样本 {i}: 缺少 output")
        elif len(item["output"].strip()) < 5:
            report["issues"].append(f"样本 {i}: output 过短 ({len(item['output'])} 字符)")

    # 2. 长度分布
    output_lengths = [len(item.get("output", "")) for item in dataset if isinstance(item, dict)]
    if output_lengths:
        report["stats"]["output 长度"] = {
            "最短": min(output_lengths),
            "最长": max(output_lengths),
            "平均": round(sum(output_lengths) / len(output_lengths), 1),
            "中位数": sorted(output_lengths)[len(output_lengths) // 2],
        }

    # 3. 指令多样性
    instructions = [item.get("instruction", "") for item in dataset if isinstance(item, dict)]
    unique_instructions = set(instructions)
    report["stats"]["唯一指令数"] = len(unique_instructions)
    report["stats"]["指令多样性"] = f"{len(unique_instructions)/len(instructions)*100:.1f}%"

    # 4. 空值统计
    empty_outputs = sum(1 for item in dataset if not item.get("output", "").strip())
    report["stats"]["空 output 数"] = empty_outputs

    # 5. 重复统计
    seen = set()
    duplicates = 0
    for item in dataset:
        key = item.get("instruction", "") + item.get("output", "")
        if key in seen:
            duplicates += 1
        seen.add(key)
    report["stats"]["重复样本数"] = duplicates

    # 6. 质量评分
    issue_count = len(report["issues"])
    report["quality_score"] = round((1 - issue_count / len(dataset)) * 100, 1)

    return report


# 使用
with open("llm_generated_dataset.json", "r", encoding="utf-8") as f:
    data = json.load(f)

report = quality_check(data)

print("=" * 50)
print("数据集质量报告")
print("=" * 50)
print(f"总样本数: {report['total']}")
print(f"质量评分: {report['quality_score']}%")
print(f"\n统计信息:")
for k, v in report["stats"].items():
    print(f"  {k}: {v}")

if report["issues"]:
    print(f"\n发现 {len(report['issues'])} 个问题:")
    for issue in report["issues"][:10]:
        print(f"  - {issue}")
else:
    print("\n未发现问题 ✓")

# 7. 抽样人工检查（打印 5 条随机样本供人工审核）
print("\n" + "=" * 50)
print("人工抽检样本（请检查质量）")
print("=" * 50)
import random
samples = random.sample(data, min(5, len(data)))
for i, s in enumerate(samples):
    print(f"\n样本 {i+1}:")
    print(f"  instruction: {s.get('instruction', 'N/A')}")
    print(f"  input: {s.get('input', 'N/A')[:50]}...")
    print(f"  output: {s.get('output', 'N/A')[:80]}...")
```

**要点**：
- 自动检查能发现格式问题，但语义质量必须人工抽检
- 指令多样性低于 30% 说明数据过于模板化，需要增加指令变体
- 常见错误：只看数量不看质量——100 条高质量数据胜过 1000 条低质量数据
