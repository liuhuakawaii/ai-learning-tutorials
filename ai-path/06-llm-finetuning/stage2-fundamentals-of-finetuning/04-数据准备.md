# 04 数据准备——训练数据格式、清洗、去重、质量控制

> 数据质量决定微调效果。垃圾数据微调出垃圾模型。

## 场景引入

你从网上爬了 5000 条问答数据，兴冲冲地跑了一晚上微调，结果模型输出全是乱码和重复内容。检查数据才发现：一半样本的 output 字段是空的，剩下的一半里有大量重复和格式错误的记录。"Garbage in, garbage out"——微调模型的质量上限由数据质量决定，不做好数据清洗和质量控制，再好的训练配置也白搭。

---

## 学习目标

- 掌握微调数据的格式要求
- 理解数据清洗和质量控制方法
- 学会构建高质量的微调数据集

---

## 一、数据格式

```
常用数据格式：

1. Alpaca 格式
   {
     "instruction": "指令",
     "input": "输入",
     "output": "输出"
   }

2. ShareGPT 格式
   {
     "conversations": [
       {"from": "human", "content": "问题"},
       {"from": "gpt", "content": "回答"}
     ]
   }

3. OpenAI 格式
   {
     "messages": [
       {"role": "user", "content": "问题"},
       {"role": "assistant", "content": "回答"}
     ]
   }
```

---

## 二、数据清洗

```python
def clean_dataset(data: list) -> list:
    """清洗数据集"""
    cleaned = []
    
    for item in data:
        # 1. 去除空值
        if not item.get("output"):
            continue
        
        # 2. 去除过短的样本
        if len(item["output"]) < 10:
            continue
        
        # 3. 去除重复
        if item not in cleaned:
            cleaned.append(item)
    
    return cleaned
```

---

## 三、数据去重

```python
from hashlib import md5

def deduplicate(data: list) -> list:
    """去重"""
    seen = set()
    unique = []
    
    for item in data:
        # 计算哈希
        hash_str = md5(json.dumps(item, sort_keys=True).encode()).hexdigest()
        
        if hash_str not in seen:
            seen.add(hash_str)
            unique.append(item)
    
    return unique
```

---

## 四、质量控制

```
质量控制方法：

1. 人工审核
   - 抽样检查数据质量
   - 修复错误数据
   - 删除低质量数据

2. 自动检查
   - 格式检查
   - 长度检查
   - 语言检查

3. 多样性保证
   - 覆盖不同场景
   - 平衡数据分布
   - 避免偏差

4. 数据增强
   - 同义词替换
   - 回译
   - 模板生成
```

---

## 五、数据集规模

```
数据集规模建议：

| 任务类型 | 最小规模 | 推荐规模 |
|----------|----------|----------|
| 简单分类 | 100 | 1000 |
| 文本生成 | 500 | 5000 |
| 对话系统 | 1000 | 10000 |
| 专业知识 | 500 | 5000 |

原则：
- 质量 > 数量
- 1000 条高质量数据胜过 10000 条低质量数据
```

---

## 六、数据构建工具

```python
class DatasetBuilder:
    """数据集构建器"""
    
    def __init__(self):
        self.data = []
    
    def add_sample(self, instruction: str, input: str, output: str):
        """添加样本"""
        self.data.append({
            "instruction": instruction,
            "input": input,
            "output": output
        })
    
    def save(self, path: str, format: str = "alpaca"):
        """保存数据集"""
        if format == "alpaca":
            with open(path, "w") as f:
                json.dump(self.data, f, ensure_ascii=False, indent=2)
```

---

## 常见误区

1. **数据越多越好**：10000 条低质量数据不如 1000 条高质量数据。低质量数据会引入噪声，让模型学到错误的模式，反而降低效果。

2. **只做精确去重不做模糊去重**：两条数据字段完全不同但语义相同（如换了种说法的同一个问题），精确哈希去重无法识别。需要用语义相似度做模糊去重。

3. **清洗时删除过多数据**：过度清洗（如删除所有短回答）可能丢失有价值的样本。清洗规则要根据业务场景定制，不能一刀切。

4. **训练集和评估集有数据泄漏**：如果评估集中的样本和训练集高度相似，评估结果会虚高。必须确保评估集是独立收集的，与训练集没有重叠。

---

## 工程建议

1. **建立数据质量 checklist**：每次构建数据集时按 checklist 逐项检查——格式正确、无空值、长度合理、无重复、多样性足够，确保质量可控。

2. **保留数据版本**：每次清洗和增强操作都保留原始数据的备份，方便回溯和对比。用 DVC 或简单的目录版本管理即可。

3. **人工抽检不能省**：自动检查能发现格式问题，但语义质量需要人工抽检。每 100 条样本至少人工审核 10 条，确保数据的业务准确性。

4. **数据分布要和业务匹配**：如果业务中 80% 是简单问题、20% 是复杂问题，训练数据也应该保持类似的比例，避免模型在简单问题上过拟合。

---

## 小结

```
本课核心要点：

1. 数据格式：Alpaca、ShareGPT、OpenAI
2. 数据清洗：去除空值、过短、重复数据
3. 质量控制：人工审核、自动检查、多样性保证
4. 质量 > 数量

---

**下一课**: [05 指令微调数据——Alpaca / ShareGPT 格式的数据构造方法](./05-指令微调数据.md)
```

---

## 练习

1. **构建题**：构建一个 100 条的微调数据集。

2. **清洗题**：清洗一个现有数据集。

3. **质量题**：对数据集进行质量检查。

---

## 参考答案

### 练习一：构建题

**思路**：使用 Python 构建一个 100 条的 Alpaca 格式微调数据集，涵盖多种任务类型，确保数据多样性和质量。

**答案**：

```python
import json
import random

class DatasetBuilder:
    """微调数据集构建器"""

    def __init__(self):
        self.data = []

    def add_sample(self, instruction: str, input_text: str, output: str):
        self.data.append({
            "instruction": instruction,
            "input": input_text,
            "output": output,
        })

    def save(self, path: str):
        with open(path, "w", encoding="utf-8") as f:
            json.dump(self.data, f, ensure_ascii=False, indent=2)
        print(f"保存 {len(self.data)} 条数据到 {path}")

# 构建数据集
builder = DatasetBuilder()

# 模板化的数据生成（实际项目中应人工编写或用 LLM 辅助）
templates = [
    {
        "instruction": "将以下文本分类为正面、负面或中性情感",
        "inputs": [
            ("这款手机拍照效果非常好，电池也很耐用", "正面"),
            ("物流太慢了，等了一周才收到", "负面"),
            ("产品还行，没什么特别的", "中性"),
        ],
    },
    {
        "instruction": "用一句话概括以下文本的核心内容",
        "inputs": [
            ("人工智能技术正在快速发展，从自动驾驶到医疗诊断，AI 的应用场景越来越广泛", "人工智能技术发展迅速，应用场景不断扩大"),
            ("公司本季度营收同比增长 30%，主要得益于新产品线的推出", "公司季度营收增长 30%，新产品的贡献显著"),
        ],
    },
    {
        "instruction": "将以下中文翻译成英文",
        "inputs": [
            ("机器学习是人工智能的一个子领域", "Machine learning is a subfield of artificial intelligence"),
            ("深度学习需要大量的训练数据", "Deep learning requires a large amount of training data"),
        ],
    },
    {
        "instruction": "根据以下描述生成一个 Python 函数",
        "inputs": [
            ("计算列表中所有偶数的和", "def sum_even_numbers(numbers):\n    return sum(n for n in numbers if n % 2 == 0)"),
            ("判断一个字符串是否是回文", "def is_palindrome(s):\n    return s == s[::-1]"),
        ],
    },
]

# 填充到 100 条
while len(builder.data) < 100:
    template = random.choice(templates)
    inp, out = random.choice(template["inputs"])
    builder.add_sample(template["instruction"], inp, out)

builder.save("finetune_dataset.json")
print(f"数据集示例：{json.dumps(builder.data[0], ensure_ascii=False, indent=2)}")
```

**要点**：
- 数据集需要覆盖多种任务类型，不能只有单一模板
- 实际项目中应该手写种子数据（50 条），再用 LLM 扩增
- 常见错误：用模板机械填充到 100 条但多样性不足——模型会过拟合到模板格式

### 练习二：清洗题

**思路**：编写数据清洗管道，处理空值、过短样本、重复数据、格式错误等常见问题。

**答案**：

```python
import json
from hashlib import md5

def clean_dataset(data: list) -> dict:
    """完整的数据清洗管道"""
    stats = {
        "原始数量": len(data),
        "空值移除": 0,
        "过短移除": 0,
        "重复移除": 0,
        "格式错误移除": 0,
    }
    cleaned = []
    seen_hashes = set()

    for item in data:
        # 1. 格式检查：必须包含 instruction 和 output
        if not isinstance(item, dict):
            stats["格式错误移除"] += 1
            continue
        if "instruction" not in item or "output" not in item:
            stats["格式错误移除"] += 1
            continue

        # 2. 空值检查
        if not item["instruction"].strip() or not item["output"].strip():
            stats["空值移除"] += 1
            continue

        # 3. 长度检查：output 不能太短
        if len(item["output"].strip()) < 5:
            stats["过短移除"] += 1
            continue

        # 4. 去重（基于 instruction + output 的哈希）
        hash_str = md5(
            (item["instruction"] + item["output"]).encode()
        ).hexdigest()
        if hash_str in seen_hashes:
            stats["重复移除"] += 1
            continue

        seen_hashes.add(hash_str)
        cleaned.append(item)

    stats["清洗后数量"] = len(cleaned)
    stats["清洗比例"] = f"{(1 - len(cleaned)/stats['原始数量'])*100:.1f}%"
    return cleaned, stats


# 测试
with open("finetune_dataset.json", "r", encoding="utf-8") as f:
    data = json.load(f)

cleaned, stats = clean_dataset(data)
print("清洗统计：")
for k, v in stats.items():
    print(f"  {k}: {v}")
```

**要点**：
- 清洗管道应该按顺序执行：格式检查 → 空值 → 长度 → 去重
- 要记录每一步移除了多少数据，方便定位问题
- 常见错误：过度清洗——删除所有短回答可能丢失有价值的样本（如分类任务的标签）

### 练习三：质量题

**思路**：对数据集进行多维度质量检查，包括格式一致性、长度分布、多样性分析。

**答案**：

```python
import json
from collections import Counter

def check_data_quality(data: list) -> dict:
    """数据集质量检查"""
    report = {
        "总样本数": len(data),
        "问题列表": [],
        "统计信息": {},
    }

    # 1. 格式一致性检查
    required_fields = ["instruction", "input", "output"]
    missing_fields = 0
    for i, item in enumerate(data):
        for field in required_fields:
            if field not in item:
                missing_fields += 1
                report["问题列表"].append(f"样本 {i}: 缺少字段 '{field}'")
    report["统计信息"]["缺少字段的样本数"] = missing_fields

    # 2. 长度分布分析
    output_lengths = [len(item.get("output", "")) for item in data]
    report["统计信息"]["output 长度"] = {
        "最短": min(output_lengths) if output_lengths else 0,
        "最长": max(output_lengths) if output_lengths else 0,
        "平均": sum(output_lengths) / len(output_lengths) if output_lengths else 0,
    }

    # 检查异常短的 output
    short_count = sum(1 for l in output_lengths if l < 10)
    if short_count > 0:
        report["问题列表"].append(f"有 {short_count} 条 output 长度 < 10 字符")

    # 3. 指令多样性分析
    instructions = [item.get("instruction", "") for item in data]
    unique_instructions = set(instructions)
    report["统计信息"]["唯一指令数"] = len(unique_instructions)
    report["统计信息"]["指令重复率"] = f"{(1 - len(unique_instructions)/len(instructions))*100:.1f}%"

    # 检查指令是否过于集中
    instruction_counts = Counter(instructions)
    top_5 = instruction_counts.most_common(5)
    report["统计信息"]["Top 5 指令"] = top_5

    # 4. 空值检查
    empty_outputs = sum(1 for item in data if not item.get("output", "").strip())
    if empty_outputs > 0:
        report["问题列表"].append(f"有 {empty_outputs} 条 output 为空")

    # 5. 质量评分
    issue_ratio = len(report["问题列表"]) / len(data) if data else 0
    report["质量评分"] = f"{(1 - issue_ratio) * 100:.1f}%"

    return report


# 使用
with open("finetune_dataset.json", "r", encoding="utf-8") as f:
    data = json.load(f)

report = check_data_quality(data)

print("=" * 50)
print("数据集质量报告")
print("=" * 50)
print(f"总样本数: {report['总样本数']}")
print(f"质量评分: {report['质量评分']}")
print(f"\n统计信息:")
for k, v in report["统计信息"].items():
    print(f"  {k}: {v}")
if report["问题列表"]:
    print(f"\n发现 {len(report['问题列表'])} 个问题:")
    for issue in report["问题列表"][:10]:
        print(f"  - {issue}")
else:
    print("\n未发现问题 ✓")
```

**要点**：
- 质量检查应该覆盖格式、长度、多样性、空值四个维度
- 指令重复率过高说明数据多样性不足，模型可能过拟合到特定指令格式
- 常见错误：只检查格式不检查语义——格式正确的数据也可能内容质量差（如胡说八道的回答）
