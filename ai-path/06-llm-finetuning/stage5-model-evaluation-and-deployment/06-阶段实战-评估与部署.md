# 阶段实战——评估、合并、量化、部署一站式完成

> 课型：项目推进课
> 目标：从 LoRA checkpoint 到可调用的线上服务

## 当前卡点

微调完成了，checkpoint 存在 output 目录里。但老板问"这个模型到底好不好"，你只能说"我试了几个 case 感觉还行"。然后要部署，发现 LoRA 权重不能直接用，要合并；合并后发现模型太大，要量化；量化后发现精度掉了，要重新评估。

评估、合并、量化、部署——这四步应该是一条线，而不是每次手忙脚乱地拼凑。

## 系统设计

```
LoRA checkpoint
  │
  ├── 评估（领域任务 + 性能基准）
  │     │
  │     ├── 不达标 → 停止，调整数据或参数
  │     │
  │     └── 达标 ↓
  │
  ├── 合并 LoRA → 基座模型
  │
  ├── 量化（可选）→ GGUF
  │
  └── 部署 → vLLM / Ollama / Docker
```

每一步都有验证环节，不盲目往下走。

## 评估器实现

```python
import json
import time
import torch
from pathlib import Path
from transformers import AutoTokenizer, AutoModelForCausalLM
from peft import PeftModel


class ModelEvaluator:
    def __init__(self, base_model: str, lora_path: str):
        self.tokenizer = AutoTokenizer.from_pretrained(base_model, trust_remote_code=True)
        base = AutoModelForCausalLM.from_pretrained(
            base_model, torch_dtype=torch.bfloat16, device_map="auto"
        )
        self.model = PeftModel.from_pretrained(base, lora_path)
        self.model.eval()

    def evaluate_domain(self, test_data: list[dict]) -> dict:
        """领域任务评估"""
        correct = 0
        results = []

        for item in test_data:
            prompt = item["prompt"]
            expected = item["expected"]

            inputs = self.tokenizer(prompt, return_tensors="pt").to(self.model.device)
            with torch.no_grad():
                outputs = self.model.generate(**inputs, max_new_tokens=256, temperature=0)

            predicted = self.tokenizer.decode(
                outputs[0][inputs.input_ids.shape[1]:], skip_special_tokens=True
            )
            is_correct = expected.strip().lower() in predicted.strip().lower()

            if is_correct:
                correct += 1
            results.append({
                "prompt": prompt[:100],
                "expected": expected[:100],
                "predicted": predicted[:100],
                "correct": is_correct,
            })

        return {
            "accuracy": correct / len(test_data),
            "correct": correct,
            "total": len(test_data),
            "details": results[:10],
        }

    def measure_performance(self, prompt: str = "你好，请介绍一下自己。", runs: int = 5) -> dict:
        """性能基准测试"""
        latencies = []
        tokens_per_sec = []

        for _ in range(runs):
            inputs = self.tokenizer(prompt, return_tensors="pt").to(self.model.device)
            start = time.time()
            with torch.no_grad():
                outputs = self.model.generate(**inputs, max_new_tokens=256)
            elapsed = time.time() - start

            new_tokens = outputs.shape[1] - inputs.input_ids.shape[1]
            latencies.append(elapsed)
            tokens_per_sec.append(new_tokens / elapsed)

        latencies.sort()
        return {
            "latency_p50": latencies[len(latencies) // 2],
            "latency_p95": latencies[int(len(latencies) * 0.95)],
            "tokens_per_sec": sum(tokens_per_sec) / len(tokens_per_sec),
            "gpu_memory_gb": torch.cuda.max_memory_allocated() / 1e9,
        }
```

## 评估报告生成

```python
from datetime import datetime


def generate_report(
    model_name: str,
    base_model: str,
    domain_results: dict,
    perf_results: dict,
    baseline_domain: dict | None = None,
) -> str:
    improvement = ""
    if baseline_domain:
        diff = domain_results["accuracy"] - baseline_domain["accuracy"]
        improvement = f"- 领域提升：**{diff:+.2%}**（基座 {baseline_domain['accuracy']:.2%} → 微调 {domain_results['accuracy']:.2%}）"

    report = f"""# 微调模型评估报告

> 生成时间：{datetime.now().strftime('%Y-%m-%d %H:%M')}
> 模型：{model_name}
> 基座：{base_model}

## 领域任务评估

- 测试集大小：{domain_results['total']} 条
- 准确率：**{domain_results['accuracy']:.2%}**
{improvement}

### 样例（前 5 条）

| # | 预期 | 预测 | 正确 |
|---|------|------|------|
"""

    for i, d in enumerate(domain_results["details"][:5], 1):
        status = "✅" if d["correct"] else "❌"
        report += f"| {i} | {d['expected'][:30]} | {d['predicted'][:30]} | {status} |\n"

    report += f"""
## 性能指标

| 指标 | 值 |
|------|-----|
| P50 延迟 | {perf_results['latency_p50']:.3f}s |
| P95 延迟 | {perf_results['latency_p95']:.3f}s |
| 吞吐量 | {perf_results['tokens_per_sec']:.1f} tokens/s |
| 显存占用 | {perf_results['gpu_memory_gb']:.1f} GB |

## 结论

- 领域任务准确率 {'✅ 达标' if domain_results['accuracy'] > 0.7 else '❌ 需改进'}
- 推理性能 {'✅ 满足要求' if perf_results['latency_p95'] < 2.0 else '⚠️ 需优化'}
"""
    return report
```

## 合并与量化

```python
from peft import PeftModel
from transformers import AutoModelForCausalLM, AutoTokenizer


def merge_lora(base_model: str, lora_path: str, output_path: str):
    """合并 LoRA 权重到基座模型"""
    base = AutoModelForCausalLM.from_pretrained(base_model, torch_dtype="auto")
    model = PeftModel.from_pretrained(base, lora_path)
    merged = model.merge_and_unload()

    merged.save_pretrained(output_path)
    tokenizer = AutoTokenizer.from_pretrained(base_model)
    tokenizer.save_pretrained(output_path)
    print(f"合并完成: {output_path}")


def quantize_gguf(model_path: str, output_path: str, quant: str = "Q4_K_M"):
    """导出 GGUF 格式"""
    import subprocess
    subprocess.run([
        "python", "convert_hf_to_gguf.py", model_path,
        "--outfile", f"{output_path}/model.gguf",
        "--outtype", quant,
    ], check=True)
    print(f"GGUF 导出完成: {output_path}/model.gguf ({quant})")
```

## 完整流程

```python
async def full_pipeline(base_model: str, lora_path: str, test_data_path: str):
    # 1. 评估
    print("Step 1: 评估")
    evaluator = ModelEvaluator(base_model, lora_path)
    with open(test_data_path) as f:
        test_data = json.load(f)
    domain_results = evaluator.evaluate_domain(test_data)
    perf_results = evaluator.measure_performance()

    # 2. 生成报告
    print("Step 2: 生成报告")
    report = generate_report(
        model_name="my-finetuned-model",
        base_model=base_model,
        domain_results=domain_results,
        perf_results=perf_results,
    )
    Path("eval-report.md").write_text(report)

    # 3. 判断是否达标
    if domain_results["accuracy"] < 0.7:
        print("❌ 领域准确率不达标，需要继续训练或调整数据")
        return

    # 4. 合并
    print("Step 3: 合并 LoRA")
    merge_lora(base_model, lora_path, "./merged-model")

    # 5. 量化（可选）
    print("Step 4: 导出 GGUF")
    quantize_gguf("./merged-model", "./gguf-model", quant="Q4_K_M")

    # 6. 部署提示
    print("Step 5: 可以部署了")
    print("  vLLM: vllm serve ./merged-model")
    print("  Ollama: ollama create my-model -f Modelfile")
```

## 部署方案选型

```
场景                    推荐方案           理由
──────────────────────────────────────────────────
内部工具，团队 < 20 人   Ollama            最简单，一行命令启动
面向用户的服务           vLLM + Docker     高吞吐，支持并发
边缘设备 / 离线          GGUF + llama.cpp  最轻量，CPU 可跑
```

### vLLM 部署

```bash
# 启动服务
vllm serve ./merged-model --host 0.0.0.0 --port 8000 --max-model-len 4096

# 测试调用
curl http://localhost:8000/v1/completions \
  -H "Content-Type: application/json" \
  -d '{"model": "./merged-model", "prompt": "如何配置 LoRA？", "max_tokens": 256}'
```

### Ollama 部署

```bash
# 创建 Modelfile
cat > Modelfile << 'EOF'
FROM ./gguf-model/model.gguf
PARAMETER temperature 0.7
SYSTEM "你是一个专业的法律助手。"
EOF

# 构建并运行
ollama create legal-assistant -f Modelfile
ollama run legal-assistant
```

## 容易犯的错

**评估报告只有分数没有分析**。不要只列"准确率 85%、延迟 0.5s"，要分析这意味着什么、是否达标、哪些方面需要改进。数字本身没有意义，分析才有价值。

**合并和量化一步到位**。应该先合并测试，再量化测试，每步都验证效果。合并后就有问题的话，量化后问题可能更严重。

**部署后不做线上验证**。离线评估和线上表现可能有差距。部署后用真实请求验证延迟、并发处理能力和输出质量。

## 练习

### 练习一：完整评估

用 `ModelEvaluator` 对你的微调模型运行完整评估，生成评估报告。要求同时包含领域任务评估和性能基准。

```python
# 你的实现
# 1. 准备测试数据集（至少 50 条）
# 2. 运行评估
# 3. 生成 Markdown 报告
```

### 练习二：合并与量化

将 LoRA 权重合并到基座模型，分别导出 HuggingFace 格式和 GGUF 格式。对比两种格式的文件大小和加载速度。

### 练习三：选择部署方案

根据你的场景（团队规模、用户量、硬件条件），选择合适的部署方案并完成部署。验证 API 调用正常，记录首次响应延迟。

---

## 参考答案

### 练习一

评估数据集要覆盖三类：常见问题（60%）、边界情况（20%）、容易出错的场景（20%）。评估集应该是独立收集的，不是训练集的子集——用训练集评估会虚高。

### 练习二

合并后用 `model.save_pretrained()` 保存，GGUF 用 `convert_hf_to_gguf.py` 转换。Q4_K_M 量化通常能把模型大小压缩到原来的 1/4，但精度损失在 2-5% 以内。先量化再评估，确认精度可接受再部署。

### 练习三

vLLM 适合高并发场景（支持 continuous batching），Ollama 适合单用户或小团队（部署最简单），GGUF + llama.cpp 适合资源受限环境（纯 CPU 也能跑）。不要一上来就选最复杂的方案——先用最简单的跑通，有性能瓶颈再升级。
