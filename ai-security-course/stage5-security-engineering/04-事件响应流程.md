# 04 - 事件响应流程

> AI 安全事件的响应与处置

## 课程信息

| 项目 | 内容 |
|------|------|
| 所属阶段 | Stage 5: AI 安全工程实践 |
| 前置课程 | 03-审计日志与监控 |
| 预计时长 | 2 小时 |
| 难度等级 | ⭐⭐⭐ 进阶 |

## 学习目标

1. 理解 AI 安全事件响应流程
2. 掌握事件分类和优先级
3. 学会制定应急响应计划
4. 掌握事件复盘方法
5. 建立持续改进机制
6. 理解自动化响应编排

## 1. 事件响应流程

### 1.1 响应流程框架

```
┌─────────────────────────────────────────────────────────────┐
│              AI 安全事件响应流程                              │
│                                                             │
│  Phase 1: 准备                                               │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  · 建立响应团队                                      │   │
│  │  · 制定响应计划                                      │   │
│  │  · 准备工具和资源                                    │   │
│  │  · 培训和演练                                        │   │
│  └─────────────────────────────────────────────────────┘   │
│                          ↓                                  │
│  Phase 2: 检测                                               │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  · 监控告警                                          │   │
│  │  · 异常检测                                          │   │
│  │  · 用户报告                                          │   │
│  │  · 自动发现                                          │   │
│  └─────────────────────────────────────────────────────┘   │
│                          ↓                                  │
│  Phase 3: 分析                                               │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  · 事件确认                                          │   │
│  │  · 影响评估                                          │   │
│  │  · 根因分析                                          │   │
│  │  · 证据收集                                          │   │
│  └─────────────────────────────────────────────────────┘   │
│                          ↓                                  │
│  Phase 4: 遏制                                               │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  · 短期遏制                                          │   │
│  │  · 长期修复                                          │   │
│  │  · 系统恢复                                          │   │
│  └─────────────────────────────────────────────────────┘   │
│                          ↓                                  │
│  Phase 5: 恢复                                               │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  · 服务恢复                                          │   │
│  │  · 验证测试                                          │   │
│  │  · 监控观察                                          │   │
│  └─────────────────────────────────────────────────────┘   │
│                          ↓                                  │
│  Phase 6: 复盘                                               │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  · 事件总结                                          │   │
│  │  · 根因分析                                          │   │
│  │  · 改进措施                                          │   │
│  │  · 文档更新                                          │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

### 1.2 AI 安全事件特有挑战

| 挑战 | 说明 | 应对策略 |
|------|------|----------|
| 攻击隐蔽性 | Prompt 注入可能伪装成正常输入 | 多层检测 + 行为分析 |
| 影响范围广 | 单个恶意 prompt 可能影响所有用户 | 隔离 + 快速回滚 |
| 证据易丢失 | LLM 对话记录可能被覆盖 | 实时日志 + 持久化 |
| 误判成本高 | 过度拦截影响用户体验 | 分级响应 + 白名单 |
| 模型黑盒 | 难以理解模型决策过程 | 输入输出审计 + 可解释性 |

## 2. 事件分类

### 2.1 事件严重等级

```python
from enum import Enum
from typing import Dict, List, Optional
from dataclasses import dataclass


class IncidentSeverity(Enum):
    P1_CRITICAL = 1    # 严重：系统不可用，数据泄露
    P2_HIGH = 2        # 高危：功能受损，安全漏洞
    P3_MEDIUM = 3      # 中等：性能下降，小范围影响
    P4_LOW = 4         # 低危：轻微问题，无实质影响


severity_definitions = {
    IncidentSeverity.P1_CRITICAL: {
        "name": "严重",
        "description": "系统完全不可用或发生数据泄露",
        "response_time": "15分钟",
        "escalation_time": "30分钟",
        "notify": ["CTO", "安全团队", "运维团队", "法务"],
        "examples": [
            "系统被入侵，数据泄露",
            "模型被投毒，输出严重错误",
            "服务完全不可用",
            "用户敏感数据通过 LLM 输出泄露",
        ],
    },
    IncidentSeverity.P2_HIGH: {
        "name": "高危",
        "description": "核心功能受损或存在安全漏洞",
        "response_time": "1小时",
        "escalation_time": "4小时",
        "notify": ["安全团队", "运维团队"],
        "examples": [
            "发现严重安全漏洞",
            "大量用户受影响",
            "关键功能不可用",
            "Prompt 注入绕过防护",
        ],
    },
    IncidentSeverity.P3_MEDIUM: {
        "name": "中等",
        "description": "性能下降或小范围影响",
        "response_time": "4小时",
        "escalation_time": "24小时",
        "notify": ["运维团队"],
        "examples": [
            "响应时间明显增加",
            "部分功能异常",
            "少量用户投诉",
            "内容过滤误报率升高",
        ],
    },
    IncidentSeverity.P4_LOW: {
        "name": "低危",
        "description": "轻微问题，无实质影响",
        "response_time": "24小时",
        "escalation_time": "72小时",
        "notify": ["值班人员"],
        "examples": [
            "界面显示问题",
            "非关键功能异常",
            "个别用户反馈",
            "日志格式错误",
        ],
    },
}
```

### 2.2 事件类型

```python
class IncidentType(Enum):
    PROMPT_INJECTION = "prompt_injection"
    DATA_LEAKAGE = "data_leakage"
    MODEL_COMPROMISE = "model_compromise"
    DOS_ATTACK = "dos_attack"
    UNAUTHORIZED_ACCESS = "unauthorized_access"
    CONTENT_SAFETY = "content_safety"
    SUPPLY_CHAIN = "supply_chain"
    INSIDER_THREAT = "insider_threat"


incident_playbooks = {
    IncidentType.PROMPT_INJECTION: {
        "name": "Prompt 注入攻击",
        "description": "攻击者通过注入恶意 prompt 操纵系统",
        "typical_severity": IncidentSeverity.P2_HIGH,
        "detection_signs": [
            "异常长的用户输入",
            "包含系统提示词关键词",
            "输出包含内部信息",
            "用户行为模式异常",
        ],
        "response_steps": [
            "1. 确认攻击范围和影响",
            "2. 阻断攻击来源 IP/账号",
            "3. 更新注入检测规则",
            "4. 审查受影响的对话记录",
            "5. 通知受影响用户",
            "6. 更新安全防护策略",
        ],
        "containment": [
            "临时禁用可疑账号",
            "启用更严格的输入过滤",
            "限制工具调用权限",
        ],
    },
    IncidentType.DATA_LEAKAGE: {
        "name": "数据泄露",
        "description": "敏感数据被非授权访问或泄露",
        "typical_severity": IncidentSeverity.P1_CRITICAL,
        "detection_signs": [
            "LLM 输出包含 PII",
            "异常的数据访问模式",
            "大量数据导出",
            "敏感 API 被频繁调用",
        ],
        "response_steps": [
            "1. 立即阻断泄露渠道",
            "2. 评估泄露范围和类型",
            "3. 保留证据和日志",
            "4. 通知相关方和用户",
            "5. 启动法律和合规流程",
            "6. 修复根本原因",
        ],
        "containment": [
            "关闭受影响的服务",
            "撤销泄露的凭证",
            "启用数据加密",
        ],
    },
    IncidentType.DOS_ATTACK: {
        "name": "拒绝服务攻击",
        "description": "通过大量请求使服务不可用",
        "typical_severity": IncidentSeverity.P2_HIGH,
        "detection_signs": [
            "请求量激增",
            "响应时间急剧增加",
            "服务可用性下降",
            "资源使用率飙升",
        ],
        "response_steps": [
            "1. 确认攻击来源",
            "2. 启用速率限制",
            "3. 配置 IP 黑名单",
            "4. 启用 CDN/WAF 防护",
            "5. 扩容应对流量",
            "6. 监控恢复情况",
        ],
        "containment": [
            "启用紧急速率限制",
            "阻断攻击 IP 段",
            "启用流量清洗",
        ],
    },
}
```

## 3. 事件管理系统

### 3.1 事件管理实现

```python
from typing import Dict, List, Optional
from dataclasses import dataclass, field
from datetime import datetime, timedelta
from enum import Enum
import uuid


class IncidentStatus(Enum):
    OPEN = "open"
    INVESTIGATING = "investigating"
    CONTAINING = "containing"
    REMEDIATING = "remediating"
    RESOLVED = "resolved"
    CLOSED = "closed"


@dataclass
class TimelineEntry:
    timestamp: datetime
    action: str
    actor: str
    details: str = ""


@dataclass
class Incident:
    incident_id: str
    title: str
    severity: IncidentSeverity
    incident_type: IncidentType
    status: IncidentStatus
    created_at: datetime
    updated_at: datetime
    assignee: Optional[str] = None
    description: str = ""
    timeline: List[TimelineEntry] = field(default_factory=list)
    resolution: str = ""
    root_cause: str = ""
    affected_users: int = 0
    tags: List[str] = field(default_factory=list)


class IncidentManager:
    """事件管理器"""

    def __init__(self):
        self.incidents: Dict[str, Incident] = {}
        self.escalation_rules: Dict[int, timedelta] = {
            1: timedelta(minutes=30),
            2: timedelta(hours=4),
            3: timedelta(hours=24),
            4: timedelta(hours=72),
        }

    def create_incident(self, title: str, severity: IncidentSeverity,
                        incident_type: IncidentType, description: str = "",
                        assignee: str = None, tags: List[str] = None) -> Incident:
        incident_id = f"INC-{uuid.uuid4().hex[:8].upper()}"
        now = datetime.now()

        incident = Incident(
            incident_id=incident_id,
            title=title,
            severity=severity,
            incident_type=incident_type,
            status=IncidentStatus.OPEN,
            created_at=now,
            updated_at=now,
            assignee=assignee,
            description=description,
            tags=tags or [],
        )

        self.incidents[incident_id] = incident
        self._add_timeline(incident, "system", "事件创建", f"严重等级: {severity.name}")
        return incident

    def update_status(self, incident_id: str, status: IncidentStatus,
                      actor: str = "system", notes: str = ""):
        if incident_id not in self.incidents:
            return
        incident = self.incidents[incident_id]
        old_status = incident.status
        incident.status = status
        incident.updated_at = datetime.now()
        self._add_timeline(incident, actor, f"状态变更: {old_status.value} -> {status.value}", notes)

    def assign_incident(self, incident_id: str, assignee: str, actor: str = "system"):
        if incident_id not in self.incidents:
            return
        incident = self.incidents[incident_id]
        incident.assignee = assignee
        incident.updated_at = datetime.now()
        self._add_timeline(incident, actor, f"分配给 {assignee}")

    def add_note(self, incident_id: str, note: str, actor: str = "system"):
        if incident_id not in self.incidents:
            return
        self._add_timeline(self.incidents[incident_id], actor, "添加备注", note)

    def resolve_incident(self, incident_id: str, resolution: str,
                         root_cause: str = "", actor: str = "system"):
        if incident_id not in self.incidents:
            return
        incident = self.incidents[incident_id]
        incident.status = IncidentStatus.RESOLVED
        incident.resolution = resolution
        incident.root_cause = root_cause
        incident.updated_at = datetime.now()
        self._add_timeline(incident, actor, "事件已解决", resolution)

    def _add_timeline(self, incident: Incident, actor: str, action: str, details: str = ""):
        incident.timeline.append(TimelineEntry(
            timestamp=datetime.now(),
            action=action,
            actor=actor,
            details=details,
        ))

    def get_open_incidents(self) -> List[Incident]:
        return [
            inc for inc in self.incidents.values()
            if inc.status not in [IncidentStatus.RESOLVED, IncidentStatus.CLOSED]
        ]

    def check_escalations(self) -> List[Dict]:
        escalations = []
        now = datetime.now()
        for incident in self.get_open_incidents():
            escalation_window = self.escalation_rules.get(incident.severity.value, timedelta(hours=24))
            if now - incident.created_at > escalation_window:
                escalations.append({
                    "incident_id": incident.incident_id,
                    "title": incident.title,
                    "severity": incident.severity.name,
                    "age_hours": (now - incident.created_at).total_seconds() / 3600,
                    "assignee": incident.assignee,
                })
        return escalations

    def generate_report(self, incident_id: str) -> str:
        incident = self.incidents.get(incident_id)
        if not incident:
            return "事件不存在"

        report = f"""# 事件报告: {incident.incident_id}

## 基本信息
- 标题: {incident.title}
- 严重程度: {incident.severity.name}
- 类型: {incident.incident_type.value}
- 状态: {incident.status.value}
- 创建时间: {incident.created_at.isoformat()}
- 更新时间: {incident.updated_at.isoformat()}
- 负责人: {incident.assignee or '未分配'}
- 受影响用户: {incident.affected_users}
- 标签: {', '.join(incident.tags) if incident.tags else '无'}

## 描述
{incident.description}

## 时间线
"""
        for entry in incident.timeline:
            report += f"- [{entry.timestamp.isoformat()}] [{entry.actor}] {entry.action}"
            if entry.details:
                report += f": {entry.details}"
            report += "\n"

        if incident.resolution:
            report += f"\n## 解决方案\n{incident.resolution}\n"
        if incident.root_cause:
            report += f"\n## 根因分析\n{incident.root_cause}\n"

        return report

    def get_statistics(self) -> Dict:
        total = len(self.incidents)
        by_severity = {}
        by_status = {}
        by_type = {}

        for inc in self.incidents.values():
            by_severity[inc.severity.name] = by_severity.get(inc.severity.name, 0) + 1
            by_status[inc.status.value] = by_status.get(inc.status.value, 0) + 1
            by_type[inc.incident_type.value] = by_type.get(inc.incident_type.value, 0) + 1

        return {
            "total": total,
            "by_severity": by_severity,
            "by_status": by_status,
            "by_type": by_type,
        }
```

## 4. 自动化响应编排

### 4.1 响应剧本

```
┌─────────────────────────────────────────────────────────────┐
│              自动化响应编排流程                               │
│                                                             │
│  ┌──────────┐                                               │
│  │ 告警触发  │                                               │
│  └────┬─────┘                                               │
│       │                                                       │
│       ▼                                                       │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ 事件分类                                              │   │
│  │  · Prompt 注入 → 剧本 A                               │   │
│  │  · 数据泄露   → 剧本 B                               │   │
│  │  · DoS 攻击   → 剧本 C                               │   │
│  └────┬─────────────────────────────────────────────────┘   │
│       │                                                       │
│       ▼                                                       │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ 自动执行                                              │   │
│  │  · 收集证据                                           │   │
│  │  · 遏制威胁                                           │   │
│  │  · 通知相关人员                                       │   │
│  │  · 创建事件工单                                       │   │
│  └────┬─────────────────────────────────────────────────┘   │
│       │                                                       │
│       ▼                                                       │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ 人工介入                                              │   │
│  │  · 确认自动操作                                       │   │
│  │  · 深入调查                                           │   │
│  │  · 制定长期修复方案                                   │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

```python
from typing import Dict, List, Callable, Any
from dataclasses import dataclass
from datetime import datetime
from enum import Enum


class ResponseAction(Enum):
    BLOCK_IP = "block_ip"
    DISABLE_USER = "disable_user"
    ENABLE_STRICT_FILTER = "enable_strict_filter"
    COLLECT_EVIDENCE = "collect_evidence"
    NOTIFY_TEAM = "notify_team"
    CREATE_INCIDENT = "create_incident"
    RATE_LIMIT = "rate_limit"
    ROLLBACK_MODEL = "rollback_model"


@dataclass
class PlaybookStep:
    action: ResponseAction
    params: Dict[str, Any]
    auto_execute: bool = True
    requires_approval: bool = False
    description: str = ""


class ResponseOrchestrator:
    """响应编排器"""

    def __init__(self):
        self.playbooks: Dict[str, List[PlaybookStep]] = {}
        self.action_handlers: Dict[ResponseAction, Callable] = {}
        self.execution_log: List[Dict] = []

    def register_handler(self, action: ResponseAction, handler: Callable):
        self.action_handlers[action] = handler

    def create_playbook(self, name: str, steps: List[PlaybookStep]):
        self.playbooks[name] = steps

    def execute_playbook(self, playbook_name: str, context: Dict) -> Dict:
        steps = self.playbooks.get(playbook_name, [])
        results = []

        for step in steps:
            if step.requires_approval and not context.get("approved"):
                results.append({
                    "action": step.action.value,
                    "status": "pending_approval",
                    "description": step.description,
                })
                continue

            handler = self.action_handlers.get(step.action)
            if handler:
                try:
                    result = handler(step.params, context)
                    results.append({
                        "action": step.action.value,
                        "status": "success",
                        "result": result,
                    })
                except Exception as e:
                    results.append({
                        "action": step.action.value,
                        "status": "failed",
                        "error": str(e),
                    })
            else:
                results.append({
                    "action": step.action.value,
                    "status": "no_handler",
                })

            self.execution_log.append({
                "playbook": playbook_name,
                "action": step.action.value,
                "timestamp": datetime.now().isoformat(),
                "status": results[-1]["status"],
            })

        return {
            "playbook": playbook_name,
            "total_steps": len(steps),
            "executed": len([r for r in results if r["status"] != "pending_approval"]),
            "results": results,
        }


# 使用示例
orchestrator = ResponseOrchestrator()

# 注册处理函数
orchestrator.register_handler(ResponseAction.BLOCK_IP, lambda params, ctx: f"Blocked IP: {params.get('ip')}")
orchestrator.register_handler(ResponseAction.NOTIFY_TEAM, lambda params, ctx: f"Notified: {params.get('team')}")
orchestrator.register_handler(ResponseAction.COLLECT_EVIDENCE, lambda params, ctx: "Evidence collected")
orchestrator.register_handler(ResponseAction.ENABLE_STRICT_FILTER, lambda params, ctx: "Strict filter enabled")

# 创建 Prompt 注入响应剧本
orchestrator.create_playbook("prompt_injection_response", [
    PlaybookStep(
        action=ResponseAction.COLLECT_EVIDENCE,
        params={"scope": "recent_logs", "minutes": 30},
        description="收集最近 30 分钟的日志",
    ),
    PlaybookStep(
        action=ResponseAction.ENABLE_STRICT_FILTER,
        params={"filter_level": "high"},
        description="启用严格输入过滤",
    ),
    PlaybookStep(
        action=ResponseAction.BLOCK_IP,
        params={"ip": "{{source_ip}}", "duration": 3600},
        description="封锁攻击源 IP 1 小时",
    ),
    PlaybookStep(
        action=ResponseAction.NOTIFY_TEAM,
        params={"team": "security", "channel": "slack"},
        description="通知安全团队",
    ),
])

# 执行剧本
result = orchestrator.execute_playbook("prompt_injection_response", {
    "source_ip": "192.168.1.100",
    "incident_id": "INC-001",
})
print(f"Execution result: {result}")
```

## 5. 事件复盘方法

### 5.1 复盘模板

```
┌─────────────────────────────────────────────────────────────┐
│              事件复盘模板                                     │
│                                                             │
│  1. 事件概要                                                 │
│     · 事件标题                                               │
│     · 发生时间                                               │
│     · 持续时长                                               │
│     · 严重等级                                               │
│     · 影响范围                                               │
│                                                             │
│  2. 时间线还原                                               │
│     · 发现时间                                               │
│     · 响应时间                                               │
│     · 遏制时间                                               │
│     · 恢复时间                                               │
│                                                             │
│  3. 根因分析                                                 │
│     · 直接原因                                               │
│     · 根本原因                                               │
│     · 贡献因素                                               │
│                                                             │
│  4. 响应评估                                                 │
│     · 做得好的                                               │
│     · 需要改进的                                             │
│     · 遗漏的                                                 │
│                                                             │
│  5. 改进措施                                                 │
│     · 短期措施 (1 周内)                                      │
│     · 中期措施 (1 月内)                                      │
│     · 长期措施 (1 季度内)                                    │
└─────────────────────────────────────────────────────────────┘
```

```python
from typing import Dict, List
from dataclasses import dataclass, field
from datetime import datetime


@dataclass
class PostMortem:
    incident_id: str
    title: str
    summary: str
    duration_minutes: int
    severity: str
    impact: str
    discovery_time: datetime
    response_time: datetime
    containment_time: datetime
    recovery_time: datetime
    direct_cause: str
    root_cause: str
    contributing_factors: List[str]
    what_went_well: List[str]
    what_to_improve: List[str]
    missed_items: List[str]
    short_term_actions: List[str]
    mid_term_actions: List[str]
    long_term_actions: List[str]
    created_at: datetime = field(default_factory=datetime.now)


class PostMortemManager:
    """复盘管理器"""

    def __init__(self):
        self.post_mortems: Dict[str, PostMortem] = {}

    def create_post_mortem(self, pm: PostMortem):
        self.post_mortems[pm.incident_id] = pm

    def generate_report(self, incident_id: str) -> str:
        pm = self.post_mortems.get(incident_id)
        if not pm:
            return "复盘记录不存在"

        mttr = (pm.recovery_time - pm.discovery_time).total_seconds() / 60

        return f"""# 事件复盘报告: {pm.incident_id}

## 1. 事件概要
- **标题**: {pm.title}
- **摘要**: {pm.summary}
- **持续时长**: {pm.duration_minutes} 分钟
- **严重等级**: {pm.severity}
- **影响范围**: {pm.impact}

## 2. 响应时间线
- 发现时间: {pm.discovery_time.isoformat()}
- 响应时间: {pm.response_time.isoformat()}
- 遏制时间: {pm.containment_time.isoformat()}
- 恢复时间: {pm.recovery_time.isoformat()}
- **MTTR (平均恢复时间)**: {mttr:.1f} 分钟

## 3. 根因分析
- **直接原因**: {pm.direct_cause}
- **根本原因**: {pm.root_cause}
- **贡献因素**: {', '.join(pm.contributing_factors)}

## 4. 响应评估
### 做得好
{chr(10).join(f'- {item}' for item in pm.what_went_well)}

### 需要改进
{chr(10).join(f'- {item}' for item in pm.what_to_improve)}

### 遗漏项
{chr(10).join(f'- {item}' for item in pm.missed_items)}

## 5. 改进措施
### 短期 (1 周内)
{chr(10).join(f'- {item}' for item in pm.short_term_actions)}

### 中期 (1 月内)
{chr(10).join(f'- {item}' for item in pm.mid_term_actions)}

### 长期 (1 季度内)
{chr(10).join(f'- {item}' for item in pm.long_term_actions)}
"""
```

## 6. 常见误区

1. **"事件响应只是技术问题"**: 需要跨部门协作，包括法务、公关、管理层
2. **"只关注修复"**: 需要根因分析和改进，否则同类事件会重复发生
3. **"没有预案"**: 需要提前准备应急计划，临时应对效率低
4. **"不记录不复盘"**: 需要完整的文档和复盘，知识需要沉淀
5. **"响应一次就够"**: 需要持续演练和改进，威胁在不断演变
6. **"自动化完全替代人工"**: 自动化是辅助，关键决策仍需人工
7. **"只关注外部攻击"**: 内部威胁同样严重，需要全面监控
8. **"复盘追责"**: 复盘应聚焦改进而非追责，否则团队会隐瞒问题

## 总结

- 事件响应是安全运营的核心能力，需要系统性的流程和工具支持
- 事件分类和优先级管理很重要，决定了响应速度和资源分配
- 自动化响应可以大幅提升效率，但关键节点仍需人工确认
- 复盘和改进是提升响应能力的关键，每次事件都是学习机会

## 练习

### 练习 1: 响应计划
为常见 AI 安全事件（Prompt 注入、数据泄露、DoS 攻击）制定应急响应计划。

### 练习 2: 事件管理系统
实现一个事件管理系统，支持事件创建、状态流转、时间线记录和报告生成。

### 练习 3: 桌面演练
进行一次安全事件桌面演练，测试响应流程，记录发现的问题。

### 练习 4: 自动化剧本
实现一个自动化响应编排器，支持多步骤剧本执行和人工审批节点。


---

**下一课**: [05 - AI 安全工具链](./05-AI安全工具链.md)
