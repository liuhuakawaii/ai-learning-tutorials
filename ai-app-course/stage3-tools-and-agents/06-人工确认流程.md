# 第6课：人工确认——高风险工具的确认流程

> **课程定位**：设计安全的操作确认机制
> **前置知识**：第 5 课的 Agent 状态机
> **预计时长**：35 分钟

---

## 学习目标

完成本课学习后，你将能够：

1. 识别需要人工确认的场景
2. 设计确认流程
3. 实现确认 UI
4. 处理确认超时

---

## 一、为什么需要人工确认

### 1.1 高风险操作

```
┌─────────────────────────────────────────────────────────────────┐
│                    需要人工确认的操作                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  写入类操作                                                      │
│  ├── 发送邮件                                                    │
│  ├── 发送消息                                                    │
│  ├── 提交表单                                                    │
│  └── 创建记录                                                    │
│                                                                 │
│  删除类操作                                                      │
│  ├── 删除文件                                                    │
│  ├── 删除记录                                                    │
│  └── 清空数据                                                    │
│                                                                 │
│  支付类操作                                                      │
│  ├── 发起支付                                                    │
│  ├── 转账                                                        │
│  └── 订阅服务                                                    │
│                                                                 │
│  权限类操作                                                      │
│  ├── 修改用户权限                                                │
│  ├── 共享敏感数据                                                │
│  └── 授权第三方                                                  │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 1.2 不确认的风险

```
如果 AI 自动执行高风险操作：

  场景 1：用户说"帮我给张三发封邮件"
    AI 理解错误，发给了李四
    → 信息泄露

  场景 2：用户说"清理一下旧文件"
    AI 删除了重要文件
    → 数据丢失

  场景 3：用户说"订阅这个服务"
    AI 订阅了错误的套餐
    → 财务损失

  人工确认 = 最后一道安全防线
```

---

## 二、确认流程设计

### 2.1 确认流程

```
┌─────────────────────────────────────────────────────────────────┐
│                    人工确认流程                                   │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  1. Agent 决定调用高风险工具                                     │
│     │                                                           │
│     ▼                                                           │
│  2. 暂停执行，进入 BLOCKED 状态                                  │
│     │                                                           │
│     ▼                                                           │
│  3. 前端显示确认对话框                                           │
│     ├── 操作描述                                                 │
│     ├── 参数详情                                                 │
│     ├── 风险提示                                                 │
│     └── 确认/取消按钮                                            │
│     │                                                           │
│     ▼                                                           │
│  4. 用户选择                                                    │
│     ├── 确认 → 继续执行                                         │
│     ├── 取消 → 终止任务                                         │
│     └── 修改 → 修改参数后执行                                   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 确认数据结构

```typescript
interface ConfirmationRequest {
  id: string
  toolName: string
  toolDescription: string
  args: Record<string, any>
  riskLevel: 'low' | 'medium' | 'high'
  riskDescription: string
  timeout?: number  // 超时时间（毫秒）
  createdAt: Date
}

interface ConfirmationResponse {
  confirmed: boolean
  modifiedArgs?: Record<string, any>  // 用户修改后的参数
  reason?: string  // 取消原因
}
```

---

## 三、实现确认机制

### 3.1 工具注册确认

```typescript
// lib/tool-registry.ts
interface ToolDefinition {
  name: string
  description: string
  schema: z.ZodType
  handler: (args: any) => Promise<ToolResult>
  requiresConfirmation?: boolean
  riskLevel?: 'low' | 'medium' | 'high'
  confirmationMessage?: (args: any) => string
}

// 注册需要确认的工具
toolRegistry.register({
  name: 'send_email',
  description: '发送邮件',
  schema: EmailSchema,
  requiresConfirmation: true,
  riskLevel: 'high',
  confirmationMessage: (args) => `确定要发送邮件给 ${args.to} 吗？`,
  handler: async (args) => {
    // 发送逻辑
  },
})
```

### 3.2 确认管理器

```typescript
// lib/confirmation-manager.ts
class ConfirmationManager {
  private pending = new Map<string, {
    resolve: (response: ConfirmationResponse) => void
    timeout: NodeJS.Timeout
  }>()

  async requestConfirmation(
    request: ConfirmationRequest
  ): Promise<ConfirmationResponse> {
    return new Promise((resolve, reject) => {
      // 设置超时
      const timeout = setTimeout(() => {
        this.pending.delete(request.id)
        resolve({ confirmed: false, reason: '确认超时' })
      }, request.timeout || 60000)  // 默认 60 秒

      this.pending.set(request.id, { resolve, timeout })

      // 觉醒前端显示确认对话框
      this.notifyFrontend(request)
    })
  }

  respond(id: string, response: ConfirmationResponse) {
    const pending = this.pending.get(id)
    if (!pending) return

    clearTimeout(pending.timeout)
    this.pending.delete(id)
    pending.resolve(response)
  }

  private notifyFrontend(request: ConfirmationRequest) {
    // 通过 WebSocket 或 SSE 通知前端
    // 实际实现取决于你的架构
  }
}

export const confirmationManager = new ConfirmationManager()
```

### 3.3 在 Agent 中集成确认

```typescript
// 在执行工具前检查是否需要确认
async function executeTool(
  toolName: string,
  args: any,
  agentId: string
): Promise<ToolResult> {
  const tool = toolRegistry.get(toolName)
  if (!tool) {
    return { success: false, error: `工具 ${toolName} 不存在` }
  }

  // 检查是否需要确认
  if (tool.requiresConfirmation) {
    const confirmationId = `confirm-${Date.now()}`

    const response = await confirmationManager.requestConfirmation({
      id: confirmationId,
      toolName: tool.name,
      toolDescription: tool.description,
      args,
      riskLevel: tool.riskLevel || 'medium',
      riskDescription: tool.confirmationMessage?.(args) || `确定要执行 ${toolName} 吗？`,
    })

    if (!response.confirmed) {
      return {
        success: false,
        error: `用户取消了操作: ${response.reason || '用户拒绝'}`,
      }
    }

    // 如果用户修改了参数
    if (response.modifiedArgs) {
      args = { ...args, ...response.modifiedArgs }
    }
  }

  // 执行工具
  return await toolRegistry.execute(toolName, args)
}
```

---

## 四、前端确认 UI

### 4.1 确认对话框组件

```typescript
// components/ConfirmationDialog.tsx
'use client'

import { useState } from 'react'

interface ConfirmationRequest {
  id: string
  toolName: string
  toolDescription: string
  args: Record<string, any>
  riskLevel: 'low' | 'medium' | 'high'
  riskDescription: string
}

interface Props {
  request: ConfirmationRequest
  onConfirm: (modifiedArgs?: Record<string, any>) => void
  onCancel: (reason?: string) => void
}

export function ConfirmationDialog({ request, onConfirm, onCancel }: Props) {
  const [showDetails, setShowDetails] = useState(false)
  const [cancelReason, setCancelReason] = useState('')

  const riskColors = {
    low: 'green',
    medium: 'yellow',
    high: 'red',
  }

  return (
    <div className="confirmation-overlay">
      <div className="confirmation-dialog">
        <div className="header">
          <span className={`risk-badge ${riskColors[request.riskLevel]}`}>
            {request.riskLevel === 'high' ? '⚠️ 高风险' :
             request.riskLevel === 'medium' ? '⚡ 中风险' : '✅ 低风险'}
          </span>
          <h3>操作确认</h3>
        </div>

        <div className="content">
          <p className="description">{request.riskDescription}</p>

          <button
            className="toggle-details"
            onClick={() => setShowDetails(!showDetails)}
          >
            {showDetails ? '隐藏详情' : '查看详情'}
          </button>

          {showDetails && (
            <div className="details">
              <p><strong>工具:</strong> {request.toolName}</p>
              <p><strong>参数:</strong></p>
              <pre>{JSON.stringify(request.args, null, 2)}</pre>
            </div>
          )}
        </div>

        <div className="actions">
          <button className="cancel" onClick={() => onCancel(cancelReason)}>
            取消
          </button>
          <button className="confirm" onClick={() => onConfirm()}>
            确认执行
          </button>
        </div>
      </div>
    </div>
  )
}
```

### 4.2 使用确认对话框

```typescript
// 在聊天组件中使用
function ChatWithConfirmation() {
  const [confirmationRequest, setConfirmationRequest] = useState<ConfirmationRequest | null>(null)

  // 监听确认请求
  useEffect(() => {
    const eventSource = new EventSource('/api/confirmation/events')

    eventSource.onmessage = (event) => {
      const request = JSON.parse(event.data)
      setConfirmationRequest(request)
    }

    return () => eventSource.close()
  }, [])

  const handleConfirm = async (modifiedArgs?: Record<string, any>) => {
    if (!confirmationRequest) return

    await fetch('/api/confirmation/respond', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        id: confirmationRequest.id,
        confirmed: true,
        modifiedArgs,
      }),
    })

    setConfirmationRequest(null)
  }

  const handleCancel = async (reason?: string) => {
    if (!confirmationRequest) return

    await fetch('/api/confirmation/respond', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        id: confirmationRequest.id,
        confirmed: false,
        reason,
      }),
    })

    setConfirmationRequest(null)
  }

  return (
    <div>
      {/* 聊天界面 */}

      {confirmationRequest && (
        <ConfirmationDialog
          request={confirmationRequest}
          onConfirm={handleConfirm}
          onCancel={handleCancel}
        />
      )}
    </div>
  )
}
```

---

## 五、确认超时处理

```typescript
// 超时后的处理策略
enum TimeoutStrategy {
  CANCEL = 'cancel',      // 自动取消
  PROCEED = 'proceed',    // 自动继续
  NOTIFY = 'notify',      // 通知用户
}

interface TimeoutConfig {
  strategy: TimeoutStrategy
  timeoutMs: number
  reminderMs?: number  // 提前提醒时间
}

// 默认超时配置
const DEFAULT_TIMEOUT: TimeoutConfig = {
  strategy: TimeoutStrategy.CANCEL,
  timeoutMs: 60000,  // 60 秒
  reminderMs: 45000,  // 45 秒时提醒
}
```

---

## 动手练习

### 练习一：识别高风险操作

列出你的应用中需要人工确认的操作：
1. 哪些是写入操作？
2. 哪些是删除操作？
3. 哪些涉及敏感数据？

### 练习二：实现确认流程

1. 注册一个需要确认的工具
2. 实现确认管理器
3. 实现前端确认对话框

### 练习三：测试超时

1. 设置 10 秒超时
2. 不点击确认
3. 验证超时后的行为

---

## 小结

本课的核心要点：

1. **高风险操作需要确认**：写入、删除、支付、权限操作
2. **确认流程**：暂停 → 显示 → 确认/取消 → 继续/终止
3. **确认管理器**：管理待确认的请求
4. **前端确认 UI**：清晰的风险提示和操作详情
5. **超时处理**：避免无限等待

---

## 下一课预告

下一课是阶段实战：我们将构建一个完整的 AI 研究助手，支持制定研究计划、检索资料、整理摘要、生成报告，并在保存文件前请求用户确认。
