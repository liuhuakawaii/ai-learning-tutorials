# 第3课：自动评估——规则评估、模型评估、人工抽检

> **课程定位**：实现自动化的质量评估
> **前置知识**：第 2 课的评估集
> **预计时长**：45 分钟

---

## 学习目标

完成本课学习后，你将能够：

1. 实现基于规则的自动评估
2. 实现基于模型的自动评估
3. 设计人工抽检流程
4. 组合多种评估方式

---

## 一、三种评估方式

```
┌─────────────────────────────────────────────────────────────────┐
│                    三种评估方式                                   │
├───────────────┬──────────────────────┬──────────────────────────┤
│  方式          │  优点                │  缺点                    │
├───────────────┼──────────────────────┼──────────────────────────┤
│  规则评估      │  快速、确定性        │  只能检查简单条件         │
│  模型评估      │  能理解语义          │  有成本、可能不准确       │
│  人工抽检      │  最准确              │  慢、贵、不可规模化       │
└───────────────┴──────────────────────┴──────────────────────────┘

推荐：规则评估 + 模型评估 + 人工抽检 组合使用
```

---

## 二、规则评估

### 2.1 基于规则的检查

```typescript
// lib/evals/rules.ts
interface RuleResult {
  passed: boolean
  rule: string
  details?: string
}

export function applyRules(
  testCase: EvalCase,
  response: { answer: string; citations: string[] }
): RuleResult[] {
  const results: RuleResult[] = []

  // 规则 1：拒答场景必须拒答
  if (testCase.expectedBehavior === 'refuse') {
    const refused = checkRefusal(response.answer)
    results.push({
      passed: refused,
      rule: '拒答检查',
      details: refused ? '正确拒答' : '应该拒答但没有',
    })
  }

  // 规则 2：必须有引用
  if (testCase.expectedCitations && testCase.expectedCitations.length > 0) {
    const hasCitations = response.citations.length > 0
    results.push({
      passed: hasCitations,
      rule: '引用检查',
      details: hasCitations ? '包含引用' : '缺少引用',
    })
  }

  // 规则 3：回答长度检查
  const minLength = 20
  const maxLength = 2000
  const lengthOk = response.answer.length >= minLength && response.answer.length <= maxLength
  results.push({
    passed: lengthOk,
    rule: '长度检查',
    details: `回答长度 ${response.answer.length}，要求 ${minLength}-${maxLength}`,
  })

  // 规则 4：不能包含敏感信息
  const hasSensitive = checkSensitiveInfo(response.answer)
  results.push({
    passed: !hasSensitive,
    rule: '敏感信息检查',
    details: hasSensitive ? '包含敏感信息' : '无敏感信息',
  })

  // 规则 5：不能包含系统 prompt
  const leaksPrompt = checkPromptLeak(response.answer)
  results.push({
    passed: !leaksPrompt,
    rule: 'Prompt 泄露检查',
    details: leaksPrompt ? '疑似泄露系统指令' : '未泄露',
  })

  return results
}

function checkRefusal(answer: string): boolean {
  const refusalPatterns = [
    /无法(找到|回答|确定)/,
    /资料中(没有|未找到)/,
    /抱歉.*无法/,
    /根据现有.*无法/,
  ]
  return refusalPatterns.some(p => p.test(answer))
}

function checkSensitiveInfo(text: string): boolean {
  const patterns = [
    /1[3-9]\d{9}/,      // 手机号
    /\d{17}[\dXx]/,     // 身份证
    /[\w.-]+@[\w.-]+/,  // 邮箱
  ]
  return patterns.some(p => p.test(text))
}

function checkPromptLeak(answer: string): boolean {
  const patterns = [
    /system prompt/i,
    /系统指令/,
    /你是一个.*助手/,
  ]
  return patterns.some(p => p.test(answer))
}
```

---

## 三、模型评估

### 3.1 使用模型评分

```typescript
// lib/evals/model-eval.ts
import OpenAI from 'openai'

const openai = new OpenAI()

interface ModelEvalResult {
  relevancy: number
  faithfulness: number
  completeness: number
  reasoning: string
}

export async function evaluateWithModel(
  question: string,
  context: string,
  answer: string
): Promise<ModelEvalResult> {
  const response = await openai.chat.completions.create({
    model: 'gpt-4o-mini',
    messages: [
      {
        role: 'system',
        content: `你是一个 AI 回答质量评估专家。请评估以下回答的质量。

评估维度：
1. relevancy（0-1）：回答是否与问题相关
2. faithfulness（0-1）：回答是否基于上下文，没有编造
3. completeness（0-1）：回答是否完整

输出 JSON 格式。`,
      },
      {
        role: 'user',
        content: `问题：${question}

参考上下文：
${context}

AI 回答：
${answer}

请评估并输出 JSON：
{
  "relevancy": 0-1,
  "faithfulness": 0-1,
  "completeness": 0-1,
  "reasoning": "评估理由"
}`,
      },
    ],
    response_format: { type: 'json_object' },
    temperature: 0,
  })

  return JSON.parse(response.choices[0].message.content!)
}
```

### 3.2 批量评估

```typescript
export async function batchEvaluate(
  cases: EvalCase[],
  responses: { answer: string; citations: string[] }[]
): Promise<ModelEvalResult[]> {
  const results: ModelEvalResult[] = []

  // 并发评估（限制并发数）
  const concurrency = 5
  for (let i = 0; i < cases.length; i += concurrency) {
    const batch = cases.slice(i, i + concurrency)
    const batchResponses = responses.slice(i, i + concurrency)

    const batchResults = await Promise.all(
      batch.map((c, j) =>
        evaluateWithModel(c.question, c.context || '', batchResponses[j].answer)
      )
    )

    results.push(...batchResults)
  }

  return results
}
```

---

## 四、人工抽检

### 4.1 抽检流程

```
┌─────────────────────────────────────────────────────────────────┐
│                    人工抽检流程                                   │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  1. 选择样本                                                     │
│     ├── 随机抽样                                                 │
│     ├── 重点关注低分样本                                         │
│     └── 关注失败的评估用例                                       │
│                                                                 │
│  2. 人工评估                                                     │
│     ├── 阅读问题和上下文                                         │
│     ├── 阅读 AI 回答                                             │
│     └── 给出评分和反馈                                           │
│                                                                 │
│  3. 记录结果                                                     │
│     ├── 评分（1-5）                                              │
│     ├── 是否通过                                                 │
│     └── 改进建议                                                 │
│                                                                 │
│  4. 汇总分析                                                     │
│     ├── 统计通过率                                               │
│     ├── 分析常见问题                                             │
│     └── 制定改进计划                                             │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 4.2 人工评估界面

```typescript
// components/HumanEval.tsx
'use client'

import { useState } from 'react'

interface EvalSample {
  id: string
  question: string
  context: string
  answer: string
  citations: string[]
}

export function HumanEval({ sample }: { sample: EvalSample }) {
  const [score, setScore] = useState<number | null>(null)
  const [passed, setPassed] = useState<boolean | null>(null)
  const [feedback, setFeedback] = useState('')

  const handleSubmit = async () => {
    await fetch('/api/evals/human', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        sampleId: sample.id,
        score,
        passed,
        feedback,
      }),
    })
  }

  return (
    <div className="human-eval">
      <h3>人工评估</h3>

      <div className="sample">
        <div className="question">
          <h4>问题</h4>
          <p>{sample.question}</p>
        </div>

        <div className="context">
          <h4>参考上下文</h4>
          <p>{sample.context}</p>
        </div>

        <div className="answer">
          <h4>AI 回答</h4>
          <p>{sample.answer}</p>
        </div>

        {sample.citations.length > 0 && (
          <div className="citations">
            <h4>引用</h4>
            <ul>
              {sample.citations.map((c, i) => <li key={i}>{c}</li>)}
            </ul>
          </div>
        )}
      </div>

      <div className="eval-form">
        <div className="score-input">
          <label>评分 (1-5)</label>
          <div className="score-buttons">
            {[1, 2, 3, 4, 5].map(s => (
              <button
                key={s}
                className={score === s ? 'selected' : ''}
                onClick={() => setScore(s)}
              >
                {s}
              </button>
            ))}
          </div>
        </div>

        <div className="pass-input">
          <label>是否通过</label>
          <div>
            <button
              className={passed === true ? 'selected' : ''}
              onClick={() => setPassed(true)}
            >
              通过
            </button>
            <button
              className={passed === false ? 'selected' : ''}
              onClick={() => setPassed(false)}
            >
              不通过
            </button>
          </div>
        </div>

        <div className="feedback-input">
          <label>反馈</label>
          <textarea
            value={feedback}
            onChange={e => setFeedback(e.target.value)}
            placeholder="输入改进建议..."
          />
        </div>

        <button onClick={handleSubmit} disabled={score === null || passed === null}>
          提交评估
        </button>
      </div>
    </div>
  )
}
```

---

## 五、组合评估策略

```typescript
// lib/evals/combined.ts
export async function combinedEval(
  testCase: EvalCase,
  response: { answer: string; citations: string[] }
) {
  // 1. 规则评估（快速、无成本）
  const ruleResults = applyRules(testCase, response)
  const rulesPassed = ruleResults.every(r => r.passed)

  if (!rulesPassed) {
    return {
      passed: false,
      method: 'rules',
      details: ruleResults,
    }
  }

  // 2. 模型评估（语义理解）
  const modelEval = await evaluateWithModel(
    testCase.question,
    testCase.context || '',
    response.answer
  )

  const modelPassed = modelEval.relevancy > 0.7 &&
                      modelEval.faithfulness > 0.8

  if (!modelPassed) {
    return {
      passed: false,
      method: 'model',
      scores: modelEval,
    }
  }

  // 3. 通过
  return {
    passed: true,
    method: 'combined',
    ruleResults,
    modelScores: modelEval,
  }
}
```

---

## 动手练习

### 练习一：实现规则评估

1. 定义 5 条评估规则
2. 实现规则检查函数
3. 测试规则评估

### 练习二：实现模型评估

1. 设计评估 Prompt
2. 实现模型评估函数
3. 对比规则和模型评估结果

### 练习三：设计抽检流程

1. 确定抽样策略
2. 设计评估界面
3. 记录评估结果

---

## 小结

本课的核心要点：

1. **规则评估**：快速、确定性，适合简单检查
2. **模型评估**：能理解语义，适合复杂判断
3. **人工抽检**：最准确，适合抽样验证
4. **组合使用**：规则 → 模型 → 人工，逐层筛选
5. **持续优化**：根据评估结果改进系统

---

## 下一课预告

下一课我们将学习 Prompt 版本管理：如何记录变更、回滚、灰度发布。
