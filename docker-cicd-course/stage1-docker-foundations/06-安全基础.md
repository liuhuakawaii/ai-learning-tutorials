# 第六课：安全基础

> **课程定位**：让镜像更安全，这是生产环境的基本要求
> **前置知识**：Dockerfile、多阶段构建（第 3-4 课）
> **预计时长**：25 分钟

---

## 学习目标

完成本课学习后，你将能够：

1. 实践非 root 用户运行容器
2. 知道常见的安全反模式
3. 了解镜像安全扫描
4. 掌握安全检查清单

---

## 一、非 root 用户运行

### 1.1 为什么需要非 root

```
容器默认以 root 运行，这有安全风险：

  以 root 运行的风险：
  ├── 容器逃逸后获得宿主机 root 权限
  ├── 可以修改系统文件
  ├── 可以安装恶意软件
  └── 可以访问其他容器的资源

  以非 root 运行：
  ├── 即使被攻破，权限有限
  ├── 无法修改系统文件
  ├── 限制了攻击的影响范围
  └── 符合最小权限原则
```

### 1.2 如何配置

```dockerfile
# ❌ 默认以 root 运行（危险）
FROM node:18-alpine
WORKDIR /app
COPY . .
CMD ["node", "src/index.js"]

# ✅ 创建专用用户（推荐）
FROM node:18-alpine

# 创建用户组和用户
RUN addgroup -g 1001 -S nodejs \
    && adduser -S nodeuser -u 1001 -G nodejs

WORKDIR /app

# 设置目录权限
COPY --chown=nodeuser:nodejs . .

# 切换到非 root 用户
USER nodeuser

CMD ["node", "src/index.js"]
```

### 1.3 验证

```bash
# 以默认（root）用户运行
docker run --rm node:18-alpine whoami
# 输出：root

# 以指定用户运行
docker run --rm --user 1001:1001 node:18-alpine whoami
# 输出：uid=1001

# 在 Dockerfile 中设置 USER 指令后
docker run --rm my-app whoami
# 输出：nodeuser
```

---

## 二、安全反模式

### 2.1 常见错误

```dockerfile
# ❌ 错误 1：在镜像中硬编码密钥
ENV DB_PASSWORD=secret123
ENV API_KEY=sk-1234567890

# ❌ 错误 2：使用 latest 标签
FROM node:latest

# ❌ 错误 3：以 root 运行（没有 USER 指令）

# ❌ 错误 4：安装不需要的工具
RUN apt-get install -y vim nano curl wget git

# ❌ 错误 5：COPY 整个目录包括 .env
COPY . .
```

### 2.2 正确做法

```dockerfile
# ✅ 正确 1：运行时传入环境变量
# docker run -e DB_PASSWORD=secret my-app

# ✅ 正确 2：使用固定版本
FROM node:18.17.0-alpine

# ✅ 正确 3：非 root 用户
USER nodeuser

# ✅ 正确 4：只安装必要工具
RUN apk add --no-cache curl

# ✅ 正确 5：使用 .dockerignore 排除 .env
```

### 2.3 真实案例：敏感 token 写入 Dockerfile

```
某个项目的 Dockerfile 中直接写死了 Sentry token：

  ENV SENTRY_AUTH_TOKEN="sntrys_eyJ..."

这个 token 会被：
  1. 写入镜像层，docker history 可以看到
  2. 推送到镜像仓库后，任何能拉取的人都能看到
  3. 即使后来删除，旧层中仍然保留

正确做法：
  - 构建时通过 --build-arg 传入（ARG 不会留在最终镜像）
  - 或通过 CI/CD 环境变量注入
  - 绝对不要写死在 Dockerfile 中
```

---

## 三、镜像安全扫描

### 3.1 为什么需要扫描

```
镜像中可能存在的安全漏洞：

  基础镜像中的系统库
  ├── openssl 可能有已知漏洞
  ├── curl 可能有已知漏洞
  └── 其他系统包

  应用依赖中的 npm 包
  ├── express 可能有已知漏洞
  ├── lodash 可能有已知漏洞
  └── 其他依赖
```

### 3.2 扫描工具

```bash
# 使用 Docker Scout 扫描（Docker Desktop 自带）
docker scout cves my-app:latest

# 使用 Trivy 扫描（开源，推荐）
trivy image my-app:latest

# 使用 Snyk 扫描
snyk container test my-app:latest
```

---

## 四、其他安全措施

### 4.1 只读文件系统

```bash
# 以只读模式运行容器
docker run --read-only my-app

# 如果需要写入临时文件，挂载 tmpfs
docker run --read-only --tmpfs /tmp my-app
```

### 4.2 限制资源

```bash
# 限制 CPU 和内存
docker run --memory=512m --cpus=1 my-app

# 限制重启次数
docker run --restart=on-failure:3 my-app
```

---

## 五、安全检查清单

```
Dockerfile 安全清单：

  基础镜像：
  ✅ 使用特定版本标签，不用 latest
  ✅ 优先选择 alpine 或 slim 版本
  ✅ 定期更新基础镜像以获取安全补丁

  构建：
  ✅ 使用多阶段构建
  ✅ 使用 .dockerignore 排除 .env 和敏感文件
  ✅ 不在镜像中硬编码密钥

  运行：
  ✅ 以非 root 用户运行
  ✅ 限制 CPU 和内存
  ✅ 设置重启策略
  ✅ 定期扫描镜像漏洞
```

---

## 六、动手练习

### 练习一：非 root 用户

```bash
# 1. 以默认（root）用户运行
docker run -it --rm node:18-alpine whoami

# 2. 以指定用户运行
docker run -it --rm --user 1001:1001 node:18-alpine whoami

# 3. 创建一个带 USER 指令的 Dockerfile，验证效果
```

### 练习二：安全扫描

```bash
# 1. 构建一个镜像
docker build -t security-test .

# 2. 使用 Docker Scout 扫描
docker scout cves security-test

# 3. 查看漏洞报告，了解有哪些问题
```

### 练习三：检查镜像内容

```bash
# 1. 构建镜像（不使用 .dockerignore）
docker build -t test:unsafe .

# 2. 检查镜像中是否有 .env
docker run -it test:unsafe cat /app/.env

# 3. 添加 .dockerignore 后重新构建
echo ".env" >> .dockerignore
docker build -t test:safe .

# 4. 验证 .env 不在镜像中
docker run -it test:safe cat /app/.env  # 应该报错
```

---

## 小结

1. **非 root 用户**：容器默认以 root 运行有安全风险，应该创建专用用户
2. **不硬编码密钥**：密钥通过运行时环境变量或 CI/CD 注入
3. **安全扫描**：定期用 Docker Scout / Trivy 扫描镜像漏洞
4. **.dockerignore**：排除 .env、密钥等敏感文件，防止泄露
5. **安全清单**：固定版本、非 root、排除敏感文件、限制资源

---

## 下一课预告

下一课是阶段实战——我们将把一个完整的 Node API 容器化，综合运用前六课学到的所有知识。
