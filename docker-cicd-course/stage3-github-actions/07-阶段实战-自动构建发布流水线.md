# 第七课：阶段实战——自动构建发布流水线

> **课程定位**：综合运用第三阶段所有知识，搭建完整的 CI/CD 流水线
> **前置知识**：GitHub Actions 全部课程（第 1-6 课）
> **预计时长**：60 分钟

---

## 学习目标

完成本课后，你将拥有一个完整的 CI/CD 流水线：

1. PR 自动运行 lint、test、build
2. 主分支合并后自动构建 Docker 镜像
3. 推送到 GitHub Container Registry
4. 可选的自动部署

---

## 一、项目结构

```
my-app/
├── .github/
│   └── workflows/
│       ├── ci.yml           # PR 检查
│       └── cd.yml           # 主分支部署
├── Dockerfile
├── docker-compose.yml
├── scripts/
│   ├── deploy.sh
│   └── rollback.sh
├── package.json
└── src/
```

---

## 二、CI Workflow（PR 检查）

```yaml
# .github/workflows/ci.yml
name: CI

on:
  pull_request:
    branches: [main]

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: npm
      - run: npm ci
      - run: npm run lint

  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: npm
      - run: npm ci
      - run: npm test

  build:
    name: Build
    runs-on: ubuntu-latest
    needs: [lint, test]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: npm
      - run: npm ci
      - run: npm run build
```

---

## 三、CD Workflow（自动构建发布）

```yaml
# .github/workflows/cd.yml
name: CD

on:
  push:
    branches: [main]
    tags: ['v*']

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: npm
      - run: npm ci
      - run: npm test

  build-and-push:
    name: Build and Push
    runs-on: ubuntu-latest
    needs: test
    permissions:
      contents: read
      packages: write
    outputs:
      image-tag: ${{ steps.meta.outputs.version }}
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      
      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=sha
      
      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
```

---

## 四、部署脚本

```bash
#!/bin/bash
# scripts/deploy.sh

set -e

VERSION=${1:-latest}
IMAGE="ghcr.io/yourusername/yourrepo:${VERSION}"

echo "Deploying version: ${VERSION}"

# 在服务器上执行
ssh deploy@server << EOF
  cd /opt/my-app
  
  # 更新 .env 中的镜像版本
  echo "IMAGE_TAG=${VERSION}" > .env.deploy
  
  # 拉取新镜像
  docker pull ${IMAGE}
  
  # 更新并重启
  docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d
  
  # 健康检查
  sleep 10
  curl -f http://localhost:3000/health || exit 1
  
  echo "Deployment successful!"
EOF
```

---

## 五、GitHub Secrets 配置

```
需要配置的 Secrets：

  Settings → Secrets and variables → Actions

  ┌──────────────────────┬──────────────────────────────┐
  │  Secret 名称          │  说明                        │
  ├──────────────────────┼──────────────────────────────┤
  │  SERVER_HOST         │  服务器 IP 或域名             │
  │  SERVER_USER         │  SSH 用户名                  │
  │  SERVER_SSH_KEY      │  SSH 私钥                    │
  └──────────────────────┴──────────────────────────────┘

  注意：GITHUB_TOKEN 自动提供，无需手动配置
```

---

## 六、验收清单

```
阶段三验收标准：

  ✅ PR 会自动跑检查
     提交 PR 后，GitHub Actions 自动运行 lint、test、build

  ✅ main 分支构建 Docker 镜像
     合并到 main 后，自动构建并推送到 GHCR

  ✅ 失败时能从日志定位问题
     GitHub Actions 提供详细的日志

  ✅ secrets 不写入仓库
     敏感信息通过 GitHub Secrets 管理
```

---

## 七、扩展练习

### 练习一：添加部署到 staging

```yaml
deploy-staging:
  needs: build-and-push
  runs-on: ubuntu-latest
  if: github.ref == 'refs/heads/main'
  environment: staging
  steps:
    - name: Deploy to staging
      run: |
        # 部署到测试环境
        echo "Deploying to staging..."
```

### 练习二：添加回滚 workflow

```yaml
# .github/workflows/rollback.yml
name: Rollback

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to rollback to'
        required: true

jobs:
  rollback:
    runs-on: ubuntu-latest
    steps:
      - name: Rollback
        run: |
          echo "Rolling back to version ${{ github.event.inputs.version }}"
```

---

## 小结

本课综合运用了第三阶段的所有知识：

1. **CI Workflow**：PR 自动检查 lint、test、build
2. **CD Workflow**：主分支自动构建和推送 Docker 镜像
3. **GitHub Container Registry**：免费的镜像仓库
4. **部署脚本**：自动化部署到服务器
5. **Secrets 管理**：敏感信息通过 GitHub Secrets 传入

你现在已经拥有一个完整的 CI/CD 流水线。下一阶段我们将学习云部署，把应用部署到实际的服务器上。
