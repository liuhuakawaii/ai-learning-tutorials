# 第七课：阶段实战——本地生产模拟环境

> **课程定位**：综合运用第二阶段所有知识，搭建完整的本地开发环境
> **前置知识**：Compose 全部课程（第 1-6 课）
> **预计时长**：60 分钟

---

## 学习目标

完成本课后，你将拥有一个完整的本地生产模拟环境，包含：

1. Web 前端（Next.js）
2. API 后端（Node.js）
3. PostgreSQL 数据库
4. Redis 缓存
5. Nginx 反向代理
6. 完整的健康检查和启动顺序

---

## 一、项目结构

```
fullstack-docker/
├── docker-compose.yml
├── docker-compose.prod.yml
├── .env.example
├── scripts/
│   ├── backup-db.sh
│   └── restore-db.sh
├── web/
│   ├── Dockerfile
│   ├── package.json
│   └── src/
├── api/
│   ├── Dockerfile
│   ├── package.json
│   └── src/
└── nginx/
    └── default.conf
```

---

## 二、docker-compose.yml

```yaml
services:
  # ---- 前端 ----
  web:
    build:
      context: ./web
      dockerfile: Dockerfile
    ports:
      - "3001:3000"
    environment:
      NEXT_PUBLIC_API_URL: http://localhost/api
    depends_on:
      api:
        condition: service_healthy
    networks:
      - app-network

  # ---- API ----
  api:
    build:
      context: ./api
      dockerfile: Dockerfile
    environment:
      DATABASE_URL: postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}
      REDIS_URL: redis://redis:6379
      NODE_ENV: ${NODE_ENV:-development}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - app-network
    restart: unless-stopped

  # ---- PostgreSQL ----
  postgres:
    image: postgres:14-alpine
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-mydb}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-secret}
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - app-network
    restart: unless-stopped

  # ---- Redis ----
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - app-network
    restart: unless-stopped

  # ---- Nginx 反向代理 ----
  nginx:
    image: nginx:1.25-alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      api:
        condition: service_healthy
    networks:
      - app-network
    restart: unless-stopped

networks:
  app-network:
    driver: bridge

volumes:
  pgdata:
  redis-data:
```

---

## 三、.env.example

```bash
# 数据库配置
POSTGRES_DB=mydb
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change_me_in_production

# 应用配置
NODE_ENV=development
```

---

## 四、Nginx 配置

```nginx
# nginx/default.conf
upstream api {
    server api:3000;
}

upstream web {
    server web:3000;
}

server {
    listen 80;
    server_name localhost;

    # API 请求
    location /api/ {
        proxy_pass http://api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 前端请求
    location / {
        proxy_pass http://web;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

---

## 五、备份脚本

```bash
#!/bin/bash
# scripts/backup-db.sh

BACKUP_DIR="./backups"
TIMESTAMP=$(date +%Y%m%d_%H%M%S)
BACKUP_FILE="${BACKUP_DIR}/backup_${TIMESTAMP}.sql"

mkdir -p "$BACKUP_DIR"

echo "Backing up PostgreSQL..."
docker compose exec -T postgres pg_dump -U postgres mydb > "$BACKUP_FILE"
gzip "$BACKUP_FILE"

# 删除 7 天前的备份
find "$BACKUP_DIR" -name "*.gz" -mtime +7 -delete

echo "Backup completed: ${BACKUP_FILE}.gz"
```

---

## 六、运行和验证

```bash
# 1. 复制环境配置
cp .env.example .env

# 2. 启动所有服务
docker compose up -d

# 3. 查看状态（等待所有服务 healthy）
docker compose ps

# 4. 测试访问
curl http://localhost          # 前端
curl http://localhost/api/health  # API

# 5. 查看日志
docker compose logs -f

# 6. 备份数据库
bash scripts/backup-db.sh

# 7. 停止
docker compose down

# 8. 停止并清理数据
docker compose down -v
```

---

## 七、验收清单

```
阶段二验收标准：

  ✅ docker compose up 一条命令启动
     所有服务自动启动，无需手动干预

  ✅ 数据库重启后数据不丢
     docker compose down && docker compose up -d
     数据仍然存在

  ✅ API 等数据库健康后再启动
     depends_on + condition: service_healthy

  ✅ .env.example 完整
     包含所有需要的环境变量和说明
```

---

## 小结

本课综合运用了第二阶段的所有知识：

1. **Compose 编排**：web、api、postgres、redis、nginx 五个服务
2. **网络**：自定义网络实现服务间通信
3. **Volume**：数据库和 Redis 数据持久化
4. **环境变量**：.env 文件管理配置
5. **健康检查**：确保服务按正确顺序启动
6. **备份脚本**：自动化数据库备份

你现在已经拥有一个完整的本地生产模拟环境。下一阶段我们将学习 CI/CD，实现自动化构建和部署。
