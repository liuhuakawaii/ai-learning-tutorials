# 第4课：告警规则

> **课程定位**：设计和实现数据产品告警系统
> **前置知识**：第3课（任务状态机）
> **预计时长**：45 分钟

---

## 学习目标

1. 设计告警规则
2. 实现告警触发
3. 配置告警通知

---

## 一、告警类型

```
┌──────────────────────────────────────────────────────────────┐
│                    告警类型                                    │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  任务告警                                                     │
│  ├── 任务失败                                                │
│  ├── 任务超时                                                │
│  └── 任务重试次数过多                                        │
│                                                              │
│  数据质量告警                                                 │
│  ├── 数据量异常（突然下降/上升）                             │
│  ├── 质量规则失败过多                                        │
│  └── 异常数据激增                                            │
│                                                              │
│  系统告警                                                     │
│  ├── 数据库连接失败                                          │
│  ├── API 响应超时                                            │
│  └── 磁盘空间不足                                            │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

---

## 二、告警规则实现

```python
from dataclasses import dataclass
from typing import Callable, Any

@dataclass
class AlertRule:
    """告警规则"""
    name: str
    condition: Callable[[dict], bool]
    severity: str  # info, warning, critical
    message_template: str

class AlertManager:
    """告警管理器"""
    
    def __init__(self):
        self.rules = []
        self.notifiers = []
    
    def add_rule(self, rule: AlertRule):
        self.rules.append(rule)
    
    def check(self, metrics: dict):
        """检查告警"""
        for rule in self.rules:
            if rule.condition(metrics):
                self.send_alert(rule, metrics)
    
    def send_alert(self, rule: AlertRule, metrics: dict):
        """发送告警"""
        message = rule.message_template.format(**metrics)
        for notifier in self.notifiers:
            notifier.send(message, rule.severity)

# 定义告警规则
alert_manager = AlertManager()

# 任务失败告警
alert_manager.add_rule(AlertRule(
    name="task_failed",
    condition=lambda m: m.get("task_status") == "failed",
    severity="critical",
    message_template="任务 {task_id} 执行失败: {error}"
))

# 数据量异常告警
alert_manager.add_rule(AlertRule(
    name="data_volume_anomaly",
    condition=lambda m: m.get("record_count", 0) < m.get("expected_count", 100) * 0.5,
    severity="warning",
    message_template="数据量异常: 期望 {expected_count}，实际 {record_count}"
))
```

---

## 三、告警通知

```python
class Notifier:
    """通知器基类"""
    def send(self, message: str, severity: str):
        raise NotImplementedError

class EmailNotifier(Notifier):
    """邮件通知"""
    def send(self, message: str, severity: str):
        # 发送邮件
        pass

class WebhookNotifier(Notifier):
    """Webhook 通知（钉钉、Slack）"""
    def send(self, message: str, severity: str):
        # 调用 webhook
        pass

# 注册通知器
alert_manager.notifiers.append(EmailNotifier())
alert_manager.notifiers.append(WebhookNotifier())
```

---

## 动手练习

### 练习一

设计告警规则，覆盖任务、数据质量、系统三类。

### 练习二

实现 Webhook 通知，集成钉钉或 Slack。

---

## 小结

1. **告警类型**：任务、数据质量、系统
2. **告警规则**：条件、严重程度、消息模板
3. **告警通知**：邮件、Webhook

---

## 下一课预告

下一课我们将学习**权限和公开分享**。
