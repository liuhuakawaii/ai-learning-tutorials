# 第1课：定时任务与手动触发

> **课程定位**：实现数据产品的自动化运行
> **前置知识**：第四阶段全部课程
> **预计时长**：50 分钟

---

## 场景引入

你负责的数据产品每天早上需要采集招聘网站数据、跑 ETL 管道、更新 Dashboard。目前这一切都是你手动在终端里敲命令完成的。某天你请假了，结果当天的数据更新就断了。你意识到：数据产品如果依赖人工触发，就永远无法真正"上线"。你需要一套定时任务系统，让数据管道在无人值守的情况下自动运行，同时保留手动触发的能力以应对突发需求。

---

## 学习目标

1. 设计定时任务系统
2. 实现手动触发机制
3. 管理任务状态

---

## 一、定时任务方案

### 1.1 方案对比

```
┌──────────────────────────────────────────────────────────────┐
│                    定时任务方案对比                            │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  cron（系统级）                                               │
│  ├── 优点：简单、稳定                                        │
│  ├── 缺点：功能有限、无重试                                  │
│  └── 适用：简单定时任务                                      │
│                                                              │
│  APScheduler（Python）                                        │
│  ├── 优点：功能丰富、支持多种调度                            │
│  ├── 缺点：需要进程管理                                      │
│  └── 适用：Python 项目                                       │
│                                                              │
│  Celery（分布式）                                             │
│  ├── 优点：分布式、高可用                                    │
│  ├── 缺点：复杂、需要 Redis/RabbitMQ                        │
│  └── 适用：大型项目                                          │
│                                                              │
│  GitHub Actions（CI/CD）                                      │
│  ├── 优点：免费、无需运维                                    │
│  ├── 缺点：有运行时间限制                                    │
│  └── 适用：开源项目、小规模                                  │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

### 1.2 APScheduler 实现

```python
from apscheduler.schedulers.background import BackgroundScheduler
from apscheduler.triggers.cron import CronTrigger
from datetime import datetime

scheduler = BackgroundScheduler()

def crawl_jobs():
    """采集任务"""
    print(f"[{datetime.now()}] 开始采集招聘数据...")
    # 执行 ETL 管道
    pipeline.run()

# 添加定时任务：每天早上 8 点
scheduler.add_job(
    crawl_jobs,
    CronTrigger(hour=8, minute=0),
    id="crawl_jobs",
    name="招聘数据采集",
    replace_existing=True
)

scheduler.start()
```

---

## 二、手动触发

### 2.1 API 触发

```python
from fastapi import APIRouter, BackgroundTasks

router = APIRouter()

@router.post("/tasks/crawl")
async def trigger_crawl(background_tasks: BackgroundTasks):
    """手动触发采集任务"""
    task_id = generate_task_id()
    
    background_tasks.add_task(run_crawl_task, task_id)
    
    return {
        "task_id": task_id,
        "status": "queued",
        "message": "任务已加入队列"
    }

async def run_crawl_task(task_id: str):
    """执行采集任务"""
    try:
        update_task_status(task_id, "running")
        pipeline.run()
        update_task_status(task_id, "completed")
    except Exception as e:
        update_task_status(task_id, "failed", str(e))
```

---

## 三、任务状态管理

```sql
CREATE TABLE task_log (
    id              SERIAL PRIMARY KEY,
    task_id         VARCHAR(50) UNIQUE NOT NULL,
    task_type       VARCHAR(50) NOT NULL,
    status          VARCHAR(20) NOT NULL,  -- queued, running, completed, failed
    trigger_type    VARCHAR(20) NOT NULL,  -- scheduled, manual
    start_time      TIMESTAMP,
    end_time        TIMESTAMP,
    error_message   TEXT,
    created_at      TIMESTAMP DEFAULT NOW()
);
```

---

## 常见误区

- **只用 cron 就够了**：cron 能处理简单定时，但没有重试、没有状态追踪、没有并发控制。当任务逻辑变复杂后，cron 会成为瓶颈。
- **手动触发不需要记录**：手动触发的任务同样需要记录状态和日志，否则出问题后无法追溯是定时还是手动触发的。
- **定时任务不需要超时控制**：如果一个采集任务卡住了，没有超时机制会导致后续任务排队等待，整个调度系统被阻塞。
- **把所有逻辑写在一个任务里**：采集、清洗、聚合应该拆分成独立任务，失败时可以单独重试，而不是每次都从头跑。

---

## 工程建议

1. **任务幂等性是底线**：定时任务可能被重复触发（手动 + 定时同时触发），确保同一任务跑两次不会产生重复数据。
2. **记录触发来源**：在 task_log 中区分 `scheduled` 和 `manual` 触发，方便排查问题和统计使用频率。
3. **任务超时与并发限制**：给每个任务设置最大执行时间和最大并发数，防止一个慢任务拖垮整个系统。
4. **先本地验证调度逻辑，再上生产**：用 APScheduler 在本地跑通调度逻辑，确认无误后再考虑分布式方案（如 Celery），避免过早引入复杂度。

---

## 动手练习

### 练习一

设计定时任务调度方案，支持：
1. 每天定时采集
2. 每小时增量更新
3. 手动触发

### 练习二

实现任务状态管理，记录任务的完整生命周期。

---

## 小结

1. **定时任务**：APScheduler、cron、GitHub Actions
2. **手动触发**：API 触发、后台执行
3. **状态管理**：queued → running → completed/failed

---

## 下一课预告

下一课我们将学习**增量更新与断点续跑**。
