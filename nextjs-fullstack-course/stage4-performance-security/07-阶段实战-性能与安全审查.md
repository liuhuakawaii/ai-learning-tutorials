# 第七课：阶段实战 — 性能与安全审查

## 场景引入

你的 SaaS 工作台已经完成了核心功能开发，但在准备上线前的 Code Review 中，团队提出了三个问题：页面在 3G 网络下加载超过 8 秒，安全扫描发现两个 API 存在 IDOR 漏洞，Lighthouse Accessibility 评分只有 65 分。这些问题如果带到生产环境，轻则影响用户体验，重则引发安全事故。你需要系统性地对项目做一次完整的性能与安全审查。

## 学习目标

综合运用第四阶段所学知识，对 SaaS 工作台进行完整的性能与安全审查：

1. 运行 Lighthouse 审计
2. 修复性能问题
3. 修复安全漏洞
4. 改善无障碍性

---

## 一、审查清单

### 1.1 性能审查

```
首屏性能：
  □ LCP < 2.5 秒
  □ INP < 200 毫秒
  □ CLS < 0.1

资源优化：
  □ 图片使用 next/image
  □ 字体使用 next/font
  □ 第三方脚本使用 next/script
  □ 无未使用的 JavaScript

缓存策略：
  □ 静态资源有缓存头
  □ API 响应有适当缓存
  □ 使用 revalidate 控制缓存
```

### 1.2 安全审查

```
认证授权：
  □ 所有 API 有认证检查
  □ 权限验证在服务端执行
  □ 无水平/垂直越权漏洞

输入验证：
  □ 所有用户输入有验证
  □ 使用 Zod schema 验证
  □ 文件上传有类型和大小验证

防护措施：
  □ CSRF 防护（Server Actions 自动）
  □ XSS 防护（React 默认转义）
  □ SQL 注入防护（Prisma 参数化）
  □ 安全头部已配置
```

### 1.3 无障碍审查

```
语义化：
  □ 使用正确的 HTML 标签
  □ 标题层级正确
  □ 列表使用 ul/ol

键盘导航：
  □ 所有交互元素可聚焦
  □ Tab 顺序合理
  □ 有跳过导航链接

ARIA：
  □ 模态框有 ARIA 属性
  □ 表单有关联的 label
  □ 错误提示使用 role="alert"
  □ 图片有 alt 属性
```

---

## 二、运行 Lighthouse

### 2.1 Chrome DevTools

```
1. 打开 Chrome DevTools (F12)
2. 切换到 Lighthouse 面板
3. 选择需要的审计类别
4. 点击 "Analyze page load"
```

### 2.2 命令行

```bash
# 安装 Lighthouse CLI
npm install -g lighthouse

# 运行审计
lighthouse http://localhost:3000 --output html --output-path ./report.html
```

---

## 三、性能优化实践

### 3.1 优化前的问题

```tsx
// ❌ 问题：客户端获取数据
'use client'

export default function Dashboard() {
  const [data, setData] = useState(null)

  useEffect(() => {
    fetch('/api/stats').then(r => r.json()).then(setData)
  }, [])

  return <div>{data ? <Stats data={data} /> : <Loading />}</div>
}
```

### 3.2 优化后的实现

```tsx
// ✅ 优化：服务端获取 + 流式渲染
import { Suspense } from 'react'

export default function Dashboard() {
  return (
    <div>
      <Suspense fallback={<StatsSkeleton />}>
        <StatsComponent />
      </Suspense>
    </div>
  )
}

async function StatsComponent() {
  const stats = await getStats() // 服务端获取
  return <Stats data={stats} />
}
```

### 3.3 优化图片

```tsx
// ❌ 问题：使用 img 标签
<img src="/hero.jpg" />

// ✅ 优化：使用 next/image
import Image from 'next/image'

<Image
  src="/hero.jpg"
  alt="Hero"
  width={800}
  height={400}
  priority  // 首屏图片
/>
```

### 3.4 优化字体

```tsx
// ❌ 问题：外部字体
<link href="https://fonts.googleapis.com/css2?family=Inter" rel="stylesheet" />

// ✅ 优化：next/font
import { Inter } from 'next/font/google'

const inter = Inter({ subsets: ['latin'] })

export default function Layout({ children }) {
  return (
    <html className={inter.className}>
      <body>{children}</body>
    </html>
  )
}
```

---

## 四、安全加固实践

### 4.1 添加安全头部

```tsx
// next.config.js
const securityHeaders = [
  {
    key: 'X-Frame-Options',
    value: 'DENY'
  },
  {
    key: 'X-Content-Type-Options',
    value: 'nosniff'
  },
  {
    key: 'Referrer-Policy',
    value: 'origin-when-cross-origin'
  },
  {
    key: 'Permissions-Policy',
    value: 'camera=(), microphone=(), geolocation=()'
  }
]

module.exports = {
  async headers() {
    return [{ source: '/(.*)', headers: securityHeaders }]
  }
}
```

### 4.2 输入验证加固

```tsx
// lib/validation.ts
import { z } from 'zod'
import DOMPurify from 'dompurify'

// 通用清理函数
export function sanitizeInput(input: string): string {
  return DOMPurify.sanitize(input)
}

// 严格的字符串验证
export const StrictStringSchema = z.string()
  .transform(val => sanitizeInput(val))
  .refine(val => val.length > 0, '不能为空')

// 文件名验证
export const FilenameSchema = z.string()
  .regex(/^[a-zA-Z0-9._-]+$/, '文件名包含非法字符')
  .max(255, '文件名太长')
```

### 4.3 修复 IDOR 漏洞

```tsx
// ❌ 问题：直接使用用户输入的 ID
export async function getProject(projectId: string) {
  return prisma.project.findUnique({
    where: { id: projectId }
  })
}

// ✅ 修复：添加权限检查
export async function getProject(projectId: string) {
  const session = await getSession()

  return prisma.project.findFirst({
    where: {
      id: projectId,
      team: {
        members: {
          some: { userId: session.userId }
        }
      }
    }
  })
}
```

---

## 五、无障碍改进

### 5.1 添加跳过导航

```tsx
// app/layout.tsx
export default function Layout({ children }) {
  return (
    <html>
      <body>
        <a
          href="#main-content"
          className="sr-only focus:not-sr-only focus:absolute focus:top-4 focus:left-4 focus:z-50 focus:p-4 focus:bg-white"
        >
          跳过导航
        </a>
        <Header />
        <main id="main-content">
          {children}
        </main>
      </body>
    </html>
  )
}
```

### 5.2 改善表单无障碍

```tsx
// ❌ 问题：没有 label
<input type="email" placeholder="邮箱" />

// ✅ 修复：添加 label
<label htmlFor="email" className="sr-only">邮箱地址</label>
<input
  id="email"
  type="email"
  placeholder="邮箱"
  aria-required="true"
  aria-invalid={errors.email ? 'true' : undefined}
  aria-describedby={errors.email ? 'email-error' : undefined}
/>
{errors.email && (
  <p id="email-error" role="alert" className="text-red-500 text-sm">
    {errors.email}
  </p>
)}
```

### 5.3 添加 ARIA 到模态框

```tsx
// components/Modal.tsx
'use client'

import { useEffect, useRef } from 'react'

export function Modal({ isOpen, onClose, title, children }) {
  const modalRef = useRef(null)

  useEffect(() => {
    if (isOpen) {
      modalRef.current?.focus()
    }
  }, [isOpen])

  if (!isOpen) return null

  return (
    <div
      className="fixed inset-0 bg-black/50 flex items-center justify-center"
      onClick={onClose}
    >
      <div
        ref={modalRef}
        role="dialog"
        aria-modal="true"
        aria-labelledby="modal-title"
        className="bg-white p-6 rounded-lg max-w-md w-full"
        onClick={e => e.stopPropagation()}
        tabIndex={-1}
      >
        <h2 id="modal-title" className="text-xl font-bold mb-4">
          {title}
        </h2>
        {children}
        <button onClick={onClose} className="mt-4">
          关闭
        </button>
      </div>
    </div>
  )
}
```

---

## 六、常见误区

1. **审查只看 Lighthouse 分数**：Lighthouse 分数是实验室环境下的快照，不代表真实用户体验。应该结合 RUM（真实用户监控）数据判断性能表现，尤其是 P75 和 P95 指标。

2. **安全审查只关注 OWASP Top 10**：除了常见的注入和 XSS，还要关注业务逻辑漏洞（如越权访问、竞态条件）、依赖供应链安全（`npm audit`）、以及敏感数据泄露（日志中的密钥）。

3. **无障碍审查只用自动化工具**：自动化工具只能检测约 30-40% 的无障碍问题。键盘导航顺序、屏幕阅读器体验、语义合理性等必须人工测试。

4. **一次审查就够了**：性能和安全是持续性工作，每次功能迭代都可能引入新问题。应该在 CI 中集成自动化审查，并建立定期人工审查机制。

---

## 七、工程建议

1. **建立审查 checklist 模板并纳入 PR 流程**：将性能、安全、无障碍的审查要点做成 checklist，在每个 PR 的 Code Review 中逐项确认。

2. **设置性能预算（Performance Budget）**：定义 JS bundle 大小上限（如 200KB gzipped）、LCP 上限（2.5s）、CLS 上限（0.1），在 CI 中自动检查。

3. **安全审查优先修复高风险项**：按风险等级排序——未授权的数据访问 > 注入漏洞 > CSRF > 安全头部缺失 > 信息泄露，优先修复高风险项。

4. **无障碍审查分阶段推进**：先修复影响最大的问题（如缺少 alt、缺少 label、键盘不可操作），再优化细节（如对比度、焦点顺序、ARIA 状态）。

---

## 八、审查报告模板

### 6.1 性能报告

```markdown
## 性能审查报告

### Lighthouse 分数
- Performance: 85
- Accessibility: 92
- Best Practices: 95
- SEO: 90

### 关键指标
- LCP: 2.1s ✓
- INP: 160ms ✓
- CLS: 0.08 ✓

### 发现的问题
1. 首屏图片未使用 priority 属性
2. 第三方脚本阻塞渲染
3. 未使用的 JavaScript 过多

### 修复建议
1. 为首屏图片添加 priority
2. 使用 strategy="lazyOnload" 加载非关键脚本
3. 使用动态导入减少首屏 JS
```

### 6.2 安全报告

```markdown
## 安全审查报告

### 已实施的防护
- ✓ Server Actions 自动 CSRF 防护
- ✓ React 默认 XSS 防护
- ✓ Prisma 参数化查询防止 SQL 注入
- ✓ Session cookie 设置 httpOnly、secure、sameSite

### 发现的问题
1. 缺少安全头部
2. 文件上传未验证文件内容
3. 部分 API 缺少权限检查

### 修复建议
1. 配置 X-Frame-Options 等安全头部
2. 添加文件内容验证（magic bytes）
3. 为所有 API 添加权限检查
```

---

## 七、验收清单

完成审查后，检查以下内容：

### 性能
- [ ] Lighthouse Performance 分数 > 80
- [ ] LCP < 2.5 秒
- [ ] 无渲染阻塞资源
- [ ] 图片已优化

### 安全
- [ ] 安全头部已配置
- [ ] 所有输入有验证
- [ ] 权限检查完整
- [ ] 无明显安全漏洞

### 无障碍
- [ ] Lighthouse Accessibility 分数 > 90
- [ ] 所有图片有 alt
- [ ] 表单有 label
- [ ] 键盘可操作

---

## 八、持续改进

### 8.1 性能监控

```tsx
// app/web-vitals.tsx
'use client'

import { useReportWebVitals } from 'next/web-vitals'

export function WebVitals() {
  useReportWebVitals((metric) => {
    // 发送到分析服务
    console.log(metric)

    // 可以发送到 Vercel Analytics
    // 或自定义后端
  })

  return null
}
```

### 8.2 定期审查

```
每周：
  → 检查 Lighthouse 分数
  → 审查错误日志

每月：
  → 安全依赖更新
  → 性能基线对比

每季度：
  → 完整安全审查
  → 无障碍测试
```
