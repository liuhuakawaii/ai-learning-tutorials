# AGENTS.md

本文件是 `ai-learning-tutorials` 仓库的项目级协作规范。它的目标不是把所有课程压成同一个模板，而是让后续新增、修改、重写课程时保持：

- 内容真实
- 结构合适
- 语言自然
- 工程判断清楚
- 示例代码可运行
- 长期维护成本可控

如果全局规范与本文冲突，以本文为准。所有中文内容必须使用 UTF-8 编码。

---

## 1. 项目定位

这是一个面向已有前端或编程基础学习者的系统化技术课程仓库，覆盖 AI 应用、Agent、RAG、前端工程、后端工程、DevOps、数据工程、性能、测试、系统设计等方向。

课程的目标不是罗列知识点，而是帮助读者建立真实工程中的判断能力：

- 为什么这个技术会存在
- 它解决什么具体问题
- 它在真实项目里如何落地
- 它有哪些边界、代价和常见误判
- 什么时候不该使用它

课程应该像一位有经验的工程师带新人做项目、排查问题、做技术决策，而不是像讲师照着大纲念知识点。

---

## 2. 仓库结构

```txt
ai-learning-tutorials/
├── ai-path/                       # AI 工程师学习路径
│   ├── 01-ai-app/
│   ├── 02-ai-agent-engineer/
│   ├── 03-llm-eval/
│   ├── 04-multi-agent/
│   ├── 05-mcp-dev/
│   ├── 06-llm-finetuning/
│   ├── 07-multimodal-ai/
│   └── graduation-project/
├── ai-specialization/             # AI 专项进阶
├── frontend/                      # 前端方向
├── backend/                       # 后端方向
├── devops/                        # 工程与运维
├── data/                          # 数据方向
├── README.md
├── AGENTS.md
└── COURSE_DESIGN_REVIEW.md
```

新增课程必须放入对应分类目录。新增分类时，需要同时更新根目录 `README.md`。

单门课程通常采用以下结构，但允许根据课程形态做合理调整：

```txt
xx-course/
├── README.md
├── stage1-xxx/
│   ├── README.md
│   ├── 01-xxx.md
│   └── requirements.txt           # 如有必要
├── stage2-xxx/
│   └── ...
└── final-project/
    ├── 项目说明.md
    ├── scripts/
    ├── reports/
    └── <project-name>/
```

---

## 3. 关键维护规则

### 3.1 README 必须同步

任何课程级变更都必须检查是否需要同步根目录 `README.md`：

- 新增课程
- 删除课程
- 修改课程名称
- 修改课时数
- 修改完成状态
- 调整课程分类
- 调整学习路径顺序

README 中的分类表必须与实际目录保持一致。

### 3.2 验证脚本不能随意删除

已有 `final-project/scripts/check.*` 的课程，修改项目代码后必须运行对应验证脚本。无法运行时，要在交付说明中写清楚原因。

常见验证命令：

```bash
cd ai-path/02-ai-agent-engineer/final-project
python scripts/check.py

cd backend/nodejs-backend/final-project
node scripts/check.js

cd ai-path/01-ai-app/final-project/ai-knowledge-workspace-kit
npm run check
npm run ingest
npm run ask
npm run eval

cd frontend/frontend-performance/final-project/performance-rescue-demo
node scripts/check-demo-files.js

cd frontend/webassembly/final-project
node scripts/check.cjs
```

### 3.3 不做无关重写

修改课程时，只处理本次任务涉及的课程、阶段、课时或项目文件。不要顺手重排全仓库结构、统一标题风格、批量改名或重写无关课程。

---

## 4. 课程设计原则

### 4.1 不使用统一课时模板

禁止把每节课都写成固定八段式，例如：

```txt
场景引入
问题拆解
核心概念
代码示例
项目实践
常见误区
工程建议
小结与练习
```

这些可以作为内容元素，但不能成为每节课的固定标题和固定顺序。

每节课应该根据内容本身决定结构。概念课、排错课、项目课、架构课、源码机制课不应该长得一样。

### 4.2 每节课必须完成的教学任务

无论采用什么结构，每节课都必须回答这些问题中的大部分：

- 这节课解决什么真实问题
- 读者为什么会遇到这个问题
- 常见的错误理解是什么
- 关键概念和工程动作之间是什么关系
- 示例为什么这样设计
- 这个方案的适用边界在哪里
- 如果放到真实项目里，还需要补哪些东西

不要为了凑完整性而写满所有方向。内容应该服务于本课目标。

### 4.3 课程结构由课型决定

写课前先判断本课属于哪种课型，再设计结构。

#### 概念建模课

适合：RAG、Agent、状态管理、渲染管线、调度、缓存、一致性、权限模型等。

推荐组织方式：

- 从一个常见误解或真实困惑进入
- 给出具体场景
- 建立心智模型
- 用最小例子验证模型
- 说明模型的边界
- 回到项目中看它如何影响设计

不要把概念写成百科词条。

#### 项目推进课

适合：带读者完成一个功能、模块或阶段项目。

推荐组织方式：

- 当前项目走到哪里
- 现在遇到什么工程卡点
- 有哪些方案可以选
- 为什么选择当前方案
- 代码如何落地
- 如何验证它真的工作
- 下一步会留下什么技术债或扩展点

项目课必须让读者看到代码如何从简单走向真实，而不是每节课都孤立写一个 demo。

#### Bug 排查课

适合：性能问题、部署问题、兼容问题、数据不一致、请求失败、爬虫反爬、模型输出异常等。

推荐组织方式：

- 先描述可观察现象
- 明确复现方式
- 列出 2-3 个合理假设
- 用日志、工具、实验逐步排除
- 定位根因
- 做最小修复
- 总结以后如何避免

不要一上来直接给答案。排查过程本身就是课程价值。

#### 架构决策课

适合：系统设计、技术选型、模块边界、状态归属、部署策略、评估体系、团队工程规范等。

推荐组织方式：

- 明确业务目标和约束
- 列出候选方案
- 比较成本、收益、复杂度和风险
- 给出决策
- 说明决策后果
- 说明未来什么情况下要重新评估

架构课的重点不是画大图，而是讲清取舍。

#### 机制与源码课

适合：浏览器机制、React 行为、Node.js 事件循环、Kubernetes 调度、模型推理流程、WebAssembly 执行模型等。

推荐组织方式：

- 从一个反直觉现象或具体问题开始
- 用实验或代码复现
- 展开底层机制
- 回到现象解释原因
- 提炼工程启发

不要把源码课写成源码翻译。读者需要的是机制如何影响工程判断。

#### 练习复盘课

适合：阶段总结、综合练习、项目验收、代码评审、性能审计、安全审计等。

推荐组织方式：

- 先给出任务或验收目标
- 明确约束条件
- 展示一份可能的实现或分析路径
- 复盘关键判断
- 指出常见错误
- 给出可继续改进的方向

不要只给题目，也不要只给答案。

---

## 5. 技术作家风格

### 5.1 基本语气

写作语气应该像一个长期做工程的人在认真解释问题：

- 克制
- 具体
- 有判断
- 有上下文
- 不装腔
- 不故作热血
- 不用培训班式口号

可以有自然的经验表达，但不要编造没有依据的经历。

推荐语气：

```txt
这个问题表面上是接口超时，实际更像是任务边界没有拆开。同步请求里混进了文档解析、向量化和模型调用，任何一步慢下来，用户都会看到同一个 loading。
```

不推荐语气：

```txt
本节课我们将深入学习接口超时问题，并掌握相关最佳实践，从而全面提升系统性能。
```

### 5.2 少用 AI 腔

避免以下表达习惯：

- “在当今快速发展的技术时代”
- “显而易见”
- “众所周知”
- “通过以上步骤，我们成功实现了”
- “这将大大提升性能和可维护性”
- “本文将带你深入理解”
- “让我们一起探索”
- “最佳实践是”

如果必须表达收益，要说清楚收益发生在哪里：

```txt
这样做减少的是重复解析配置的成本，不会减少网络请求时间。代价是配置变更后需要明确处理缓存失效。
```

### 5.3 正文要有推理过程

不要只堆短句和列表。列表适合整理信息，不适合替代解释。

每个关键判断都应该说明原因：

- 为什么这里需要拆模块
- 为什么这个状态不放全局
- 为什么这里暂时不引入缓存
- 为什么这个方案在小项目里反而更复杂
- 为什么这个错误在本地不明显，上线后会放大

### 5.4 不写假完整

不要为了显得全面，把所有相关概念都塞进一节课。

一节好课可以只讲透一个判断。宁可少而准，不要全而空。

如果某个话题只是后续会展开，可以明确收住：

```txt
这里先不展开重排序模型。现在只需要知道：召回阶段解决“可能相关”，重排序阶段解决“谁更值得放在前面”。后面的评估章节会专门处理这个问题。
```

### 5.5 允许课程有个性

不同课程可以有不同的写法：

- AI 应用课可以偏产品工程
- 性能课可以偏实验和测量
- 架构课可以偏决策复盘
- 爬虫课可以偏现场排错
- 图形学课可以偏直觉、图像和数学模型
- DevOps 课可以偏环境、失败案例和恢复流程

不要强行把所有课程写成同一种声音。

---

## 6. 代码与示例规范

### 6.1 示例必须服务于问题

禁止为了演示 API 写脱离业务的 demo。

不推荐：

```ts
const data = await fetchData()
console.log(data)
```

推荐：

```ts
const searchResults = await searchKnowledgeBase({
  query: userQuestion,
  limit: 5,
  minScore: 0.72,
})
```

示例命名必须有业务语义。避免 `foo`、`bar`、`data`、`item`、`list`、`temp` 这类无意义命名。

### 6.2 示例要完整可运行

代码题、项目课和关键示例必须尽量完整可运行。不能只给伪代码或缺少关键 import。

如果为了教学简化了代码，必须说明简化了什么：

```txt
为了让重点落在流式响应，这里先省略鉴权和限流。真实服务端不能直接暴露这个接口给未登录用户。
```

### 6.3 讲设计原因，不逐行翻译语法

不要解释“这一行定义了一个变量”。重点解释设计判断：

- 为什么这个函数放在服务层
- 为什么这个参数不能由前端传入
- 为什么错误要分类
- 为什么这里不马上引入队列
- 为什么这个抽象现在还太早

### 6.4 优化必须有前提

不要泛泛写“提升性能”。性能结论必须说明：

- 优化对象是什么
- 适用条件是什么
- 代价是什么
- 如何验证

例如：

```txt
这里的缓存只适合文档内容很少变化的场景。它减少的是重复 embedding 的成本，但会引入缓存失效问题。如果文档经常更新，需要把文档版本号纳入缓存 key。
```

### 6.5 前端课程的额外要求

涉及 React、Next.js、Vue、可视化、动画、移动端等内容时，必须关注：

- 组件职责边界
- 状态归属
- 响应式布局
- 可访问性
- loading / error / empty 状态
- hydration 或客户端渲染成本
- 动画性能
- 移动端交互

不要只写 happy path。

### 6.6 AI 工程课程的额外要求

涉及 LLM、RAG、Agent、评估、微调、多模态等内容时，必须关注：

- 模型能力边界
- 上下文构造
- 数据来源和质量
- 失败路径
- 评估方法
- 成本和延迟
- 可观测性
- 安全与权限

不要把“调 API 成功”写成“AI 应用完成”。

---

## 7. 练习与答案

课程可以有练习，但练习不能只是形式。

### 7.1 什么时候需要练习

以下课程通常需要练习：

- 概念需要读者迁移应用
- 代码实现需要读者亲手改动
- 阶段结尾需要验收
- 容易出现误判，需要通过题目暴露

以下情况可以不强行加练习：

- 课程本身已经是完整项目推进
- 本节是纯复盘或设计评审
- 练习只会重复正文，没有新价值

### 7.2 有练习就必须有解析

如果写了 `## 练习`，必须提供 `## 参考答案` 或 `## 解析`。

答案不能只给结论，必须包含：

- 解题思路
- 关键判断
- 参考实现或分析
- 常见错误
- 如果有多种合理答案，说明判断标准

推荐格式：

```markdown
## 练习

### 练习一：为检索接口增加失败分类

题目描述...

---

## 参考答案

### 练习一

**思路**：先区分用户输入问题、检索服务问题和模型生成问题，否则前端只能展示同一个失败提示。

**参考实现**：

\`\`\`ts
// 完整可运行或可嵌入项目的代码
\`\`\`

**常见错误**：

- 把所有异常都转成 `500`
- 在前端根据错误文案判断错误类型
```

---

## 8. 准确性要求

### 8.1 涉及版本和官方行为必须核对

涉及以下内容时，必须以官方文档或项目实际代码为准：

- 框架 API
- SDK 调用方式
- CLI 参数
- 配置文件字段
- 部署行为
- 安全机制
- 兼容性差异
- 模型能力和限制

不确定时不要写成绝对结论。

### 8.2 术语必须稳定

同一门课中术语要前后一致。例如不要一会儿写“检索增强生成”，一会儿写“知识库问答”，一会儿又写“文档机器人”，除非明确说明它们之间的差别。

### 8.3 不夸大效果

禁止把工具、框架、模式写成银弹。

不推荐：

```txt
引入 Zustand 后，状态管理问题就可以彻底解决。
```

推荐：

```txt
Zustand 解决的是跨组件共享状态和更新入口分散的问题。它不会自动解决状态建模问题。如果筛选条件、URL 参数和服务端查询之间的关系没有设计清楚，换任何状态库都会混乱。
```

---

## 9. 修改课程前的工作流

### 9.1 新增课程

新增课程前先完成课程设计，不要直接批量生成课时文件。

必须先明确：

- 课程面向谁
- 学完后能做什么
- 不覆盖什么
- 主线项目是什么，或者为什么不需要项目
- 阶段如何递进
- 每个阶段的能力变化
- final-project 如何验收

课程阶段数和课时数由内容决定，不允许为了整齐强行设计成固定数量。

### 9.2 修改已有课程

修改已有课程前先判断任务类型：

- 修正文案
- 修正技术错误
- 重写单节课
- 调整阶段结构
- 补充项目代码
- 改善练习与答案
- 统一术语

只做本次任务需要的改动。发现更大的结构问题时，先记录或说明，不要顺手大改。

### 9.3 重写课程

重写一节课时，先保留它在课程路径中的职责，再重新设计表达方式。

重写时优先处理：

- 是否有真实问题
- 是否有清晰主线
- 是否有推理过程
- 是否有工程判断
- 示例是否贴近业务
- 是否摆脱固定模板标题

不要只是把原文换一种说法。

---

## 10. 质量检查清单

交付课程内容前，至少自查这些问题：

- 这节课的结构是根据内容设计的，还是套模板套出来的
- 开头是否足够具体，能让读者知道为什么要学
- 正文是否有推理过程，而不是短句堆叠
- 关键判断是否讲了原因和边界
- 示例代码是否完整、可读、命名有业务语义
- 是否出现“显而易见”“最佳实践”“大大提升”这类空泛表达
- 是否有不准确或过度绝对的技术结论
- 练习是否真的帮助读者迁移能力
- 如果修改了课程状态、课时数或分类，README 是否同步
- 如果修改了 final-project，验证脚本是否运行

---

## 11. Git 提交规范

- 使用中文 commit message
- 常用前缀：
  - `feat:` 新课程、新功能、新项目
  - `fix:` 修复技术错误、代码错误、文档错误
  - `docs:` 文档更新、课程说明更新
  - `refactor:` 结构调整，不改变课程目标

示例：

```txt
feat: 新增 RAG 工程化课程
fix: 修正 useMemo 课程中的性能表述
docs: 更新课程状态与课时统计
refactor: 调整 AI 应用课程阶段结构
```

---

## 12. 一句话标准

这个仓库里的课程应该像“资深工程师带读者理解真实项目中的判断过程”，而不是“AI 按固定栏目生成的一组知识点说明书”。
